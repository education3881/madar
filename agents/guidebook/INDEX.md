# Guidebook — index of durable working knowledge

**Maintained by:** the Manager (consolidated in the weekly review); **Section 1 owned by the Researcher** from 2026-06-14.
**Last consolidation:** 2026-09-20 (weekly review, covering 2026-09-14 → 2026-09-20 — **the largest reconciliation this register has ever needed.** The ruling register extended **#1–#48 → #1–#58**: ten rulings had been filed on disk by the daily runs, cited by number in commits, briefs, verdicts and the edition ledger, while the §3 heading, the §3 table and the series-integrity line all still read `#1–#48`. **Section 1 was further behind than Section 3** — its table stopped at **row 39** (2026-09-06) while rows 40–53 lived only as dated files, so fourteen rows are merged into the table today and rows 40, 41, 42, 43, 47, 48 and 50 are assigned here, the rest at the numbers their own files already claim. The §3 families heading read **"three families"** while listing seven, and has read that since 2026-08-23 through four families being named under it — corrected to eight. **No new family was named**, which is itself the week's finding: five of the ten rulings joined the **assertion-discipline** family, taking it from five members to ten and making it the largest in the register, and its second five ask a different question from its first five — not *would this check catch the defect* but ***what is this check's answer actually about***. The **non-numeric** family's own forward prediction — the attribution verb — was tested on 09-14 and bit. The **enumeration** family, whose members have always lived in the RUNBOOK, gains its first numbered member in #50 and is now listed here. **Archive roll:** the 08-23 → 09-06 addenda and the 09-06 consolidation section moved verbatim to `ARCHIVE-2026-08-23_2026-09-06.md` per the two-cycle rule; nothing deleted, nothing summarised.)
**Prior consolidation:** 2026-09-13 (weekly review, covering 2026-09-07 → 2026-09-13 — the ruling register extended **#1–#47 → #1–#48** with the canonical one-line statement for **#48 a-trend-line-can-have-a-seam**, and the register's **three-way disagreement reconciled** (header read `#1–#46`, table stopped at #47, series line read `#1–#47`); a **seventh family named — whether the words around the number are true**, the **non-numeric family** (#45, #46, #47), *discovered* by three consecutive verification verdicts returning six findings and zero numbers; the family's repeat shape — `sources[]` right, both bodies wrong, twice in two days across two drafters — codified as a RUNBOOK rule; **no archive roll** (two-cycle rule already satisfied); a derived-register assertion carried to the Web Developer so the header stops being hand-maintained.)
**Prior consolidation:** 2026-09-06 (weekly review, covering 2026-08-30 → 2026-09-06 — the ruling register extended **#1–#39 → #1–#43** with one-line canonical statements for the week's four new numbers (#40 a-mirror-can-misname-the-instrument, #41 supersede-don't-resurrect, #42 a-language-toggle-is-not-a-translation, #43 the-podium-is-not-the-document); a **sixth family named — which register speaks for the owner** (#38, #40, #42, #43, with #41 as its vintage corollary), and the register hierarchy stated once (document > tables > first-party written > reported speech > mirror); **Section 1's table reconciled to its own header** — rows 26–38 had been filed as dated addenda since 08-23 while the header claimed thirty-three rows and the table stopped at 25, so thirteen rows are merged into the table today, numbers as first cited; the 08-09, 08-16 and 08-23 consolidation sections and their addenda (08-10 → 08-18) rolled into `ARCHIVE-2026-08-09_2026-08-23.md` per the two-cycle rule, nothing deleted.)
**Prior consolidation:** 2026-08-30 (weekly review, covering 2026-08-24 → 2026-08-29 — the ruling register extended #1–#32 → **#1–#38** with one-line canonical statements for the week's five new numbers (#34 a-total-must-be-added, #35 prove-against-a-control, #36 promise-derived-not-declared, #37 a-contract-in-prose-is-a-declaration, #38 a-source-can-drift-against-itself) and row 33's no-number routing addendum merged into the single table; a **fourth register family named — whether the artefact agrees with itself** (#34, #38) — beside an **assertion-discipline family** (#16/#35/#36/#37); Section 1 stands at **thirty-three rows**, rows 27–33 filed by the daily runs 08-24 → 08-29 with row numbers already assigned, verified no gaps, no dupes; housekeeping: the 2026-08-23 review built the §3 register but left this header line stale at 08-16 — reconciled today.)
**Prior consolidation:** 2026-08-16 (weekly review, covering 2026-08-10 → 2026-08-16 — Section 1 reconciled to **twenty-three rows** with the five Ed05-recon source indices (19 UAE, 20 Egypt, 21 Sierra Leone, 22 Sudan, 23 best-system-in-Africa) assigned proper row numbers; the **scoped-instrument family** named and consolidated as one rule with four shapes (rows 15/19/20/21); row 22's lineage method note **promoted to ruling #30**, and the same day's daily run filed **#29** (a ranking is not a measurement, row 23) — two new numbers in one day, breaking a three-week hold at #1–#28; prior: 2026-08-09, which grew Section 1 from 13 to 18 rows and flipped the Ed04 rows to LIVE).

---

## 0. Figure-integrity checklist — the seven axes a number travels with

**Consolidated 2026-07-27** from rulings #12/#12b, #14, and #21–#25 (the Verifier's compounding series). A figure is never just a value; it carries context that inverts the story if dropped or swapped. Before any load-bearing number ships, the drafter states — and the Verifier confirms against the primary, *reading the source beside the draft* — all seven:

| # | Axis | The question | Ruling |
|---|---|---|---|
| 1 | **Administration / coverage** | Who ran the instrument, and over what population? (self-administered ≠ external; census ≠ sample) | #12 / #12b |
| 2 | **Funnel stage** | Which pipeline stage is this — target, potential, choice, placed? Don't chain stages into a trend. | #14 |
| 3 | **Vintage** | Has the system of record *adopted* the number, or is it a later recount by another author? Attribute recounts to author + date. | #21 |
| 4 | **Scope** | What does each qualifier govern? Don't split a shared qualifier or narrow one the source drew broadly. | #22 |
| 5 | **Direction** | Trace the hedge ("almost" / "over") to the *same sentence* that gave the value; adjacent vintages flip it. | #23 |
| 6 | **Position** | A series point carries its column and base note; never assert cross-year sameness of denominators. | #24 |
| 7 | **Condition** | Write "out of ___" for every rate; check the draft's population matches the source's; don't chain rates from different conditions. | #25 |

The unifying verification move across all seven: **check at the context, not the figure** — list what the number is conditioned on, confirm the draft is conditioned on the same. Five of these seven (vintage, scope, direction, position, condition) were catchable *only* by live re-fetch of the primary beside the draft — the standing argument for the drafter≠verifier split. Full provenance for each axis lives in its dated addendum below.
**Purpose:** one entry point to everything the team has learned the hard way. The durable manual (`slow-cadence-publication-guidebook.html`) is the *replicable* model for any subject; the files indexed below are *Madār-specific* verified reference and method. A writer should be able to start any piece, or render any asset, without re-running reconnaissance that has already been done and banked.

---

## 1. Verified primary-source indices (Test 1 pre-cleared at the index level) — owned by the Researcher

**Sixty rows, 1 → 60, no gaps and no duplicates** (rows 59 and 60 added 2026-09-25 at the point of filing — two rulings in one run, #64 from the Arabic Editor's gate and #65 from the QA lane — together with §3's range and §3's heading, the three counts the 09-23 amendment says a filing run owes, moved in one commit and counted off the rows rather than off yesterday's number. Row 58 added 2026-09-24 at the point of filing, together with §3's range and §3's heading. Row 57 added 2026-09-23 at the point of filing. **Reconciled against the rows today and it was wrong by two:** this line read *"Fifty-four rows, 1 → 54"* while rows **55** and **56** sat in the table below, both filed by the 2026-09-22 run — which moved §3's range to #61 in the same commit, as the 09-20 rule requires, and did not move §1's count. The rule is three days old and has already leaked once, in the direction it was written to stop; the leak is *which* counts the filing run owes, not *whether* it owes them. **A filing run moves every count its ruling touches — §3's range, §3's heading, and §1's count line — or the rule has only moved the drift.** Row 54 added 2026-09-21; rows 1 → 53 verified 2026-09-20.) *This count line is new,
and it is here because the section had none.* §3 drifted visibly for a week because its heading claimed
a range its table contradicted; §1 drifted for **two** weeks without anyone noticing, because a table
that claims nothing cannot be caught claiming something false — the table simply stopped at row 39 and
rows 40–53 lived as loose files. A section with no count is not safer than one with a wrong count; it is
only quieter. Reconcile this number against the rows below at every consolidation (#57).

| File | Subject | What it gives | Status |
|---|---|---|---|
| `2026-06-06-sierra-leone-tsc-source-index.md` | Sierra Leone TSC / GIS teacher-deployment matching | Institutions, four named voices (2024–25) + AR transliterations, the 2024 hard figures with the "source from the technical report not the blog" rule, mandatory counter-texture, live URLs | **Shipped** 2026-06-08; figures traced in full via web tools |
| `2026-06-09-arabic-diglossia-early-literacy-source-index.md` | Arabic diglossia & early literacy (IQRA / Ras Al Khaimah) | Research spine (Saiegh-Haddad 2025; the non-deficit counter-frame), the IQRA intervention as the figure-bearing tier, the two-studies-same-name fence, AR transliteration candidates | **Shipped** 2026-06-10 on the evaluator-on-record tier (J-PAL report follow-up logged) |
| `2026-06-11-korea-aidt-source-index.md` (+ `2026-06-16-korea-aidt-voice-gate-closed.md`) | Korea AI digital textbooks (AIDT), adoption → statutory reversal | Statutory/ministry primaries, named humans by tier, the same-figure-different-scope fence (₩533.3B 2024 allocation vs ₩1T teacher-training 2024–26; $850M dropped as conflation), pilot scope | **SHIPPED** 2026-06-17 (EN+AR, build-verified, 33 pages) — voice gate closed 06-16 (Kim Cha-myung, elementary teacher, *Rest of World* 15 Oct 2025); figures re-verified live at draft. Figure-fence survives as durable reference |
| `2026-06-04-saudi-ece-source-index.md` (+ `2026-06-16-saudi-jordan-park-notes.md`) | Saudi early-childhood (ECE) sourcing landscape — and the Jordan refugee double-shift candidate | Tiered source map, the GASTAT 2025 data-led inversion route, transliteration rulings for minister + KG director; the sprint voice-only re-recon notes for both Saudi and Jordan | **Parked** (both) — operational-tier voice not reachable in open primaries (Saudi newest named voice 2019; Jordan no 2024+ operator); each un-parks on one named 2024+ operator |
| `2026-06-14-vietnam-tuition-free-source-index.md` | Vietnam public-school tuition exemption (Politburo 28 Feb 2025 → NA Res. 217/2025/QH15, 26 Jun 2025) | Two-stage instrument named (don't collapse them); scope (preschool→grade-12, public full / private capped support); the *tuition-free ≠ fee-free* fence with HCMC fee figures (Tuoi Tre); fiscal line ~VND 30,600bn/yr (~US$1.2bn); named voices w/ diacritics — incl. **operational** voice Principal Nguyễn Thị Vân Hồng (Tiền Phong, 3 Mar 2025) | **SHIPPED** 2026-06-16 (EN+AR, build-verified, 31 pages) — figure gate closed, voice gate closed; load-bearing URLs re-verified live at draft. Researcher's first commission, carried end-to-end |
| `2026-06-16-egypt-arabic-literacy-source-index.md` | Egypt NPDA / Arabic diglossia early-literacy (national scale) | Measured tier (UNICEF Innocenti quasi-experimental: 180k/980 schools/+17%) vs announced tiers (comms ½m; govt 1.38m/27 govs); the **measured-vs-announced** fence (ruling #9); channel-attributed operational voice (Naglaa Ragheb); the cut unverified-minister quote; dated learning-poverty baseline | **SHIPPED** 2026-06-18 (EN+AR, build-verified, 35 pages) — figures re-verified live; first piece on the AR compose/verify split |
| `2026-06-16-philippines-mtb-mle-source-index.md` | Philippines mother-tongue (MTB-MLE) statutory reversal | Reversal chain named (RA 10533 2013 → MATATAG/DO 24 s.2023 drops MT-subject → **RA 12027** 10 Oct 2024 ends MT-medium K–3 → DO 20 s.2025, SY2025-26); the **RA 11476 ≠ MTB** trap; the **PISA-can't-judge-K-3** instrument caveat (ruling #10); expert/collective voice tier (Nolasco by position; teachers'-groups quote); **Arzadon dropped** (unverified live) | **SHIPPED** 2026-06-19 (EN+AR, build-verified, 37 pages) — statute triangulated (LawPhil + SC e-library + Rappler), figures re-verified live; curated explainer (no classroom voice → upgrade path logged) |
| `2026-06-16-oman-school-evaluation-source-index.md` (+ `2026-06-20-publish-what-you-can-read.md`) | Oman National System for Evaluating School Performance (OAAAQA) — Ofsted-style external inspection, public per-school verdicts | Launch/instrument primaries (School Education Law RD 31/2023; launched 2 Dec 2024 under HH Sayyid Theyazin; results public Jan 2026); the **band-tally withheld** (secondary 44/3/11/1 = 59 ≠ 58, portal unreachable → ruling #11) with the triangulated subset that *did* run (58-school reach; al-Hassan bin Hashim, Seeb, at the bottom); the **Ofsted-retreat** comparative anchor (one-word judgements scrapped Sep 2024, Perry); CEO al-Shukaili labelled authority-tier, operator-voice gap stated in-piece | **SHIPPED** 2026-06-20 (EN+AR, build-verified) — banked CONDITIONAL, closed via route 2 (figure withheld + voice-gap disclosed) |
| `2026-06-24-brazil-crianca-alfabetizada-source-index.md` (+ `2026-06-25-comparability-is-not-independence.md`) | Brazil Compromisso Nacional Criança Alfabetizada (CNCA) + the Indicador Criança Alfabetizada | Instrument (Decreto 11.556/2023, MEC-coordinated federal compact); the measured multi-year series **56% (2023) → 59.2% (2024) → 66% (2025)** vs 64%/80% targets, pinned to the INEP primary PDF; the **self-administered-vs-Saeb seam** (ICA 56% vs Saeb **49.3%** 2023, same 743 cutoff → ruling #12, refined #12b); system-operator voice **Eliana Estrela (Seduc-Ceará, Mar 2026)** + Ceará/Sobral PAIC model; PT→AR transliterations; four mandatory counter-textures | **SHIPPED** 2026-06-25 (EN+AR, build-verified, 41 pages, parity 18=18) — figure gate re-verified live at draft (incl. Saeb 49.3% + the census-vs-sample nuance); voice seated at system-operator tier (not route-2). Edition-02 **piece 8 of 10**; first Ed02 LatAm, 2nd LatAm overall — fills the most underrepresented region (LatAm was 1/17) |

| `2026-07-02-kenya-cbe-grade10-source-index.md` (+ `2026-07-02-pathway-funnel-five-numbers.md`) | Kenya CBE senior-school transition — Grade-10 algorithmic pathway placement (Jan 2026) | First CBC cohort: **1,130,459** KJSEA candidates (KNEC, results 2025-12-11); blended placement score **60% KJSEA + 20% school-scored SBA + 20% KPSEA** (the #12 axes *inside one number*); the **four-STEM-numbers funnel** (target 60% · potential 59.09% · choice ~53% · placed ~50% → ruling #14); the review ledger (**355,457 filed / 211,636 approved / 143,821 rejected**, second window Jan 6–9); TSC 58,590-teacher deficit; operator voice **Willy Kuria (KESSHA chair, Jan–Feb 2026)** + principals' collective position; guardrails (mechanism-only; drop the opinion-carried "30% reported"; KCPE comparisons factual only) | **SHIPPED 2026-07-04** (Edition-02 piece 9, EN+AR, full gate; figures re-verified live at draft per the recovery rule — post-recon updates folded: post-review split **51/38/11** w/ 88% per own selection; **TSC scrapped the two-subject rule**, KESSHA conf. Mombasa, Jun 2026); the Africa-beyond-Sierra-Leone slot closed |

| `2026-07-16-acara-naplan-source-index.md` (row 11) | Australia NAPLAN — the 2023 proficiency reset & series restart | Registers by load (media releases PDF-only/binary here; results portal holds the equity decimals; key-dates page interleaves schools-list dates — read the row label); series **restarts 2023**, "below Strong" is two populations (state the derivation, never render *Developing* as failing) per #19; **mirror-verification route** (Mirage/medianet News Hub carry ACARA releases verbatim incl. background; ACARA PDF stays citation of record, coverage corroborates but never replaces the decimals) — third banked instance of #20 | **LIVE 2026-08-08** with the Ed04 wave as `australia-naplan-reset`, EN+AR (drafted 07-16, verified 07-17 — ruling #23 *an approximation has a direction*; verified in place on the deployed sitemap 08-09) |
| `2026-07-28-ofsted-england-report-cards-source-index.md` (row 12) | England / Ofsted report-card regime — single overall grade abolished (EIF in force 10 Nov 2025) for a five-band card | **Remit fence: Ofsted = England only** (Scotland HMIE / Wales Estyn / NI ETI keep their own — never "UK schools"). Registers: GOV.UK EIF + toolkits (grade bands, "secure fit"); the **Ofsted education-inspection blog fetches clean as served text** (primary voice channel for named-director quotes); trade press (Schools Week/Tes fetch clean) = citation-of-record for card-distribution tallies, attributed "analysis of published cards" per #19; judiciary.uk for the NAHT/Saini J judgment (name refusal vs the lost appeal). Traps: first-wave 150 grades are **22 volunteer schools** (carry "volunteer" in-sentence); survey rates are **of 100 self-selected heads** (7.9%/24.5% skew) per #25; new bands **not** old grades renamed; **frame-sibling with Oman** (one orienting Ruth Perry sentence, cross-link don't re-narrate) | **LIVE 2026-08-08** with the Ed04 wave as `england-report-cards-first-term`, EN+AR (drafted 07-28, verified 07-29 — CLEARED, no item failed, ruling #27 *two independent channels satisfy the split*; moved from staging into the build path at the wave gate). AR name-routing addendum: `2026-08-08-england-ofsted-ar-name-routing-and-wave-completion.md` — British bylines route by **heard vowel not letter** (Tymms → تِمز short /ɪ/ beside Whiteman → وايت diphthong), a non-Anglo surname on a British bench routes by **its own origin** (Saini J → القاضي سايني), extends #28b |

| `2026-08-06-nz-ncea-source-index-and-te-reo-term-routing.md` (row 13) | New Zealand / NCEA co-requisite literacy-numeracy gate + the te reo Māori term-routing convention | Corpus splits by **institution-job**: **NZQA** owns the canonical series (per-round tables = the only home of the May-2026 Event-1 numbers; the by-Year-11 attainment 69.5→73.8 + Marshall quote + equity finding), **Beehive** owns the political framing + funding + the **71.5→79.6 recount** (Incapsula-walled → Scoop mirror per #20), **ncea.education.govt.nz** owns the instrument definition (20-credit rule, CAA twice-yearly, transition to end-2027), **RNZ** carries ERO qualitatively. Two-base trap (Level-1 entrants ≠ co-requisite whole-cohort, #25) + vintage trap (#21) carried in-sentence. **Transliteration:** te reo Māori content-terms route by Māori phonology + Latin tag + one-time gloss (te reo matatini → تي ريو ماتاتيني; pāngarau → بانغاراو) — extends #28b from name-parts to embedded terms; Jann Marshall → جان مارشال CONFIRMED-by-convention | **LIVE 2026-08-08** with the Ed04 wave as `nz-ncea-corequisite`, EN+AR (verified 07-21 scoped re-check CLEARED) |
| `2026-08-07-netherlands-doorstroomtoets-source-index-and-dutch-name-routing.md` (row 14) | Netherlands / doorstroomtoets — the test that grades the advice | Corpus splits by **institution-job**, and the split IS the discipline: the **Inspectie van het Onderwijs** speech (Oppers, 15 Apr 2026) is the only home of the *event-given-eligibility* pair (IJsselstein 8/10 vs Stadskanaal 4/10) + "wettelijk verplicht … tenzij"; **PO-Raad** (23 Jun 2026) owns the *eligibility* spread (<1-in-10→>half), the island detail, IEP/LiB divergence + both Dries quotes; **DUO + OCW** own the statutory calendar (Jan → 24 Mar); **PO-Raad + Tweede Kamer** own the reversal (one central test 2029-2030). The **themarapportage dashboard** = origin case of **#26** (client-side JS value, un-quotable); the two-rate trap = **#25**'s origin. **Dutch name-routing** (digraph `ie` = long /i/ → ي; `oe`→و, `ij/ei`→اي, `ui`→أوي, `g/ch`→خ/غ; Oppers → أوبِرس, Dries → دريس) extends #28b | **LIVE 2026-08-08** with the Ed04 wave as `netherlands-doorstroomtoets`, EN+AR (verified 07-27 — CLEARED, no item failed) |
| `2026-08-09-england-dfe-teacher-workforce-source-index.md` (row 15) | England / DfE teacher workforce — the 6,500-teacher pledge count | Registers split by **register-job**: gov.uk press releases (political; the 4 Jun 2026 "70% of pledge" release read in served text — 4,654/6,500 = 3,008 secondary+special + 1,646 FE); explore-education-statistics (statistical; **dashboard-first → #26**: headline pages quotable, explorer tables are observations); NFER annual report (analytic; forecasts carry "forecast", leaver *rates* vs government leaver *counts* — never chain); Hansard/written statements (statutory; HCWS171 3.5% award 2026/27, +3% Sept 2027); Full Fact/Schools Week (watchdog; the Feb-2026 scope timeline). **Method note: a pledge is a scoped instrument** — the progress figure and the scope-definition-with-date are one citable pair (+4,654 in-scope and −1,900 whole-workforce FTE are simultaneously true; quoting either without the other's existence is the error). Generalises to every target-carrying reform on the Ed05 slate | **Ed05 recon bank, entry 1** (`content-drafts/recon/2026-08-09-ed05-uk-teacher-pledge-count.md`, filed 2026-08-09) |
| `2026-08-03-india-parakh-source-index-and-lakh-convention.md` (row 16; filed 08-03 as a daily addendum, row assigned at the 08-09 consolidation) | India / PARAKH Rashtriya Sarvekshan + Holistic Progress Cards | Achievement-survey primaries (4 Dec 2024: 2.1m students / 74,229 schools / 781 districts; class averages + the maths spine 60·46·37); **the lakh-carry convention** — carry the Indian-register figure and the international one as a pair (21.15 lakh / 2.1 million), never silently convert; HPC scope fence (**school report card, not board exam**); Bhaduri → بهادوري CONFIRMED-by-convention | **LIVE 2026-08-08** with the Ed04 wave as `india-parakh-hpc`, EN+AR (verified 07-11) |
| `2026-08-04-singapore-psle-source-index-and-name-origins.md` (row 17; filed 08-04 as a daily addendum, row assigned at the 08-09 consolidation) | Singapore / PSLE Achievement Levels + Full Subject-Based Banding | All four MOE registers (design filing Jul 2016 verbatim; SEAB conducts / MOE sets policy; cohort figures with dragon-year base note; tie-break order; COP "vary up to a few points"); the **declined-in-the-same-reply** discipline (per-group figures refused — the refusal is citable, the gap is not fillable); **the three name-origins of a Singapore byline** (Malay/Muslim → Arabic root restored: عاشق; Chinese → attested romanization convention: تشان تشون سينغ; English given → phonetic: غريس), extends #28b | **LIVE 2026-08-08** with the Ed04 wave as `singapore-psle-sbb`, EN+AR (verified 07-12) |
| `2026-08-05-chile-simce-source-index-and-spanish-name-routing.md` (row 18; filed 08-05 as a daily addendum, row assigned at the 08-09 consolidation) | Chile / Simce return — census delivery at the start of the school year | Mineduc/Agencia primaries (6 Mar 2026 delivery, third consecutive start-of-year; 2024/2025 series with significance qualifiers **scoped per #22**); the May-2026 CNED episode (plan cut proposed → rejected → 6° restored); IDPS association-not-cause fence; critics carried not adjudicated; **Spanish-vs-Italian-origin routing in one Chilean byline** (Cortez terminal-z = /s/ → كورتيس; Gino Italian-origin → جينو), extends #28b | **LIVE 2026-08-08** with the Ed04 wave as `chile-simce-return`, EN+AR (verified 07-15) |

| `2026-08-10-uae-teacher-nationalization-source-index.md` (row 19; filed 08-10 as a daily addendum, row assigned at the 08-16 consolidation) | UAE / nationalization of the teaching workforce (Emiratisation of teaching) | Corpus splits by register **and by level of government** — Abu Dhabi Media Office (emirate/political; the ADEK–Aldar compact of 20 May 2026 read in served text: the ninefold claim, the three-year all-schools social-studies direction, Kon Moallim/ECAE, steering-committee metrics; official AR mirror on-platform), WAM (state wire, bilingual — likely home of the 300/100 figures **absent** from the Media Office release, a misattribution trap), u.ae/MoHRE/Nafis (federal: 1,000/yr → 4,000 over four years), KHDA/Dubai Media Office (3,000 in private schools by 2033 — **baseline unknown, draft-blocking**), adek.gov.ae (instrument register, unprobed, #26). **Scoped-instrument shape 2: a multiplier without a base** — "ninefold" with no disclosed baseline is honest only as "ninefold on an undisclosed base," and the undisclosed base is itself reportable. Also: the **bilingual-register inversion** (official AR editions exist end-to-end → the AR trap shifts from transliteration to **version drift**; trace each quote in the edition cited); Sahar Cooper = the #28b routing family's cleanest mixed-origin byline (سحر restore-rule + كوبر heard-vowel) | **Ed05 recon bank, entry 2** (`content-drafts/recon/2026-08-10-ed05-uae-teacher-nationalization.md`). **Deferred with the teaching-question slate** at the 2026-08-11 founder frame redirect (Ed05 = Africa); the row is durable regardless. ADEK adjacency **moot for Ed05** |
| `2026-08-11-egypt-teacher-deficit-source-index.md` (row 20; filed 08-11 as a daily addendum, row assigned at the 08-16 consolidation) | Egypt / the teacher-deficit closure claim + the mass hiring drive | Registers: the minister's **own WEF byline** (4 Feb 2026, read in served text — "every core subject classroom now has a teacher," CAPMAS provenance, attendance >80%); **EIPR** watchdog (read in served text — the funnel 90,000 sat / 28,000 passed phase 1, the Military Academy second screening with live court appeals, Hegazy's 20,000/yr replacement figure, the 1997 Ganzouri freeze as origin); CAOA instrument register (معلم مساعد waiting lists drawing 21 Jul 2026; per-session results 4 Aug 2026 — the drive is live); the domestic claim series (90% closed Nov 2024 → 460k "eliminated" Dec 2025, **headline drifts 469k ↔ 460k between registers**, #21/#23); UNESCO GEM as the corroboration channel the minister himself cites. **Scoped-instrument shape 3: a deficit is a ratio, not a headcount** — hours required ÷ hours per teacher closes by appointments, by per-lesson labour (160,000 معلمو الحصة), or by **denominator arithmetic** (longer year, more weeks, lower quota); a closure claim is unreadable without which-instrument-closed-what-share, and the absence of a published formula is itself reportable. Funnel discipline (#14): target ≠ sat ≠ passed ≠ screened ≠ **appointed** — the appointed count is the story's missing number. Access: **URL-length wall** on percent-encoded Arabic slugs (route via state EN mirrors / EIPR PDFs); dostor.org short-IDs return empty bodies (#26). **#28b inversion** — first slate story where the *EN* side carries the routing risk (Arabic-native names take their attested EN forms of record) | **Ed05 recon bank, entry 3** (`content-drafts/recon/2026-08-11-ed05-egypt-teacher-deficit-closure.md`). **Carries over into the Africa frame** (Egypt is African); the *angle* re-commissions positive (the 2025 transformation story) per the founder's 08-11 posture — Editor's call at commission |
| `2026-08-12-sierra-leone-sleic-source-index.md` (row 21) | Sierra Leone / SLEIC outcomes fund — the RCT-verified, payment-by-results basic-education programme | **Second Sierra Leone row** — the 2026-06-06 row covers the TSC deployment story; **different instrument, do not merge** (ruling #30 applied before it was numbered). Registers: **EOF "Challenge Accepted"** (findings-of-record, 8 Dec 2025, read in served text — the six takeaways verbatim incl. "outperform roughly 70% of comparable programmes globally" and the candour sentence "does not definitely prove that outcomes-based financing is a better way"); **OPM evaluation report** (the RCT of record — five parallel RCTs, one per provider; both result PDFs at direct URLs, the draft's Test-1 anchor); **Save the Children's honest miss** (6 Jan 2026, read in served text — "Our results are not what we would have hoped them to be," Grade Effect .359 SD → two-thirds of a year, English not significant, partial payment, open-data repo); funder register (EOF/FCDO, 11 Jun 2026); completion register (participants drift **134k design → 130k+ funder → 100k+ completion**, #13/#21); MBSSE voice located but **video = transcript wall** (#26). **Scoped-instrument shape 4: an equivalent-years or percentile claim carries its ruler** — Save the Children computed the same SD on two rulers and published the smaller ("two-thirds of a year" on its own local Grade Effect, rejecting the flattering LMIC standard that would have claimed 1.1–1.6 years); the choice of ruler is itself evidence, and a not-significant gain is carried as **no demonstrated gain**, never a small one. **Payment-is-the-verdict discipline:** an outcomes contract paying *less* than hoped is the instrument working, not failing — the accomplishment the positive posture reports. #28b: Richard Mustapha → ريتشارد مصطفى (the Sahar-Cooper case run the other way) | **Ed05 recon bank, entry 4** (`content-drafts/recon/2026-08-12-ed05-sierra-leone-sleic-outcomes.md`) — **fills founder-mandated Africa slot #1** |
| `2026-08-13-sudan-digital-learning-source-index.md` (row 22) | Sudan / digital learning — the no-school classroom (eLS → CWTL → wartime learning spaces) | **Second Sudan row** — the Ed03 exam-continuity piece is the certification machine; this is learning delivery. Registers: **origin** (UNICEF Sudan 2019, read in served text — e-Learning Sudan b. 2014, the four-partner build incl. Ahfad University, the math-game + solar + tablet + community-facilitator chassis); **program** (UNICEF Sudan 2020, read — Arabic + math, ages 7–9; named facilitator Fatima Abbas Tahir; **official AR edition on-platform** → row-19 version-drift discipline); **evidence** (Brown et al., *J. Development Effectiveness* 2020 — the trial of record; War Child's "2.7×/2×" marketing series quoted **only** from the paper's own effect sizes); **wartime** (UNICEF USA Aug-2024, read — 699 safe learning spaces since start-2024, 60 digital, 12,660+ children, 54% girls, eight states; Kassala BMZ/KfW slice 20/~5,000; one Makanna's anatomy 631/4/15); **export** (War Child AR2025 — CWTL's ~125,000 across Ukraine, Uganda, Lebanon; **Sudan not on the list**); **system/forward** (Sudan Tribune 4 May 2026 — **BRIDGE**, GPE-funded, 2026–28, 328k children / ~850 schools / 7 states / 9,000+ teachers / 1,800 alternative learning spaces, **all announced**; the war's denominators ~8m deprived, half of schools closed, N Darfur 97% vs S 13% / W 27% non-functional). UNESCO article pages **JS-walled** (#26). **Origin case of ruling #30 — a lineage claim is a figure** (see `2026-08-16-a-lineage-claim-is-a-figure.md`): the tempting spine "the wartime spaces run the platform Sudan invented" is supported by place, technology and idea, and by **no served-text source stating the identity** — the draft carries the two builds side by side until the trace exists. **Third native-AR story** on the slate: Makanna is Arabic already (مكانّا) — recover the program's own spelling, never back-transliterate | **Ed05 recon bank, entry 5** (`content-drafts/recon/2026-08-13-ed05-sudan-digital-learning.md`) — **fills founder-mandated Africa slot #2** |

| `2026-08-16-a-ranking-is-not-a-measurement.md` (row 23) | Africa / the "best educational system" question — the ruler-choice problem, and the composite-ranking source class | Opened by **Ed05 recon 6** (`content-drafts/recon/2026-08-16-ed05-best-system-africa.md`, founder-mandated Africa slot #3). Doubles as **ruling #29**: a composite rank is citable only if its ruler is published — **named input indicators, weights, vintages, coverage/comparison set, all four**; absent any one, report the underlying indicator direct from its own register and let the reader compare. A composite may not appear as evidence at any level of hedging, and attributing it to its ingredients ("a ranking based on UNESCO and World Bank data") launders it — it inherits none of their authority and all of the blender's arbitrariness. Binding on Madār's own copy as well as on sources, which is what the founder's mandate to find the best system "by research, not assumption" actually requires | **Ed05 recon bank, entry 6** — filed by the daily run of 2026-08-16; row number assigned by that run, table row added at the weekly reconciliation the same day |

| `2026-08-17-a-voluntary-uptake-figure-carries-its-counterfactual.md` (row 24) | Egypt / the **Egyptian Baccalaureate** (بكالوريا مصر) — the second secondary-leaving certificate, and the near-unanimous opt-in | **Third Egypt row, third instrument** — row 20 is the teacher workforce, the shipped corpus holds the Knowledge Bank (05-27) and early-grade Arabic literacy (06-18); by ruling #30 none of the four merge. Registers: **instrument/ministry** (`moe.gov.eg` EN, 15 Jan 2025, read in served text — 7 subjects over two years, 4 tracks, two sittings a year; the minister's before-figures density **120 → under 50**, attendance **9% → 85%+**, **32** secondary subjects, **3m** secondary students; Article 24 anchor); **the law** (ratified **18 Aug 2025**, **17 articles**, **Article 37bis = enrolment is optional** — Gazette text **unread, draft-blocking**); **state EN mirror** (Ahram explainer 9 Jan 2025, read — subject lists, **first attempt free / EGP 500 per retake**, all attempts recorded, four-year main-stage cap); **independent** (Al Manassa, two domains, both read — **700-point** scale replacing **320**, "without abolishing" Thanaweya Amma, and the coercion question: MP **Freddy Elbaiady** after the minister's **31 Aug 2025** meeting with **~4,000** school directors, *"coercion masquerading as awareness-raising"*); **watchdog** (EIPR — **>1/3 in poverty**, wealthiest decile **22×** the poorest on education 2019-20, religion pass mark **50%→70%**, constitutional **4%/2% of GDP** — quoted at second hand this run, **draft-blocking**); **university-admission** (joint MoE/MoHE, **25 Jul 2025**, read via the Ain Shams mirror — track→faculty map published two years before the first exams, explicit "parity" with the old streams; **the mapping tables are JPEGs — a new image wall**, kin to #26); **uptake** (**99% opt-in / ~832,000 for 2026-27, ~95% for 2025/26 — single register, body unread, DRAFT-BLOCKING**); **forward voice** (minister **Mohamed Abdellatif**, Education World Forum London, 18 May 2026, read — 25m pre-university students; *"If we evaluate memorisation only, we teach memorisation"*). **Vintage finding (#21): the announced instrument is not the enacted one** — Jan 2025 state register says "designed to **replace**"; Jul 2025 bill introduces it "**without abolishing**"; both true of their own date. **Doubles as ruling #31 — a voluntary-uptake figure carries its counterfactual.** EN-side name-routing risk (row 20 inversion); the minister's own EN spelling drifts Abdellatif ↔ Abdel Latif, the Arabic is the anchor | **Ed05 recon bank, entry 7** (`content-drafts/recon/2026-08-17-ed05-egypt-baccalaureate.md`) — **fills the Egypt carry-over**, re-angled positive per the founder's 08-11 posture; row number assigned by the daily run of 2026-08-17 |

| `2026-08-18-a-credit-line-is-not-an-output-line.md` (row 25) | Rwanda / the certification of ~24,000 "uncertified" teachers — the quality debt of an access expansion, and the two instruments that opened and closed it | **East Africa slot** (the slate's first; bank was West/North/continental before). **Second Rwanda row** — `2026-07-06-rwanda-cbc-assessment.md` is the exam/CBC ruler-change story, this is the workforce; by ruling #30 they do not merge. Two instruments in one piece: the **emergency** (REB Special Programme, opened **April 2024** — four condensed TTC modules on weekends and public holidays, **36 facilitators → 600 trainers → all 30 districts**, micro-teaching against observation checklists, onsite childcare, **NESA-assessed** written + final + practical → Professional Teaching Certificate; **Wave II final assessment 10/11/12/17/18/19 Oct 2025**, eligibility = A2 holders in non-education fields) and the **permanent** (MINEDUC, **10 Aug 2026** — TTC **3 → 5 years from Sept 2026**; parental contributions rise Jan 2027, Frw 975→2,000 / 19,500→24,000 / 85,000→110,000). Origin of the debt: hiring criteria **deliberately relaxed** to staff the classroom build, **~25,000** entering as a named category. Registers: **World Bank feature 27 Mar 2026** (read — the 24,000 headline, the cascade, the salary series +10% 2019 / +40–88% 2022; ⚠ **co-authored by the implementer**, WB Senior Education Specialist *and* REB Head of Teacher Development — a programme record, **not an independent evaluation**, attribute in-sentence); **MINEDUC × WB progress note** (read — **11,000 classrooms**, 14,680 latrines, 13/16 model schools 98%, 9/16 TTCs 91%, 621 retaining walls 100%; **page carries no visible date — pin before print**); **NESA** (Wave II announcement located only via aggregator mirror — corroborate on `nesa.gov.rw`); **Education Statistical Yearbook 2024/25** = the **denominator register, unread, draft-blocking**. **Doubles as ruling #32 — a credit line is not an output line.** Also banked: the **duration seam** ("in less than one year" vs April 2024 → Oct 2025 and two waves — name the clock or drop it, #19); **funnel gap (#14)** — one number (24,000) standing in for five, pass rate unpublished, **draft-blocking**; **voice tier unusually complete but single-channel** (Pelagie Abayikunda, primary French teacher, Kigali — operational, quoted at length; Bahati/NESA, Murasira + Mutezigaju/REB; no independent evaluator or union voice → state the absence, Oman route-2 precedent #11); **#28b gains Kinyarwanda routing** — names route by Kinyarwanda phonology, never by French-influenced readings of the Latin spelling (`u` = /u/ → و) | **Ed05 recon bank, entry 8** (`content-drafts/recon/2026-08-18-ed05-rwanda-teacher-certification.md`) — **fills the East Africa gap**; row number assigned by the daily run of 2026-08-18 |
| `2026-08-23-a-parity-check-counts-files-not-language.md` (row 26; filed 08-23 as a daily addendum, merged into the table at the 09-06 consolidation — likewise rows 27–38) | QA / bilingual parity — ruling **#33** | A parity check must read the words on the page, not count the artefacts behind it; markup that declares a language is a claim, and a claim is checked (`lang="ar"` around Latin text passes every audit built for a *missing* `lang`). A controlled vocabulary is data; data crossing a language boundary needs a display layer (`i18n-geo`). Opened by the QA pass, not a recon | **Applied 2026-08-23** — the Arabic display layer shipped; `qa_ar_language` = standing assertion |
| `2026-08-24-a-total-is-a-figure-and-must-be-added.md` (row 27) | Figure discipline — ruling **#34** | Every published table is added before any number in it is quoted, including its own total row — the one figure verifiable with no second source; when the printed total and its line items disagree, quote neither bare: line items, printed total, delta. Origin: the Zambia statute's own schedule | **Applied** in the Zambia pair (banked 08-27) |
| `2026-08-25-an-assertion-must-be-proved-against-a-control.md` (row 28) | QA method — ruling **#35** | A new check must FAIL on the defect it was written for AND stay silent on a case already known correct; the first number a new assertion reports is a hypothesis about the assertion. Carries two sub-findings: a guard fails on the condition it names, not its neighbours (the `sitemapLastmod` three-failure split); the assistive-technology surface | **Standing** — every assertion since (#8 robots, #9 clusters, the verify job) proved both ways before landing |
| `2026-08-26-a-promise-must-be-derived-not-declared.md` (row 29) | Markup contracts — ruling **#36** | When markup claims something about an asset, compute the claim from the asset in the same expression so the two cannot be separately true; a broken promise has two fixes (supply the thing / make the promise underivable without it) — only the second fixes the class. Origin: `twitter:card` promised a large image the front doors never had (93 days) | **Applied** — `Base.astro` derives card type from the asset; front-door brand card shipped |
| `2026-08-26-zambia-arabic-composition-and-bantu-name-routing.md` (row 30) | Arabic composition — Bantu orthography (extends #28b) | `ph` in Bantu orthography is an aspirated /pʰ/, never /f/ — Phiri → **بيري** (not فيري); the sibling `ch` = /tʃ/ (شيلالا) matches English habit, so nobody generalises either way. Converts an unresolvable per-name question into a resolvable per-language one; five named humans CONFIRMED | **Applied** in the Zambia AR (banked 08-27); durable for the 17-country edition |
| `2026-08-27-a-contract-in-prose-is-a-declaration.md` (row 31) | Interface conventions — ruling **#37** | A convention living in a comment binds nobody, including its author within 24 hours; compute the invariant where the value enters and assert by EXISTENCE (dereference every promise back to `dist`), not presence. Origin: the five brand-card callers broke `Base.astro`'s own commented path rule the day after the card defect was fixed | **Applied** — `qa_consumer_surface` dereferences every `og:image`; `qa_jsonld` dereferences 608 promises |
| `2026-08-28-a-source-can-drift-against-itself.md` (row 32) | Figure discipline — ruling **#38** | Vintage discipline applies INSIDE a document: a figure carries its page. When one source states a fact twice and disagrees, cite the register (body vs table vs list), pick by a stated hierarchy, never average, never choose silently; when internal arbitration fails, drop to an external register. Origin: the OPM SLEIC evaluation (0.278 body/annex vs 0.280 table; NYAF expanded two ways) | **Applied** in the Sierra Leone pair (banked 08-30) |
| `2026-08-29-sierra-leone-arabic-composition-and-labial-velar-routing.md` (row 33) | Arabic composition — West African labial-velar (extends rows 19/30) | `kp` /k͡p/ is ONE co-articulated consonant and Arabic renders both closures: Kpakra → **كباكرا** (never قاكرا or باكرا); Sackey → ساكي by heard vowel; plus Gauld → غولد, Fergal → فيرغال, Castellnou → كاستيلنو — orthographies routed by heard vowel against their letters. Seven named humans 7/7 CONFIRMED | **Applied** in the Sierra Leone AR (gated 08-29) |
| `2026-08-30-a-delivery-fingerprint-must-vary-with-the-delivery.md` (row 34) | Deploy verification — ruling **#39** | A delivery check must probe a value that CHANGES with every delivery; an invariant probe proves reachability, not currency, and false-passes on the failure it exists to catch. Probe of record: `sitemap-0.xml`; re-prove the probe's variance whenever the artifact's construction changes | **Applied** — the `verify` job (three green live runs: 08-30, 09-02, 09-04) |
| `2026-08-31-a-mirror-can-misname-the-instrument.md` (row 35) | Provenance — ruling **#40** | A programme's NAME is a figure: traced to the instrument's owner, never taken from a mirror's headline; mirror-sourced names are PROVISIONAL until the owner serves them and are never transliterated before then. Corollary: recon freshness is register-specific (two of seven Sudan registers changed serving state in 18 days, zero figures false). Origin: BRIDGE (Tribune) → **BRIDGES** (UNICEF PR) | **Applied** in the Sudan pair (banked 09-04); the Editor's re-verify-before-commission instruction is standing |
| `2026-09-01-supersede-dont-resurrect.md` (row 36) | Provenance / vintage — ruling **#41** | When a figure's register dies, ask what the owner serves today, not where the dead figure survives: a superseding first-party figure replaces it; mirrors still carrying the dead figure are not re-anchor candidates. Corollaries: a search summary is not a served register; a URL is a figure, cited as fetched, never composed from a pattern | **Applied** in the Sudan EN draft (Learning Passport register, 750 spaces / 870,000 reached, April 2024 vintage) |
| `2026-09-03-a-language-toggle-is-not-a-translation.md` (row 37) | Provenance / bilingual registers — ruling **#42** | A language switcher on a served page is a promise in the source's chrome, not a document: "an official Arabic edition exists" is a figure proved by fetching the toggle's destination and reading the target language in the served body. Corollaries: read the toggle's served state, not its label; two editions are two registers with their own dates and figures; the owner's Arabic overrules a defensible reading (Onoor → **أنور**, UNICEF's own edition). #36/#37 turned outward | **Applied** in the Sudan AR (gated 09-03); the 08-31 recon claim corrected by dated append |
| `2026-09-04-the-podium-is-not-the-document.md` (row 38) | Provenance / register hierarchy — ruling **#43** | When an owner's launch remarks and its own published report disagree about what a number means, the report rides and the podium is reportable only as the disagreement. Hierarchy stated once: document > tables > first-party written > reported speech > mirror; never average across ranks. Corollaries: a count reported by press is a mirror's count (58 → 59); a schedule is a plan (PASEC 2024: planned 2025 → announced Q4 2026); a piece built on a ruler about to be re-issued carries its own expiry. Verifier trace row: which register was each institutional figure read in | **Applied** in the slot-3 re-verification and commission (09-04); the piece is dated by design |
| `2026-09-06-the-footnote-rides-with-the-table.md` (row 39) | Provenance / register hierarchy — ruling **#44** | A disclaimer the owner prints on a table travels with every figure taken from it: a policy list set beside a gain may be reported only in the owner's shape (juxtaposed, disclaimer in the same paragraph); the causal reading is the analyst's, attributed. Corollaries: a converted figure has two owners — cite the one whose page you read (−45 HLO = −60.1 PASEC, p. 222); a trend chapter has its own coverage (10 not 14; Senegal *reduced* inequality, p. 225; Niger's national-language pupils excluded, p. 220); the owner's own hedge anchors the vintage clause (p. 224). Origin: PASEC2019 Table 5.13 fn. 47 vs the CGD blog's Niger story. AR: المعلمون المتعاقدون · فسخ العقود · عتبة الكفاءة «الكافية» · Monseur/Ki PROVISIONAL | **Filed 2026-09-06** (daily); closes Ed05 piece-4 draft-blocker 1 |

| `2026-09-08-a-numbers-noun-is-part-of-the-figure.md` (row 40) | Figure discipline — ruling **#45** | *Price · saving · target · reached · planned · avoided* are the figure, not framing around it: the same digits under a different noun are a different figure. **The eighth trace axis: type.** #27's two channels cannot catch a type error — only a read of the source **sentence** can, so the trace quotes the clause and not the number. | **Filed 2026-09-08** (Verifier, Ed05 verdict 1); row assigned at the 2026-09-20 consolidation |
| `2026-09-09-a-quotation-is-verbatim-or-it-is-paraphrase.md` (row 41) | Register discipline — ruling **#46** | Quotation marks promise the **register**, character for character; an emendation is marked or the quotation's boundary moves. **Corollary (the clock):** a plan is only a plan until its date arrives — every forward-looking date is re-read against the calendar at compose, not only against its source. | **Filed 2026-09-09** (Verifier, Ed05 verdict 2); row assigned at the 2026-09-20 consolidation |
| `2026-09-10-a-description-has-a-date-too.md` (row 42) | Non-numeric discipline — ruling **#47** | Every non-numeric attribute of a programme — what it taught, where it ran, what it was called, what prize it held, who ran it, where a sentence was said — is dated as strictly as a figure and inherits its register's vintage. A description read at one stage does not describe another. | **Filed 2026-09-10** (Verifier, Ed05 verdict 3); row assigned at the 2026-09-20 consolidation |
| `2026-09-11-a-trend-line-can-have-a-seam.md` (row 43) | Scoped instruments — ruling **#48** | A long series is not automatically a series: where the owner spliced populations, grades, instruments or scales, the join is a construction with a measured size, and that size rides with every figure crossing it. Prefer the owner's certified window; never repair a seamed figure to its arithmetic value. | **Filed 2026-09-11** (Researcher, TIMSS 2019 national-report read); row assigned at the 2026-09-20 consolidation |
| `2026-09-14-a-superlative-has-a-horizon.md` (row 44) | Provenance / register hierarchy — ruling **#49** | A superlative carries two scopes — the **ruler** and the **horizon** — and the horizon is the clause a headline cuts, because it is the one that reads like qualification. A superlative found in a title is unscoped until the body is read, even when the source is first-party: the owner wrote the sentence, the owner's sub-editor wrote the headline. | **Filed 2026-09-14** (Manager, at the slot-3 drafting) |
| `2026-09-14-a-correction-has-a-blast-radius.md` (row 45) | Correction discipline — ruling **#50** | A verdict that changes a piece changes every artefact composed from it. The correction is finished when every **derived** artefact — caption, packet, blurb, memo, brief — has been re-read against the corrected text, not when the article is fixed. | **Filed 2026-09-14** (Growth, composing the Ed05 wave packet) |
| `2026-09-15-a-claim-inherits-the-scope-of-its-register.md` (row 46) | Scope discipline — ruling **#51** | Where a claim was read is its boundary: a qualifier belongs to the cell it was printed in, a comparison to the set that was actually opened. The wider claim is almost always the more natural sentence, which is why this is a discipline and not an instinct. Binds our instruments too — an assertion's floor is scoped to the region it measures. | **Filed 2026-09-15** (Editor, Ed05 slot-3 pair verdict) |
| `2026-09-15-an-assertion-names-the-artefact-it-read.md` (row 47) | Assertion discipline — ruling **#52** | Before an assertion can be trusted about an artefact it must be able to say **which artefact it read**. A check that guesses its own input is a coincidence, and a check that sweeps zero of the thing it exists to check has failed, not passed. | **Filed 2026-09-15** (Manager + Web Developer, standing pass); row assigned at the 2026-09-20 consolidation |
| `2026-09-15-a-re-read-answers-the-question-it-was-sent-with.md` (row 48) | Non-numeric discipline — ruling **#53** | When a register is re-opened to settle one item, everything the piece takes from that register is re-settled in the same pass. **The fact a re-read is most likely to miss is the one that was never in dispute** — nothing in the errand points at it. | **Filed 2026-09-15** (Editor, Ed05 slot-3 pair verdict); row assigned at the 2026-09-20 consolidation |
| `2026-09-16-a-404-is-not-a-death-certificate.md` (row 49) | Reachability / provenance — ruling **#54** | A status code is a fact about a URL; a citation is a promise about a document. Three unreachabilities, three dispositions: **404/410** — search the publisher for the document before concluding anything (a re-slug leaves no trace); **403/429** — a wall denying *us*, carry as fetched-on-date; **refused/timeout/DNS** — prove your own egress first, then name the layer. | **Filed 2026-09-16** (held-set source re-probe) |
| `2026-09-17-a-property-of-the-content-is-not-a-property-of-the-component.md` (row 50) | Assertion discipline — ruling **#55** | When a presentational rule is a property of the **content** — script, language, direction — implementing it per-component applies it where somebody remembered and nowhere else. Express it once, keyed to the property, at a rank no component can win back; and the check reads the **resolved** value, never the declaration. | **Filed 2026-09-17** (daily QA pass; standing assertion 17); row assigned at the 2026-09-20 consolidation |
| `2026-09-18-a-convenience-answer-is-not-the-index.md` (row 51) | Assertion discipline — ruling **#56** | Where an instrument can answer with *a* representative case or with *the whole set*, the cheap question returns a well-formed answer to a narrower one. Read the index, not the summary — and when the summary is all you have, record which endpoint you asked, because the next run cannot tell from the conclusion. | **Filed 2026-09-18** (the red `qa_lastmod` deploy and the archive that said nothing was archived) |
| `2026-09-19-equal-counts-are-not-an-agreement.md` (row 52) | Assertion discipline — ruling **#57** | A count is a claim about a set, and two different sets of the same size are indistinguishable from their counts alone. Where two checks' populations must stand in a relation, the relation is asserted **by name**, element for element, every difference declared and every declared difference required to exist — never by arithmetic. | **Filed 2026-09-19** (three instruments printing 120 about three different sets; standing assertion 19) |
| `2026-09-20-a-comparator-that-returns-zero-is-a-decision-delegated.md` (row 53) | Assertion discipline / build determinism — ruling **#58** | Sorting is not a filter: a comparator that can return 0 has handed the tied elements' order to whoever supplied the input. Where the sort key is not unique the tiebreak is part of the contract and is written down; a sort whose output depends on enumeration order is a build that is not a function of its sources. **Order is a property of a list, not of a page or a corpus.** | **Filed 2026-09-20** (the same commit built twice, five pages and both feeds in a different order; standing assertion 20) |
| `2026-09-21-a-comparator-with-no-locale-asks-the-machine.md` (row 54) | Assertion discipline / build determinism — ruling **#59** | A comparison that consults the environment is not a property of the data: name the locale, the collation and the numbering system explicitly, because every default is read off the runner — and choose the fix so the assertion can state the expected answer in both languages of the repository. | **Filed 2026-09-21** (daily run, testing the 09-20 forward question; assertion 20 extended to the facet rows, `qa_ar_language` to the listing date) |
| `2026-09-22-a-date-is-not-a-moment.md` (row 55) | Assertion discipline / build determinism — ruling **#60** | A calendar date is a day and an instant is a point on a timeline; every conversion between them invents a timezone. Pin the instant to the zone the value was **parsed** in, explicitly, at every point it is read back out — never to the machine formatting it. A rendered value is asserted against **the field that produced it**. | **Filed 2026-09-22** (daily run, testing the 09-21 forward question; standing assertion 22, `qa_date_identity`) |
| `2026-09-22-three-reproductions-are-three-compressions.md` (row 56) | Sourcing discipline / non-numeric — ruling **#61** | Reproductions of one document differ in **how much of the clause each keeps**, not only in which is reachable. Take the clause from the fullest, name which one, and never quote the gloss printed beside a text. **A recorded disagreement on one axis is not a check on the others.** | **Filed 2026-09-22** (Editor's pair verdict, Ed05 row 5 — a bounded fee read as a price where the statute grants a permission) |
| `2026-09-23-a-date-that-matches-is-not-a-date-that-agrees.md` (row 57) | Sourcing discipline / non-numeric — ruling **#62** | A register's date belongs to the register; it becomes the date of the **event** only where the register says so (*today*, *yesterday*, a dateline over the act). Absent that, date the **account** and never the act. An archive capture timestamp is never a publication date. **String equality between an annotation and a sentence is not agreement between them** — the digits match while the referent moves from the document to the thing the document reports. | **Filed 2026-09-23** (Verifier's verdict, Ed05 row 5 — three of the piece's six date-transfers unlicensed, all three closed in-run in both languages) |
| `2026-09-24-a-validator-is-not-the-content.md` (row 58) | Assertion discipline / storage vs content — ruling **#63** | An identifier minted by the server that **stores** a file is a property of the storage, not of the file: two replicas of byte-identical content mint different validators. And an assertion that makes **two requests has run two experiments** — anything the infrastructure may vary between them is a variable, not a constant. Pin the scope that mints the identifier (one connection, one host, one build), or compare the content itself. **When a check flakes, ask what it is about before widening its tolerance.** | **Filed 2026-09-24** (daily run, QA lane — the 09-23 deploy went red on a green publication; standing assertion 24, `qa_feed_validators`) |
| `2026-09-25-a-digit-set-belongs-to-the-register.md` (row 59) | Composition discipline / the annotation and the sentence — ruling **#64** | Which digits a number is written in is a property of the **register**, not of the language: one Arabic page carries prose (the piece's own digits), design furniture (the designer's series set) and machine-rendered dates (the formatter's), and all three are right at once. Prose follows the piece, because a caption exists to be checked against it by eye. **And a ruling cited outside its scope licenses nothing** — read it before citing it. | **Filed 2026-09-25** (Arabic Editor's gate on eight distribution captions — two written in Arabic-Indic digits against a piece that writes Western ones, "per #59", which governs assertions and not prose) |
| `2026-09-25-run-it-twice-and-the-verdict-is-not-the-record.md` (row 60) | Assertion discipline — ruling **#65** | An assertion has two outputs: the **verdict** the build gates on and the **record** later runs read as true. The verdict can be a pure function of the artefact while the record is not. Probe it by running each check **twice against a byte-identical artefact** and comparing output *and* exit code; then close the half two runs a second apart cannot see — a coarse clock — by reading the sources. **A ruling closed in the product is not closed in the toolchain until the toolchain is read.** | **Filed 2026-09-25** (daily run, QA lane, testing the 09-24 forward question — 21 of 21 identical; `qa_dist_input` printing its record through `time.localtime`, #60's defect surviving in an instrument) |

Per-draft caveat: Test 1 is re-run per draft. The index pre-clears the *landscape*; the writer/Researcher re-checks each URL at draft time.

## 2. Production / method notes

| File | Method | One-line rule |
|---|---|---|
| `2026-06-05-rendering-stills-to-png.md` | SVG line-art still/tile → PNG via `cairosvg`, no browser | Our stills are pure line-art (`grep -c '<text'` == 0) and the wordmark is path-based, so `cairosvg` renders both exactly — pad to 1:1, never crop; eyeball a 3×3 contact sheet before sign-off |
| `2026-06-11-brand-fonts-via-npm-fontsource.md` | Rendering **text-bearing** assets (esp. Arabic) | **Line-art → cairosvg. Any real text → Playwright headless Chromium** (Chromium shapes Arabic via HarfBuzz; cairosvg silently produces disconnected letterforms — never ship cairosvg-rendered Arabic). Fonts come from npm `@fontsource/*` (GitHub release assets are egress-blocked), `@font-face` via `file://`. Always verify by *looking at the PNG*. |
| `2026-06-08-egress-wall-is-bash-only-use-web-tools.md` | Tracing figures when the primary host fails from `bash` | **The egress wall is bash-only.** `curl`/`wget`/`requests` are restricted; `WebSearch` + `web_fetch` are not. Pattern: `WebSearch` the source (puts the canonical URL in provenance) → `web_fetch` that exact URL. Exhaust this *before* concluding a figure is untraceable or escalating to a founder allowlist action. **Supersedes the operative conclusion of `2026-06-07-sourcing-across-egress-boundary.md`** (retained as the origin note; its "allowlist the hosts" founder ask is **withdrawn — resolved**). |
| `2026-06-12-url-binding-migration-playbook.md` | When to move to a custom domain (the migration-cost clock) | A URL is an accumulating ledger of bindings; migration cost is a *clock, not a constant* and steps up each time a new system (Search Console, newsletter, inbound links, citations) copies your URL into storage you don't control. **Migrate before the next binding fires, or pay twice.** Includes the verified GitHub Pages cutover sequence. |
| `2026-06-15-astro-build-in-sandbox-vite-cache-route.md` | Running a real `astro build` inside the sandbox | The cold build is **not** a capability gap. `node_modules` is present on FUSE; the only obstacle is FUSE's `unlink` ban (Vite dep-cache + `dist/` cleanup). **Build from a tmpfs copy with `VITE_CACHE_DIR` set** → exit 0. Retires the "cold build carried to weekly" caveat. Count pages on tmpfs `dist/`, never on FUSE (stale files inflate it). **Refined 2026-06-25:** `mktemp -d` + `cp -RL` (not `cp -a` into a reused `/tmp` dir), invoke `./node_modules/.bin/astro` directly (not `npx`), and delete `.fuse_hidden*` ghosts from the copy before building (they cause spurious duplicate-id warnings; gitignored, so production never sees them). |
| `2026-06-16-sourcing-one-party-policy-reform-and-vietnamese-transliteration.md` | Sourcing a one-party/parliamentary reform + Vietnamese→Arabic names | **Name BOTH instruments** (Party decision *and* legislature's resolution) with their own dates; never collapse them; distinguish adoption from signing/certification. Carries the *tuition-free ≠ fee-free* fence (source the gap to the country's own press). Includes the reusable **Vietnamese→Arabic transliteration table** and a **frontmatter-key-parity** QA ruling (a missing `hero:` block builds clean, ships broken). |
| `2026-06-17-arabic-dek-character-cap-includes-tashkeel.md` | Capped frontmatter strings (`dek`/`title`) vs Arabic tashkeel | The 200-char `dek` cap counts **Unicode code points**, so each tashkeel mark adds +1 — a fully-voweled AR dek fails at ~150 visible chars. **Keep the body voweled, the dek lightly voweled; count code points before staging** (both languages). Includes a copy-paste check for the pre-stage routine. Surfaced by the real build (Korea AIDT). |
| `2026-06-18-measured-vs-announced-and-dating-recirculated-baselines.md` | A programme that reports its own scale through several bodies (Egypt NPDA) | **Same programme, different reporting body = different currency.** Lead with the independently-**measured** figure (Innocenti 180k/+17%), label the rest **announced** (comms ½m; govt 1.38m), and **never sum across** — the seam is the story. Companion: **date recirculated baselines** (Egypt's "6-in-10" is pre-programme learning-poverty data, not a 2025 result). Third dimension of ruling #4. |
| `2026-06-19-the-wrong-instrument-figure.md` | A real figure wielded in a policy dispute it cannot adjudicate (Philippines PISA) | **A correctly-read number can still be the wrong instrument.** Before a figure carries causal/adjudicating weight, match it to the policy's **population, timing, mechanism** (PISA: 15-yr-olds / post-grades / non-home-language → fails all three vs a K–3 home-language policy). If any fails → descriptive not dispositive; **say so in print, deny it to both sides.** Fourth dimension of ruling #4. Carries the **reversal-honesty frame** (dispute not verdict; locate where it's actually settled). |
| `2026-06-20-publish-what-you-can-read.md` | Primary surface unreachable + secondary tallies inconsistent (Oman OAAAQA ratings) | **Publish only the subset you can read; state what you withheld.** When the portal can't be reached and the press band-tally won't reconcile (59 ≠ 58), run only the triangulated facts (58-school reach; the one named bottom-band school) and say in print that the breakdown is withheld — on a transparency story the disclosure *is* the credibility signal. Carries the **adopt-as-the-model-retreats** timing check (Oman/Ofsted) and the **route-2 conditional-closure** pattern. Fifth dimension of ruling #4 → ruling #11. |
| `2026-07-06-local-build-is-not-the-deploy.md` | Ruling #16 — the gate runs what the runner runs (07-02→07-06 outage, layer 1: the build) | **A passing local build is not a passing deploy.** When dependencies change, the gate runs `npm ci` from the committed lockfile — the only check that runs what Actions runs; lockfiles are never hand-edited; live deploys are verified by a marker only the new deploy can serve. Third instance of the meta-pattern (sitemap 06-07, dek-count 07-04, lockfile 07-06): *verify the thing itself, in the environment that judges it.* |
| `2026-07-07-deploy-pages-is-its-own-failure-class.md` | Ruling #17 — origin-commit-is-not-the-deploy-either (outage layer 2: the publish) | **The deploy is two gates: build, then publish.** A green build and a landed push can still leave the site stale — `deploy-pages@v4` fails on Pages *configuration* (Source ≠ "GitHub Actions", environment protection rules), which no code push can fix. Diagnostic: read the run's **jobs** via the Actions API, never its headline conclusion; settle build-side vs publish-side before touching code. **Closed by resolution 07-07** (founder flipped Pages Source → GitHub Actions; runs 38–40 green). |
| `2026-07-09-reconcile-deployed-corpus-vs-brief.md` | Ruling #18 — state verification when the brief and the repo disagree (Ed03 premature-publish) | **A brief is a plan; the commit is fact; the sitemap is truth — verify the deployed state, never the narrative.** The reliable hold is the `approved:false` **flag**, not a moved file (`git add -A` + the FUSE boundary sweep directory moves back in). Step-1 checklist: approved-EN == approved-AR == sitemap count; last brief's panel == `madar_stats.py` **approved** numbers. Companion to #16/#17 (the corpus has its own gate); paired with the 07-09 `madar_stats.py` fix (panel now reports the live corpus + discloses held drafts). |
| `2026-07-11-carry-the-guardrail-in-the-sentence.md` | Ruling #19 — turning recon §6 risk notes into reader-facing prose (Ed04 India verification) | **When recon flags a risk on a claim, write the guardrail into the sentence that makes the claim — don't just avoid the error silently.** India carried all four recon guardrails (comparability / cadence-status / scope / press-tier) in-print, which protected the reader *and* collapsed verification to one pass. Generalises ruling #4 from figures-with-scope to the whole class of guardrails; verify-side check: *each risk-bearing claim self-fences in the prose, not only in recon.* |

## 3. The ruling register — #1–#65, one line each (CANONICAL)

**Built 2026-08-23** (weekly review, §3). Until today the ruling series existed in three places at once: §3b's long-form prose (which stops at #13), §0's seven axes (which covers #12, #14, #21–#25 only), and the dated addenda (which carried #15–#32 as narrative and nothing else). **Ten rulings — #16, #17, #18, #20, #26, #27, #28, #29, #30, #31, #32 — had no one-line statement anywhere a drafter would look.** A rule a writer cannot find in ten seconds is not a rule the team has; it is a rule the team wrote down. This table is now the single canonical register. Long-form for #1–#13 is §3b below; long-form for #14–#32 is the dated file named in the last column, and the origin case is in the archived provenance ledger.

**How to use it:** the Researcher reads the register at recon, the drafter re-reads the rows their figures touch, the Verifier works the seven axes (§0) plus every register row the piece's *shape* triggers. A ruling that finds nothing to check has **failed**, not passed (the silent-pass trap, 2026-08-16).

| # | Name | The rule, in one line | Long-form |
|---|---|---|---|
| 1 | Operational, not ceremonial, voice | Unpark on the person who *runs* the mechanism, datelined 2024+ — never the one who announced it. | §3b·1 |
| 2 | Tiered-sourcing asymmetry | A flagship over-documents the minister and under-documents the classroom, in both languages; plan sourcing around that from day one. | §3b·2 |
| 3 | Evaluator-on-record trace tier | A figure may run without the primary report only if all five conditions hold; a named evaluator describing her own trial *is* a primary source. | `2026-06-10` |
| 4 | A program name is not a citation | Pin every figure to report · cycle · scope · administrator; never average two scopes that share a name. | §3b·4 |
| 5 | The blocked tool is not the unreachable resource | A failed `curl` is a statement about bash, not about the open web — re-run through the web tools first, not last. | `2026-06-08` |
| 6 | Render is a method, not a gap | Confirm an asset class is not simply an un-banked one-time recipe before calling it a reason to hire. | §3b·6 |
| 7 | URL-binding migration clock | Any platform that copies our URL into storage we don't control is a binding; sequence the domain decision ahead of it. | `2026-06-12` |
| 8 | The dual instrument | A reform's headline is not its legal instrument; name the direction-setting decision and the budget-carrying law separately, each with its own date. | `2026-06-16` |
| 9 | Measured vs announced | Independently-measured and programme-announced figures are different currencies: lead with the measured one, label the rest, never sum across the seam. | `2026-06-18` |
| 10 | The right instrument | Before a figure carries causal weight, match it to the policy on population, timing and mechanism; if any fails it is descriptive, and say so in print. | `2026-06-19` |
| 11 | Publish what you can read | When the settling surface is unreachable and second-hand tallies don't reconcile, publish only the triangulated subset — and state the withholding. | `2026-06-20` |
| 12 | Self-administered vs external | Ask who held the ruler; tag every headline figure by administration model, and when two disagree print both and name the gap. | `2026-06-24` |
| 12b | Comparability is not independence | Administration and coverage are two axes; the more independent measure is not automatically the more comparable one. | `2026-06-25` |
| 13 | Frame-sibling spacing | A diptych is a strength only if the siblings share the throughline and differ in entry, take, still and spacing. | `2026-06-25` |
| 14 | The pathway funnel | Target · potential · choice · placement · arrival are five numbers owned by different bodies; name the stage, never chain them into a trend. | `2026-07-02` |
| 15 | Terminology routes by origin | Arabic terminology and transliteration are decided by the term's own origin and register, not by the English spelling in front of you. | `2026-07-04` |
| 16 | The gate runs what the runner runs | A local build is not the deploy; verify in the environment that judges, not in `dist`. | `2026-07-06` |
| 17 | The deploy is its own failure class | A green build and a green deploy are two different runs — `deploy-pages` fails on config the build never sees. | `2026-07-07` |
| 18 | The repo outranks the brief | When brief and repo disagree, the repo and the live site are the state; the brief is yesterday's artefact. | `2026-07-09` |
| 19 | Carry the guardrail in the sentence | A recon's risk note becomes reader-facing prose in the sentence the figure rides in, not a caveat somewhere else. Repetition across outlets is not independence. | `2026-07-11` |
| 20 | A 404 is an observation, not a verdict | A wall means this route failed, not that the source is absent; record the wall, try another route, never infer absence from it. | `2026-07-12` |
| 21 | A recount is not a revision | **Vintage axis.** Has the system of record *adopted* the number, or is a later author recounting it? Attribute recounts to author and date; a dated wrapper is not a dated indicator. | `2026-07-13` |
| 22 | A qualifier keeps its scope | **Scope axis.** Don't split a shared qualifier or narrow one the source drew broadly. | `2026-07-15` |
| 23 | An approximation has a direction | **Direction axis.** Trace "almost" / "over" to the same sentence that gave the value; adjacent vintages flip it. | `2026-07-17` |
| 24 | A series point carries its position | **Position axis.** A point carries its column and base note; never assert cross-year sameness of denominators. | `2026-07-21` |
| 25 | A conditional rate carries its condition | **Condition axis.** Write "out of ___" for every rate; don't chain rates from different conditions. | `2026-07-22` |
| 26 | A number you cannot quote is not a citable figure | The video wall and the image wall: a source served but not machine-readable yields an observation, not a citation. Mark figures read off images or dashboards as such. | `2026-07-27` |
| 27 | Two independent channels satisfy the split | The drafter≠verifier split is satisfied by two genuinely independent verification channels, not by two readings of the same fetch. | `2026-07-29` |
| 28 | Arabic composition conventions | **28a:** acronyms keep their source-language Latin tag, never re-initialised from the Arabic gloss. **28b:** names route by origin, part by part — the mixed-origin byline is the general case, not the exception; on native-Arabic stories the risk inverts to the English side. | `2026-08-02` |
| 29 | A ranking is not a measurement | A composite rank is citable only if its ruler is published — named indicators, weights, vintages, coverage. A superlative is a figure and carries its comparison set. A commission is not a source. | `2026-08-16` |
| 30 | A lineage claim is a figure | "B descends from A" is a load-bearing claim traced to a source *stating* the relationship, not one from which it could be inferred. | `2026-08-16` |
| 31 | A voluntary-uptake figure carries its counterfactual | A take-up rate measures preference only if the alternative stayed genuinely available on comparable terms. All three answered → "N% chose"; any unanswered → "N% enrolled," gap named. Attaches to the word *optional*. | `2026-08-17` |
| 32 | A credit line is not an output line | Whose ledger is this number from? National figures attach to national programmes, project figures to projects; where only the credited framing exists, write "a national programme that X part-financed." | `2026-08-18` |
| 33 | A parity check counts files, not language | A bilingual parity check must read the words on the page, not count the artefacts behind it. **Corollary:** markup that declares a language is a claim, and a claim is checked — `lang="ar"` around Latin text passes every audit built to catch a *missing* `lang`. A controlled vocabulary is data; data crossing a language boundary needs a display layer, and the display layer needs its own assertion. | `2026-08-23` |

| 34 | A total is a figure and must be added | Every published table is added before any number in it is quoted — including, and especially, its own total row; the total is the one figure verifiable with no second source. **Corollary:** when a printed total and its own line items disagree, the disagreement is the story — quote neither bare; give the line items, the printed total, the delta. | `2026-08-24` |
| 35 | An assertion is proved against a control | #16's proof is half a proof: a new check must FAIL on the defect it was written for AND stay silent on a case already known correct. **Corollary:** the first number a new assertion reports is not a finding — it is a hypothesis about the assertion. | `2026-08-25` |
| 36 | A promise is derived, not declared | When markup makes a claim about an asset, the claim is COMPUTED from the asset in the same expression, so the two cannot be separately true. **Corollary:** a broken promise has two fixes — supply the missing thing, or make the promise underivable without it; only the second fixes the class, take it in the same commit. | `2026-08-26` |
| 37 | A contract in prose is a declaration | An interface convention that lives in a comment binds nobody, including its own author within twenty-four hours. An invariant is computed where the value enters — and asserted by EXISTENCE (dereference every promise back to `dist`), not by presence. | `2026-08-27` |
| 38 | A source can drift against itself | Vintage discipline applies INSIDE a document — a figure carries its page. When one source states a fact twice and disagrees, cite the register (body vs table vs list), pick by a stated hierarchy, never average, never choose silently; when internal arbitration fails, drop to an external register. | `2026-08-28` |
| 39 | A delivery fingerprint must vary with the delivery | A check verifying delivery must probe a value that CHANGES with every delivery — an invariant probe proves the origin is reachable, not current, and false-passes on the failure it exists to catch. Choose the probe by asking what varies; prefer the widest single-file fingerprint; re-prove the probe's variance whenever the artifact's construction changes. | `2026-08-30` |
| 40 | A mirror can misname the instrument | A programme's NAME is a figure — traced to the instrument's owner, never a mirror's headline; mirror-sourced names are PROVISIONAL until the owner serves them, and never transliterated before then. **Corollary:** recon freshness is register-specific — re-verify every register against the web as served before commission. | `2026-08-31` |
| 41 | Supersede, don't resurrect | When a figure's register dies, ask what the owner serves today, not where the dead figure survives; a superseding first-party figure replaces it, and mirrors still carrying the dead figure are not re-anchor candidates. **Corollaries:** a search summary is not a served register; a URL is a figure — cited as fetched, never composed from a pattern. | `2026-09-01` |
| 42 | A language toggle is not a translation | A switcher in a source's chrome is a promise, not a document: an edition exists only when the toggle's destination is fetched and the target language is read in the served body. **Corollaries:** read the toggle's served state, not its label; two editions are two registers (own dates, own figures — a pair carries only what both share); the owner's own Arabic overrules a defensible reading. | `2026-09-03` |
| 43 | The podium is not the document | When an owner's launch remarks and its own published report disagree about what a number means, the report rides and the podium is reportable only as the disagreement. **Hierarchy, stated once:** document > tables > first-party written > reported speech > mirror — never average across ranks. **Corollaries:** a press count is a mirror's count; a schedule is a plan; a piece built on a ruler about to be re-issued carries its own expiry. | `2026-09-04` |
| 44 | The footnote rides with the table | A disclaimer the owner prints on a table travels with every figure taken from it — the owner's juxtaposition may be reported, the analyst's causal reading is attributed to the analyst; a converted figure has two owners (cite the page you read); a trend chapter's coverage is narrower than the study's; the owner's own hedge anchors the vintage clause. | `2026-09-06` |

| 45 | A number's noun is part of the figure | *Price / saving / target / reached / planned / avoided* are the figure, not framing around it — two sentences with the same digits and different nouns carry different figures. **The eighth trace axis: type.** **Corollaries:** a saving is not a price, and inferring the missing term adjudicates a source against itself (#38); the word *unambiguously* is a claim and is traced harder than the figures beside it; two channels of *figure* checking (#27) cannot catch a *type* error — only a read of the source **sentence** can, so the trace quotes the clause, not the number. | `2026-09-08` |

| 46 | A quotation is verbatim or it is paraphrase | Quotation marks promise the **register**, not the meaning: anything inside them is what the source served, character for character. An emendation — even a correct one, even an obvious typo fix — is **marked**, or the quotation's boundary moves so the disputed word falls outside it. **Why it is not pedantry:** an unmarked emendation is indistinguishable, afterwards, from the source having changed underneath us. **Corollary 1:** capture the quotation verbatim at compose, because #27's two routes prove nothing about an orthography — a second reading of a changed page cannot tell you what the first page said. **Corollary 2 (the clock):** *a plan is only a plan until its date arrives* — every forward-looking date is re-read against the calendar at compose, not only against its source; a source is checked for what it says, a date for whether it has happened. Adds two non-figure checks to the trace: **quotation** and **clock**. | `2026-09-09` |

| 47 | A description has a date too | Every **non-numeric** attribute of a programme — what it taught, where it ran, what it was called, what prize it held, who ran it, where a sentence was said — is dated exactly as strictly as a figure, and inherits the vintage of the register it was read in. **A description read at one stage does not describe another stage.** **Why it needed a number:** our figure discipline is entirely numeric — multiset comparison, one-sided grep, prohibited-figure grep, the eight axes — and all three of the Sudan items were invisible to every one of them; item 2 was invisible *precisely because the correct number was absent*, and a check that compares the numbers present cannot see a year that was never written. **Corollary 1:** an award year and its announcement year are two dates (generalises to edition / fiscal / cohort / publication year, of which a press page usually prints only the last). **Corollary 2:** a setting is a claim about a register — *at the ceremony*, *in a speech*, *at the launch* are read off the register, never inferred from the occasion it marks; #43 mirrored onto the reporting side. **Corollary 3:** a guardrail is a claim and a claim is checked (#33 turned on our own hedging) — the invented venue sat *inside* a fence the piece built in order to be careful. **Corollary 4:** the annotation is written with the register open and the body from memory of the annotation — second consecutive verdict where `sources[]` was right and both bodies wrong on the same fact, two pieces and two drafters apart, which makes it a workflow property. | `2026-09-10` |

| 48 | A trend line can have a seam | **A long series is not automatically a series.** Where the owner has spliced measurements of different populations, grades, instruments or scales, the join is a construction *the owner made*, it has a measured size, and that size is part of every figure crossing it. Three obligations, in order: find the seam before quoting the span (ask what changed between the endpoints besides the thing measured); a figure that crosses a seam carries the seam in the same sentence, at the size the owner gives it; **prefer the owner's certified window** — a longer span assembled by a reader is the reader's construction, not the owner's finding. **Why nothing caught it:** both endpoints were real, the arithmetic was right, the owner was first-party — a multiset check compares numbers *present*, and #27's two channels confirm both endpoints precisely because both are true; the seam is not a number but a boundary the figure steps across, found only by reading the axis of the chart. **Corollary (a bridge is a finding):** the seam's size is itself a citable measurement — the owner measured both grades in one year *in order to* join the arms, and in South Africa 2003 a school year was worth 21 TIMSS points. **Corollary (the unit's owner):** *one standard deviation* on a scale fixed at mean 500 / SD 100 **by construction** is a SCALE UNIT, not an effect size on the measured population — say "N points, which is one standard deviation on the X scale", never "a one-SD gain". Fourth shape of the scoped-instrument family. **Never repair a seamed figure to its arithmetic value** — 0.9 SD does not become correct at 0.96. | `2026-09-11` |

| 49 | A superlative has a horizon | A superlative is a figure (#29) and carries **two** scopes: the **ruler** it is measured on and the **horizon** it is measured within. A superlative that arrives with its horizon in the *next* clause arrives without it. **Corollaries:** a headline is where the horizon goes missing — not through bad faith but through length, because the bounding clause is the one that reads like qualification, so treat a superlative in a title as unscoped until the body is read; **time-superlatives are the least examined** (*the best system* is heard as a ranking and challenged, *the last evaluation* is heard as a fact about the world and repeated — both are rankings, the second ranked on a timeline); a headline does not ride even when the source is first-party, because the owner wrote the sentence and the owner's sub-editor wrote the headline (#43 moved from the podium to the masthead). **The test is one question: bounded how, and by when?** | `2026-09-14` |
| 50 | A correction has a blast radius | A verdict that changes a piece changes **every artefact composed from that piece**. The correction is finished when each derived artefact — social caption, packet, newsletter blurb, engagement note, brief, edition memo, review — has been re-read against the corrected text. **Why nothing saw it:** the Verifier traces the piece and a packet has no `sources[]`; the standing assertions enumerate `dist` and `social-drafts/` is not built; the 08-23 artefact-perimeter rule reaches rendered HTML and a markdown packet is neither published nor rendered. **Corollary:** a promise about *figures* cannot see a claim — the packet's own header forbade unshipped figures and the defect was a **noun**. Seventh member of the enumeration family and the second outside `dist`. | `2026-09-14` |
| 51 | A claim inherits the scope of its register | Every claim is read somewhere, and where it was read is its **boundary**: a qualifier belongs to the cell it was printed in, a comparison to the set that was actually opened. Carrying either wider is a new claim that nobody made. **Corollaries:** a significance mark belongs to its cell — *significant at that level* is not what a footnote on one column says; a comparison is bounded by the set actually opened (one country brief against its own regional averages supports *clear of the region's pattern*, never *the continent's outlier*); **the wider claim is almost always the more natural sentence**, which is why this is a discipline and not an instinct; a superlative asserted while a neighbour is deliberately unnamed is the same claim with the evidence withheld (#49 from the other side); **and it binds our instruments** — a floor set over a region where the defect cannot appear is not a measurement, so the bite is measured *at the floor*, not merely shown to have changed the artefact. | `2026-09-15` |
| 52 | An assertion names the artefact it read | Before an assertion can be trusted about an artefact it must be able to say **which artefact it read**; a check that guesses its own input is not an assertion but a coincidence, and a check that sweeps **zero** of the thing it exists to check has failed, not passed. Origin: the same tool, same build, same minute, printed `pages 67 · nodes 0 · CLEAN` from the repository root and `pages 120 · nodes 308` from a directory where it could not guess wrong. **The family's third question:** #16 asks *in which environment*, #35 *against which control*, #52 *on which artefact*. | `2026-09-15` |
| 53 | A re-read answers the question it was sent with | When a register is re-opened to settle one item, **everything the piece takes from that register is re-settled in the same pass** — a re-read that fixes only its errand certifies the whole citation by having touched part of it. **The sharper half:** the fact a re-read is most likely to miss is the one that was never in dispute — it has been there since compose, nothing in the errand points at it, and it is therefore invisible. Origin: a register re-opened to fix an attribution verb returned three facts, two travelled into the prose, and the third — the cited person's **name** — was filed in the annotation and left out of both bodies. Third instance of the non-numeric family's repeat shape. | `2026-09-15` |
| 54 | A 404 is not a death certificate | A status code is a fact about a **URL**; a citation is a promise about a **document**. When a cited URL stops serving, the question is not *is this source dead* but *where does this document live now* — answered by searching the publisher, never by reading the code. **The three unreachabilities, which are three findings:** **404/410** with a served body means a live publisher denying a **path** — search it for the document before concluding anything, because a re-slug is the commonest cause and leaves no trace; **403/429** or a challenge means a server denying **us** — a wall, not a death, carry as fetched-on-date; **connection refused / timeout / DNS** means nothing answered and carries no information about the document at all — prove your own egress (#52), then name the layer and never call it a 404. #41's missing third option. **Fourth signature, 09-17:** when the publisher's own root 404s there is no publisher left to search, and the disposition falls to the archive tier (#43). | `2026-09-16` |
| 55 | A property of the content is not a property of the component | When a typographic or presentational rule is a property of the **content** — its script, its language, its direction — implementing it per component guarantees it is applied where somebody remembered and nowhere else. Express it **once**, keyed to the property itself, at a rank no component can win back. **And the check reads the RESOLVED value, not the declaration:** a grep over source enumerates the rules somebody wrote, while the defect lives in the cascade, where the worst instances arrive by inheritance from a rule that names no component at all. Origin: 149 Arabic runs served with their letter-joins tracked apart, across both editions, from a stylesheet that had the principle right for headings and nowhere else. **The family's fourth question: at which layer?** | `2026-09-17` |
| 56 | A convenience answer is not the index | Where an instrument can answer either with **a** representative case or with **the whole set**, asking the cheap question returns a well-formed answer to a narrower question than the one you meant. **Read the index, not the summary** — and when the summary is all you have, record **which endpoint you asked**, because the next run cannot tell from the conclusion. The danger is not that the convenience answer is malformed; it is that it is *perfect* — correctly dated, correctly marked 200, correctly derived — and nothing in it says *there are four more of me* or *I do not govern that URL*. Two origin cases in one day: an availability API returning `{}` for a URL whose CDX index held five captures, three of them readable in full; and a `lastmod` check that modelled two of its resolver's three branches and failed the deploy on the one URL it had never been checking. | `2026-09-18` |
| 57 | Equal counts are not an agreement | A count is a claim about a **set**, and two different sets of the same size are indistinguishable from their counts alone — so the equality of two cardinalities is **not** evidence that two instruments agree about anything. Where two checks' populations must stand in a relation, the relation is asserted **BY NAME**, element for element, with every difference declared and every declared difference required to exist. Stated as the failure it prevents: a page can be dropped from the sitemap and a different page added to it in the same commit, and every number this operation prints will be unchanged. Origin: three instruments printing **120** about three different sets of 120 pages, side by side in every QA log we have written, with every arithmetic identity between them true. | `2026-09-19` |
| 58 | A comparator that returns zero is a decision delegated | **Sorting is not a filter.** A comparator that can return 0 has not ordered the tied elements — it has handed the decision, silently, to whoever supplied the input. Where the sort key is not unique the **tiebreak is part of the contract and is written down**; a sort whose output depends on enumeration order is a build that is not a function of its sources. **Corollary 1:** a stable sort does not make a partial order total — stability guarantees only that equals keep their *incoming* order, which means the answer is exactly as determined as the input was. **Corollary 2:** order is a property of a **list**, not of a page or a corpus — two adjacent lists may each be correctly ordered and their concatenation may not be. Origin: the same commit built on two runners served five sitemap pages and both feeds with the same pieces in a different order, because eight of nine sort sites compared dates only and 24 of 38 pieces sit in a tie group. | `2026-09-20` |
| 59 | A comparator with no locale asks the machine | A comparison that consults the environment is not a property of the data. Where a build sorts, formats or folds text, the locale, the collation and the numbering system are named **explicitly** — every one has a default and every default is read off the runner. **Corollary 1:** the defect hides behind the data you happen to have — the key that exposes it can sit in the corpus for weeks, held out of the sorted set by a threshold that is a fact about our own output, not a guarantee. **Corollary 2:** the fix is chosen so the assertion can predict it — ICU collation is deterministic but unstateable in Python, so the comparison is normalised (NFD, combining marks dropped, lower-cased) and the two halves agree by construction (#36). **Corollary 3:** formatting is the same slip in a second costume — `Intl.DateTimeFormat('ar')` numbers in `latn` and `'ar-EG'` in `arab`. #58 asked *is the order determined*; this asks **determined by what**. Origin: `facetsFor` tie-broken by a bare `localeCompare`, which reverses `Türkiye` against `Turkmenistan` between two runners; and nine Arabic pages serving `7 يوليو 2026` for seven days beside an Arabic home serving `٢٨ يوليو ٢٠٢٦`. | `2026-09-21` |

| 60 | A date is not a moment | A calendar date is a **day**; an instant is a point on a timeline. Every conversion between them invents a timezone, so the instant is pinned to the zone the value was **parsed** in, explicitly, wherever it is read back out — never to the machine that formats it. **Corollary:** a rendered value is asserted against *the field that produced it*, never against another rendering of the same field. Origin: `z.coerce.date()` parses a bare `YYYY-MM-DD` as UTC midnight, eight `Intl.DateTimeFormat` sites formatted it with no `timeZone`, and the same commit built under `TZ=UTC` and `TZ=America/Los_Angeles` differed on **116 of 121 pages** — a 2026-07-07 piece printing *July 6, 2026*. Four months of correct datelines were luck: our CI is UTC and our calendar is UTC+4. The three date checks we owned all compared a date to something other than its own frontmatter, which is exactly why all three were green. | `2026-09-22` |
| 61 | Three reproductions are three compressions | Reproductions of one document differ in **how much of the clause each one keeps**, not merely in which one is reachable — so the clause is taken from the fullest, that one is named, and the others' compressions are recorded rather than averaged. **Corollary 1:** a gloss printed beside a text is the text you will quote — #43's hierarchy applies *inside* a single document. **Corollary 2:** a recorded disagreement on one axis is the strongest camouflage for the axes nobody looked at. **Corollary 3:** a maximum that exists only if an authority acts is a permission, and is reported with its actor and its condition in the same sentence as the number. Origin: a statute's retake fee — *200 per subject per attempt*, with a Cabinet-approved permission to raise it to *400 per subject* — reached both bodies and both annotations as a flat *200 per attempt, not exceeding 400 per subject*, the unit taken from a news reproduction and the ceiling from a law practice's plain-language summary, while the one reproduction carrying the whole clause was open on the same day. | `2026-09-22` |
| 62 | A date that matches is not a date that agrees | A register's date belongs to the **register**. It becomes the date of the **event the register reports** only where the register says so — *today*, *yesterday*, a dateline over the act; a publication date over a report of an act licenses nothing about the act, and an **archive capture timestamp is never a publication date**. Absent a licence, date the account: *"In an account published on X, Y said…"*. And because both readings are written with the same digits, **string equality between an annotation and a sentence is not agreement between them** — the check passes on identity while the referent moves from the document to the thing the document describes. Corollary: a guardrail written into the prose protects the sentence it is written in and no other. | `2026-09-23-a-date-that-matches-is-not-a-date-that-agrees.md` |
| 63 | A validator is not the content | An identifier minted by the server that **stores** a file is a property of the storage, not of the file: two replicas holding byte-identical content mint different validators, because what the identifier derives from — an mtime, an inode, a path, a build host — differs between them. **And an assertion that makes two requests has run two experiments:** if it requires the two to agree about something the infrastructure is free to vary between them, it is a coin flip with a stated expectation. Pin the scope that mints the identifier — one connection, one host, one build — or compare the content itself. **When a check flakes, ask what it is about before widening its tolerance.** Origin: the 09-23 deploy red on a green publication; GitHub Pages unpacks each deploy onto replicas that finish a second apart, so every feed URL carries two permanent ETags and the cache step had been a coin flip since the feeds shipped, green twice by luck. *(Row added 2026-09-25 — filed 09-24 with §1's row, §3's heading and the range all moved, and this one-liner missed; the 09-20 rule's second leak, found by counting §3's rows against its own range.)* | `2026-09-24-a-validator-is-not-the-content.md` |
| 64 | A digit set belongs to the register, not to the language | Which digits a number is written in is a property of the **register it sits in**, not of the language of the sentence. One Arabic page carries three, each right: **prose** (body, dek, caption copy) takes the digits *the piece itself uses*, because a caption exists to be checked against the piece by eye; **design furniture** (a still's `caption:`, a hero's `alt:` series mark) takes the set the Designer has applied corpus-wide; **machine-rendered values** take what the formatter is told to emit, named explicitly (#59) and asserted every build (#60). Ask what the number is part of, never what language it is in. **Corollary 1:** a normalising check is silent on presentation *by design* — assertion 23 must fold Arabic-Indic to Western to compare an Arabic caption against an Arabic body at all, and that silence belongs to a gate, not to a wider check. **Corollary 2:** copy outside `web/src/content/` had never had an Arabic register, and the first gate over it found something on its first pass. **Corollary 3:** the ornament register has never been gated by anyone. **And the limb: a ruling cited outside its scope licenses nothing** — a register only means something while its numbers are load-bearing. Origin: a caption corrected to «منذ ٢٠١٨» "per #59", against a piece that reads «منذ 2018»; the build passed it correctly, because it is not a wrong figure, only one the reader cannot match by eye. | `2026-09-25-a-digit-set-belongs-to-the-register.md` |
| 65 | Run it twice: a correct verdict does not make a reproducible record | An assertion has two outputs and usually only one is checked — the **verdict** the build gates on, and the **record** it prints into a log that later runs read as true. The verdict can be a pure function of the artefact while the record is not. **The probe:** run each check twice against a **byte-identical** artefact and compare output *and* exit code; anything that differs reads something that is not the artefact — a clock, a network, an enumeration order, a locale, a replica, a hash seed — and must say so in its own header. **The limit, stated wherever the probe is:** two runs a second apart cannot see a **coarse** clock, so the probe's resolution is its own interval and the rest is closed by reading the sources. **Corollary 1:** the record is an artefact with readers, and #41 does not stop applying because the reader is us. **Corollary 2:** a clean determinism result says the check is *about* the artefact, never that it is correct — a check can be reproducibly wrong. **Corollary 3:** a ruling closed in the product is not closed in the toolchain until the toolchain is read. Origin: 21 of 21 identical with hash randomisation live, and `qa_dist_input` rendering its timestamps through `time.localtime` — the same artefact logged at 09:38 on the runner and 13:38 in Asia/Dubai, #60's defect surviving in an instrument three days after #60 closed it in the publication. | `2026-09-25-run-it-twice-and-the-verdict-is-not-the-record.md` |


**Series integrity: #1–#65, no gaps, no duplicates** (**range extended to #65 at the point of filing, 2026-09-25** — two rulings in one run, **#64** from the Arabic Editor's gate on the distribution captions and **#65** from the QA lane testing the 09-24 forward question; Section 1 rows 59 and 60, §1's count line and §3's heading all moved in the same commit. **And the reconciliation found the rule's second leak:** §3's table had **no row for #63**. The 09-24 run moved the range to #63, wrote its Section 1 row and its §3 heading, and did not write the one-line register entry those numbers were counting — so for one day the range claimed a row the table did not have. Written today at the number its own file claims; nothing renumbered. The leak is the same shape as 09-23's and the correction is the same: **a filing run moves every count *and every row* its ruling touches**, and the only way to catch the difference is the 09-20 instruction actually executed — *count the rows, never trust the previous number.* Previously **extended to #63 at the point of filing, 2026-09-24** — #63 from the daily run's QA lane, the second ruling in the series about a *check* being wrong about a publication that was right; Section 1 row 58, and §1's count line moved in the same commit, which is the whole of what the 09-23 amendment asked for. Previously **extended to #62 at the point of filing, 2026-09-23** — #62 from the Verifier's verdict on Ed05 row 5; Section 1 row 57. **The same commit also repaired §1's count line, which read `1 → 54` against 56 rows** — the 09-20 rule's first leak, three days old: the 09-22 run moved §3's range as required and left §1's count where it was, so the rule now reads *every count the ruling touches*, not *the range*. Previously **extended to #61 at the point of filing, 2026-09-22** — two rulings in one run, #60 from the QA lane and #61 from the Editor's pair verdict; Section 1 rows 55 and 56. Previously **extended to #59 at the point of filing, 2026-09-21**, per the 2026-09-20 RUNBOOK rule — the run that files a ruling moves the range, the §3 heading and its own Section 1 row in the same commit as the ruling file, and leaves the canonical prose and the family narrative to the weekly consolidation. #59 filed 2026-09-21 by the daily run testing the previous run's forward question; Section 1 row 54. #1–#58 verified 2026-09-20 at the weekly consolidation, file by file against this directory — **the largest single-week addition the register has ever taken, and the largest drift it has ever carried**: ten rulings, #49–#58, were filed by the daily runs between 09-14 and 09-20 and cited by number in commits, briefs and verdicts while this header, this table and this line all still read `#1–#48`. Section 1 was further behind than Section 3 — its table stopped at **row 39** while rows 40–53 existed only as dated files, so rows 40, 41, 42, 43, 47, 48 and 50 are **assigned here** and the rest are merged at the numbers their own files already claim. Nothing is renumbered. **#58 filed 2026-09-20** by the daily run diagnosing a red deploy — the first ruling in the series about the *build* rather than about a source or a check, and the one that found the publication was not a function of its sources; **#57 filed 2026-09-19**, **#56 2026-09-18**, **#55 2026-09-17**, **#54 2026-09-16**, **#51–#53 2026-09-15** (three in one run, the only such day on record), **#49 and #50 2026-09-14** (the last desktop run and the first cloud run, one each). **The week's shape, read off the numbers:** seven of the ten — #52, #55, #56, #57, #58, and #49/#51 on the reading side — are about *what an instrument's answer is actually about*, which is where a week spent on red deploys and standing assertions was always going to put them; #1–#48 verified 2026-09-13, prior verification 2026-09-06 at #1–#43 and 2026-08-30 at #1–#39.)

**The eight families the register makes visible, now that the rules sit side by side.** Worth knowing because a new ruling almost always joins one of them rather than standing alone — and this week every one of the ten did. *(The heading read "three" from 2026-08-23 until today, through four families being named under it; corrected at the 2026-09-20 consolidation.)*
- **What the number is conditioned on** — the seven axes, §0 (#12/#12b, #14, #21–#25), joined by **#51 a claim inherits the scope of its register** — #22's *a qualifier keeps its scope* generalised from the qualifier to the whole claim, and from the source's sentence to the cell, the column and the set actually opened. *Out of what?*
- **What produced the number** — the scoped-instrument family (#29 ruler, #31 alternative, #32 agent, #44's corollary a converted figure has two owners, **#48 a seamed span and a scale unit that belongs to the ruler rather than the measured**, plus §1 rows 15/19/20/21: pledge scope, multiplier base, deficit denominator, equivalent-years ruler). *Produced how, and by whom?*
- **Whether the number can be read at all** — the reachability family (#20 the 404, #26 the video and image walls, #11 publish-what-you-can-read, #27 the two-channel split, and **#54 a 404 is not a death certificate** — which splits #20's single word *unreachable* into three signatures with three different dispositions, and supplies #41's missing third option). *Can we quote it, and did two channels see it?*
- **Whether the artefact agrees with itself** — the internal-consistency family, named 2026-08-30 (#34 a table must add up, #38 a source can drift against itself; kin: row 21's three-register participant drift). *Does the document survive its own cross-check?*
- **How a check earns trust** — the assertion-discipline family, named 2026-08-30 with five members and standing at **twelve** today (**#59 added 2026-09-21, #60 added 2026-09-22**), the largest and fastest-growing family in the register (#16 judge in the judging environment, #35 prove against a control, #36 derive the promise, #37 compute the contract and assert by existence, #39 probe a value that varies with the delivery, **#52 name the artefact it read**, **#55 read the resolved value, at the layer the property lives on**, **#56 read the index, not the summary**, **#57 assert the relation by name, never by cardinality**, **#58 write the tiebreak down, because a comparator returning zero delegates the decision**, **#59 name the locale, because a comparator with none asks the machine**, **#60 pin the value to the zone it was parsed in, and assert a rendering against the field that produced it**). Its questions, in the order they were earned: *in which environment · against which control · derived from what · asserted by existence or by presence · probing what that varies · on which artefact · at which layer · from which endpoint · over which set, by name · in which order · decided by what · against which input.* **Five of this week's ten rulings joined here**, and the shape is worth naming: the first five members ask whether a check would catch the defect; the second five ask **what the check's answer is actually about** — which is the question that only becomes askable once the checks are numerous enough to disagree with each other. *Would this check catch the defect — and only the defect — and about what, exactly, is it reporting?*
- **Which register speaks for the owner** — the register-hierarchy family, named 2026-09-06 (#38 a source drifts against itself *within* a document; #40 a mirror misnames the instrument; #42 a toggle is not the edition; #43 the podium is not the document — with #41 *supersede, don't resurrect* as the family's vintage corollary, **#49 a superlative has a horizon** moving the split from the podium to the masthead — the owner wrote the sentence, the owner's sub-editor wrote the headline — and **#54** as its reachability corollary). Four rulings in five runs, one question each time: *of the several places this owner speaks, which one is the document?* The answer is a ranked list, stated once in #43 — document > tables > first-party written > reported speech > mirror — and the discipline is never to average across ranks. This family predicts where the next ruling comes from: a register that outranks the document (a court filing, a statute) disagreeing with the report.
- **Whether the words around the number are true** — the **non-numeric family, named 2026-09-13** (#45 a number's noun · #46 a quotation's orthography and a plan's tense · #47 a description's date, with its setting, its award year and its own hedge · **#53 a re-read answers the question it was sent with**, the family's repeat shape caught one layer further out — the fact most likely to survive a re-read is the one that was never in dispute). *The figure is right — is the sentence?* This family was not designed; it was **discovered by running the Verifier three days in a row**. Edition 05's first three verification verdicts returned **six findings and not one of them was a number** — on three different pieces, by three different drafters, including the most figure-dense piece the publication has ever shipped. The operation's entire verification toolkit is numeric (numeral-multiset comparison, one-sided-figure grep, prohibited-figure grep, the eight trace axes, #27's two channels), and **all six items were invisible to every instrument in it** — one of them, #47's laureate year, invisible *precisely because the correct number was absent*, and a check that compares the numbers present cannot see a year nobody wrote. Read as a finding about the discipline rather than a run of luck: the numbers are now hard enough that the remaining errors have moved into the prose. **The family's sharpest member is its repeat** — twice in two days, on two pieces and two drafters apart, `sources[]` carried the hedge correctly and **both** language bodies dropped it, which makes it a property of the workflow rather than a slip (#47 corollary 4, and the RUNBOOK rule codified today: *the annotation and the sentence are written from the same open register*). What the family predicts: the next non-numeric surface is the **attribution verb** — *said · announced · confirmed · found · estimates* each promise a different register, and nothing checks them. **Seventh member, and the first the family's own instrument cannot catch: #62 a date that matches is not a date that agrees** (2026-09-23) — the 09-13 rule's `annotation == sentence` line was written for *annotation right, body wrong*, and #62 **is** that shape and still slips through, because the annotation and the sentence print the **same digits** while the referent moves from the document to the event the document reports. Every member before it is caught by putting the citation beside the prose; this one is what is left after you have done that and the two agree. *(Register tally at filing: seven members, and the family's prediction is now 1 for 1 — the attribution verb was tested on 09-14 and bit; the date referent was tested on 09-23 and bit three times in one piece.)*

  **Update, 2026-09-20 — the family's own forward prediction was tested and it bit.** The 09-13 review closed by naming the **attribution verb** as the next non-numeric surface. It was tested on **09-14**, by a language crossing rather than a verification pass: Arabic cannot render *"the Principal Investigator **said** it"* without choosing a gender, so the HSRC release was re-opened — its verb is **cautions**, and it places the caution *inside applause*. Both restored in both languages, and the same re-read produced **#53** by leaving a third fact behind. A family that predicts where the next ruling comes from, and is then right, has stopped being a taxonomy and started being an instrument. **The surface named for next: whether a hedge survives its own paragraph** — #51's narrowing and #46's clock both appeared this week *inside sentences whose first half was correct*, which is a different failure from a hedge dropped outright and nothing looks for it.
- **Whether a green check can see the surface at all** — the **enumeration family**, whose members live in the RUNBOOK rather than the register (the flag-sweep inversion 08-09, the chrome 404 08-16, the orphan sweep 08-17, the consumer-format rule 08-18, the founder-artefact perimeter 08-23, the held-file derivation rule 09-06) and which gained its first numbered member this week in **#50 a correction has a blast radius** — the second to sit outside `dist` entirely, and the first to sit outside the *publication*. *A green check is scoped to what it enumerates — so what can today's sweep not structurally see?*

---

## 3b. Long-form for rulings #1–#13 — the generalizable lessons (apply to every candidate)

These are the transferable rules the source-specific notes paid for. The Researcher applies them at recon; the Editor checks them at verdict.

1. **The operational-not-ceremonial-voice rule (Sierra Leone / Saudi).** When a strategy PDF is the candidate, the unpark hinges on the person who *runs the mechanism*, datelined 2024+ — not the one who *announced* it. Sierra Leone cleared (Marian Abu, on classroom impact, Dec 2024); Saudi and Korea parked (only the launch/announcement tier was reachable). Run this test *before commissioning*.

2. **The tiered-sourcing-asymmetry rule (Saudi).** A government flagship over-documents the minister and the target, under-documents the classroom — in *both* languages. Plan the sourcing around that asymmetry from day one. When the operational tier is unreachable, consider the **data-led inversion** (let an official statistics report carry the why-now, require one named operational voice for dignity) before abandoning the candidate.

3. **The evaluator-on-record trace tier (2026-06-10 ruling — the load-bearing new rule).** A figure may run *without the primary report in hand* only when ALL five hold: (1) the evaluator is on the record by name + affiliation describing the finding; (2) the operator confirms separately by name; (3) the trial design is stated falsifiably (sample/arms/period); (4) every figure carries in-text attribution to the named evaluation, never Madār's own voice; (5) a standing follow-up is logged to re-verify when the report publishes. If any fails: trace to primary or park. A named evaluator describing her own randomised trial *is* a primary source — a person, not a PDF. Verdict checklist line: *Trace tier: ☐ primary in hand · ☐ evaluator-on-record (5 verified, follow-up logged) · ☐ neither → park.*

4. **A program name is not a citation — pin every figure to its report, cycle, and scope.** Two failure modes, same fix (state the discriminator in print next to the number): the **two-studies-same-name trap** (IQRA ~2020 vs 2024–25 J-PAL trial — check year + sample) and the **same-figure-different-scope trap** (Korea ₩533.3B = 2024 project allocation vs ₩1T = teacher training 2024–26; never average or reconcile — fence each to its scope or drop both).

5. **The blocked-tool-is-not-the-unreachable-resource rule (2026-06-08).** A failed `curl` is a statement about `bash`, not about the open web. Re-run through `WebSearch` + `web_fetch` first, not last. Distinguish *the tool that is blocked* from *the resource that is reachable* — it is faster and cheaper than a founder settings change, and it retires most "allowlist" escalations before they're raised.

6. **The render-is-a-method-not-a-gap rule (IG tiles).** Before treating an asset class as a capability gap (a reason to expand the team), confirm it is not simply an un-banked one-time method. Image rendering read as a Designer bottleneck across two runs; it was a `cairosvg` (then Playwright) recipe, now banked.

7. **The URL-binding migration-clock rule (2026-06-12).** Before adopting any external platform or registration, ask: *does this copy our URL into storage we don't control?* If yes, it is a binding; the migration clock steps up when it fires; sequence the domain decision ahead of it.

8. **The dual-instrument rule (2026-06-16, Vietnam).** A reform's *headline is not its legal instrument*. In one-party-plus-legislature systems a reform usually has two distinct instruments — a Party/executive **decision** (direction) and a legislature's **resolution/law** (budget) — and outlets collapse them. Name both with their own dates; distinguish **adoption** from **signing/certification**; conflate neither. The policy-instrument twin of #4. Companion fence: when a reform "removes a cost," establish what that cost legally *is* and what it is *not* (*tuition-free ≠ fee-free*), sourced to the country's own press.

9. **The measured-vs-announced rule (2026-06-18, Egypt NPDA) — the third dimension of #4.** When one programme reports its scale through several bodies at several magnitudes, they are **different currencies, not the same number**: an *independently-measured* figure (Egypt: UNICEF Innocenti, 180k students, +17%, quasi-experimental) and *announced* figures (programme comms ½m; government 1.38m / 27 governorates). **Lead with the measured one, label the rest "programme-reported," and never sum or average across the seam** — doing so launders a tested number into an untested one. The discriminator (*which figure a system leads with*) is more revealing than any single figure, and is itself the durable take. Companion sub-rule: **date recirculated baseline statistics** — a crisis baseline is often an older measurement recirculated as current (Egypt's "six-in-ten cannot read" is pre-programme learning-poverty data, WB/UNESCO, nearer seven-in-ten post-pandemic). Present it as a standing baseline with its vintage, not a fresh result; give the attributed range when credible vintages disagree.

10. **The right-instrument rule (2026-06-19, Philippines MTB-MLE) — the fourth dimension of #4.** A figure can be correctly read, in its own currency, and *still* be the **wrong instrument** for the claim it is drafted into. The Philippines reading score (PISA 2022 = 347) is real and severe, but it tests **15-year-olds**, in **Filipino/English**, **years after** the disputed K–3 mother-tongue grades — so it can name the crisis but cannot show the policy *caused* it or that ending it *cures* it, even as both camps cite it as proof. Before any figure carries **causal/adjudicating** weight (not merely descriptive), check three matches against the policy: **population, timing, mechanism**. If any fails, the number is descriptive, not dispositive — **say so in print and deny it to both sides as proof.** The number is right; the *inference* is the error. (Distinct from #4 = misreading, #9 = summing across currencies; the three compound — read correctly **and** kept in currency **and** the right instrument.) Companion: on a statutory **reversal**, frame as dispute not verdict, give both camps their strongest honest case, and locate where the question is actually settled (here the law's three-year review → the forward-looking #10 test: *measure the children you just moved*).

11. **The publishable-subset rule (2026-06-20, Oman) — the fifth dimension of #4.** When the primary surface that would settle a figure is **unreachable** *and* the second-hand tallies **don't reconcile**, the number does not run. Publish only the subset that triangulates from reachable primaries (Oman: the 58-school first-phase reach and the single named bottom-band school — **not** the press band-tally that sums to 59 ≠ 58), and **state the withholding in print.** On a transparency story the disclosure is the credibility signal, not an apology. Compounds with #4 (read-it-right) / #9 (currency) / #10 (right-instrument): a figure must clear all four to carry weight. Two companion frames banked the same day: the **adopt-as-the-model-retreats** check (when country X imports country Y's instrument, ask what Y is doing with it *now* — Oman built Ofsted-style single-verdict inspection as England scrapped it, Sep 2024), and the **route-2 conditional-closure** pattern (a candidate banked figure-OPEN + voice-GAP can still ship as an explicit systems piece **iff** the figure is withheld and the voice-gap is named in-piece — Editor's call, on record).

12. **The self-administered-vs-external-assessment rule (2026-06-24, Brazil ICA) — the sixth dimension of #4.** When a system reports its own headline outcome, ask **who held the ruler.** A figure *applied and scored by the same systems it grades* (Brazil's state-administered Indicador Criança Alfabetizada: 66% in 2025) and a figure from an *external assessor* (the federal Saeb: ~49% comparable) are **different currencies** even for the same skill and year — and the ~7-point gap is **the credibility seam, not noise to average away.** Tag every headline figure by administration model (self-administered / externally-administered / programme-announced — ascending independence; the last is #9's *announced* tier); when two disagree, **print both, attribute each, name the gap.** Watch for the instrument *settling under you* (a rising series partly reflects a comparability standard still being hardened — Brazil's INEP *IN 02/2025*), and lead with the **between-unit spread** (Ceará 85.3% vs national 66%), never the mean. Adds **administrator** as a fourth pin to #4 (report · cycle · scope · administrator). Full note: `2026-06-24-self-administered-vs-external-assessment.md`.
   - **#12b — comparability is not independence (2026-06-25, Brazil ship).** Independence and comparability are **two axes, not one**. Ruling #12 ranks figures by administration *independence* (self → external → audited); but the **more independent** measure is **not** automatically the better one to compare units against. Brazil: the federal **Saeb is independent but a sample** (per-state error >10 pts → not unit-comparable), while the state-administered **ICA is a census** on the same external cutoff (unit-comparable but not independent) — so the *self-administered* number is, here, the *more comparable* one. Tag every assessment figure on **both** axes (administration: self/external/audited · coverage: sample/census/moderated); when two disagree, **name which axis explains the gap**, and don't reflexively lead with the more independent one. Full note: `2026-06-25-comparability-is-not-independence.md`.

13. **The frame-sibling-spacing rule (2026-06-25, RAK↔Egypt; founder-flagged).** A **diptych** (two pieces sharing a throughline by design — RAK Iqra trial ↔ Egypt NPDA at national scale) is a strength **only if the two share the throughline and differ in execution.** When they also share the *staging* — near-identical opening move, majority-overlapping sources, twin hero stills, stacked in one edition — they read as one report split in two. At commission the Editor checks a frame-sibling against its nearest kin on three axes: **(1) different entry** (don't re-stage the sibling's opening — lead from a different door: scale, instrument, scene, measurement); **(2) its own durable take** (RAK = meet-the-child-in-her-language; Egypt = measured-vs-announced — if the takeaways collapse, it's one piece); **(3) spacing + visual distinction** (prefer different editions; never twin the stills). Verdict line: *Frame-sibling check: nearest kin = <slug> · entry differs ☐ · own take ☐ · still not a twin ☐ · spacing OK ☐.* Disciplines clusters; does not discourage them. Full note: `2026-06-25-frame-sibling-spacing.md`.

## Provenance ledger — archived 2026-08-23

The dated consolidation notes and daily-run addenda covering **2026-06-14 → 2026-08-09** live in [`ARCHIVE-2026-06-14_2026-08-09.md`](./ARCHIVE-2026-06-14_2026-08-09.md) — the origin cases, walls and mirrors behind rulings #1–#28. Those covering **2026-08-09 → 2026-08-23** (the 08-09, 08-16 and 08-23 consolidations and the 08-10 → 08-18 addenda — rulings #29–#32, rows 19–25, the Ed05 recon bank's opening) live in [`ARCHIVE-2026-08-09_2026-08-23.md`](./ARCHIVE-2026-08-09_2026-08-23.md), rolled 2026-09-06.

Why the split: the ledger had grown to be the longest part of this file and was read almost never, while the tables above are read constantly. The rules are canonical here; the *stories* behind them are one click away. Those covering **2026-08-23 → 2026-09-06** (rulings #33–#44, Section 1 rows 26–39, and the 09-06 consolidation section) moved to [`ARCHIVE-2026-08-23_2026-09-06.md`](./ARCHIVE-2026-08-23_2026-09-06.md) at the **2026-09-20** consolidation. Everything from the 2026-09-11 daily addendum forward stays below, and rolls into the archive two consolidations after it is written.

---

> **Addenda for 2026-08-23 → 2026-09-06 (rulings #33–#44, Section 1 rows 26–39) and the 2026-09-06 consolidation section are archived.** Moved verbatim to [`ARCHIVE-2026-08-23_2026-09-06.md`](./ARCHIVE-2026-08-23_2026-09-06.md) at the 2026-09-20 weekly consolidation, per the two-cycle rule. Nothing deleted, nothing summarised; the rules stay canonical in §1 and §3 above.

---

> **Daily-run addendum — 2026-09-11 (ruling #48; slot 3's second day-long blocker CLOSED — the TIMSS 2019 national report read in the body, one figure rides and its twin turns out not to exist; the publisher entity found dangling on all 76 pages for 19 days; `qa_jsonld` extended, proved both ways, and finally wired into CI).** State first, plainly: **local = origin = `1b26b2d`**, but **the 09-10 block went unrun** — the Sudan verdict, the heldAssets root-resolution fix, `qa_sources_alive.py`, ruling #47, the status memo and Brief 70 all sat uncommitted. One day staged, **nothing lost and nothing live stale**: `qa_live_drift` read **CLEAN against the origin before today's change**, because the whole 09-10 day was verdicts, tools, rulings and edits to *held* files, which by design contribute no bytes. Content: **draft-blocker 2 CLOSED** (`recon/2026-09-11-ed05-timss-2019-national-report-read.md`) — the HSRC/DBE TIMSS 2019 Grade 9 report read in the body from the national TIMSS centre's copy, HSRC launch release as first-party channel two. **+104 (maths, 2003→2019, Gr 9) RIDES**, printed twice, decomposed +67/+20/+17, every cycle-to-cycle difference statistically significant, and the report hands over unprompted both things the standing prohibition demands beside an improvement claim — the **deceleration** (7.4 → 4.6 points/year) and the **floor** (5th–95th range 320 → 252, top end flat since 2011). **The 0.9 SD (1995–2015) does not exist**: the report prints *one* SD, for *2003–2019*, and the reconstructed 276 → 372 crosses a **grade seam** (Gr 8 → Gr 9) the report itself measures at **21 points**, across a window it calls *no significant change*. Also recorded: the launch release compresses at-or-above-intermediate (13%) into "reached the intermediate benchmark" where Figure 8 shows 10% in-band + 3% above (#43, sixth instance of the family); Figure 8's 2011/2015 rows reconstructed by arithmetic and therefore **do not ride** (#41). Both day-long blockers now closed (1 on 09-06, 2 today); **drafting unblocked, Editor's call.**
>
> **Section 1 gains row 43 — `2026-09-11-a-trend-line-can-have-a-seam.md`, filed as ruling #48.** **The rule: a long series is not automatically a series — when the owner has spliced measurements of different populations, grades, instruments or scales, the join is a construction with a size, and that size belongs to every figure that crosses it.** Three obligations: find the seam before quoting the span; carry the seam in the same sentence; prefer the owner's certified window over a longer one a reader assembled. **Second half, on the figure that survived: "one standard deviation" is a SCALE UNIT, not an effect size** — the TIMSS scale is fixed at mean 500 / SD 100 by construction, while South Africa's own 5th–95th spread was 320 points; a reader hears an effect relative to the people measured, and that reading flatters. **Fourth shape of the scoped-instrument family** (#19 multiplier-without-base, row 20 deficit-is-a-ratio, #44's converted-figure-has-two-owners). Corollaries: the axis is part of the figure; **a bridge is a finding** (a year of schooling was worth 21 TIMSS points in South Africa in 2003 — an operation that mines a series only for endpoints throws away the best number in the chapter); the seams to look for are grade/age, instrument or scale, coverage (cf. #44's Niger exclusion), definition (cf. Zambia moving the end of primary), and owner; the longest available window is a rhetorical choice, usually one a secondary channel already made for you; **a figure can fail on its span alone, with both endpoints verifying.** Added to the Verifier's trace beside #43's register question: *does this figure cross a join, and does the sentence name it?* **First ruling of Edition 05 produced by a research read rather than a verdict** — the run of three verdict-born rulings (#45, #46, #47) ends here.
>
> **Growth + Quality — the publication had no machine-readable identity, and the check that should have seen it reported CLEAN an hour earlier** (`agents/growth/2026-09-11-dangling-publisher-entity.md`). Since **08-23** all 76 article pages pointed `author`, `publisher` **and** `isPartOf` at `…/madar/#organization` — **a node defined nowhere on the site**, because `organizationNode()` was written that day, exported, and never called; the two front doors, both editions indexes and About carried **zero** JSON-LD. **228 dangling references, two languages, 19 days**, with 0 of 82 URLs indexed and a gate target of 11 October. A second defect in the same three lines: `isPartOf` pointed at the Organization, a category error — an Article is part of a *WebSite*. Fixed by **emitting `Organization` + a new `WebSite` node on every page that references them** (both front doors and all 76 articles), because consumers resolve `@id` **within a document** and none fetches the home page to complete an article's publisher; same `@id` everywhere so nodes merge rather than duplicate. Conservative per the file's own posture: no `sameAs`, no invented `foundingDate`, and **deliberately no `SearchAction`** — the site has no search and declaring one promises a surface that 404s. Nodes site-wide **152 → 308**. **`qa_jsonld` gains assertion 4** — every bare `@id` reference must resolve to a node defined in the same document, with the one legitimate cross-document exception (`workTranslation`/`translationOfWork`) **named in the code rather than left silent**. Proved per #35 against real state: **BITE FAIL(228)** on the pre-fix build, each defect naming page and field; **CONTROL silent** on the rebuild (308 nodes, 608 URL promises, **306 references resolved**). **Why it had 19 days: the script has existed since 08-29 and was never in CI** — it ran only by hand. Now a build-job step, so **this commit touches `.github/workflows/` and needs the workflow-scope PAT**. Gap named for the 09-13 review: **six of ten standing assertions gate the deploy, four do not** (`qa_a11y_lang`, `qa_ar_language`, `qa_consumer_surface`, and `qa_jsonld` until today) — an assertion that runs when someone remembers is a checklist item, not a gate; recommending all four into CI. Lesson filed with the ruling: **an assertion that dereferences *documents* cannot see a broken reference *inside* one** — it was checking the right string for the wrong thing.
>
> **Quality — the full pass ran in a real git clone** with the working tree copied over it, closing the proof 09-10 could not finish (the route-around build cannot produce `lastmod`). Build exit 0 twice, 82 pages; parity 38/38; sitemap 82 URLs / 82 lastmod with **held files contributing no date**; both feeds 38 items / 38 enclosures; held-slug leak **zero** across pages, sitemap, feeds and assets (6 assets for 3 held pieces withheld by name at build time); **all ten standing assertions green** as a regression set; live drift **CLEAN**. Publish gate run in writing (`agents/logs/2026-09-11-publish-gate.md`) — **nothing ships editorially**, corpus unchanged at 38 EN / 38 AR, 35 countries, no approved flips, all three Ed05 pairs still held. Ruling series now **#1–#48, no gaps, no dupes.** Brief 71.

---

## Consolidation — 2026-09-13 (weekly review; covers 2026-09-07 → 2026-09-13)

**The week the Verifier ran three days running and the numbers came back clean every time.** Four runs (09-08, 09-09, 09-10, 09-11; 09-07 and 09-12 dark), four rulings — the largest single-week addition to the register since it was built, and the first week in which the register's growth came from *verdicts on our own published prose* rather than from reading other people's documents.

- **Ruling register #1–#47 → #1–#48.** The canonical one-liner for **#48** (*a trend line can have a seam*) is added to the table; it had been filed as a Section 1 row on 09-11 and cited by number in the commit and the brief, but the register's own table stopped at #47 and its header still read `#1–#46` while the series line underneath read `#1–#47` — **the header, the table and the series line disagreed three ways about how many rulings exist.** All three reconciled today. (Third consecutive week the register has drifted against its own header between consolidations; see the standing note below.)

- **A seventh family named: whether the words around the number are true — the non-numeric family** (#45 a number's noun · #46 a quotation's orthography and a plan's tense · #47 a description's date). **This family was discovered, not designed.** Edition 05's first three verification verdicts (Zambia 09-08, Sierra Leone 09-09, Sudan 09-10) returned **six findings and not one was a number**, across three pieces and three drafters — including Sierra Leone, the most figure-dense piece this publication has shipped, which passed every numeric test we own. The whole verification toolkit is numeric; all six items were invisible to all of it. The family's own prediction of where the next one comes from is recorded in §3: the **attribution verb**.

- **The repeat inside the family is the week's real finding.** Two consecutive verdicts, two pieces, two drafters, one identical shape: the hedge was carried **correctly in `sources[]` in both languages** and **dropped from both bodies** (Sierra Leone's *planned* launch; Sudan's *2018* laureate year announced in 2019). A defect that reproduces across drafters is a property of the workflow, not of a writer — codified today as a RUNBOOK rule (*the annotation and the sentence are written from the same open register*).

- **#48 is the outlier that proves the rest.** It is the week's only ruling born of a *research read* rather than a verdict, and the only one that sent a figure back **stronger**: South Africa's +104 came out of the owner's own national report carrying both of the things our standing prohibition demands beside an improvement claim — the deceleration and the floor — supplied unprompted, in the same eight pages. The companion figure died on a **grade seam**, with both endpoints real and the arithmetic correct. Standing instruction attached: **a seamed figure is never repaired to its arithmetic value** (0.9 SD does not become correct at 0.96).

- **No archive roll this week.** The two-cycle rule leaves exactly two consolidation sections in the file (09-06 and today); the 08-09 → 08-23 window went to `ARCHIVE-2026-08-09_2026-08-23.md` at the last consolidation and nothing else has aged out. Nothing pruned, nothing duplicated: the four ruling files (09-08, 09-09, 09-10, 09-11) are each cited exactly once from §3 and exist once on disk.

- **Standing note for the next consolidation — the register drifts against its own header, every week, in the same direction.** 08-23 built the table and left the header stale; 09-06 found rows 26–38 filed as addenda while the header claimed thirty-three; today the header, table and series line disagreed three ways. The cause is structural and benign: **a ruling is filed by the run that earns it, and the header is maintained by the review**, so between Sunday and Sunday the register is *always* behind by however many rulings the week produced. The fix is not more diligence — it is to stop hand-maintaining a count that can be derived. Carried to the Web Developer as a candidate assertion, not a defect: **`qa_guidebook_register` — parse §3's table, assert the header's range, the series-integrity line and the row set agree, and that every `#N` cited in the table has a file on disk.** Same shape as every gate that has earned its place here: a contract in prose is a declaration (#37), and a promise is derived, not declared (#36) — applied, for once, to our own ledger rather than to the site.

---

> **Daily-run addendum — 2026-09-14 (ruling #49; Edition 05 slot 3 is DRAFTED — the mandated continental piece, the schema's first new field since launch, and the browse surface this publication has never had).** State first, plainly: **local = origin = `d196390`**, and **the 09-13 weekly block went unrun** — the weekly review, the three CI assertion wirings, the taxonomy audit and the scope decision all sat uncommitted for a day. This push carries **two days**. Nothing live went stale: every uncommitted change was to held files, agent artefacts, tools and the workflow, none of which contributes bytes to the origin. **2026-09-12 was dark** — no run, no brief.
>
> **Content — mandated slot 3 is drafted, and the piece is the edition's structural keystone.** `2026-09-14-africa-best-system-ruler` — *Four rulers, four crowns, and one about to be re-read*, ~2,170 words, title 53/100 and dek 162/200 counted at compose, held `approved: false`. Both day-long blockers were closed before drafting began (PASEC 2019 report 09-06, TIMSS 2019 national report 09-11); every figure rides on the register that closed its blocker, with its page. The piece names four rulers and awards four crowns — **level** (Mauritius, in the past tense the World Bank brief's own pre-COVID statement requires), **learning relative to income** (Senegal and Benin, on the analyst's HLO axis and attributed to him, never to PASEC, whose own income analysis finds no correlation), **improvement** (Niger on PASEC, largest gain in all four trend series, *among pupils tested in French*, with the policy list set beside the gain and footnote 47's causal disclaimer quoted where the list appears; South Africa on TIMSS, +104 with its deceleration and its floor and its grade seam all named per #48), and **shared gains** (Senegal, on the report's own sentence, with the report's own limits in the same paragraph). It states outright that the four cannot be averaged. The vintage clause is in-sentence (#21), Seychelles is unnamed, the aggregator tier is cited nowhere and named once in the lede, and the word "sacked" appears nowhere in our voice — the instrument was contractual and the report's word is termination.
>
> **Section 1 gains row 44 — `2026-09-14-a-superlative-has-a-horizon.md`, filed as ruling #49.** **The rule: a superlative is a figure with two scopes, not one — the RULER it is measured on and the HORIZON it is measured within; missing either, it has not made a claim.** Earned on a first-party headline about the piece's own instrument: *« Le PASEC 2024 sera la dernière évaluation internationale »*. Read in the body, the next sentence supplies the bound — **before 2030, the end point of SDG 4** — and the claim becomes "the last cycle whose data reach the 2030 stocktake", not "the last cycle"; the same owner's February 2026 register names a **PASEC 2028** cycle, and the interview is dated **January 2023**, not 2026. Had the headline ridden, the piece's spine would have inverted. Corollaries: **a headline is where the horizon goes missing** (the bounding clause is the one that reads like qualification and gets cut); **time-superlatives are the least examined** because "the last X" sounds like a fact about the world rather than a ranking on a timeline; **a superlative in a first-party headline is still a second register** (#43 moved from the podium to the masthead). Sits at the junction of the **scoped-instrument** family (#19 · row 20 · #44 · #48) and the **non-numeric** family named at the 09-13 review: it contains no number at all, and its unit is a date. Verifier's trace gains a row beside #43's and #48's; recon rule attached — when a headline's superlative would change a piece's argument, the body is read *before drafting*, not before verification.
>
> **Quality — the schema's first new field since launch, and its assertion landed in the same commit.** A continental piece does not fit a single `country:`. Per the Editor's decision of 09-06 the file lands as `country: Mauritius` + `region: Africa` + a new **optional** `countries: [Mauritius, Senegal, Niger, Benin, South Africa]`, rendered in the marginalia in both languages through the i18n-geo display map and counted by `madar_stats.py` as the **union** of the two fields, so "countries covered" stays honest. `qa_geo_fields.py` lands as **standing assertion #12** in the same commit as the field and the first file to use it (#37 — a field with no consumer is a promise to nobody; a field with no assertion is one nobody finds out about). **It has two halves for a reason worth recording: the piece introducing the field is HELD, so an output-side-only check would have had its first real run on flip day.** The SOURCE half reads the content files regardless of hold state (every name resolves in the display map; `country` is a member of `countries`; no duplicates; a list of one is a country; EN/AR twins agree); the OUTPUT half reads the rendered marginalia for whatever is actually in dist and reports held pieces as **skipped**, never as passed. Proved both ways per #35, and the output half was proved **on a temporarily-approved build in a throwaway workspace** rather than left to flip day: control silent twice, bites on an unmapped name, a list missing its own `country`, a duplicate, a list of one, the rendered line stripped, one name silently dropped from it, and a Latin country name injected into the Arabic line — the last of which `qa_ar_language` independently caught as well. Wired into the CI build job, so **this push needs the workflow-scope PAT**. Two smaller things found by doing it: the first bite attempt **did not bite**, because it was written against the source's `<p>` and Astro serves a scoped-CID attribute on it — *a bite must be injected against the served shape, not the authored one*; and the check's first draft searched the whole marginalia block, which let the primary `country` on the line above mask a list that had failed to render — *an assertion must not be able to read its own answer off the field it exists to supplement*.
>
> **Growth — the week's bet shipped whole: the corpus can be browsed.** Until today `web/src/pages/` served **seven route files and not one grouped articles by anything**; a reader finishing a piece about Africa or about teachers had no page to go to. Shipped `/browse/` and `/ar/browse/` plus **18 index pages per language** derived from the approved collection at build time (#36) — 7 regions, 8 topics, 3 countries — on the audit's own selection rules: a topic gets a page only if it is on **fewer than half** the corpus and on **at least two** pieces, because `government-led programs` sits on 38 of 38 and a tag true of everything carries zero information, and three tags hold one piece each and a page listing one article is a dead end. **Sitemap 82 → 120. 140 new edges into articles** (the audit predicted ~140) **and 360 between index pages, 500 in all.** Arabic labels come from the display map, never the schema; held pieces are invisible to every derivation; hreflang clusters are reciprocal by construction because each language derives its own set and a divergence fails `qa_hreflang_clusters`. The **only** inbound edge into 38 new URLs is a `Browse` / «تصفّح» item added to both the home and the **article** nav — deliberately the article nav, because the reader who wants more of a subject is the one who has just finished a piece about it, and because a sitemap entry is a declaration while a link is an edge (the VALENCE orphan, 08-25). At the Edition 05 flip this compounds with no new work: Africa moves 5 → 11 and becomes the second-largest region page in the publication. Carried to the Editor, unresolved and correctly so: **a controlled vocabulary in which one term is always true needs revising, not just excluding** — and the piece drafted today, whose entire subject is measurement, had to be filed under three tags none of which is assessment.
>
> **Quality — the full pass ran in a real git clone with the working tree copied over it.** Build exit 0, **120 pages**; parity **38/38**; sitemap **120 URLs / 120 lastmod** with held files contributing no date; both feeds 38 items / 38 enclosures; held-slug leak **zero** across pages, sitemap, feeds and assets for **four** held pieces; **all twelve standing assertions green**, including the new one; `qa_reachability` **zero orphans on the 120-page graph**, which was the growth bet's own pass condition. One stale-cache trap caught and named rather than absorbed: a duplicate-id warning appeared in a rebuild and was **rsync-carried `.astro` content-store debris from the throwaway proof build**, not a corpus defect — cleared, rebuilt, silent. Hero still drawn the same run (four rulers over one continent outline, four scales that do not line up, the kiln-orange mark a zero set down clear of its own ruler's edge) and the share card generated from it — and the og-card step, hand-retyped every run since 08-18, is now a documented script on disk at `web/scripts/make-og-card.mjs`. Publish gate run in writing; **nothing ships editorially**, corpus unchanged at 38 EN / 38 AR, no approved flips, all four Ed05 pairs held and mutually rail-bound.
>
> **Edition 05 now stands at four drafted of six**, three of them fully banked and verified. Next: the Arabic composition of slot 3 and its gate, then Egypt re-verification, with the three confirmation reads interleaved one per run. Two source URLs on the new piece are carried as **read-on-a-stated-date rather than fetched today** and are the confirmation read's first job. Ruling series now **#1–#49, no gaps, no dupes.** Briefs 72 (and the 09-13 weekly review, which this push also carries).

---

> **Daily-run addendum — 2026-09-14, second run (ruling #50; the first Madār run executed in the cloud; Edition 05 slot 3 is a PAIR).** The morning run drafted slot 3 and moved the operation onto GitHub Actions; this run is the first one the new architecture actually completed, four dispatch attempts after the cutover commit. Three of the four failures had three different causes and only the last was readable, because attempt 3 turned on full output: **the `CLAUDE_CODE_OAUTH_TOKEN` secret contained a line break at character 80** — 110 characters on 2 lines. Nothing in the repository could have fixed it, and the two commits that preceded it were hypotheses about the file we could see rather than the error we could not. Recorded because the lesson generalises past today: **a masked error makes you debug the artefact you can read.** The first fix to try, when a failure is unreadable, is the one that makes it readable.
>
> **Section 1 gains row 45 — `2026-09-14-a-correction-has-a-blast-radius.md`, filed as ruling #50.** **The rule: when a verdict changes a piece, it changes every artefact composed from that piece; the correction is finished when the descendants have been re-read, not when the article is fixed.** Earned by Growth while composing the wave packet's fourth caption. The Sudan captions, EN and AR, were composed 09-03 and said the 2014 programme was *"games that teach Arabic and maths"* — the exact claim the Verifier's 09-10 verdict struck from the piece's opening sentence in both languages (the owner's register says a **numeracy trial**; Arabic literacy belongs to the later programme). The article was corrected that day; the packet was not, and was staged to publish. **Nothing was published** — the packet is held with the wave — but that is luck from an unrelated hold, not a control. Every existing check was structurally blind: the Verifier traces the piece, the thirteen assertions enumerate `dist`, the 08-23 perimeter rule reaches *rendered* artefacts (brief, review) and a markdown packet is neither published nor rendered, and the packet's own header promised *"no figure appears here that is not in the shipped piece"* — **the defect was not a figure, it was a noun** (#47, met a second time one artefact downstream). Corollaries: the derived artefact is **stale by default**; the blast radius is widest for the artefacts that travel furthest, because a caption reaches more people than the paragraph it summarises and reaches them without it; **re-read at the point of use, never track at the point of edit** (a dependency ledger would itself go stale — the 2026-06-07 rule one level up); and a verdict that changes a *claim* rather than a *figure* says so in words a later reader can grep. Seventh member of the enumeration family and the second to sit outside `dist`, but it asks the family's question backwards: not *what can this check not see?* but **what does this correction not reach?** Action of record: both captions fixed, and re-reading every caption against the shipped text of its piece is now **step 3 of the packet's flip-day checklist** rather than a good intention.
>
> **Content — slot 3 is a pair.** `2026-09-14-africa-best-system-ruler` composed in Arabic and gated the same day (`verdicts/2026-09-14-africa-best-system-ruler-ar-gate.md`): **PASS**, held `approved: false`. Dek **145/200** code points, title 58/100, measured at compose. Numeral multiset EN↔AR: **zero AR-only figures**; the only EN-only tokens are the grade labels (*Grade 9/8/5*, which Arabic renders as ordinal words — the Zambia-pair precedent) and the caption's Curated number in Arabic-Indic digits. Five named humans: four confirmed, one **restored** (عبد الرحمن بابا موسى — Arabic in origin, francophone in spelling, and no CONFEMEN register serves an Arabic edition, so the restoration is stated as one). The nine-author TIMSS list is **deliberately not transliterated** — eight of the nine have no served Arabic register, and inventing eight renderings to fill a citation line is a figure composed from a pattern, applied to people; the Arabic carries ريدي وزملاؤه with the Latin list retained (#28a applied to humans). Orthography brought to house: the corpus uses no Persian-augmented letters, so `باسيك (PASEC)`, `تيمس (TIMSS)`, `(CONFEMEN)` on first use, per the `«بيزا» (PISA)` precedent. TIMSS had never before appeared in an Arabic Madār piece.
>
> **The 09-13 review's forward surface — the attribution verb — was tested, and it bit.** The review named it and predicted nothing would check it. Arabic is what checked it: the English read *"The Principal Investigator **said** it at the launch"*, and Arabic cannot compose that sentence without choosing a gender, so the register had to be re-opened instead of remembered. The served HSRC release says **"Reddy cautions that while we applaud the improvement in educational achievement, 'the rate of achievement improvement is decreasing.'"** Three corrections, both languages: the verb is *cautions*, not *said* (#43's hierarchy turned on our own verbs — *said* is the flattest verb available and it deletes the direction of the speech act); the caution sits **inside applause**, and quoting it without the applause is our framing rather than the register's (#47's setting corollary); and the Principal Investigator is **named and titled on the served page** — Dr Vijay Reddy — which is what now anchors the Arabic's الباحثة الرئيسة in a register instead of an assumption. **Fourth consecutive non-numeric finding in this edition, and the first found by a language crossing rather than by a verification pass.**
>
> **Quality — the 09-09 forward question was owed for three runs, and it bit on six-hour-old code.** The 09-09 QA log named *the design tokens are a contract nothing checks* and the three runs that followed wrote no QA log and named no forward question at all — a binding rule since 08-23, lapsed silently, which is the 09-13 *"a hand-run assertion is a habit, not a gate"* finding met in prose form. Asked today, before anything new: the browse surface shipped this morning styles the country label on every listed piece and both hover states with **`var(--color-accent)`, a token this design system does not define** (its accent is `--color-orange`). An undefined `var()` does not error — **it evaporates**, so 76 pages in both languages served the declaration and rendered nothing, through a clean build and twelve green assertions. Fixed, and closed as a gate: **`qa_css_tokens.py` is the thirteenth standing assertion and the twelfth to gate the deploy**, proved both ways per #35 — control silent on the real 120-page build with VALENCE's runtime-set `--c` correctly *not* cried wolf on; bites on the real defect reinjected into the **served** stylesheet, on an undefined token in an inline `<style>` block, and on a "definition" placed in prose rather than a CSS region, whose own control (the same definition inside a `<style>` block) goes green. One injection **failed to change the file** and the harness caught it — yesterday's ruling about proving the bite as carefully as the control, working on the day after it was written. **The gate does not live where the other eleven do, and the reason is a permission rather than a preference:** the push that added it to `.github/workflows/astro-pages.yml` was rejected — *refusing to allow a GitHub App to create or update workflow ... without `workflows` permission* — and that is not grantable from inside a workflow, by design, because a job that can rewrite its own workflow can grant itself anything. The operation moved to the cloud this morning to stop depending on one person and found the same afternoon that one class of change still does. The gate was not dropped: it is invoked from `web/package.json` as `postbuild`, which `npm run build` — the deploy job's own command — runs, so a failure fails the build and nothing deploys; proved through that entry point, exit 0 on the real tree and exit 1 with the defect reinjected at source. Stronger in one respect (it gates every local build too) and weaker in another (`astro-pages.yml` stops being the complete register of gates). Filed as **founder decision #6** with a stated default of *document the split* and a stated date of 2026-09-20.
>
> **Register housekeeping, owed to the next weekly consolidation:** §3's canonical table still ends at **#48** and its series-integrity line still reads *#1–#48*, while **#49** (filed by this morning's run) and **#50** (filed here) exist only as Section 1 rows and daily addenda. This is the drift the 09-06 and 09-13 reviews both recorded and neither closed — noted here, deliberately not fixed by a daily run, because merging the table is the consolidation's job and a daily doing it by hand is how the three-way disagreement of 09-13 was created in the first place. **Series: #1–#50, no gaps, no duplicates.**

---

> **Daily-run addendum — 2026-09-15 (ruling #51; the first run to look at a pixel).** The second cloud run, and the first ordinary one: the day before had a cutover, four failed dispatches and a two-run day in it, and today simply ran. State verified first — origin serves `cc44be9`, the 09-14 deploy is green, the dispatch-by-name fix of issue #6 worked, and there is **no unserved commit and no dark day** to name.
>
> **Section 1 gains row 46 — `2026-09-15-a-claim-inherits-the-scope-of-its-register.md`, filed as ruling #51.** **The rule: every claim is read somewhere, and where it was read is its boundary — a qualifier belongs to the cell it was printed in, a comparison to the set that was actually opened, and carrying either one wider is a new claim nobody made.** Earned three times in one run. Twice in Edition 05's slot 3, at the pair verdict: a `***` significance mark printed against the **mathematics** column was carried as *"at that level"* and so contradicted the piece's own fourth ruler four sections later (the report records Senegal at +27.5\*\*\* in end-of-primary reading, which is where ruler four's crown comes from); and *"Mauritius was the continent's outlier"* stood on **one** country brief read against **one** comparison, in the piece whose entire argument is that a rank without its ruler is an opinion with decimal places, while the commission kept a neighbouring island state deliberately unnamed. Then a third time, on our own instrument, within the hour — see Quality. Fifth member of the non-numeric family (#45 #46 #47 #49) and the first whose error is a **quantifier of scope**; the first to bind a sentence and an assertion with the same words. Operationally it adds the Verifier's **ninth trace axis, `scope`**, and a hand-off line naming the comparison set behind any *best / largest / only / outlier*.
>
> **Content — slot 3 is BANKED, and it is the first Ed05 pair that did not pass on first reading.** `content-drafts/verdicts/2026-09-15-ed05-best-system-pair-verdict.md`: **four notes returned, all four closed in-run in both languages**, re-read, PASS. Notes 1 and 2 are the ruling above. Note 3: **Dr Vijay Reddy and Hilaire Hounkpodoté were named in `sources[]` in both languages and in neither body** — the piece quoted her by pronoun and closed, at its rhetorical peak, on *"the man who has spent his career building the francophone instrument"*, a biographical description no register read for this piece carries (#47). The 09-13 shape one layer out: annotation right, both bodies smoothed, and what was smoothed this time was a **name**. Note 4: the commission's own prohibition — *improvement is never reported without its floor* — was honoured for South Africa and for francophone Africa generally and **not for Niger, the country ruler three crowns**; the report prints Niger's distribution on the same page as Benin's (P10 **+51.6** against P90 **+139.3**), and it is now in both bodies with both figures added to both annotations. Numeral multiset re-checked after every edit: EN 106 tokens, AR 99, **zero Arabic-only figures**, the seven English-only tokens still the grade labels Arabic renders as ordinal words. **Edition 05 stands at four pairs of six, four banked, three verified; the Verifier's verdict on slot 3 is next.**
>
> **Quality — the 09-14 forward question was *nothing this operation owns has ever checked that a page RENDERS*, and it was answered with `qa_render.py`, standing assertion 14.** Thirteen assertions read structure — present, resolves, parses, matches, agrees — and not one had ever looked at a pixel, while three render-class defects sat in the record, all three found by a human happening to look (the VALENCE Arabic letter-joins 08-18, the edge-to-edge brief 08-16, the wrong-coloured country label on 76 pages 09-14). The check rasterises four pages and one share card with headless Chrome and asserts **properties, never identity** — a screenshot diff fails on every legitimate edit, which is why this was a question and not a task. Three properties, each chosen from a defect that has actually bitten: **not blank**, **legible** (ink-dark pixels present on a light ground, the white-on-white case), and **accent present**. PNG decoded in pure Python; the browser is the first dependency the QA toolkit has ever taken, and it exits 1 rather than skipping if none is found. **Proved four ways:** control silent on the real 120-page build; bite A, the accent token neutralised in the served CSS; bite B, body text set to the paper colour; bite C, one page's body emptied — each injection verified to have changed the artefact *before* the check was run on it (the 09-14 addendum). Gated from `web/package.json` as a second `postbuild` step, beside `qa_css_tokens`, because this identity is still refused write access to `.github/workflows/**`. **13 of 14 standing assertions now gate the deploy.**
>
> **And the proving produced ruling #51's third instance.** Bite A fired on the two article pages (accent 674 → 0, 629 → 0) and **not** on the two home pages (949 → 900, 849 → 817): the hero still carries its own kiln-orange as an SVG literal, does not read the token, and sat inside the region being measured. A floor set there would have passed forever with the accent entirely dead — the 09-14 masking trap, one artefact over instead of one field over. P3 is now scoped per target and each scope is the one proved able to fail: the chrome band on the home pages (49 → 0, 32 → 0), the whole page on the article pages, where the chrome band carries no accent at all.
>
> **Growth — every share of every piece has travelled with a picture and no description of it since 2026-05-25.** 113 days, both languages, every page: `og:image` declared everywhere, `og:image:alt` declared nowhere, and the text existed the whole time — the hero still's alt, composed in both languages by the people who commissioned the drawing, spent on the page and withheld from the share. Now emitted from `Base.astro` on all 120 pages, per language, cut to the last whole sentence inside the 420 characters Twitter accepts (a machine may choose where to stop, never how to phrase); the brand card gets its own composed description in each language; VALENCE, which is hand-written and outside the layout, had the og alt since 08-18 and was **missing the twitter one**. Asserted by extending `qa_consumer_surface` — already a CI step, so no workflow write was needed — proved both ways on a stripped tag and a 430-character one. Same family as 08-18: *resolves* is narrower than *works*, and *works* is narrower than **works for a reader who cannot see it**.
>
> **Forward question for the 2026-09-16 run, per the 08-23 rule:** `qa_render` proves a page draws ink, keeps its ground and carries its accent. It cannot yet see **whether Arabic renders as Arabic** — joined script rather than disconnected letterforms or tofu — which is the one render defect this operation has actually shipped (VALENCE, 08-18, letter-joins broken by a `letter-spacing` that is never applied to Arabic). The obstacle is named so tomorrow does not rediscover it: joining has no stable pixel oracle, and the candidate is not a picture but a **computed style** — no rule that applies to an Arabic run may set `letter-spacing` to anything but `normal` — which needs a driver we do not have, or a static read of the built CSS against the selectors that reach `[lang="ar"]`. Decide which, then prove it on the VALENCE card, which is the known-broken control.

---

> **Daily-run addendum — 2026-09-15, SECOND run of the day (the desktop cadence; rulings #52 and #53).** **Two full daily runs executed this date and collided.** The cloud run (`845578a`, `2d9d13f`, 09:32 UTC) and the desktop run (`ab3ced0`, parked on branch `desktop-2026-09-15`) each did a complete five-function day against the same commit, each wrote a brief, a QA log and a pair verdict for 09-15, and each filed a ruling numbered **#51**. The cloud run's is the record: its editorial verdict returned **four** notes where the desktop's returned one, and one of the three it found alone — *the crowned improver's own floor was missing*, a standing prohibition of that commission — **the desktop run checked and passed**. The desktop artefacts are retained under a `-desktop` suffix; its verdict file was **dropped** rather than archived, because two verdict files for one pair is a hazard to the Verifier tomorrow and the cloud's strictly contains it. **The cloud's ruling keeps #51; the desktop's two renumber to #52 and #53, rows 47 and 48.** Founder decision taken the same evening: **the cloud is the operation and the desktop schedules are disabled** — see the cadence note below.
>
> **Section 1 gains row 47 — `2026-09-15-an-assertion-names-the-artefact-it-read.md`, filed as ruling #52.** **The rule: before an assertion can be trusted about an artefact, it must be able to say WHICH artefact it read; a check that guesses its own input is a coincidence, and a check that sweeps zero of the thing it exists to check has failed, not passed.** Found by running the standing pass before anything new and reading a number that could not be true: `qa_jsonld: pages 67 · nodes 0 · CLEAN` on a site of 120 pages and 308 nodes. Two correct decisions and one convenience compose into it — `qa_jsonld` was **the one assertion of the set that ignores `sys.argv[1]`** (it called a `find_dist()` helper that searched and guessed; the path handed to it was accepted by the shell and discarded by the program — **the bare invocation in `astro-pages.yml` was the tell**), and `web/dist` in the founder's working tree was a build from **2026-07-29** — 48 days old, 67 pages, no `browse/`, zero `ld+json` because it predates the 08-23 markup, and `.gitignore`d, which is exactly why it never showed in `git status`. Reproduced deliberately before any fix: same tool, same build, same minute, two answers. **This is the third question in the assertion-discipline family and genuinely a third — SCOPE asks what a check cannot see (08-16 → 09-06), ORACLE asks whether its bite is real (#35, 09-14), INPUT asks which bytes it read; a perfectly-scoped, properly-proved assertion pointed at the wrong directory is worth nothing, and it is the only one of the three that leaves no trace in the check's own source.** Note the shape it shares with the cloud run's #51 filed the same morning — *a claim inherits the scope of its register* — and with that run's own masking finding on `qa_render`: three findings in one day about **an instrument reporting on a scope other than the one it names**. Action of record: **`qa_dist_input.py` lands as standing assertion #16 and runs FIRST**, asserting identity (it prints the resolved absolute path whether it passes or fails), freshness (dist no older than `web/src`, `web/public`, `astro.config.mjs`, `package.json`) and non-vacuity; `qa_jsonld` now reads `argv[1]`, *refuses* a path that is not a built dist rather than substituting one, and gains a non-vacuity floor. Proved both ways per #35 with the 09-14 discipline — control silent on the 120-page build, and four bites: the real 07-29 dist, an empty directory, a directory with no sitemap, and a copy with one source file `touch`ed twelve minutes into its future with the mtime change asserted *before* the run. The freshness bite **fired for the wrong reason on the first attempt** and was redone deliberately. Corollaries: **print the input, not just the verdict**; **a convenience that guesses is a defect with a delay**; **an argument the program ignores is worse than one it rejects**; **the bare invocation is the tell.** Wired into `astro-pages.yml` as the first step after the build — which only the desktop path could do, because the cloud identity is refused write access to `.github/workflows/**`.
>
> **Section 1 gains row 48 — `2026-09-15-a-re-read-answers-the-question-it-was-sent-with.md`, filed as ruling #53.** **The rule: when a register is re-opened to settle one item, everything the piece takes from that register is re-settled in the same pass — a re-read that fixes only what it was sent for certifies the whole citation by having touched part of it. Corollary: the fact a re-read is most likely to miss is the one that was never in dispute.** Earned on the slot-3 pair: the piece rests its TIMSS section on one person's caution and says so — *"the words are hers"* — while **Dr Vijay Reddy** appeared twice in `sources[]` in both languages and zero times in either body. What makes it a ruling rather than a slip is that **09-14 re-opened that exact register** (Arabic cannot render *"the Principal Investigator **said** it"* without choosing a gender) and returned three things from one served page: the verb *cautions*, the placement inside applause, and her name and title. **Two of three travelled into the prose; the third was recorded in that run's own log as a finding and left in the annotation.** **Independently confirmed the same day:** the cloud run, working the same piece in parallel and without sight of this file, returned the identical defect as its Note 3 — and found the **second** instance the desktop run missed, **Hilaire Hounkpodoté**, likewise named in both `sources[]` and reduced in both bodies to *"the man who has spent his career building the francophone instrument."* Two independent readers, one piece, the same class, twice — and the second is the stronger evidence, because that periphrasis reads as a deliberate stylistic choice rather than an omission, which is why neither the drafter nor the first reader questioned it. **Third instance of the 09-13 shape** (annotation right, both bodies wrong) after Sierra Leone item 2 and Sudan item 2, and **the first since the rule those two produced** — which shows the rule as written does not yet cover the case: it says the hand-off carries an `annotation == sentence` line per source and does not say what the comparison is *over*, so it was run on the axes the previous failures taught (hedge, date) and not on the name. **A checklist built from past failures cannot contain the next one** — which is why this is filed as a pass discipline, not another axis. Binding: a re-read is scoped to the **register**, not the errand, and says so in the hand-off; the `annotation == sentence` line names its axes — hedge · noun · date · setting · **named human** · attribution verb, open by construction; a human the piece leans on is named in the body **or the piece says why not**; and **when a run's own log records a fact it did not use, that is an open item, because a log is not a queue.** Fourth member of the non-numeric family and the first about *process* rather than about a kind of fact; also #50 pointed the other way — #50 asks what a fix fails to reach downstream, #53 asks what a re-read fails to reach inside the same sentence.
>
> **Growth (desktop) — `agents/growth/2026-09-15-topic-vocabulary-audit.md`: half the corpus has no topic page.** Reading the 09-13 taxonomy bet one day after it shipped: **38 approved English pieces, 14 tags, 127 applications, 3.34 per piece — and 20 of 38, 53% of the corpus, are reachable from no topic page at all**, because the only tags they carry are the three true of most of the corpus (`government-led programs` **100%**, `access` 89%, `value of teachers` 68%; 23 pieces carry all three). Stated precisely: those pieces are **not orphaned** — every one is reachable by region and `qa_reachability` is right to pass — what they lack is the **subject** axis, the one a reader uses after finishing a piece. Diagnosis: the field is doing two jobs and only one well. The three universal terms are the publication's **editorial commitments**, and being true of everything is a sign the commitments held, not that tagging slipped — but a commitment says *what we publish* and a facet says *which of it you want next*, and the schema has one field for both. **Proof it is structural: the piece whose entire subject is measurement is filed under three tags none of which is `assessment`, because no `assessment` tag exists.** Four facets the corpus has already earned on the browse surface's own rules (≥2 pieces, <50%): assessment/examinations (~7), conflict and displacement (~9, against the existing term's 2), curriculum reform (~4), teacher workforce (~3). Routed to the Editor with four asks and **no pages built**, because building on unapproved terms would be Growth setting doctrine by shipping.
>
> **A parser fragility, recorded not fixed.** The Growth audit first reported **37** approved pieces against every other instrument's **38** — chased rather than rounded. Cause: splitting frontmatter on the literal string `---`, where `2026-07-07-netherlands-doorstroomtoets` carries a source URL containing `…schooljaar-2025---2026-vastgesteld`. The split truncated the frontmatter and that file's `approved: true` fell outside it. **No content defect and no site defect** — Astro parses YAML properly. Recorded because it is #52's own shape one level down. Binding note for any analysis script: **frontmatter ends at the first `---` on a line of its own**, never at the first occurrence of the string.
>
> **Cadence — the collision is closed by a founder decision, taken 2026-09-15.** The cloud run is the operation; **the desktop schedules are disabled**. The cloud cron is moved off the peak `:00` minute, because today's run landed roughly five and a half hours after `0 4 * * *` and a peak minute is the documented cause. One consequence to carry rather than forget: **the cloud identity cannot write `.github/workflows/**`**, so any change there — like wiring #52's assertion — needs a hand-run desktop session plus the founder's workflow-scope token. The desktop path is retired as a *schedule*, not as a *capability*. **Series: #1–#53, no gaps, no duplicates.** Desktop brief 74; the cloud run's brief is the day's record.

---

> **Daily-run addendum — 2026-09-16 (desktop cadence; ruling #54).**
>
> **Section 1 gains row 49 — `2026-09-16-a-404-is-not-a-death-certificate.md`, filed as ruling #54.** **The rule: a status code is a fact about a URL; a citation is a promise about a DOCUMENT. When a cited URL stops serving, the question is not "is this source dead?" but "where does this document live now?" — and before a dead citation may be superseded, carried as fetched-on-date, or dropped, the run searches the publisher for the document itself.** Origin case, six days old and sitting in the Edition 05 flip checklist as a blocking item: the held Sierra Leone pair cites a Save the Children blog post that returned a real 404 on 09-10 — 87 KB of branded error page — and the ledger recorded its disposition as *"supersede or carry as fetched-on-date (#41)"*, **two options, both of which accept the loss**. Re-probed today: still 404; the publisher's **root serves 200**; so the post was searched for by its headline rather than mourned, and it is **alive at a different slug** (`…/blogs/2026/results-are-out-summary-save-childrens-evaluation-results-sierra-leone-education-innovation`, 200, 116 KB). Same publisher, same year segment, same post — **a re-slug with no redirect**. The document never moved; only the address did, and forty characters of slug had stood in the flip checklist as a death. The hunt also returned **three live registers the pair does not cite, including the programme's Final Learning Report 2026** — a *document*, which outranks a blog post outright on #43's hierarchy — so **a dead-link hunt is also the cheapest source-quality upgrade this operation gets**, because it re-opens at today's evidence a question that was settled at compose time. **Why it needed a number: it is #41's missing third branch.** #41 asks *what does the owner serve today?* and answers with a **superseding** figure — the case where the world moved on. It has no language for the case where the world did not move at all. Under #41 as written a re-slug is indistinguishable from a retraction, and the two demand opposite actions: a superseded figure means the piece must change what it SAYS; a re-slugged document means the claim is still exactly right and only the citation moves. Treating the second as the first loses a source for nothing; treating the first as the second reinstates a dead figure at a live URL. **The three unreachabilities, named so a sweep never reports them as one thing again: 404/410 with a served body** — a server denying a **path**, publisher alive, *search before concluding*; **403/429 or a challenge** — a server denying **us**, a wall and not a death; **connection refused / timeout / DNS** — nothing answered, the failure is at the network layer and carries **no information about the document at all**. The first and third had been reading as the same event and they are opposites: a 404 is the strongest evidence available that the publisher is reachable, which is exactly when searching is cheap. Second finding of the same probe, and the reason the sweep is trustworthy at all: **`parliament.gov.zm` is not 404 — it is connection refused at :443**, identically from a second client on a different network six days later, which is what #20 sends a second probe to establish; and before a single failure was read as a fact about the world the run proved its own egress with four controls (`example.com` 200, the Save the Children root 200, **`unicef.org` 403 — a server going to the trouble of refusing us**, `parliament.gov.zm` 000). **Mirror of #52 one layer out:** #52 says an instrument that cannot name the artefact it read is worthless; this says the same of a citation — the URL is the instrument, the document is the artefact, and a run reporting on the URL while claiming to report on the document has made #52's error with a link checker instead of a Python file. Corollaries: **the publisher's root is the first probe after a 404**; **search by the headline, never by the slug** (the slug is the part the publisher feels free to rewrite; title, author and date survive a CMS migration); **a re-slug is not a correction and is never logged as one** (#50 pointed backwards — a verdict that records it as a content fix sends a later reader hunting for a change that never happened); **whatever replaces the link is re-ranked on #43's hierarchy, not swapped in at the same tier**; and **do not fix this by making `qa_sources_alive` cleverer** — a tool that searched for replacements would start proposing them, and a proposed citation is a figure composed from a pattern (#41's second corollary). The interpretation layer is a human reading the tool's output against the three-signature table; the memo is the artefact, not the exit code. Binding: the Verifier's confirmation-read checklist gains a row — *for every non-200 source, which signature, and if 404 was the publisher searched?* — and a 404 disposed of without a search is an open item, exactly like an untraced figure.
>
> **Ed05 flip checklist, updated in this commit:** Sierra Leone's **hard 404 is CLOSED** (live URL on file; Final Learning Report 2026 routed to the Verifier as a candidate citation upgrade, which is the Verifier's call and not Growth's — no source line was edited by this run). Zambia's four `parliament.gov.zm` URLs stay **carried as fetched-on-date**, with the failure layer now named. Not attempted today and named rather than skipped: a third route (different-country egress, or an archive), because a third failure would add little and a third *success* would raise a geography question a sandbox cannot answer — that belongs in the confirmation read, with the answer stated either way.
>
> **Egypt (Ed05 row 5) re-verified 30 days on** (`content-drafts/recon/2026-09-16-ed05-egypt-reverification.md`, #40 corollary). **Nothing has gone false**; the ministry's own January-2025 register re-read in full and unchanged word for word. **Three developments since 08-17**, and one changes what the piece is about: the state has signed a **cooperation agreement with the International Baccalaureate Organization** and the IBO has **approved the second-year curricula** (two independent registers) — a state buying external validation for a domestically legislated certificate; plus the **admissions arithmetic** published 3 Aug 2026 (same coordination office, seat share proportional to overall score, with a worked example), which makes the 08-17 *doors-published-first* finding a year stronger. **Draft-blocker 1 is still open after a second hunt on two language routes, and the park is now firmer than the figure**: the Egyptian state has published in its own voice the tracks, subject lists, exam format, faculty map, admissions arithmetic, school calendar and an international agreement for this certificate — and has published nowhere this operation can find **how many students are in it**. That absence is more reportable than the 99% would have been, and it is ruling #31 applied to its own origin case; three options put to the Editor, whose call it is. **Two draft-blocking guardrails recorded before anyone can draft into them:** the *Egyptian* / *International* Baccalaureate **naming trap** (the served verbs are *cooperation agreement*, *framework for reviewing*, *approval of the second-year curricula* — none of which is accreditation or equivalence; #43's verb hierarchy, and the 09-13 forward surface exactly), and **two mass meetings of school leaders a year apart** — ~4,000 secondary directors on 31 Aug 2025, to which the coercion allegation attaches, and ~4,500 principals from 21 governorates in Aug 2026, to which it does not. #47 corollary 2 with the two registers unusually easy to confuse.
>
> **Cadence — the 09-15 collision decision has not taken effect, and today is its second instance.** The founder decision recorded yesterday was *the cloud run is the operation; the desktop schedules are disabled*. **The desktop schedule fired again this morning at 08:44 Asia/Dubai**, and yesterday's desktop block — brief, QA log, growth audit, two rulings, `qa_dist_input.py` and the workflow wiring — **is still uncommitted in the working tree**, invisible to the cloud run, which works from `origin/main` in its own checkout. So the decision is on disk and not in the world, and the reason is structural rather than anybody's slip: **the change that would disable the desktop schedule lives in `.github/workflows/**`, which is the one path the cloud identity is refused write access to** — the same constraint that made `qa_dist_input` a desktop job in the first place. The instruction and the only hand that can execute it are on opposite sides of the boundary. **What this run did about it, rather than colliding again:** it took the lane the cloud run's own queue does not head for — the cloud's next item is the slot-3 Verifier verdict, P1 clock 09-15 → 09-22 — and wrote the lane split into the status memo, which is the file both runs read first. **General form, and the reason it is worth writing down: a decision that removes a capability is executed by the capability it removes.** The desktop path cannot retire itself without the founder's push, and until that push lands the honest planning assumption is **two runs a day, not one**, with the lane split declared in writing in the memo both read. Same shape as the 09-14 rule one turn further out — an autonomy that removes a human from the work but not from the publication has moved the dependency; here it has moved the dependency *for the instruction that would close the gap*. **Series: #1–#54, no gaps, no duplicates.** **Brief 75** — and the numbering carries its own scar: both 09-15 runs independently wrote **Brief 74**, so the series has one number and two artefacts at it. Not renumbered, because the number is printed inside two files already written; recorded instead, which is the same disposition the 09-15 addendum gave the two #51s.

---

> **Daily-run addendum — 2026-09-17 (ruling #55; the day the forward question bit on first contact, twice).** State verified before planning: origin serves `70f54e7`, the 09-16 deploy is green, the live site's `last-modified` matches it, tree clean, HEAD == `origin/main`. **No dark day, no unserved commit, and no desktop run has fired today** — so the 09-16 lane split did not need arbitrating and the cloud queue's head was taken as written: the Verifier's verdict on Edition 05 slot 3, P1 clock 09-15 → 09-22, **closed on day 2 of 7.**
>
> **Section 1 gains row 50 — `2026-09-17-a-property-of-the-content-is-not-a-property-of-the-component.md`, filed as ruling #55.** **The rule: when a presentational rule is a property of the CONTENT — its script, its language, its direction — implementing it per-component guarantees it is applied where somebody remembered and nowhere else; express it once, keyed to the property itself, at a rank no component can win back. And the check that verifies it must read the RESOLVED value, not the declaration.** Earned by answering the forward question carried unanswered since 09-15: *`qa_render` cannot see whether Arabic renders as Arabic.* Arabic is cursive; `letter-spacing` pulls the joins apart and the word stops being a word. **The operation knew that and had written it down — for headings only.** The RTL block has carried `letter-spacing: 0` on Arabic `h1`–`h4` since the first build, and tracking is not a property of headings. Read as built today through a browser's computed-style API: **149 Arabic runs tracked across seven pages, on every page of both editions** — the nav labels, the kickers, the country and date spans, the footer rights line, the Still caption, and on the ENGLISH side the cross-language link **العربية**, which is the whole path a reader takes into the Arabic edition. **The worst instance had no component to be filed under:** VALENCE sets `letter-spacing:.005em` on `body`, and its one Arabic run inherits it from a rule naming no class, no element and no language — invisible to any audit of selectors, because there is nothing to audit. **And it is #50 pointed at ourselves with exact arithmetic:** the 2026-08-18 fix removed tracking from the Arabic wordmark on the VALENCE **share card** and was correct and complete *for the card*; the page one directory away kept the defect for another thirty days. **The family's fourth question.** SCOPE asks what a check cannot see (08-16 → 09-06), ORACLE whether its bite is real (#35), INPUT which artefact it read (#52) — and a check can satisfy all three and still read a **LAYER** at which the defect is invisible, because the artefact the reader receives is the source *after something ran*. 2026-08-18's *resolves is narrower than works* turned inward. **Action of record: `qa_arabic_shaping.py` lands as standing assertion 17**, loading each page unmodified inside a same-origin iframe probe and reading computed style — chosen over the statically obvious CSS grep, and the ruling says why: ~130 `letter-spacing` declarations in the built CSS, and deciding by hand which reach an Arabic run is writing a worse browser. **Proved four ways with the 09-14 discipline, every injection verified to have changed the artefact before the check ran on it:** control silent on the real build (7 targets, **326** Arabic-bearing elements, all `normal`); bite A, the fix deleted — **148** defects, *and the count is its own proof*, being the 149 found minus VALENCE whose fix lives in a file the bite did not touch; bite B, a higher-specificity `!important` rule added with the fix left in place — **46** defects, proving the check reads computed style and not our own rule's presence; bite C, non-vacuity — an Arabic page stripped of Arabic, CSS untouched and correct, which a tracking-only check would have called CLEAN. **Fix:** `:lang(ar){letter-spacing:normal!important}` — keyed to language not direction, because the Arabic runs on the *English* pages sit inside a Latin layout; the only `!important` outside the print block, because a property of a writing system should not lose a specificity argument to a design decision. Same rule added to VALENCE's own stylesheet, which the build does not touch. Wired into `postbuild` in the same commit that proved it (09-13 rule) because this identity still cannot write `.github/workflows/**` (09-14 rule, clause 4). **15 of 17 standing assertions now gate the deploy, and the 2 that do not say why in the workflow file** — read from the file, not asserted. Full build with all three postbuild gates: **18 seconds**. Verified by eye per the 08-23 rule: Arabic joins restored, **Latin tracking untouched** on both editions' chrome.
>
> **Amendment to #54, filed inside #55 rather than given a number, because it extends a rule rather than replacing one.** #54 says *the publisher's root is the first probe after a 404*, reasoning that a 404 proves the publisher reachable and so makes searching cheap. Today the Educ'Action interview behind **ruling #49** returned 404 — **and so did the publisher's root, and every other path on the host.** A fourth signature, which *inverts* #54's prescription instead of satisfying it: **404 at the path AND at the root** means the host answers and the *publisher* is gone, so the search branch has no publisher to search, and whatever survives elsewhere is re-ranked on #43 as an archive of the document rather than the document. Second note from the same hour: **an archive can refuse you too** — the Internet Archive confirmed a snapshot exists and is marked 200, then declined to serve its body on four routes, and *a snapshot known to exist and unreadable today* is a third state, distinct from both "archived" and "gone". A run that collapses it into either is reporting a fact it does not have.
>
> **Content — Edition 05's verification backlog is ZERO: four pairs banked, four verified.** `content-drafts/verdicts/2026-09-17-africa-best-system-ruler-verification.md`: **FAIL with five items, three closed in-run in both languages, two routed** — and **not one of the five is a number**, which makes it the edition's fourth consecutive verdict with that property and the non-numeric family's clearest week yet. All four primaries re-fetched and read in the body today, three of them as PDFs downloaded and extracted rather than summarised, which is what made items 1 and 2 visible. **Item 1: the uniqueness claim carried into a cell where the report names two countries.** The piece had *"Senegal … is the only country that … manages to reduce inequality of performance — a finding it repeats for mathematics."* The report does not repeat it: p. 227 names **Senegal *and Chad*** as both showing a significant fall in variability (Table 5.8: SD −12.9 and −10.3) and distinguishes their mechanisms. **This is ruling #51 recurring in the same piece the ruling was filed on, two sections from the note that produced it** — a qualifier printed in one cell, carried into the next; a ruling filed on a piece does not sweep the piece. **Item 2: a hedge absent from both bodies *and* both annotations.** The report reads *« Le Sénégal **apparaît comme** le seul pays… »*; the piece read *is*. **This is the 09-13 shape at its root and worse than its previous instances:** #53 and the 09-13 rule both describe *annotation right, body wrong*, and the `annotation == sentence` hand-off line **cannot catch a defect identical on both sides of the comparison**. Only re-opening the register catches it, which is what #53 requires a re-read to do. **Item 3, routed to the Editor and the sharpest of the five: ruler two crowns two of the four countries its own register names, with no reading behind the narrowing.** The register clears **Gabon, Senegal, Benin and Burkina Faso** over 410; the piece crowns Senegal and Benin. Everything the register says about those two specifically is about *progress*, which is ruler three. On a ruler whose stated question is *how much a system produces given its means*, the piece drops the upper-middle-income country and the near-poorest without a clause either way — **an unsourced narrowing inside the piece about unsourced narrowings.** Not fixed, because choosing a ruler's crown is the Editor's and option 2 (composing a ranking from GDP per capita and HLO) would be a figure composed from a pattern, which the piece itself forbids. **Item 4: two vintages the register prints and both bodies dropped** — learning poverty *40 percent **(2006)*** and spending *4.8 percent **(2018)*** — where the piece's whole vintage defence was the brief's blanket *pre-COVID*, which is **a floor and not a date**, fourteen years adrift on the figure doing the most work. Closed, with the gap named in both languages. **Item 5 is the #54 amendment above.** Numeral multiset re-computed after every edit: EN 109, AR 102, **zero Arabic-only figures**; the seven English-only tokens remain the grade labels. Carried to the gate: the piece is **2,332 words against a ≤2,300 commission cap** — every added word a hedge, a scope or a vintage, and the Editor owns its own cap; and the annotation claiming Le Nestour's PASEC advisory years sit "on the served page" — **read twice today, the page carries a byline and no biography at all** (#52's shape applied to a citation).
>
> **Growth — the defect sat on the bilingual entry point, and the count needs nobody's permission.** Measured from the build: **61 English pages, 100% of them carrying an Arabic-marked element, 264 such elements in all** — `العربية` ×119, the wordmark `مدار` ×60, the cross-language rail `النسخة العربية ↗` ×38, the Still caption ×38. The CHARTER's growth loop calls Arabic *a distribution advantage, not just a translation cost*; the string that delivers that advantage was typeset with its joins pulled apart on every English page of the site. The brand share card was inspected and is **correct** — which is the finding, not the reassurance: the 08-18 fix reached the card and never the page.
>
> **Forward question for the 2026-09-18 run, per the 08-23 rule — and it is the residue of today's, stated honestly.** Assertion 17 proves no Arabic run is *tracked*. **It does not prove the glyphs SHAPED.** A missing font, a broken `font-feature-settings`, or tofu would pass it in silence, because computed style reports what was asked for and not what the font delivered. Joining still has no stable pixel oracle and today's ruling does not claim one — it removes the cause we know about. The candidate, named so tomorrow does not rediscover the obstacle: **render one known Arabic string at a known size and measure its advance width against the same string rendered with `letter-spacing` forced**, using the browser's own text-measurement API rather than pixels; if the two widths are identical the font is not shaping, because a shaped cursive run is *narrower* than its unshaped letters. Prove it on a deliberately missing font family, which is the control this operation does not yet have. **Second surface, carried unanswered from 09-16 and still owed: nothing compares the assertions' counts to one another across instruments** — pages built, sitemap `<loc>`, reachability, jsonld, a11y, hreflang, and now assertion 17's target count: seven numbers that must agree within known offsets, already emitted, never read together. **Series: #1–#55, no gaps, no duplicates.** Brief 76.

---

> **Daily-run addendum — 2026-09-18 (ruling #56; the day the deploy was red and the check was the reason).** State verified from git **and from the live origin** before planning, which is the only reason today went the way it did: tree clean, `HEAD == origin/main == 5e1be85`, no dark day — **and the site a day stale**, because the 09-17 deploy run failed and the whole of yesterday's work sat on `main` unread. A brief the founder can open is delivered by the deploy, not by the commit.
>
> **Section 1 gains row 51 — `2026-09-18-a-convenience-answer-is-not-the-index.md`, filed as ruling #56.** **The rule: when an instrument can answer either with *a* representative case or with *the whole set*, asking the cheap question returns a well-formed answer to a narrower question than the one you meant — read the index, not the summary, and when the summary is all you have, record which endpoint you asked.** The danger is never that the convenience answer is malformed. It is that it is *perfect*: nothing about a correctly-dated snapshot says "there are four more of me", and nothing about a correctly-derived ceiling says "I do not govern that URL". **Two origin cases, one day, two instruments.**
>
> **Origin case A — the archive that said nothing was archived.** Source 9 of Edition 05's slot 3 is the Educ'Action interview, the register **ruling #49 was earned on** and cited by number in four artefacts; its publisher answers 404 on every path including its root (#54's fourth signature). On 09-17 the run asked the Internet Archive's **availability API** and got one snapshot, 2025-04-27, marked 200, whose body it could not read on four routes — an honest-looking conclusion that put *retiring the demonstration #49 stands on* among the Editor's three options. Today the same endpoint returned **`{"archived_snapshots": {}}`** for the same URL, for a fact about the past that cannot change; the **CDX index** returned **five captures**, four of them 29–33 KB, and **three were read in full on the first attempt**. The availability API answers *"what is the nearest capture?"* and had handed over the **5,201-byte outlier**, a fifth the size of its siblings. It was never wrong; it was never asked the question we meant. **The read strengthened #49 rather than rescuing it:** the bounding clause « Avant 2030 qui est le point final de l'ODD4 » sits in the sentence *after* the headline's superlative, and the entire answer is given in reply to the question **« Quel est l'accent mis sur 2030 ? »** — the headline did not drop a qualification, it dropped the subject it was answering. One correction to our own record while we were there, and it is a **#47**: the interview is **published 7 February 2023**; 21–31 January 2023 is the *workshop*. The article's annotation attached January to the workshop and was right; the ledger, the guidebook and two QA logs attached it to the interview and were wrong.
>
> **Origin case B — the ceiling that governed two branches of three.** `sitemapLastmod.mjs` dates article pages, index pages **and `/valence/`**, the last from its own file under `web/public/`. `qa_lastmod`, its output-side twin, computed **one** ceiling — `max(newest approved content commit, newest chrome commit)` — and applied it to all 120 URLs. `web/public` is in neither set. Commit `bd3c48e` edited `web/public/valence/index.html` (#55's fix, on the one page the build does not style), the served bytes genuinely changed, the resolver emitted the truthful date, and **the check failed the deploy** — costing a full day of service for a defect in the instrument and not in the site. The half nobody had noticed makes the fix a **strengthening**: under one global ceiling that URL's date was only ever tested for being too *large*, so any value below the ceiling passed, including one with no relationship to the file the page is built from. **The one URL the check failed on is the one URL it had never actually checked.** Proved by construction with a scratch chrome commit: a fabricated `/valence/` date between its own file's commit and the raised ceiling prints **PASS on the old check and DEFECT on the new one**. Fixed per-URL, with the static set **discovered** from `web/public/**/index.html` rather than named (#36). **Explicitly not** fixed by widening the ceiling to include `web/public` — held pieces' stills and og cards live there, and that ceiling would have risen on every commit touching a held draft, re-opening the exact leak (`9886987`, `642fef2`) the file was written to close. **Never widen a bound to admit an honest case; narrow it per input.**
>
> **Family placement — the fifth question.** The enumeration family has asked what a check cannot *see* (08-16 → 09-06), whether its bite is *real* (#35), which *artefact* it read (#52), and at which *layer* (#55). This one asks the question underneath all four: **which question did the instrument actually answer?** Nearest sibling #52 — that says an instrument which cannot name the artefact it read is worthless; this says one which cannot name the *question it answered* is worse, because it looks like it answered yours.
>
> **Amendment, filed inside #56 rather than numbered — the oracle that could not run.** The 09-17 log's named forward question was tested today **before anything was built on it**, and the answer was **no**. The candidate — *force `letter-spacing` and compare widths* — fails on a perfectly healthy page, twice over: the inference measures tracking rather than joining, and more finally, **#55's own fix makes it impossible to run at all** (`:lang(ar){letter-spacing:normal!important}` means tracking cannot be forced onto an Arabic element here; measured, forced-tracking width equals natural width **to the pixel** on all four probe strings, so the check would read its own failure condition on every correct build). **A rule and the instrument proposed to succeed it were written hours apart in the same log and are incompatible** — the fix composed in the cascade's vocabulary, the oracle in the renderer's, neither author the other's reader. General form: **a forward question is a hypothesis, not a work order**, and the 08-23 rule that the next run *tests* it before adding anything new is vindicated by the first time testing one returned a negative.
>
> **Quality — standing assertion 18, `qa_arabic_joining.py`.** The oracle that does work: compare an Arabic run against **itself with U+200C ZERO WIDTH NON-JOINER between every letter pair** — the known-unshaped case by construction, same font, same page, same line, so the control the 09-17 log said we lacked turns out not to be needed. Measured: `بببب` **0.433**, `العربية` **0.518**, `لا` **0.783** (ligature) — and **`مدار` 0.972**, which is why *the probe string is chosen for joining density, never for being ours*: dal and alef do not join leftward, so a correctly shaped brand is 3% narrower than a broken one and no threshold can tell them apart. Two halves — **FONT** (a fixed probe in the page's inherited face) and **SERVED** (the page's own longest real Arabic run) — because the 09-17 defect was in the CSS with the font fine, and a font outage leaves the text unchanged. **Proved four ways**, each injection verified to have changed the artefact first: control CLEAN (12 runs, worst font probe 0.630 against 0.75, worst served run 0.730 against 0.90, margins now **printed** rather than merely met); bite A, shaping features disabled in the built CSS — **9 defects, ratios 1.000**, VALENCE silent because its stylesheet is its own file, the count again its own proof; bite B, 1,768 ZWNJ injected into `/ar/` alone — **SERVED half only, FONT half clean**, proving the halves independent; bite C, a page stripped of Arabic — the floor fails. **It bit three times on its own machinery before it bit on anything real**, and all three are the same family: it selected the **JSON-LD block** as the page's longest Arabic text (the 09-14 masking trap); then the density floor added to fix that **counted the injected ZWNJs**, making the check structurally unable to select the one text it exists to catch — *a check that could not run, wearing the costume of a check that found something*; and its measurement span was built from the **`font:` shorthand**, which resets `font-feature-settings` and would have made it blind to the defect class it exists for. Wired into `postbuild` in the same commit that proved it (09-13 rule), because this identity still cannot write `.github/workflows/**` (09-14 rule, clause 4). **16 of 18 standing assertions now gate the deploy, and the 2 that do not say why in the file.** Honest limit in the tool's own head: **Amiri is fetched from `fonts.googleapis.com` at read time**, so this measures the font *this runner* resolved and never the reader's.
>
> **Content — Edition 05's last editorial obstruction is cleared.** `content-drafts/verdicts/2026-09-18-ed05-editor-decisions.md`, four calls. **Item 3 closed by option 1:** ruler two now crowns **all four countries the register names** — Gabon, Senegal, Benin, Burkina Faso — with the piece saying in its own voice that naming fewer would be *our* narrowing and not the analyst's. Option 2 was closed by #41 (composing an income-adjusted ranking is a figure composed from a pattern, which this piece's own argument forbids) and option 3 by the title (*Four rulers, four crowns* with three crowns is a false claim; and ruler three already crowns two countries, so a shared crown is the piece's established shape). **No new factual claim was added** — the four names, the 410 threshold and the group-level conclusion were already in the paragraph above, verified verbatim on 09-17; the only addition is a *negative about the register*, which is the Verifier's own finding. Numeral multiset after the edit: **EN 110 / AR 103**, zero Arabic-only figures, the seven English-only tokens still the grade labels. **Item 5 closed by option 2** — see origin case A. **The word cap waived to 2,400 once, with the reason stated**: every word over it was added by a verification finding, and a cap that forced the removal of a bounding clause would be working against the publication. **`countries:` stays at five** — the field reports *coverage*, not mentions; the piece has no reading of Gabon's system or Burkina Faso's, and adding them would be **#51 committed by a metadata field instead of a sentence**. The tension (a reader may see ruler two crown Gabon and find no Gabon in the marginalia) is real, recorded, and routed to the weekly review as a definition question the field has never been asked. **Egypt (row 5) unblocked by option 1** — draft without the enrolment figure, which is a single business-newsletter register with an unread body and does not ride in any fencing — **but the spine is not the absence**, and this is where the Editor departs from the Manager's recorded view: Edition 05's posture is positive by the founder's frame, the spine is the doors-published-first finding, and the missing headcount rides as a named limit in one paragraph. *A limit stated once is honesty; a limit made into a thesis is a different edition's piece.*
>
> **Growth — the privacy posture measured instead of asserted.** Every `href` and `src` in all 121 built pages, grouped by origin (`agents/growth/2026-09-18-third-party-surface-audit.md`). The standing claim — *no third-party tracker, by design* — is **true, verified from the bytes for the first time**: no analytics, no tag manager, no pixel, no embed. **Exactly two origins are fetched without the reader acting, and they are one vendor: `fonts.googleapis.com` and `fonts.gstatic.com`, on 120 of 121 pages.** The finding is not privacy, it is **distribution**: Amiri is the one asset in the Arabic edition we do not control, and the Arabic edition is written for the readers most likely to sit behind a network that cannot fetch it. `/madar/valence/` is the exception and the proof — zero third-party origins, because it was built as a standalone file. The ~180 remaining origins are `sources[]` citations, which are links a reader *chooses* to follow and must never be counted as third-party requests. Recommendation on file to self-host all five families (all openly licensed); **deliberately not bundled into the push that repairs a broken deploy.**
>
> **Series: #1–#56, no gaps, no duplicates.** Brief 77.
>
> **Daily-run addendum — 2026-09-19 (ruling #57; the day three instruments printed the same number about three different sets).** State verified from git **and** from the live origin before planning: tree clean, `HEAD == origin/main == 78d6daa`, no dark day, and the origin serving `Fri, 18 Sep 2026 09:34:56 GMT` — the 09-18 deploy, green. Yesterday's repair of `qa_lastmod` reached readers.
>
> **Section 1 gains row 52 — `2026-09-19-equal-counts-are-not-an-agreement.md`, filed as ruling #57.** **The rule: a count is a claim about a SET, and two different sets of the same size are indistinguishable from their counts alone — so the equality of two cardinalities is not evidence that two instruments agree about anything. Where two checks' populations must stand in a relation, the relation is asserted BY NAME, element for element, with every difference declared and every declared difference required to exist.** Stated as the failure it prevents: *a page can be dropped from the sitemap and a different page added to it in the same commit, and every number this operation prints will be unchanged.*
>
> **How it was earned — the forward question, owed for three runs, tested first and failing in the wrong direction.** The question asked whether the assertions' counts agree within known offsets. **They do, on the first try, every one of them**: 121 served pages against 120 sitemap URLs differing by exactly one; 62 EN + 59 AR = 121; 59 clusters × 2 + 2 no-alternate = 120; 76 approved article pages = 2 × 38 slugs. Had the check been the one the question described, today's log would read CLEAN. Underneath the arithmetic: **`qa_lastmod` prints 120 meaning `served − {404}`, `qa_jsonld` prints 120 meaning `dist/**/index.html`, and `qa_consumer_surface` prints 120 meaning `served − {valence}`** — three deliberate, individually correct exclusions, printed side by side in one grid in every QA log this operation has ever written, as though they were one number. **The 08-17 orphan lesson from the opposite side**: there a difference of exactly one went unnoticed because nothing compared the counts; here the counts compare perfectly and the sets do not.
>
> **Five corollaries, four of them paid for within the hour.** (1) *A count carries the name of its set or it is a number, not a count* — `qa_dist_input` printed `120 built page(s)` for a dist containing **121**, because it enumerated `index.html` and the branded 404 is not one; harmless to the freshness test it exists for and wrong in the log, which is where the number is consumed. (2) *An exemption is asserted in BOTH directions* — anything undeclared in `served − sitemap` is a defect, **and** anything declared that the build does not serve is also a defect. (3) ***An invariant that has held for every case so far is still an assumption — do not divide by it***: the census derived held slugs as `len(held files) // 2`, and **bit on itself, in production**, on the first build where a piece had no Arabic twin — today's Egypt draft is English-only, so nine held files are **five** slugs and the halving said four. `qa_held_assets` was right and the new instrument was wrong. (4) *A meta-instrument reads the number the instrument **prints**, not the one it computes* — corollary 1 is the proof that is not a theoretical distinction. (5) ***An assertion's answer must not depend on who called it***: `qa_body_links` and `qa_held_assets` resolve the content collection relatively, so from `web/` — which is exactly where `postbuild` runs — they look for `web/web/src/content` and exit 2 and 1. Latent for weeks because the workflow has always called them from the root; the census was the first caller from anywhere else. Fixed in the **caller**. **#16 turned around and pointed at the caller.**
>
> **Family placement — the sixth question.** *What can this check not see?* (08-16 → 09-06) · *Is its bite real?* (#35) · *Which artefact did it read?* (#52) · *At which layer?* (#55) · *Which question did it answer?* (#56) · **Which set did it count?** (this). Nearest sibling is #56, filed yesterday: the two are one shape seen from either end — an instrument that cannot name the question it answered looks like it answered yours; one that cannot name the set it counted looks like it agrees with its neighbour. Both are invisible because the output is well formed.
>
> **Quality — standing assertion 19, `qa_census.py`.** Seventeen numbers from eleven instruments, each against the population that instrument declares it means, plus the set relations by name. **Proved both ways per #35**, every injection verified to have changed the artefact first: control CLEAN; bite 1, an orphan page in `dist` — fails **naming the page**; **bite 2, one `<loc>` swapped for a URL the build does not serve — served 121, sitemap 120, every cardinality identical to the clean build and every arithmetic identity still true, and it fails naming both the URL that appeared and the one that vanished**, which is the bite the file exists for; bite 3, a printed number altered without a behaviour change; bite 4, a pattern matching nothing — fails naming the tool it could not read rather than comparing the rows it found. And the gate proved rather than assumed: `npm run build` **exits 1** when the census fails. **The first attempt at bite 3 used a regex that did not match the print statement, so the injection never happened and the census printed CLEAN — a bite that fails to change the artefact is indistinguishable from a passing control (09-14), and the `assert` is what caught it.** Second consecutive run in which that discipline has paid. Wired into `postbuild` in the same commit that proved it (09-13 rule); **17 of 19 assertions now gate the deploy**, and the 2 that do not say why in the workflow file.
>
> **Quality — assertion 17's census corrected, 326 → 310.** The disagreement the 09-18 log predicted and could not measure: `qa_arabic_shaping` counted **16 elements no reader ever sees laid out by our CSS** — 5 JSON-LD `<script>` blocks, 5 HTML `<title>`, 6 SVG `<title>` (the stills' accessible names, never painted). The stills' visible SVG `<text>` captions stay. Measured two independent ways that agree exactly. **The first bite failed and that is the instructive half**: a plain `p,h2,a{letter-spacing:.05em!important}` injection does not reach an Arabic element at all, because **#55's `:lang(ar){letter-spacing:normal!important}` outranks a type selector** — so since #55, a tracking defect in our own stylesheet would have to be written deliberately specific to land. Re-injected above that specificity, the old version reports **11 defects on `<title>` and `<script>`** and the new one is silent, while a rendered injection still bites both.
>
> **Content — Edition 05 row 5 is drafted.** `2026-09-19-egypt-baccalaureate-published-first`, *The doors were published before the exams existed*, 1,894 words, held `approved: false`, still and og card drawn the same run. The Editor's decision 4 executed: no enrolment figure in any fencing, and **the spine is the doors-published-first finding, not the absence**. Eleven registers read in served text today, four of which the recon had never had — and the draft closed **two of the recon's seven blockers**. **Blocker 3 (Article 37bis verbatim) is CLOSED on three independent Arabic reproductions of Law 169 of 2025**, and the statute carries something no recon had: alongside the optionality guarantee, **«لا يجوز التحويل منه أو إليه من أنظمة التعليم الثانوي الأخرى أثناء سنوات الدراسة»** — no transfer into the system or out of it during the years of study. The statute grants the choice and closes the exit behind it, which is **ruling #31's counterfactual answered from the instrument rather than from the allegation**. **Blocker 4 (EIPR in its own words) is CLOSED** from an Internet Archive capture of 21 July 2025, the publisher's own host having refused this runner. **A correction to our own record, and it is a #47:** the recon has said since 08-17 that the amendments were *ratified 18 August 2025*; three registers agree the law was **issued at the Presidency on 19 Safar 1447, corresponding to 13 August 2025**, and the statute's own commencement clause names no date at all — it takes effect *from the academic year following the date of its publication*. **A fee figure did not ride:** the recon's *EGP 500 per subsequent attempt* comes from a January-2025 announcement this runner could not open today, and the enacted chapter says **first sitting free, retake bounded at 200 and not exceeding 400 per subject** — while EIPR's objection to *two hundred to two thousand pounds* is a **different clause** of the same bill, and the two are never narrated as one number. **A guardrail found rather than inherited:** the three reproductions **disagree about which numbered bis-article carries the fee provision**, so the piece cites the provisions by content and never by sub-article number.
>
> **Growth — what the operation can still read.** Four of the eleven registers this piece needed **refused this runner with 403** (`moe.gov.eg`, `eipr.org`, `ibo.org`, `english.ahram.org.eg`), and **two of those four have no Internet Archive capture at all** — including the ministry whose reform the piece is about, which a desktop run read in full three days ago. Nothing here is a death (#54): every host is alive and serving ordinary readers and refusing *us*, on a datacentre IP. The editorial cost is visible in today's piece — the minister's own before-figures were dropped, correctly, but **the decision was made by the network and not by the Editor, and that is the wrong order.** Recommendation on file: **archive the register at recon, not at draft** — one Save Page Now request per URL marked READ IN SERVED TEXT, which is the whole difference between the `eipr.org` row and the `moe.gov.eg` row.
>
> **Series: #1–#57, no gaps, no duplicates.** Brief 78.
>
> **Daily-run addendum — 2026-09-20 (ruling #58; the day the publication was found telling two readers two different stories about what it published first).** State verified from git **and** from the live origin before planning: tree clean, `HEAD == origin/main == 8208eb7`, no dark day. **But the 09-19 deploy was RED** — `build` and `deploy` both green, the site correctly serving yesterday's bytes, and the `verify` job failing on the feed-cache step. That failure is what started the day, and it is not what the day found.
>
> **Section 1 gains row 53 — `2026-09-20-a-comparator-that-returns-zero-is-a-decision-delegated.md`, filed as ruling #58.** **The rule: sorting is not a filter. A comparator that can return 0 has not ordered the tied elements — it has handed the decision to whoever supplied the input, silently. Where the sort key is not unique, the tiebreak is part of the contract and is written down; a sort whose output depends on enumeration order is a build that is not a function of its sources.** Corollary 1: **a stable sort does not make a partial order total** — stability preserves the *incoming* order of equals, which means the answer is exactly as determined as the input, and the input was `readdir`. Corollary 2: **order is a property of a LIST, not of a page** — two adjacent lists may each be correctly ordered and their concatenation may not be. Stated as the failure it prevents: *every piece can be present, correct, linked, dated, cited and served, and the publication can still tell two readers two different stories about what it published first.*
>
> **How it was earned — by diagnosing a red deploy, not by looking for it.** The `verify` job's feed-cache step failed on a conditional GET. Asking whether the feed we serve is the feed we built, this build was byte-compared against the live origin **at the very commit this build was made from** — and **five of 120 sitemap URLs differed, and so did both feeds**: identical byte lengths, identical piece sets, different order. Cause: one line repeated eight times across six files, comparing dates and nothing else, on a corpus where **24 of the 38 approved English pieces sit in a tie group and 16 share 2026-07-07 alone**. Unfixed since the first publish, 2026-05-25. **The ninth sort site already had it right** — `facetsFor` in `taxonomy.ts` breaks its count ties on the key, in a file the other eight import from.
>
> **Why nothing caught it — the enumeration family's seventh member, and the first whose missing enumeration is not a set.** Nine assertions read these exact pages every build, and every one is a claim about **membership**: reachability, hreflang clusters, rendered `related:` slugs, sitemap parity, and #57's element-for-element census. **A set has no order.** Not one could have failed on this, and every one would have been right. #57 said two sets of the same size are not the same set; #58 says **the same set, twice, is not the same page.**
>
> **Quality — standing assertion 20, `qa_stable_order.py`, landed and gated.** Asserts the **served** order is the canonical order — newest first, ties broken by slug — with the expectation derived from frontmatter (#36) and never read off the page being judged. **"Build it twice and compare" was rejected on evidence, not taste:** two consecutive builds on this runner are **byte-identical**, on a tree whose comparator had just been proved defective, so that control would have certified the defect as fixed (the 09-14 trap). **Proved eight ways**, every injection asserted to have changed the artefact first: control CLEAN over 45 dated lists, 2 feeds, 76 rails and **42 tie groups actually exercised**; **bite A — the real historical defect rebuilt — 5 defects and `npm run build` exits 1**, its footprint matching the five surfaces measured against the live origin; bite B a wrong-but-total tiebreak, 24; four artefact bites naming page and position; and four non-vacuity floors at exit 2. **The tie floor had to be proved twice** — its first fixture exited 2 for the *rails* floor, the right code for the wrong reason. **Two false-fails during construction are kept in the file, not hidden:** the first draft compared whole pages and broke on a tie run spanning an edition boundary, and the second judged the `related:` rails by date, returning 76 defects on 76 correct pages — the right check applied to the wrong list. Every list is now judged against the order **its own source** declares; a list matching neither contract is a defect, not a skip. **18 of 20 assertions gate the deploy**, and the 2 that do not say why in the workflow file.
>
> **Quality — the red deploy, fixed at the step that had no retry.** The feed-cache step is the only step in `verify` that talks to the origin without one, and it failed twice in nineteen days (09-09, 09-19), both times ~12 seconds after the byte-compare above it had just succeeded, both times on `rss.xml` while `ar/rss.xml` passed in the same loop. A day later the property holds perfectly: **twelve conditional GETs, twelve distinct edge nodes by `x-served-by`, twelve 304s.** The origin is not one server, and in the seconds after a publish the HEAD that fetches a validator and the GET that presents it can be answered by two nodes holding two versions. The step now retries the **pair** (re-using a validator from a superseded node re-asks the same unfair question with a staler answer) and prints `x-cache`/`x-served-by` on a miss — **because the mechanism above is inferred from timing and recovery, not observed**: the old step captured no headers, so a third occurrence would have been as undiagnosable as the first two. Proved both ways against the live origin before landing: control converges on attempt 1 for both feeds; a validator the origin cannot match refuses to converge and names the node.
>
> **What today does NOT fix, said plainly.** GitHub Pages derives `ETag` from mtime and size and every deploy rewrites every file, so **both feeds get a new `ETag` and `Last-Modified` on every deploy whether or not their content changed.** That is the host's behaviour. What was ours is that the bytes behind those validators also churned, for no editorial reason. The output-bounded manifest named on 09-06, 09-18 and 09-19 remains owed and is the instrument for the other half.
>
> **Content — the wave's reciprocal rails, executed early rather than at the flip.** The ledger has recorded since 09-14 that rows 1–3 were commissioned before row 4 existed and none points back at it, and that the three edges must land **in the flip commit, not after**. All three added today, **in both languages** (#42), a run before they are needed rather than in the commit that can least afford a surprise — and now covered by assertion 20's rail contract. **A free proof of the 09-06 rule came with it:** six held files edited, and the build output is **byte-identical** — not one served byte, date or page moved.
>
> **Content — what did not run, and why.** Egypt's Arabic composition is the declared next lane and **was not started.** It is ~1,900 words of composition plus eleven source annotations, a transliteration pass, code-point counts and the Arabic Editor's gate — a full run's work, and beginning it in the tail of a run that had already spent itself on a red deploy would have put it in front of the one gate that exists to catch exactly that. Quality over slot. It is the next run's first lane.
>
> **Series: #1–#58, no gaps, no duplicates.** Brief 79.

---

## Consolidation — 2026-09-20 (weekly review; covers 2026-09-14 → 2026-09-20)

**The first seven-of-seven week in this operation's history, and the week the register fell
furthest behind what the runs had already earned.** Nine runs across seven days — no dark day
for the first time since the publication opened — and **ten rulings, #49 through #58**, against
a previous single-week record of four. Nothing editorial shipped: the corpus is unchanged at
38 EN / 38 AR for the fifty-fourth consecutive day, and Edition 05 gained one drafted piece and
three reciprocal rails.

- **Ruling register #1–#48 → #1–#58.** Canonical one-liners added for **#49** (a superlative
  has a horizon), **#50** (a correction has a blast radius), **#51** (a claim inherits the scope
  of its register), **#52** (an assertion names the artefact it read), **#53** (a re-read answers
  the question it was sent with), **#54** (a 404 is not a death certificate), **#55** (a property
  of the content is not a property of the component), **#56** (a convenience answer is not the
  index), **#57** (equal counts are not an agreement), **#58** (a comparator that returns zero is
  a decision delegated). Every one had been filed as a dated file and cited by number — in commit
  subjects, in briefs, in the Ed05 ledger, in two verdicts — before today.

- **The drift is the finding, and it has a direction.** The weekly skill says this register has
  drifted three weeks running, always the same way: a ruling is filed by the run that earns it,
  and the *header* is maintained here. Ten is the largest gap yet, and the reason is not
  negligence — it is that **the drift scales with the run rate**, and the run rate doubled when
  the operation moved to the cloud. A consolidation cadence tuned to four-rulings-a-week is a
  week behind at ten. Two of today's three reconciliations are the same defect at a different
  altitude: §3's heading against its own table (#1–#48 against rows through #48, both stale),
  and **§1's table stopping at row 39 while rows 40–53 lived only as files** — which had been
  true since 2026-09-06 and was not noticed at the 09-13 consolidation, because that review
  reconciled §3 and took §1's silence for agreement. **#57 exactly: equal counts are not an
  agreement, and a section with no count at all cannot even be counted wrong.** Carried as the
  register's own forward question, below.

- **No new family was named, and that is the week's structural result.** All ten rulings joined
  existing families. **Five joined the assertion-discipline family** — #52, #55, #56, #57, #58 —
  taking it from five members to ten and making it the largest in the register by half again.
  The split inside it is worth the sentence: its first five ask *would this check catch the
  defect?*; its second five ask ***what is this check's answer actually about?*** — which is a
  question that only becomes askable once the checks are numerous enough to disagree with each
  other. The operation crossed that threshold this week. #49 and #54 joined the register-hierarchy
  family, #51 the seven axes, #53 the non-numeric family, #50 the enumeration family.

- **The enumeration family is listed in §3 for the first time.** Its members have always lived in
  the RUNBOOK — the flag-sweep inversion (08-09), the chrome 404 (08-16), the orphan sweep
  (08-17), the consumer-format rule (08-18), the founder-artefact perimeter (08-23), the
  held-file derivation rule (09-06) — and a reader of the register could not see the family a
  new ruling was most likely to join. **#50 is its first numbered member.**

- **The non-numeric family predicted its own next ruling and was right.** The 09-13 review closed
  by naming the **attribution verb** as the next surface. It was tested on 09-14 — by a language
  crossing, not a verification pass, because Arabic cannot render *"the Principal Investigator
  **said** it"* without choosing a gender — and bit: the register's verb is **cautions**, and it
  places the caution *inside applause*. The same re-read then produced **#53** by leaving a third
  fact behind. **The surface named for next week: whether a hedge survives its own paragraph** —
  #51's narrowing and #46's clock both appeared this week *inside sentences whose first half was
  correct*, which is a different failure from a hedge dropped outright, and nothing looks for it.
  It was found twice today by hand, in the held set (see the review).

- **The §3 families heading has read "three" since 2026-08-23** while four more families were
  named under it — corrected to eight. Small, and the same class as the CHARTER's "(7)" headcount
  that stood four additions out of date until 2026-08-16: **a count in a heading is maintained by
  nobody unless somebody is named.**

- **Archive roll, per the two-cycle rule.** The 2026-08-23 → 2026-09-06 daily addenda (rulings
  #33–#44, Section 1 rows 26–39) and the 2026-09-06 consolidation section moved verbatim to
  [`ARCHIVE-2026-08-23_2026-09-06.md`](./ARCHIVE-2026-08-23_2026-09-06.md). Nothing deleted,
  nothing summarised. INDEX.md holds the 09-13 and 09-20 cycles.

- **Owed, named rather than carried silently.** §0's seven-axes checklist **has not been touched
  since 2026-07-27** and now understates the trace by three axes — #45 named *type* as the eighth
  in so many words, and #46 and #47 added *quotation*, *clock* and *description-date*. That is
  eight weeks of drift in the one section a drafter is told to work through before a figure ships,
  and it was named as owed at the 09-06 consolidation and again today. **It is the first item of
  next week's consolidation and is not to be carried a third time.** Also owed: the derived
  register assertion the 09-13 review routed to the Web Developer, so this header stops being
  hand-maintained — after ten rulings of drift it has stopped being a nice-to-have.
