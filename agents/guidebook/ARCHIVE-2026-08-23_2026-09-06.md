# Guidebook archive — 2026-08-23 → 2026-09-06

**Rolled out of `INDEX.md` at the 2026-09-20 weekly consolidation**, under the two-cycle rule:
the index retains the two most recent consolidation cycles (2026-09-13 and 2026-09-20) and
everything older moves here. **Nothing is deleted and nothing is summarised** — the blocks below
are verbatim.

What this file holds: the daily-run addenda for **2026-08-23 → 2026-09-06** (rulings **#33–#44**,
Section 1 rows **26–39**) and the **2026-09-06 weekly consolidation** section in full. The period
covers Edition 05 opening, the Zambia / Sierra Leone / Sudan pairs being drafted, composed and
banked, the register-hierarchy family being named, and the assertion-discipline family growing
from #35 to #39.

Earlier archives: [`ARCHIVE-2026-06-14_2026-08-09.md`](./ARCHIVE-2026-06-14_2026-08-09.md) ·
[`ARCHIVE-2026-08-09_2026-08-23.md`](./ARCHIVE-2026-08-09_2026-08-23.md).

The rules themselves stay canonical in `INDEX.md` §1 and §3. What lives here is the *story* behind
them — the origin cases, the walls hit, the numbers as first read.

---

> **Daily-run addendum — 2026-08-23 (four dark days named; ruling #33; the Arabic corpus becomes Arabic).** State first, plainly: **2026-08-19, 08-20, 08-21 and 08-22 produced no run, no brief and no work at all — four consecutive dark days,** the longest stretch since 07-30→08-01. Nothing was lost and nothing live went stale: origin and local HEAD both sit at `dcb9f35` (08-18), the tree is clean, and the 08-18 block — which cleared the Arabic About 404, the VALENCE orphan and the og:image SVG defect — is deployed and serving. There is no push gap today. The failure was cadence, not craft, and it earns one clean recovery day, never a forced double-batch.
>
> **Section 1 gains row 26 — `2026-08-23-a-parity-check-counts-files-not-language.md`, filed as ruling #33.** Unusually, it was opened by the **QA pass, not a recon** — the second consecutive defect found by asking what the *consumer* receives rather than what the *repository* contains. **The rule: a bilingual publication's parity check must read the words on the page, not count the artefacts behind it.** "38 Arabic files exist for 38 English files" is a statement about the repository, not about what an Arabic reader sees. **Corollary, and the sharper half: markup that declares a language is a claim, and a claim is checked** — the presence of correct language metadata around incorrect content is the most durable form of this defect, because it survives every audit designed to catch its *absence*.
>
> **Origin case.** From the first publish on **2026-05-25 until today — 90 days, the entire life of the publication** — every Arabic page printed the English controlled vocabulary to an Arabic reader: the byline block, the marginalia «المرجع الميداني» (`Sierra Leone · Africa` / `المرحلة · K-12` / `المحاور · value of teachers، ECE access`), the related-reading rail under an Arabic headline, the country tag on the AR home and AR editions index, and the `<category>` of every item in the Arabic feed. The worst line in the codebase, and the reason nothing caught it: `<span class="piece__country" lang="ar" dir="rtl">{piece.data.country}</span>` — markup that **asserts** the content is Arabic, sets the direction for Arabic, and interpolates `Sierra Leone`. Parity counted 38 against 38. The build was clean. Links resolved. `hreflang` was reciprocal at 476 pairs. `lang` and `dir` were present *and correct*. **Not one check read the words.**
>
> **Why it is structural, not careless.** `content.config.ts` is one schema shared by both collections, deliberately — that is what makes the corpora comparable and what `madar_stats.py` counts. So the data was right and the display was wrong, and nothing in the pipeline distinguished the two. **The general shape: a controlled vocabulary is data; data crossing a language boundary needs a display layer, and the display layer needs its own assertion.** Every field a *writer* composes was Arabic from day one because a human wrote it in Arabic; every field the *site* generated from the schema was English because no human wrote it at all. **The defect lives exactly where authorship ends and templating begins** — where editorial review does not look and automated checks do not read.
>
> **The fix.** `web/src/lib/i18n-geo.ts` — a display layer, not a data change: `COUNTRY_AR` (35 shipped + 17 pre-mapped for Ed05, so the first Arabic composition of the Zambia piece cannot land on a fallback), `REGION_AR` (the enum exactly), `LEVEL_AR`, `THEME_AR`. Schema untouched, no article file edited, English renders identically. **Fail-loud, never fail-quiet:** an unmapped key returns English so a new country cannot break a build, but every fall-through is recorded and `missingArabicGeoKeys()` reports it — a silent English fallback is the exact defect the module exists to end. `agents/tools/qa_ar_language.py` is the standing assertion, now in the daily gate: it reads the **chrome** and fails on Latin-script controlled vocabulary there, and **deliberately does not check body text or the sources list**, because Latin script there is correct and required under **#28a** (*Universidad de Santiago de Chile*, *India TV News (PTI)*, *Uruguay XXI* — 8 such hits today, all legitimate; **a check that flagged them would train the team to ignore it**). Allowlists carry reasons and are scoped to elements that *declare* `lang="en"`, so correct markup earns the exemption and bare Latin text does not — `English version ↗` is right on an Arabic page because a language switch is labelled in its target language, mirroring «النسخة العربية ↗»; `lang="en" dir="ltr"` was added to it this run, the English page having carried `lang="ar" dir="rtl"` on its counterpart since launch. Zero chrome fields located = exit 2, not exit 0 (the 08-16 trap). **The check was proved against the defect, not merely run: 235 defects on the pre-fix build, CLEAN on the post-fix build, same 235 fields.** A new assertion never shown to fail on the bug it was written for is not evidence of anything (#16 generalised).
>
> **The feed, same lens, second finding.** Yesterday's forward question asked whether the feeds are readable by anything but us; **well-formedness is to a feed what "resolves" was to the og:image.** `<title>` is plain text by RSS 2.0 spec — no markup, so no `dir` — and a reader drops the string into a paragraph whose base direction is the reader's UI language, LTR for most subscribers, under which trailing punctuation and digit runs (**"12,348"**, **"2026"**) resolve to the **wrong side** of an Arabic string. **27 of our 38 Arabic deks mix Arabic with Latin or digits and not one character of direction metadata appeared anywhere in either feed.** Fixed: **U+200F RLM** prefix on every AR title and category (the only lever a markup-free field offers); `<div dir="rtl" lang="ar">` on every AR description (use markup where markup is available); and `<enclosure>` on all 76 items across both feeds pointing at the 08-18 raster cards — **raster only**, the 08-18 rule binds the feed exactly as it binds `og:image`, and the assertion checks both the extension and that the file exists on disk.
>
> **Binding.** Not retroactive to prose; no shipped Arabic body text is reopened. **The Verifier's checklist gains one question — *does every field on this page that the site generated, not the writer, appear in the page's own language?*** Two acceptable answers: it does, or it is a foreign-language string that declares its own `lang` and is exempted by name. **"The parity check passed" is not an acceptable answer**, and per the 08-16 trap an Arabic surface raising no language question has not been checked. Generalises past Arabic: a third language means a second display map, not a schema change.
>
> **Also this run.** **Growth — structured data, the first on the site:** until today **not one of 82 pages carried JSON-LD**. The head was otherwise strong (canonical, reciprocal hreflang + x-default, absolute raster og:image, twitter card, robots) but all of it describes the page's *identity* and none describes what the page *is*. `web/src/lib/structuredData.ts` now emits `schema.org/Article` on all **76** article pages in both languages, and the load-bearing field is **`citation`** — the publication's whole claim is that every piece rests on named primary sources, and we were shipping the evidence while hiding the bibliography. Conservative by construction: only fields populable truthfully from frontmatter, `datePublished` as a **date not a timestamp** (the corpus records a date; inventing an hour would be a fabrication), and `author` as the Organization because pieces are bylined «هيئة التحرير», never an individual. The Arabic edition gets the **Arabic** `about` and `keywords` — #33 binds the machine-readable description of an Arabic page exactly as it binds the visible chrome. **Content — Ed05 recon 9 (`content-drafts/recon/2026-08-23-ed05-zambia-free-education-statute.md`) fills the last empty regional quarter, Southern Africa,** and the evidence moved the answer away from the reputational shortlist: **Zambia commissioned** on the Education (Amendment) Bill 2026 read in full as served text (s.15(1) *"A child who is a citizen shall have the right to free education"*; the **citizenship gate** at s.15(3); a **3-year criminal penalty** at s.15(4) for aiding a non-citizen child; **accommodation** brought inside the fee ban and defined as "bed space, a bed and mattress"; an undefined ministerial carve-out at s.119(2); and primary redefined **grade 1–7 → 1–6** on a 30 May 2026 → 2031 transition) — **Namibia banked** (N$663m funds **one quarter**, not a year; the verifiable content is a disbursement failure, wrong posture for this edition), **South Africa banked** (its own DBE TIMSS 2023 register reverses the improvement premise: grade-5 maths 376→374→**362**, grade-9 gains **not statistically significant**), **Botswana declined** (President **Duma Boko** called the system *"in shambles"* in the government's own news agency, March 2025). Three draft-blockers stand: the assent date (both settling registers served empty), the **`edu.gov.zm` total wall** behind which the entire Education Statistics Bulletin series sits with no mirror, and the committee report behind a provenance wall. Ruling series now **#1–#33, no gaps, no dupes.**

> **Daily-run addendum — 2026-08-24 (three draft-blockers cleared; ruling #34; Zambia commissioned; the machine consumers get their surface).** State first: no dark day and **no push gap** — origin and local both at `5e16db2`, tree clean, the 08-23 recovery block deployed and serving. Yesterday's forward question was aimed at three surfaces that had never been asked *what does the consumer actually receive*; **all three were defective, and a fourth was found beside them.**
>
> **Section 1 gains row 27 — `2026-08-24-a-total-is-a-figure-and-must-be-added.md`, filed as ruling #34.** **The rule: every published table must be added before any number in it is quoted — including, and especially, its own total row.** All seven existing axes check a figure against something *outside* itself (source, vintage, denominator, funnel stage, ruler, counterfactual, agent); **none checks a table against itself**, and the total is the one figure in any document verifiable with no second source, no fetch and no judgement. **Corollary:** when a printed total and its own line items disagree, the disagreement is the story — quote neither bare; give the line items, then the printed total, then the delta.
>
> **Origin case.** The Auditor General's *Performance Audit Report on the Implementation of the Free Education Policy 2020–2024*, read in full as served text once the provenance wall fell. The Ministry's own teacher-recruitment table: ECE **548**, primary **1,057**, secondary **2,595** — printed total **7,841**. The rows sum to **4,200**, short by **3,641**. **Three of the four columns add exactly** (planned 2024 = 8,000 ✓, planned 2023 = 5,971 ✓, actual 2023 = 7,481 ✓), which is what makes the fourth usable rather than ambiguous: the table's own convention is established, so the failure is internal arithmetic and not a scope we invented. The printed total reads **98.0%** delivery; the line items read **52.5%** — and the shortfall sits exactly on our remit, **ECE 548 of 2,000 planned, 27.4%**, in a country whose new Act writes day care, nursery and reception into statute. The number is government-published, correctly attributed, correctly dated, in its own primary register, with its denominator beside it. **It passes every check we own. It does not pass addition.** Addition also *rescues* numbers: 115 GRZ secondary schools = 69 + 36 + 10 ✓, ZEEP 202 = 82 + 120 ✓, ZEEL 222 = 110 hub + 112 satellite ✓ — and 69 + 82 = **151 operational**, which converts the search-stage "151 secondary schools completed" into a traced, composed figure for the first time.
>
> **Content — Ed05 Zambia COMMISSIONED; the recon bank closes at nine and drafting opens.** All three of recon 9's draft-blockers moved today. **(1) Assent — RESOLVED: 4 June 2026.** Open Zambia's own next-day report (served) states the signing as done; Lusaka Times carries two same-day items; the Zambian Observer supplies an *independent non-date anchor* — the signing fell on the President's 64th birthday. `sh.gov.zm` and `znbc.co.zm` still serve empty, so the wall holds and the **Act number remains untraced**: the piece cites the Act by name and assent date, never by number. **(2) The provenance wall FALLS** — the committee report is read in full and is the independent implementation audit the recon wanted. **(3) The `edu.gov.zm` ESB wall HOLDS** — every URL still serves an empty body — but the audit supplies something better than the series: the Ministry's admission, on the record, that the **2022–2023 Bulletin was published only in November 2024**, a two-year lag. That is *why* the "2.3 / 2.5 / 2.6 million returned" ledgers never reconciled, and it lets us decline the aggregate with a citation instead of a suspicion. Also banked from the same source: the audit's **self-declared urban sampling frame** (schools near the CBD; 61 districts questionnaired, **27 responded** — 44%), which must be named in any sentence that leans on it; the 2 million claim finally *sourced* — as a Minister's attribution inside an audit, i.e. a perfect specimen of #32 rather than a usable figure; and the **Grade-7 spine sourced at both ends**, the 2002 policy having been defined on *"grades 1 to 7"* and the 2026 Act redefining primary as 1–6.
>
> **Method note worth its own line, and it bit twice today.** A **search-result summary is not a served source.** It is a provenance instrument — it can put a URL inside the fetch tool's reach, which is precisely how both walls were approached this run — and it is never a figure. Proof it bites: the identical pair **154,304 → 158,504 teachers** arrived attached to **2022→2023** in one search summary and **2023→2024** in another. Same two numbers, two different year-pairs, neither served. **The ESB series stays unusable.** Filed inside row 27 rather than as its own number: it governs how a source is *reached*, and #20/#26 already carry the wall doctrine.
>
> **Quality — the consumer lens, third consecutive run, four findings.** `agents/tools/qa_consumer_surface.py` is the new standing assertion, proved against the pre-fix build first (**169 defects, exit 1**) and clean after (**exit 0**), per #16. **(a) `og:locale` was absent on all 82 pages** — and absence is not neutral here: the Open Graph default is `en_US`, so every Arabic page has served an Arabic title and an Arabic description under an implicit claim of American English since **25 May**. This is **#33 one surface further out** — markup declaring a language is a claim, *and so is the claim made by omission*. **(b) `og:type` was hardcoded `article` on all 82 pages**, including both home pages, About and both editions indexes; the default is now `website` and article routes opt in, the fail-safe direction. **(c) `og:site_name` absent** on every Astro page. **(d) No `@media print` rule had ever existed** — the question asked was whether Arabic prints RTL; the answer was that *no page had print handling at all*, so every printed piece carried the skip link, nav, share rail and related rail, and **dropped every source URL from the bibliography.** For a publication whose entire claim is that each piece stands on named primary sources, and whose likeliest printer is a ministry adviser or a researcher, that is the same defect family as the SVG card and the Latin-script Arabic chrome: the artefact existed, every check passed, the consumer received something unusable. Fixed, with source URLs now printed after each citation.
>
> **Growth — the sitemap gets a `lastmod`, from git.** 82 URLs had carried **zero** `lastmod` since the hand-patched file was retired on 07-02, while we revised all 76 article pages twice in one week. Build time was rejected (every page claiming to change on every deploy is the canonical unreliable pattern, and Google discounts what it judges unreliable); the frontmatter publication date was rejected (it asserts the page has not changed since publication, false for every page here). We use the **last commit that touched the files a page is built from** — a real modification date. `web/src/styles` is deliberately excluded from the chrome set: presentation is not content, so today's print block must not bump 82 dates. Timestamps pass through **untruncated**, because truncating to a date made `@astrojs/sitemap` re-inflate it to an invented midnight — the same fabrication the 08-23 structured-data work refused. And the resolver **throws on a shallow clone**: `actions/checkout@v4` defaults to depth 1, where this would have passed locally and emitted nothing in CI — ruling **#16**, verify in the judging environment — so `fetch-depth: 0` lands in the same commit. Ruling series now **#1–#34, no gaps, no dupes.**

> **Daily-run addendum — 2026-08-25 (Ed05 drafting opens; ruling #35; the assistive-technology surface; a deploy-breaker caught before it ever pushed).** State first, plainly: **no dark day, but a PUSH GAP — the whole of 08-24 was produced and never committed.** Origin and local both sit at `5e16db2` (08-23) while the working tree carries yesterday's entire block: the `og:locale`/`og:type`/`og:site_name` fixes, the print stylesheet, the git `lastmod` resolver, `qa_consumer_surface.py`, guidebook row 27, the Zambia commission verdict and Brief 56. **Nothing was lost; nothing live went stale; the site simply has not received a day's work it was told about.** Today's push carries two days.
>
> **Section 1 gains row 28 — `2026-08-25-an-assertion-must-be-proved-against-a-control.md`, filed as ruling #35.** **The rule: #16 says prove a new assertion FAILS on the defect it was written for; that is half a proof. The other half is proving it does NOT fire on a case already known to be correct.** An assertion pointed only at broken output cannot tell "I found the bug" from "I flag everything" — both produce a number and a satisfying red. **Corollary: the first number a new assertion reports is not a finding, it is a hypothesis about the assertion.**
>
> **Origin case — three wrong numbers in one hour, from a check that was itself never checked.** The consumer question aimed for the first time at a reader using a screen reader, which takes its voice from the nearest `lang` and never from the script of the characters. **Attempt 1 reported 40** — a regex with no model of inheritance, the exact mechanism it was auditing; two hits were the Arabic `<em>`s inside `<div class="about__col ar" lang="ar">`, correct as written. **Attempt 2 reported 542** and flagged `مدار` on the editions index — the wordmark, which has carried `lang="ar" dir="rtl"` in header and footer since the site was built, i.e. **the one place somebody had already solved this exact problem. That is the control, and it failed**; cause was stack discipline, void elements pushed and never popped. **Attempt 3 reported 473, then 471**, after the scope was inverted from a denylist of prose (which had been *guessing* the class the markdown renderer wraps body text in, and so reported the name *Elsevier* and a Spanish pull-quote as defects) to an **allowlist of the chrome we compose ourselves**. **Only then did the control pass, and only then was the number worth quoting.**
>
> **The 471, and their shape.** The site was careful about bilingual markup in exactly one place and nowhere else — the signature of a rule applied by hand rather than by system. **81 `العربية`** and **79 `English`** on the language switchers (`hreflang` describes the *destination* document and says nothing about the language of the link text — `lang` is the attribute that governs pronunciation); **79 `Madār`**, whose Arabic twin has carried `lang="ar"` since launch while the Latin half never carried `lang="en"`; **40 × `Skip to content` on every Arabic page — not a `lang` defect at all but an untranslated string, and the first thing a screen reader announces**; 40 `RSS`; **114 across `WhatsApp` · `X` · `Email`**, where every `aria-label` on the share component was localised and the three *visible captions* were not — the usual oversight runs the other way; and **38 still colophons** (`سكون · The Still · Curated NN · Country`), genuinely bilingual inside one span, fixed by splitting into script runs in `web/src/lib/scriptRuns.ts` because **`lang` describes a run of text, not a box**. Re-run after fixes: **0 defects across the same 3,230 checked nodes.**
>
> **Two findings filed inside row 28 rather than as their own numbers.** **(a) A guard must fail on the condition it names, not on its neighbours.** Yesterday's `sitemapLastmod` resolver — written to fail loudly rather than emit an empty `lastmod` in CI — carried two defects, both caught this morning, **neither ever pushed**: it merged *shallow repo* (data would be **wrong**) with *no repo at all* (data is **absent**) into one fatal path, making the site unbuildable outside a git work tree; and its pathspec `web/src` resolves relative to the **current directory**, while the workflow sets `working-directory: web`, where it matches nothing, git exits 0 and the map comes back empty. **The guard written to prevent a silent CI failure would have caused a loud one on the first push carrying it** — #16 biting the very file whose docblock cites #16. Fixed with git's repo-root-relative pathspec (`:/web/src`), which resolves identically from any cwd; verified by building with `CI=true` inside a real non-shallow clone, **82 URLs, 82 `lastmod`**. **(b) A sitemap entry is a declaration; a link is an edge.** VALENCE, published 08-08, returned 200 with every link on it resolving and sat in the sitemap — so the **08-18 sweep recorded the orphan as CLEARED. Nothing on the site linked to it, and nothing ever had**: 17 days reachable only by someone who already knew the URL. Our sweeps asked *does this link resolve* and *is this page in the sitemap*; **neither asks the question a crawler answers — from the front door, following links, where can I get to?** Now standing as `agents/tools/qa_reachability.py`, proved both ways (**exit 1 on the pre-fix build naming VALENCE, exit 0 after**), and urgent precisely because **0 of 82 URLs are indexed**: the first crawler to arrive walks the graph, and anything off the graph stays invisible after the rest starts working. Fixed by giving VALENCE a home on the editions index under **Standing pieces** — 82 pages, **82 reachable, 0 orphans**, click-depth 0/1/2/3 = 1/11/40/30.
>
> **Content — the first Edition 05 draft is written.** `web/src/content/articles/2026-08-25-zambia-free-education-act.md`, *"The law that moved the finish line"*, ~2,270 words, **held `approved: false`** pending the Editor's five tests and the Arabic composition. Every prohibition from the 08-24 commission is honoured in the text: **no "children returned" aggregate** (declined *in* the piece, citing the Ministry's own record of a two-year bulletin lag rather than our suspicion); **no ESB figures in any form**; **no Act number** (cited by name and assent date, 4 June 2026); **the teacher table given as line items, then printed total, then the statement that they do not reconcile while the other three columns do** — no side picked, nothing rounded; **151 shown with its 69 + 82 composition**; the audit's **urban sampling frame and 27-of-61 district response named in the section that leans on it**; the 70.26% pass rate scoped in the sentence that uses it (full certificates among candidates who *sat*, 28.22% partial statements, denominator entrants not cohort, cohort swollen by the intake the piece is about); the 2022 ministerial statement used at the **allocated** funnel stage, never as achieved; the **August 2026 general election named, not tiptoed around**. Hero still drawn (`2026-08-25-zambia-free-education-act.svg`) — the finishing marker lifted one course short of its socket, the gate laid flat, the bed brought inside the threshold. **Corpus unchanged at 38 EN / 38 AR; no article approved or flipped this run.**
>
> **Also this run.** The build caught a **P1 in our own draft before any human read it** — the dek at 232 characters against a 200 cap, trimmed to 177 in the same run, the same shape as the 08-08 England AR dek and a reminder that a held piece still validates against the schema even though it is held out of the rendered build. **QA green** on a `CI=true` build inside a real clone: 82 pages exit 0, parity 38/38, 82 sitemap URLs with 82 `lastmod`, 476 hreflang, 84/84 stills resolving, 0 SVG `og:image`, both feeds 38 items / 38 enclosures, and all four standing assertions clean — `qa_ar_language` 235 chrome fields, `qa_consumer_surface` exit 0, `qa_a11y_lang` 0 of 3,230 nodes, `qa_reachability` 0 orphans. Ruling series now **#1–#35, no gaps, no dupes.**

> **Daily-run addendum — 2026-08-26 (ruling #36; the front doors had no card; Zambia composed in Arabic; the reachability check was walking one language).** State first, plainly: **no dark day, and the 08-25 block was committed — but it was never pushed.** Local `HEAD` is `f66162d`, `origin/main` is still `5e16db2` (08-23), working tree clean. So the shape is new: not work stranded outside git, but a *commit* stranded outside origin. **The live site has been serving the 08-23 block for three days** while two days of fixes sat one command away. Per the 08-16 push-gap escalation rule this is day 1 unpushed with a full commit staged, so the push block moves to the top of the brief.
>
> **Section 1 gains row 29 — `2026-08-26-a-promise-must-be-derived-not-declared.md`, filed as ruling #36.** **The rule: when markup makes a claim about an asset, the claim must be COMPUTED from the asset, in the same expression, so the two cannot be separately true.** **Corollary: there are always two fixes for a broken promise — supply the missing thing, or make the promise underivable without it. Only the second fixes the class; take it in the same commit as the first.**
>
> **Origin case.** `Base.astro` emitted `twitter:card = summary_large_image` **unconditionally** while emitting `og:image` **conditionally** — two adjacent lines about the same asset, one guarded and one not. Result: `/`, `/ar/`, `/editions/`, `/ar/editions/` and `/about/` — **including both front doors, the URLs a reader is most likely to share** — promised every scraper a large card and delivered nothing, since **25 May, 93 days**. This is the **08-18 SVG-card defect in a second costume**, and that day's fix did not prevent it because the fix enumerated **articles**, and every article has an image. **Sixth statement of *a green check is scoped to what it enumerates*, and the first where the previous member of the family is itself what failed to generalise.** Why it survived: `twitter:card` **reads as configuration** and is in fact a per-page factual claim. Fixed by derivation (`ogImageAbs ? 'summary_large_image' : 'summary'`), so a page without a card can no longer promise one whatever anybody writes in a template later; the asset was supplied too, but **the guard is the fix of record**. `qa_consumer_surface` gains two assertions — every served page has an `og:image`, and card and image agree **in both directions**. Proved per **#35**: **10 defects on exactly the 5 known-broken pages, silent on all 77 known-good ones.**
>
> **Growth — the front doors get a share card, drawn with zero live text.** `/og/brand-card.png`, 28 KB, 1200×600, now on all five. The first version set a strapline in Newsreader and Amiri; **neither brand face is installed in the rasteriser**, so the Arabic came back in a fallback sans and the English strapline ran off the right edge. **A share card's judging environment is the flat PNG** — the consumer never sees the SVG, so there is no second chance to fall back. The shipped card carries only the wordmark, which is real Newsreader + Amiri outlines extracted to paths and therefore pixel-identical in every renderer, on the brand's own orbit motif. Filed inside row 29 as a second-order note on **#16**; found by **looking at the output**, still the cheapest check we own.
>
> **Quality — `qa_reachability` was walking one language and reporting on two.** Yesterday's new assertion started at `/index.html`, the **English** front door, and therefore proved something narrower than it printed: that every page is reachable *by a reader willing to pass through English pages*. An Arabic reader arrives at `/ar/`. Had an Arabic article been linked only from an English hub, the check would have said PASS over a hole in the Arabic edition — the 08-17 orphan, one language over. The walk now runs **per language root, confined to that language's pages**, with the whole-site walk kept as a third pass for language-neutral pages like VALENCE. **The Arabic subgraph turned out complete on first contact — 40/40, max click-depth 2, better than the English side's 3** — so this is a control that passed, and it was proved to bite separately: with the article links severed from the two Arabic hubs it reports **12 orphans, while the old whole-site pass reports 0**, which is precisely the blind spot demonstrated.
>
> **Also: the 08-25 QA log did not carry its forward question**, which the 08-23 ruling makes a **required** written output of every pass, not a good habit. Named here rather than quietly corrected. Today's is at the foot of `agents/logs/qa-2026-08-26.md`.
>
> **Content — Zambia composed in Arabic; row 30 files the routing rule the Africa edition needs.** `2026-08-26-zambia-arabic-composition-and-bantu-name-routing.md`. **`ph` in Bantu orthography is an aspirated /pʰ/, never /f/** — Phiri is *pee-ree*, so **ميليسيانا بيري**, not فيري. Latin orthography is a false friend here in a specific, predictable way, and the reader of the Arabic cannot recover the error; the rule converts an unresolvable per-name question into a resolvable per-language one, which is what keeps such names off the PROVISIONAL list forever. Sibling stated only for contrast: `ch` is /tʃ/ (**شيلالا**), which *does* match English habit — so nobody generalises "trust the English reading". Five named humans gated, all CONFIRMED, all appearing in source glosses only. **AR dek 191/200 with tashkeel, checked before the build rather than by it** — the EN dek of this same piece was yesterday's P1 at 232. **Corpus unchanged at 38 EN / 38 AR**; both Zambia files held `approved: false` pending the Editor's five tests on the pair and the Ed05 wave gate. Ruling series now **#1–#36, no gaps, no dupes.**

> **Daily-run addendum — 2026-08-27 (ruling #37; the origin becomes a judging environment; Zambia banked as a pair; Sierra Leone commissioned; the feed debt paid).** State first, plainly: **the push gap reached four days and compounded.** Local carries the 08-25 commit (`f66162d`) that origin (`5e16db2`, 08-23) has never seen, **plus the whole 08-26 day uncommitted in the working tree** — two gap shapes stacked. No dark day: 08-26 ran and produced Brief 58; the work is real, versioned or on disk, and **not one line of it has reached a reader since 08-23.** Push block at the top of the brief, per the 08-16 escalation rule, third day running.
>
> **Section 1 gains row 31 — `2026-08-27-a-contract-in-prose-is-a-declaration.md`, filed as ruling #37.** **The rule: an interface convention that lives in a comment binds nobody — including its own author, including within twenty-four hours. An invariant is computed where the value enters, or it is a description of yesterday's callers.** Origin case: `Base.astro`'s own comment — *"callers pass base-prefixed paths"* — obeyed by every article caller since 07-16, broken by the five brand-card callers added **the day after the 93-day card defect was fixed**: `/og/brand-card.png` absolutised without `/madar`, a 404 card on both front doors, present/raster/absolute and wrong. **Fixed by derivation per #36** (Base.astro applies the base itself; the mistake is no longer expressible) and **asserted by existence, not presence**: `qa_consumer_surface` now dereferences every `og:image` URL back to a file in `dist`. Proved per #35 — 5 defects on exactly the 5 broken pages, silent on 77.
>
> **Quality — the 08-26 forward question answered in code: `qa_live_drift.py`, the first check that reads the ORIGIN.** Every prior check read `dist`; the deploy died for four days in July and the origin sat three days stale in August with every local light green. The new assertion fetches the live sitemap and sampled heads and diffs against the build. Proved both ways: **control** = live vs a build of `origin/main` — clean (82 URLs, 4/4 heads identical), which also proves deploys are deterministic end-to-end; **bite** = live vs today's tree — **84 named drifts**, and the bite's own output is what caught the base-less card URL. Second-order finding, filed in the row: **a new check's first bite, pointed at real state instead of synthetic breakage, is a free defect sweep.**
>
> **Content — Zambia is BANKED as a pair; the pipeline moved to mandated slot #1.** The Editor re-ran the five tests on the PAIR (`2026-08-27-ed05-zambia-pair-verdict.md`): trace survives composition, all nine prohibitions hold in Arabic at full weight, posture intact, spine dek-to-close in both languages, and test 5 closed by the 08-26 composition + Arabic Editor gate (row 30). **First Edition 05 piece complete — both files stay `approved: false` until the founder's wave gate; zero build leak re-verified.** Sierra Leone commissioned same run (`2026-08-27-ed05-sierra-leone-commission.md`) off the 08-12 PROCEED recon: the SLEIC story with the reversed scoped-instrument spine — *the measured party chose the harsher ruler on itself* — with the OPM report read and a named MBSSE voice as draft-blockers, and مصطفى/Mustapha flagged to the Arabic Editor as the row-19 mixed-origin case.
>
> **Growth — the feed debt, owed since 08-18 and named three times, is PAID — in the consumer, from the origin.** Both live feeds parsed with feedparser (a real reader's library, not our own regex): **bozo=0 both languages; 38 items each; all 152 item links + enclosures dereferenced 200 from the origin; every enclosure `image/png`; and the Arabic direction work survives the parse — 38/38 titles carry the RLM as parsed, 38/38 descriptions keep their `dir="rtl"` wrapper.** The 08-23 markup is confirmed as a render, which is what "verified" was always supposed to mean on this surface. **Corpus unchanged at 38 EN / 38 AR; no approved flips.** Ruling series now **#1–#37, no gaps, no dupes.**

> **Daily-run addendum — 2026-08-28 (ruling #38; both Sierra Leone draft-blockers cleared and the piece drafted; breadcrumbs for the machine; the gap reaches five days).** State first, plainly: **the push gap is now five days and carries three shapes.** origin/main still `5e16db2` (08-23); local still carries the stranded `f66162d` (08-25); the 08-26 and 08-27 days remain uncommitted; today stacks a third uncommitted day. `qa_live_drift` counts the same **84 drifts** a reader is waiting on. Nothing lost, nothing delivered — one block clears all of it.
>
> **Section 1 gains row 32 — `2026-08-28-a-source-can-drift-against-itself.md`, filed as ruling #38.** **The rule: vintage discipline applies INSIDE a document — a figure carries its page. When one source states a fact twice and disagrees, cite the register (body vs table vs acronym list), pick by a stated hierarchy, never average, never choose silently.** Origin cases, both in the OPM SLEIC evaluation read today: the pooled maths effect is **0.278 SD in body text and annex, 0.280*** in its own Figure-1 transform table** (body+annex agree → 0.278 is the figure of record, table noted); and **NYAF is expanded two different ways in the same report while its own acronym list omits the name entirely** (internal arbitration fails → external register → the piece uses the bare acronym, per institution-keeps-its-own-name). Extends rows 13/21 (cross-document vintage) to the intra-source case that no cross-source trace can catch — both readings are "confirmed by the primary."
>
> **Content — both Sierra Leone draft-blockers cleared same-day, and the piece is DRAFTED.** The 264-page OPM evaluation fetched from the Challenge-Accepted page and read via pdftotext (`recon/2026-08-28-ed05-sierra-leone-opm-read.md`): pooled **0.278 SD maths / 0.102 English**, lot map EducAid/Street Child/Rising/StC/NYAF = Lots 1–5, lot-level maths 0.130–0.384 with **only Lots 2 and 3 sustaining Y2→Y3**, and **the "green provider" identified as Lot 3, Rising Academy Network** — largest maths effect and the only lot with significant English across both genders. The named MBSSE voice found in served text: **Hon. Conrad Sackey**, keynote at the 15 Jan close-out — including his election to lead with the programme's weakest subject (English, the finding that "made me pause") — plus CEO **Edward Kpakra**'s under-one-hour-of-teaching-per-day fact. `2026-08-28-sierra-leone-sleic-outcomes.md` staged at ~1,900 words, **held `approved: false`, zero build leak verified**; hero still drawn (the shorter ruler standing at the chalk mark, the taller one face-down); dek 194/200 — trimmed from a first cut at exactly 200, at compose not by the build. Arabic routing flagged, not assumed: ساكي, كباكرا (row-30 family), مصطفى (row-19), and NYAF stays Latin per row 32.
>
> **Growth — the machine-readable layer learns where pages SIT: BreadcrumbList on all 76 article pages.** The 08-23 Article node says what a page IS; nothing said where it sits, and once indexing starts the likeliest entry point is an article URL (articles outnumber hubs 76:6). `breadcrumbJsonLd` derives every value from the page's own lang+path (#36), reuses the SiteHeader's nav strings, and ships in both languages — verified in the build: 76/76 pages, parses as valid JSON-LD array, Arabic crumbs in Arabic with correct absolute AR paths. `Base.astro`'s `jsonLd` prop widened to accept a node array.
>
> **Quality — QA green on the /tmp route-around build:** 82 pages exit 0, parity 38/38, sitemap 82/82 lastmod, all four standing assertions clean (a11y 2,993 nodes · AR chrome 235 fields · reachability 82/82, 0 orphans · consumer surface CLEAN), both feeds 38 items, held-leak zero. Forward question filed in `qa-2026-08-28.md`: structured data is emitted and parses, but no check asserts it against the consumer's contract — candidate `qa_jsonld` dereferencing every breadcrumb item and workTranslation back to dist, the #37 existence pattern applied to the machine layer. **Flip-day note banked: Zambia + Sierra Leone og PNG cards must be generated at the wave gate.** **Corpus unchanged at 38 EN / 38 AR; no approved flips.** Ruling series now **#1–#38, no gaps, no dupes.**

> **Daily-run addendum — 2026-08-29 (row 33: the labial-velar routing rule; Sierra Leone composed in Arabic and gated; `qa_jsonld` closes the meaning gap; the push gap reaches six days).** State first, plainly: **origin/main is still `5e16db2` (08-23) — six days stale, fourth consecutive day the staged block went unrun.** Local HEAD `a340782` (08-28) committed everything; the working tree was clean at open, so nothing new is at risk — but not one line has reached a reader since 08-23, and `qa_live_drift` counts the same **84 drifts**. Per the 08-28 note, **tomorrow's weekly review treats founder-push availability as the constraint.**
>
> **Section 1 gains row 33 — `2026-08-29-sierra-leone-arabic-composition-and-labial-velar-routing.md` (no new ruling number; extends rows 19/30).** **The rule: the West African labial-velar stop /k͡p/ (written `kp`) is ONE co-articulated consonant, and Arabic renders BOTH closures: `kp` → كب** — Kpakra → **كباكرا**, never قاكرا (invented uvular) or باكرا (deleted velar half). The sibling that keeps it honest, same shape as row 30's `ch`: **Sackey → ساكي** by the heard vowel (*SAH-kee*, terminal `-ey` = /i/), which *does* match English habit — so nobody generalises either way. The same gate routed three more orthographies by heard vowel against their letters: Scottish **Gauld → غولد** (/ɔː/, not a letter-diphthong), Irish **Fergal → فيرغال** (Fearghal's hard /ɡ/, not English soft-g), Catalan **Castellnou → كاستيلنو** (/-ˈnɔw/, not French /u/). Seven named humans, 7/7 CONFIRMED, none PROVISIONAL; the commission's مصطفى flag recorded **moot** — the name did not survive to the final English draft.
>
> **Content — Sierra Leone is COMPOSED in Arabic and the Arabic gate is PASSED.** `articles-ar/2026-08-28-sierra-leone-sleic-outcomes.md`, «البلدُ الذي دفع ما قاله القياس», ~1,340 words at register, **held `approved: false`**, zero build leak proved. The register work that had to survive composition did: 0.278/0.102 carried from the body+annex register per #38 (the 0.280 table variant never appears); the two percentile claims stay unblended; *no demonstrated gain* renders as لا مكسبَ مُثبَت, not a diminutive; the three reach registers keep their names and their rare downward direction (يُدقَّق نزولًا); NYAF stays a bare Latin acronym in Arabic exactly as in English. AR dek **185/200 with tashkeel, checked at compose** (row 17). Gate verdict in writing: `verdicts/2026-08-29-sierra-leone-sleic-outcomes-ar-gate.md`. **Next: the Editor's five-test PAIR verdict, then Sudan recon resumes.**
>
> **Growth/Quality — the 08-28 forward question answered in code: `qa_jsonld`, the sixth standing assertion.** Presence was checked; meaning was not. The new check asserts the machine-readable layer against the consumer's contract: every JSON-LD block parses; breadcrumb positions sequential and every `item` **dereferenced to a file in dist** (#37 applied to the machine layer); Article required keys, absolute URLs, image raster (08-18 family), translation links dereferenced; and `inLanguage` must **agree with the page's own path language** (#36 — the 08-23 Arabic-chrome defect class, now closed where the crawler reads). Proved per #35: **control** = today's build, 0 defects, 152 nodes, **608 promises dereferenced**; **bite** = three synthetic breaks (retargeted breadcrumb, missing JSON-LD image, flipped inLanguage) → **FAIL(3)**, each naming page + field, silent on 79 pages. New forward question filed in `qa-2026-08-29.md`: every check runs at build time; nothing verifies the origin *at deploy time* — candidate: a post-deploy curl step inside the Actions workflow itself. **Corpus unchanged at 38 EN / 38 AR; no approved flips.** Ruling series unchanged at **#1–#38, no gaps, no dupes.**

> **Daily-run addendum — 2026-08-30 (ruling #39; Sierra Leone BANKED as a pair — Ed05 stands at two complete; the deploy pipeline gains its missing gate; the push gap reaches seven days).** State first, plainly: **origin/main is still `5e16db2` (08-23) — seven days stale, fifth consecutive day the staged block went unrun.** Local HEAD `a340782` (08-28) plus one committed-in-full uncommitted day (08-29) on disk; `qa_live_drift` counts the same **84 drifts**. Today's weekly review runs concurrently and treats **founder-push availability as the constraint**, per the standing note. One block clears all seven days.
>
> **Section 1 gains row 34 — `2026-08-30-a-delivery-fingerprint-must-vary-with-the-delivery.md`, filed as ruling #39.** **The rule: a check that verifies delivery must probe a value that changes with every delivery — an invariant probe proves reachability, not currency, and false-passes on exactly the failure it exists to catch. Choose the widest single-file fingerprint; re-prove the probe's variance when the artifact changes, because probe validity itself drifts.** Origin case: the new `verify` job's first candidate probe, `sitemap-index.xml`, is **deploy-invariant on the live 08-23 build** (no lastmod — it would have stayed green through the very July outage the job answers) and became variant only when the 08-25 resolver added `<lastmod>` — the same probe changed meaning between two builds in one week, silently. Probe of record: `sitemap-0.xml`, all 82 URLs with lastmod. Rejects a probe class outright — the structural move of #29 applied to verification.
>
> **Quality — the 08-29 forward question answered in the workflow itself: the deploy pipeline's missing gate.** `astro-pages.yml` gains a third job, `verify` (needs: deploy): downloads the exact published artifact, polls the origin up to 5 minutes until `sitemap-0.xml` matches byte-for-byte, then byte-compares a five-file bilingual sample (`index.html`, `ar/index.html`, both feeds, `sitemap-index.xml`), failing the run loudly on any mismatch. Ruling #16's judging environment now includes the minutes *after* the push (#17's second gate reports its own failure). **Proved per #35 against real state:** bite = today's 7-day-stale origin (probe DIFFERS, index DIFFERS); control = dist vs itself, silent; byte-determinism licensed by the 08-27 drift control. YAML parsed, shell syntax checked; the job's first live run is the next push itself.
>
> **Content — Sierra Leone is BANKED as a pair; Edition 05 stands at two of its slate complete.** The Editor ran the five tests on the PAIR (`2026-08-30-ed05-sierra-leone-pair-verdict.md`): the figure trace survives the crossing register-by-register (0.278/0.102 body+annex per #38, the .359→two-thirds-of-a-year scoped ruler, the three reach registers in order with their rare downward direction); all prohibitions hold in Arabic at full weight (لا مكسبَ مُثبَت, percentile claims unblended, NYAF bare Latin); posture is the honesty architecture carried at full adverse weight; the spine — *paid what the measurement said* — runs dek-to-close in both languages; test 5 closed by the 08-29 composition + gate (row 33). **Both files stay `approved: false` for the founder's wave gate; zero build leak re-verified. Pipeline moves to mandated slot #2, Sudan — with the Editor's instruction that the 08-13 recon index be re-verified against the web as served today before commission (#16 applied to recon vintage).**
>
> **Growth — the flip-day checklist loses its only asset item: og raster cards generated for both Ed05 pairs.** `og/2026-08-25-zambia-free-education-act.png` + `og/2026-08-28-sierra-leone-sleic-outcomes.png`, 1200×600 from the same stills via sharp, 6–7 KB each, paper background — the exact derivation the article route already declares (#36: the card path is derived from the still path). Assets, not pages: zero held-leak re-proved with both cards on disk. When the wave gate opens, no share arrives cardless for even a first crawl. **Corpus unchanged at 38 EN / 38 AR; no approved flips.** Ruling series now **#1–#39, no gaps, no dupes.**

> **Daily-run addendum — 2026-08-31 (ruling #40; the push landed and the pipeline proved it — first CLEAN drift read in the operation's history; Sudan re-verified and COMMISSIONED; the verify job gains the feed cache axis).** State first, plainly: **the seven-day gap is CLEARED.** Founder ran the 08-30 block; local = origin = `9886987`; the origin serves the 08-30 build (sitemap lastmod 08-30 07:58). `qa_live_drift` reads **CLEAN — 82 URLs, 0 lastmod drift, 4/4 sampled heads identical — its first clean read ever**: yesterday's 84 drifts went to zero through the deploy whose maiden `verify` job was built to prove exactly this. For the first time since 08-23, the reader sees the work.
>
> **Section 1 gains row 35 — `2026-08-31-a-mirror-can-misname-the-instrument.md`, filed as ruling #40.** **The rule: a program's name is a FIGURE — it carries provenance axes and is traced to the instrument's OWNER, never taken from a mirror's headline; a mirror-sourced name is PROVISIONAL until the owner's register serves it, and is never transliterated before then.** Origin case: the 08-13 Sudan recon carried "BRIDGE" from the Sudan Tribune headline; the first-party UNICEF PR (same date, read 08-31) serves **"BRIDGES"** — *Building Resilience to Improve Development and Growth in Education in Sudan* — expansion printed beside it. Eighteen days of a wrong instrument name in the index while every traced figure stayed clean: the name was not an axis; now it is. Corollary, proven on first use of the Editor's re-verify-before-commission instruction: **recon freshness is register-specific** — two of seven Sudan registers changed serving state in 18 days (Tribune bot-walled; UNICEF USA adaptation unreachable) with zero figures going false, and the re-anchor hunt surfaced a *better* register than the one lost (first party corrected the name, added a third voice — Belay Addise, GPE — and carries an official Arabic edition).
>
> **Content — Sudan re-verified and COMMISSIONED (mandated slot #2); the lineage gate CLOSES on the honest side.** The Researcher re-verified all seven 08-13 registers against the web as served today (`recon/2026-08-31-ed05-sudan-reverification.md`): registers 1–2 serve unchanged; the wartime national counts (699/60/12,660+) lost their serving register and **do not ride until re-anchored**; Brown et al.'s abstract read in full — n=221, ages 7–9, 6 months, significant gains in maths + Arabic literacy + wellbeing vs the government ALP, **plus the adverse findings the charity's marketing omits: null on hope, significant NEGATIVE effect on self-esteem** (the multipliers "2.7×/2×" are NOT in the abstract and do not ride unless the OA body is read); War Child's own 2025 accounting puts CWTL at ~125,000 in Ukraine/Uganda/Lebanon and names Sudan only for a NEW mental-health response — **so no served source ties the wartime digital spaces to CWTL, and the piece is commissioned as TWO true stories joined by place and idea, never by asserted program identity.** Editor's commission filed (`briefs/2026-08-31-sudan-digital-learning-brief.md`); drafting begins 09-01; four Test-1 homework items named.
>
> **Quality/Growth — the 08-30 forward question answered in the same job it named: the feed cache axis.** The `verify` job gains a second assertion step: both feeds must serve **ETag + Last-Modified**, and a conditional GET with that ETag must return **304** — the exact mechanism a polling reader uses to decide "anything new?"; values logged each run so cross-deploy variance is comparable by eye (#39: an ETag that never changes is an invariant probe wearing a cache header). Structurally cannot false-pass on a stale origin: it runs only after the byte-compare has bitten. YAML parsed, bash -n clean on both steps; live proof is the next push (same staging discipline as the verify job itself on 08-30). Honest limit named in the QA log: sandbox web restrictions + browser approval unavailable in an unattended run mean the 304 mechanics could not be probed from here today — the job self-proves in the judging environment instead (#16). **Corpus unchanged at 38 EN / 38 AR; no approved flips.** Ruling series now **#1–#40, no gaps, no dupes.**

> **Daily-run addendum — 2026-09-01 (ruling #41; Sudan DRAFTED — Edition 05 mandated slot #2 moves from commission to draft in one run; the inbound half of discoverability gains its standing assertion).** State first: origin current at `9886987`, but **the 08-31 block went unrun — one day staged, not lost**; today's block carries both days. Content: **the Sudan piece is DRAFTED** (`2026-09-01-sudan-cant-wait-to-learn`, ~2,000 words, held `approved:false`, hero still on disk same run, title 52/100 · dek 192/200 counted at compose). All four Test-1 items closed before a word was drafted: (1) the dead national counts were **superseded, not resurrected** — the owner's living register (Learning Passport, read served) carries 750 safe learning spaces / 870,000+ reached, April 2024 vintage in-sentence, and the 699/60/12,660 block does not appear; (2) the named-adult hunt landed **three** — Tahir Onoor Ali (Badareer e-learning centre, Aroma, Kassala, pre-war vintage marked), Teacher Amani (wartime SLS, Al Maimona, Gezira), Fatima Abbas Tahir (Tamantai, held over from recon) — plus a named *origin*: UNESCO's own prize page credits the idea to **Dr. Aiman Badri, Ahfad University for Women**; (3) the UNESCO prize rides via the owner's served register (2018 King Hamad laureate), not the JS-walled prize page; (4) the Brown et al. body stayed unread (tandfonline serves nothing), so **the multipliers do not ride** — the abstract's own words are the ceiling, adverse findings carried at full weight (null on hope, negative self-esteem, authors' caveat). The lineage gate holds as commissioned: two true stories, one orienting sentence, no asserted program identity.
>
> **Section 1 gains row 36 — `2026-09-01-supersede-dont-resurrect.md`, filed as ruling #41.** **The rule: when a figure's register dies, the question is not "where else does this figure survive?" but "what does the owner serve today?" — a superseding first-party figure replaces the dead one, and mirrors still carrying the dead figure (Forbes syndication, search-engine summaries) are not re-anchor candidates.** Two corollaries proved in the same hunt: **a search summary is not a served register** (the "76,000 / March 2025" figure was declined at any freshness — no page read as served carries it), and **a URL is a figure too, cited as fetched, never composed from a pattern** (a pattern-composed War Child URL was caught not serving and replaced by the sister entity's fetched register before staging — #37's shape at source level).
>
> **Quality/Growth — the 08-31 forward question answered same-day: `qa_robots.py`, standing assertion #8.** The inbound half of discoverability, asserted: robots.txt must EXIST in dist, advertise exactly one Sitemap URL **derived from astro.config's own site+base** (#36), pointing at a file that **exists in dist** (#37), with zero Disallow lines; the legacy `/sitemap.xml` stub must point only at files that exist. Proved per #35 before landing — bites on a removed robots.txt, bites on a `Disallow: /ar/` injection, silent on the real build — and wired into the CI build job so a drifted crawler surface fails the deploy loudly. Finding worth naming: robots.txt already existed and was already correct (the 07-02 legacy-stub comment shows someone solved this once) — the defect was that **nothing asserted it**, the same silent-pass family as row 23's zero-items trap. QA green: 81 routed pages exit 0, parity 38/38, all **eight** standing assertions clean, drift CLEAN (second consecutive), held-leak zero with three held pairs' assets on disk. **Corpus unchanged at 38 EN / 38 AR; no approved flips.** Ruling series now **#1–#41, no gaps, no dupes.**

> **Daily-run addendum — 2026-09-03 (ruling #42; Sudan COMPOSED in Arabic and gated — Edition 05's third pair is one verdict from banked; the cluster assertion bit on first contact; one dark day named).** State first, plainly: **local = origin = `642fef2`** — the 09-01 block landed on 09-02 (run 69 success → the `verify` job's feed-cache step and the robots gate both green on their first live runs). **2026-09-02 produced no run and no brief** — a dark day, the first since 08-19→22 — and today's run started at 21:55, not 08:00. Recovery shape applied. Content: **Sudan is composed in Arabic** (`articles-ar/2026-09-01-sudan-cant-wait-to-learn.md`, «الصفُّ الذي اختُرِع قبل الحرب التي احتاجته», ~1,570 words at register, held `approved:false`, dek 178/200 with tashkeel, title 42/100 at compose) and the **Arabic Editor's gate is PASSED in writing** (`verdicts/2026-09-03-sudan-cant-wait-to-learn-ar-gate.md`): twelve named humans, **three CONFIRMED from the owner's served Arabic editions** (فاطمة عباس طاهر، طاهر أنور علي، جابر منصور أدوما), nine by convention, none PROVISIONAL; the programme's Arabic name is the owner's own attested **«التعليم لا ينتظر»** (not coined); Makanna's spelling **«مكانّا»** recovered from a UN Arabic register; Addise → **بيلاي أديسي** confirmed in writing; the lineage gate survives the crossing. Pair verdict tomorrow.
>
> **Section 1 gains row 37 — `2026-09-03-a-language-toggle-is-not-a-translation.md`, filed as ruling #42.** **The rule: a language switcher on a served page is a promise in the source's chrome, not a document — "an official Arabic edition exists" is a figure, proved by fetching the toggle's destination and reading the target language in the served body; the toggle's presence proves nothing.** Origin case: the 08-31 re-verification recorded the BRIDGES press release as carrying "an Arabic edition on-site (العربية toggle)"; the anchor is served `class="has-no-translation"` with the English href — the claim lived three days and reached the EN draft's own source annotation (corrected today; a dated correction appended to the recon, not a rewrite). Three corollaries bitten in one hour: **read the toggle's served state, not its label**; **two editions are two registers** (the AR editions carry their own dates — 31 Aug vs 27 Aug 2020, 2 Nov vs 1 Nov 2022 — and one carries figures the English lacks, declined; a pair carries only what both share); **the owner's Arabic overrules a defensible reading** — *Onoor* routes to النور by any heard-vowel reading of Sudanese romanisation, and UNICEF's own edition serves **أنور**. #36/#37 turned outward: a source's chrome is dereferenced exactly as our own.
>
> **Quality — the 09-01 forward question answered as `qa_hreflang_clusters.py`, standing assertion #9, and it FAILED its own control.** Every cluster reciprocal, dereferenced to dist, equal to the sitemap's URL set (each sitemap URL in exactly one cluster or a *named* no-alternate page), no cluster naming a URL the sitemap omits, sitemap `xhtml:link` alternates equal to the page-level cluster (#36 across two files). Control on the real build: **FAIL(4), one cause — the English home's canonical and og:url were `…/madar` (no trailing slash) while its own cluster, the Arabic home and the sitemap say `…/madar/`.** `Astro.url.pathname` for the root route under `base` has no slash; GitHub Pages 301s it — **the front door declared a redirect as its canonical from 2026-05-25, 101 days**, invisible to every sweep because none compared a canonical with its cluster. Fixed in `Base.astro` (canonical derived from the served shape, #36); post-fix control silent, two synthetic bites loud; wired into the CI build job after the robots gate. Growth: Sudan og card generated and inspected; the Ed05 wave packet gains its third entry. QA green: 81 routed pages exit 0 (×2), parity 38/38, **nine** standing assertions clean, held-leak zero (3 EN + 3 AR held), drift = exactly the one changed field. **Corpus unchanged at 38 EN / 38 AR; no approved flips.** Ruling series now **#1–#42, no gaps, no dupes.**

> **Daily-run addendum — 2026-09-04 (ruling #43; Sudan PAIR BANKED — Edition 05 stands at three complete pairs; slot #3 re-verified and COMMISSIONED, dated by design; one day's block staged and unrun).** State first, plainly: **local = origin = `642fef2`**; the 09-03 block (Sudan AR + gate, `qa_hreflang_clusters`, the Base.astro canonical fix, ruling #42, Brief 65) sits **staged and unrun — one day, nothing lost**; the origin still serves the 09-02 build and the home's canonical is still the redirect until the block runs. Content: **the Editor's five tests on the Sudan PAIR — PASS, all five, BANKED** (`verdicts/2026-09-04-ed05-sudan-pair-verdict.md`): the numeral multiset of both bodies is identical (zero EN-only, zero AR-only, zero count differences), every figure traced register by register to the ten-source index, the prohibited figures (dead national counts, the 2.7×/2× multipliers, *Let Us Learn*) absent from both by grep, the lineage gate at full strength in Arabic, announced-vs-reached carried three ways, the trial's four registers intact, the spine four-for-four in both languages; a note to the Verifier that the AR source rows citing the owner's Arabic editions are their own dereference targets, not mirrors of the EN rows. **Slot #3 re-verified against the web as served** (`recon/2026-09-04-ed05-best-system-reverification.md`): nothing went false; the DBE TIMSS 2023 Highlights read as primary corrected two press-mirrored figures (59 not 58; the 2019→2023 change is *not statistically significant … relatively stable* with the IEA reliability reservation, not "+8 upward trend"); the +104 / 0.9 SD trend figures sit outside that primary (trend chapter starts 2011) and do not ride until the TIMSS 2019 report is read; the PASEC 2019 report moved from wall to *serves, partially read* via the owner's archive; the HCI Mauritius brief and the CGD analysis confirmed verbatim; **and the piece acquired its date — PASEC 2024 results are announced for the last quarter of 2026** (served: *La Nouvelle Tribune*, 11 Feb 2026; the owner's own slides had planned 2025). **COMMISSIONED** (`verdicts/2026-09-04-ed05-best-system-commission.md`) to CC II as the ruler question, Mauritius crown / Niger counter-crown, eight draft-blockers, the vintage clause mandatory, Seychelles unnamed, no draft date set.
>
> **Section 1 gains row 38 — `2026-09-04-the-podium-is-not-the-document.md`, filed as ruling #43.** **The rule: when an owner's launch remarks and the owner's own published report disagree about what a number means, the report rides and the podium is reportable only as the disagreement** — a voice is a register with its own vintage, audience and incentive; the document is the finding; a figure warranted by "the department said" against the department's report has been laundered through its own owner. Origin case: DBE TIMSS 2023 — "+8, a remarkable upward trend" at the launch vs *not statistically significant, relatively stable* in the report, plus the reliability footnote. Three corollaries bitten in one re-verification: **a count reported by press is a mirror's count** (58 → 59); **a schedule is a plan** (PASEC 2024: planned 2025 → announced Q4 2026; the search summary's "October, Dakar" is not a served register, #41); **when the ruler is about to be re-issued, the piece carries its own expiry** (#21 applied to the instrument, not just the figure — the piece names its sequel in-sentence). The register hierarchy stated once: document > tables > first-party written > reported speech > mirror; never average across ranks. **Verifier's trace row added:** every institutional figure records *which register* it was read in; reported speech fails as sole warrant when a document exists. AR routing filed: Hounkpodoté → إيلير هونكبودوتي (PROVISIONAL, row-33 labial-velar), Baba-Moussa → عبد الرحمن بابا موسى, Avodagbe → ديوانو أفوداغبي (PROVISIONAL), Chetty → مارك تشيتي.
>
> **Quality — the 09-03 forward question's second half answered: the no-alternate allowlist is now ASSERTED, not printed.** `qa_hreflang_clusters` gains `NO_ALTERNATE_ALLOWED = {about/, valence/}` and fails both ways — a no-alternate page outside the list (an EN page without its AR twin, or a one-sided leak) and a listed page that now has alternates or is not built (a stale allowlist is a green light wired to nothing). Proved per #35: control silent on the real build; bite 3 (a third no-alternate sitemap URL) FAIL(1); bite 4 (About removed from the list) FAIL(1); bite 5 (a ghost entry) FAIL(1). No workflow change needed — the CI step already runs the script. QA green on the route-around build: 81 routed pages exit 0, parity 38/38, sitemap 82/82 lastmod, all nine standing assertions clean, held-leak zero (3 EN + 3 AR held, assets only on disk), feeds 38/38, drift = the one staged canonical field. Growth: the dated `site:` observation recorded (zero from the domain, eleven days after 08-24 and five days after the README edge landed — an observation, not a figure); **engagement slice 3** (the measurement-of-systems beat, ten entries) derived; **the PASEC 2024 launch named as the operation's first externally dated distribution window** — the ruler piece must be on the origin before it, which on a weekly push window means clearing the wave gate by mid-November. **Corpus unchanged at 38 EN / 38 AR; no approved flips.** Ruling series now **#1–#43, no gaps, no dupes.**

## Consolidation — 2026-09-06 (weekly review; covers 2026-08-30 → 2026-09-06)

**The week's job was the Sudan pair and a register that kept disagreeing with itself.** Four runs (08-31, 09-01, 09-03, 09-04; 09-02 and 09-05 dark), four rulings, each filed the day it bit, each already cited by number before today.

- **Ruling register #1–#39 → #1–#43.** Canonical one-liners added for **#40** (a mirror can misname the instrument — *BRIDGE* → *BRIDGES*, 18 days in the index), **#41** (supersede, don't resurrect — the dead wartime counts replaced by the owner's living Learning Passport register; a URL is a figure, cited as fetched), **#42** (a language toggle is not a translation — `class="has-no-translation"` behind the *العربية* label; the owner's own أنور over a defensible النور), **#43** (the podium is not the document — DBE's "+8, upward trend" at the launch vs *not statistically significant, relatively stable* in its own report). Series verified no gaps, no dupes.
- **A sixth family named: which register speaks for the owner.** #38, #40, #42 and #43 are one question asked of four surfaces — *of the several places this owner speaks, which one is the document?* — and #43 answers it once with a ranked list (document > tables > first-party written > reported speech > mirror). #41 is the family's vintage corollary. The family sits beside the five named on 08-23/08-30 in §3 and predicts its own next member: a register that *outranks* the document disagreeing with the report.
- **Section 1's table reconciled to its own header.** Since 08-23 the header has claimed thirty-three rows (now thirty-eight) while the table stopped at row 25; rows 26–38 existed only as "Section 1 gains row NN" addenda. Two consolidations (08-23, 08-30) verified the *series* and left the *table* unmerged — the ledger drifting against itself, #38 applied to us for the second week running. Today the thirteen rows are in the table, numbers exactly as first cited in commits and briefs, each with its subject, the rule in one line, and where it has been applied. Nothing renumbered.
- **Archive roll.** The 08-09, 08-16 and 08-23 consolidation sections and the 08-10 → 08-18 addenda (rulings #29–#32, rows 19–25) moved verbatim to `ARCHIVE-2026-08-09_2026-08-23.md` per the two-cycle rule. The INDEX now carries the ledger from the 08-23 daily addendum forward. Nothing deleted; the pointer paragraph lists both archives and the precedence order.
- **Composition knowledge for the Africa edition, cumulative:** Bantu `ph` (row 30), West African `kp` (row 33), and this week the bilingual-register discipline for native-Arabic stories (row 37): when the owner serves an Arabic edition, its names are *read*, never routed — three of Sudan's twelve named humans were CONFIRMED that way, nine by convention, none PROVISIONAL.
- **Pruning: nothing deleted.** `history.jsonl` keeps its duplicate same-day snapshots (harmless, read by date). One stale line corrected: the §3 heading still read "#1–#38" after the 08-30 review filed #39 concurrently — reconciled with the register.
- **Owed, named for next week:** the §3 families paragraph now lists six families; §0's seven-axes checklist has not been touched since 07-27 and does not yet carry the register axis (#43's "which register was this read in" — the Verifier's new trace row). Candidate for the 09-13 consolidation: an eighth axis, *register*, or a pointer from §0 to the family.

> **Daily-run addendum — 2026-09-06 (ruling #44; Ed05 piece-4 draft-blocker 1 CLOSED — the PASEC2019 report read in the body; standing assertion #10 lands and bit on first contact; the rail dead-end audit; one dark day named; the weekly review ran concurrently).** State first, plainly: **local = origin = `fad150f`** at open (09:10) — the 09-04 block landed the same morning it was staged (run 70 success; `verify` job green; drift **CLEAN**, second clean read ever). **2026-09-05 was a dark day** (no run, no brief). The weekly review opened at 09:00 and wrote its consolidation (#1–#43 into §3, Section-1 rows 26–38 merged, the 08-09→08-23 ledger archived), the IndexNow key + default-off `verify` switch, the RUNBOOK's held-date rule, `_EDITION_05_STATUS.md` and the review itself while this run read a 444-page PDF; the later writer reconciled — this addendum appended, §3 extended in place to #44, nothing of the review's overwritten. Content: **Content Creator II read the PASEC2019 international report** from the owner's archive URL (Chapter 5 in full, §3.1–3.2, §3.5.3.1, Tables 2.6/2.7, front matter) and filed the sixteen-row re-anchoring table (`content-drafts/recon/2026-09-06-ed05-pasec-2019-report-read.md`): every CGD figure now has the report's page and scale, or a named owner, or falls; **the report crowns Senegal on the equity ruler in its own words** (p. 225 — the recon's "nobody" was the blog's); **footnote 47 on Table 5.13 disclaims causality** between Niger's policy list and its gains; the trend is **ten countries, not fourteen**, with Niger's national-language-tested pupils excluded (p. 220); Niger's contract teachers were **70% of its primary teachers** and the instrument was *résiliation des contrats*; class size fell in eight of ten, flat in Niger, **up in Congo** (Graphique 3.62, p. 143); 48%/38% confirmed at Tables 2.6/2.7 (the report's own framing: 52.1% *below* the reading threshold); the blog's 43→49 / 41→39 shares, +15 HLO, the 45-year line and the HLO bands are the analyst's and ride only attributed; the report's own hedge — *devra être confirmée par la prochaine étude PASEC* (p. 224) — anchors the vintage clause. The Editor appended three decisions to the commission: ruler 4 crowns Senegal at end of primary with the report's limits in the same paragraph; the Niger sentence carries the owner's disclaimer and attributes the inference to Le Nestour; the schema gains an optional `countries:` list **in the same commit as the article**, with `madar_stats.py` counting the union that day. Draft-blocker 2 (TIMSS 2019 national report) is the Researcher's next day; no draft date set.
>
> **Section 1 gains row 39 — `2026-09-06-the-footnote-rides-with-the-table.md`, filed as ruling #44.** **The rule: a disclaimer the owner prints on a table travels with every figure taken from that table — the owner's juxtaposition (policy list beside gain) may be reported in the owner's shape, and the causal reading is the analyst's claim, attributed to the analyst, never the report's and never ours.** Mirror of #43: there the document outranked the owner's launch; here the document's own hedge outranks the analyst's confidence. Three corollaries bitten in one read: **a converted figure has two owners — cite the one whose page you read** (−45 HLO is −60.1 PASEC points at p. 222; +15 HLO is +19.8 at p. 224; the blog's Burkina sentence does not survive as one sentence — at start of primary the weakest fell ~60 while the *average did not rise*; at end of primary the average rose ~20 while the weakest stood still); **a trend chapter has its own coverage** (ten, two exclusions; Senegal's Rho fell in three of four series); **the owner's own hedge is the vintage clause's best anchor.** The Verifier's trace gains a row — *does the primary disclaim any relation this sentence asserts?* — and a scale column. Also filed: the report's running footer reads "PASEC2020" on Chapter 5's odd pages (#38 — cite by title); AR routings for the ruler piece's vocabulary (المعلمون المتعاقدون · فسخ العقود · عتبة الكفاءة «الكافية» · معامل الارتباط داخل الفئة) and two PROVISIONAL names.
>
> **Quality — the 09-04 forward question answered as `qa_body_links.py`, standing assertion #10, and its first finding was a silent pass.** "Every in-body internal link dereferences" found **0 links in 76 articles** — no piece composes a markdown link in its body; the reader's exits are the sources list and the related rail, both generated from frontmatter. Re-derived to what an article *promises*: every `related:` slug names an approved article in the same collection **and is rendered in `<nav class="related">`** (RelatedReading.astro drops unresolvable slugs *silently by design* — the loud half now exists: bite 1 added a bogus slug, the site rebuilt **exit 0**, the assertion **FAIL(1)**); every `sources[].url` is rendered as a link and no approved page carries a placeholder source (bites 2 and 3, FAIL(1) each); in-body links reported as a count. Wired into the CI **build** job as the third QA step. Then **Growth's rail dead-end audit** (`agents/growth/2026-09-06-rail-dead-end-audit.md`): **six approved pages had no rail at all** (all Edition 01 — Bahrain, Egypt EKB, Ghana, Mosul, Indonesia, Qatar; Mosul is the corpus's most-pointed-at page and sent nobody anywhere), **six had no piece pointing at them** (Morocco, Chile, Gaza, India, Poland, Rohingya), and **Singapore's Arabic rail had drifted** from its English twin — both rails resolved, so parity (files, not content) never saw it. The Editor approved twelve editorial clusters, reciprocal where honest; 24 frontmatter files changed (`related:` only, no flag flipped); the assertion gained the matching rule — **twin rails agree, no rail dead ends** — proved in order: **bite 4 on the old corpus FAIL(25)**, control on the rebuilt corrected corpus PASS (196 related promises, 38 twins compared, 0 dead ends). Standing from today (Editor): **a piece is commissioned with its rail** — the commission names what it points at and what points back, so a wave flip never ships a dead end again. QA green on both builds: 82 pages exit 0, parity 38/38, sitemap 82/82, feeds 38/38, held-leak 0 (3 EN + 3 AR held), ten assertions clean, drift CLEAN against the origin. Forward: the review's held-date leak fix is first on 09-07; carried behind it, `qa_sources_alive` as a *weekly* sampled sweep of the 571 external promises (alive, not merely rendered), never a deploy gate. Growth: the audit is the day's action — twelve structural exits removed from a publication whose thesis is being worth staying in; no traffic figure claimed, none exists by design. Corpus unchanged at 38 EN / 38 AR, 35 countries, no approved flips. Push note: two edits to `.github/workflows/` today (the review's `verify` switch, this build step) — workflow-scope PAT. Brief 67.
