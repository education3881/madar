# Growth — 2026-08-02 · live sitemap audit + the "measurement" internal cluster asset

**Return-rate posture, not raw counts. No third-party trackers (privacy by design).**

## 1. Live discoverability audit (done today)
Re-fetched `education3881.github.io/madar/sitemap-0.xml` after the AR increment:
- **30-piece live corpus** listed (30 EN + 30 AR mirrors) + structural pages (home, /about, /editions, AR home, AR editions) — 65 locs.
- **Reciprocal hreflang** (en / ar / x-default) present on homepage, AR home, and editions.
- **Zero held-draft leak** — no Ed04 slug (naplan, naep, singapore-psle, india-parakh, nz-ncea, chile-simce, netherlands, england) appears; correct. The Australia AR added today is `approved: false` and stays out of the sitemap.
Surface is clean and current. This is the standing return-signal readiness check; privacy-clean traffic signals (Substack opens / return rate / attributable shares) switch on when Issue 01 sends — still blocked on the two founder-gated items (custom domain, whether the Substack publication exists).

## 2. Forward asset unlocked by today's AR — the bilingual "measurement" cluster
Ed04's spine is the measurement question. Three of its verified pieces plus one shipped piece form a natural internal-link cluster, and **AR composition doubles it into a bilingual cluster** (Arabic is a distribution advantage, not just a translation cost):
- **us-naep** (US — one word, 51 rulers) ↔ **australia-naplan-reset** (a bar raised on purpose) ↔ **nz-ncea-corequisite** (the gate that outlived the qualification) — all three carry `related:` to each other and to **oman-school-performance-ratings** (shipped; the single public verdict).
- With the Australia AR now composed, the day the wave flips, this cluster resolves **reciprocally in both languages** — EN↔EN and AR↔AR — concentrating the search value each "assessment reform" query earns alone. The EN Australia file's `arabicVersion:` now has its pair; the AR's `related:` mirrors the EN.
**Action for the wave gate:** confirm the `related:` slugs resolve reciprocally across all four pieces in both languages at pre-ship (no orphan refs), so the cluster ships whole. Logged; no live change today (held wave).

## Bet status
The standing weekly growth bet — **stage Substack Issue 01 as the Ed04-launch issue** (packet on file, `content-drafts/social-drafts/2026-07-28-issue-01-ed04-launch-staging.md`) — is unchanged and send-ready-pending the two founder-gated blockers. Nothing here needs a founder decision except those two standing items.
