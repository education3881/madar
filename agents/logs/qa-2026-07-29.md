# QA + publish-gate log — 2026-07-29

**Run:** madar-daily-operation · **Manager/CEO** · no push from the sandbox (founder's action).

## State verification (before any plan)
- `git branch`: on `main` (+ `gh-pages`). `git log -15`: latest 262246c (2026-07-28, Brief 39).
- `git fetch origin main`; **HEAD == origin/main == 262246c**; `git status` clean before this run.
- Live surface: sitemap 67 locs (30 corpus + AR mirrors + structural); **England absent** from sitemap; reciprocal hreflang (en/ar/x-default) on homepage head. **No gap since 07-28.**

## Publish gate (in writing — no push staged that flips any flag; this is a hold, not a ship)
1. **Editor / Verifier verdict.** England: Editor five-test APPROVE (07-28) + **Verifier CLEARED, no item failed (07-29)** — all 8 trace-list items independently re-traced through a second channel. Drafted slate 8/8, verification-complete. No `approved` flip this run.
2. **Hero stills on disk.** Not required this run (no ship). Ed04 stills batch (10) still outstanding — flagged for the wave gate, not this push.
3. **`astro build` clean.** In-place build renders all 65 pages then throws `EPERM` only on the final `dist/_noop-middleware.mjs` unlink at the FUSE mount boundary — a workspace artefact, not a code error. Clean-tree route-around (`/tmp` copy + symlinked `node_modules` + `VITE_CACHE_DIR`): **exit 0, 65 pages, "Complete!"**.
4. **Parity.** EN 30 = AR 30 approved corpus (100%), per `madar_stats.py`.
5. **Held-leak.** Zero. England is doubly safe: `approved:false` **and** staged in `content-drafts/staged-ed0304/`, outside `web/src`. No staged Ed03/04 draft carries `^approved: true`. The only "England" strings in `dist/` are inside the **Oman** piece (legitimate — the deliberate frame-pair discusses Ofsted).
6. **Brand chrome.** Wordmark inline (`<span class="mark-latin">Madār</span>` + Arabic span) in built output; no `<img src>` for wordmark/glyphs. Hero stills use `<img src>` (correct — the inline rule is wordmark/glyphs only).

## P0 / P1
None.

## Files this run
- `content-drafts/verdicts/2026-07-29-england-report-cards-verification.md` (the eighth verdict — CLEARED)
- `agents/guidebook/2026-07-29-two-channel-verification-satisfies-the-split.md` (ruling #27) + INDEX addendum
- `content-drafts/_EDITION_04_STATUS.md` (updated to 8/8)
- `agents/briefs/2026-07-29-daily-brief.html` (Brief 40)
- `agents/logs/qa-2026-07-29.md` (this log)

— Manager · 2026-07-29
