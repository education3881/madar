# QA pass — 2026-07-12 (daily run)
**Context:** run concurrent with today's weekly-review session (which committed the 07-11 work as `8d68575` at 16:33 and held `.git/index.lock` mid-run). This daily made **no git writes** — file writes only; the founder's push block carries everything. Live lock ≠ stale lock: routed around, not removed.

## Build (route-around: mktemp + cp -RL + node_modules symlink + VITE_CACHE_DIR)
- `astro build` **exit 0, 65 pages** (30 EN articles + 30 AR + home ×2 + editions ×2 + about) — includes today's hreflang change. `sitemap-index.xml` emitted by the integration.

## Today's code change verified in the built output (hreflang-in-head, Growth P2 from 07-11)
- **30/30 EN** article pages and **30/30 AR** article pages emit identical reciprocal triples: `hreflang="en"` + `hreflang="ar"` + `x-default`→EN, absolute URLs under the site base.
- Home EN/AR and Editions EN/AR emit their pairs; **about emits none** (no AR twin — a pair pointing at a missing page would be worse than no pair; opt-in prop design in `Base.astro`).
- Article pairs derive from frontmatter (`arabicVersion`/`englishVersion`), not mechanical prefixing — same data the in-body cross-links already trust.

## Corpus + flags
- Live corpus 30 EN / 30 AR / parity 100% / 27 countries (stats script, snapshot appended).
- Ed04 holds: **6× `approved: false`** verified in `web/src` (5 EN + 1 AR us-naep).
- **Staged-duplicate sweep completed:** `staged-ed0304` copies of us-naep, singapore, nz, chile all carried latent `approved: true`; set to `false` this run (India was defused 07-11). Staging now agrees with `web/src` everywhere.

## Live site
- `education3881.github.io/madar` serves Ed03 (lead Chad + 9 siblings), matching `origin/main` aa37be1. Brand chrome: wordmark inlined SVG ✓. Live-site content will trail local by design until the founder's next push (which adds head-hreflang + today's/yesterday's records).

## Source-ledger spot re-resolve (Singapore verification)
- 4 MOE registers re-fetched live: 2016 press release (flaked 2-of-3, then served verbatim — see ruling #20), 2025 results release ✓, Feb 2025 parliamentary reply ✓, posting FAQ ✓. No rot; no repointing needed.

## For the Web Developer (carried)
- P3 cosmetic: article kicker `Curated · Curated` redundancy (from 07-11 audit) — unchanged, parked for a polish pass.

— Manager (Quality function) · 2026-07-12
