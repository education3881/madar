# QA log — 2026-07-13
**Run:** daily (Manager) · **State at open:** HEAD == origin/main == `e5eecba` (07-12 20:14 push, runs green), working tree clean, live site verified current (Ed03 homepage, hreflang in head).

## 1. Flag sweep (ruling #18 family) — ONE TRAP FOUND AND DEFUSED
- `web/src/content/articles/`: 30 × `approved: true` (the live EN corpus) + 5 × `approved: false` (the held Ed04 drafts: us-naep, india-parakh, singapore-psle, nz-ncea, chile-simce). ✓ correct.
- `web/src/content/articles-ar/`: 31 files — 30 × `true` (live AR corpus) + us-naep AR at `false`. ✓ correct.
- `content-drafts/staged-ed0304/`: **`articles-ar/2026-07-07-us-naep-honesty-gap.md` carried `approved: true`** — the AR staging duplicate of a held Ed04 piece. The 07-12 sweep covered the four EN staged copies (its log says so) but did not sweep `articles-ar/`. **Defused this run: `true` → `false`.** Fifth instance of the staged-flag family (07-11 India EN, 07-12 four EN, today one AR). The 20 remaining staged `approved: true` files are Ed03 duplicates whose live copies are also `true` — clutter (cleanup line stands under the push block), not traps: they agree with `web/src`.
- **Sweep-rule tightening for future runs:** the staged-flag sweep must cover BOTH `articles/` AND `articles-ar/` under any staging dir. One grep, both dirs.

## 2. Content edit under Editor's eye (Verifier verdict item 1)
- `web/src/content/articles/2026-07-07-nz-ncea-corequisite.md` ¶4 — the "was later revised to 79.6" sentence re-cut to attribute the ministerial recount and carry the register fence in-sentence ("a recount, not a restatement: the authority's own year-end series still records 2024 at 71.5"). Verdict: `content-drafts/verdicts/2026-07-13-ed04-nz-ncea-verification.md` (FAIL, two items; item 2 date-bound to the 20 July NZQA release).
- Same edit mirrored to the staging duplicate `content-drafts/staged-ed0304/articles/…` — no divergent copy on disk.

## 3. Build verification (ruling #16 route-around: /tmp copy + symlinked node_modules + VITE_CACHE_DIR)
- `astro build` exit 0 — **65 pages** (30 EN articles + 30 AR articles + listings), sitemap-index created, sitemap-0 carries 65 `<loc>`.
- **Held-piece leakage: 0** — no Ed04 slug in `dist/articles/` or `dist/ar/articles/`.
- **Parity: 30 = 30.** ✓
- **hreflang (07-12 implementation): reciprocal triples verified in build output** on the Morocco pair — EN page: en + ar + x-default; AR page: 3 alternates likewise. ✓

## 4. Live-product spot checks
- Homepage current (Edition 03 lead + 9 secondaries render, wordmark inline SVG, footer chrome intact) — fetched live at run open.
- Live hreflang on deployed article pages — see growth note (Search Console preflight).

**Verdict: QA PASS** — one trap found & defused, one prose defect fixed under verdict routing, build clean, no leakage, parity intact.

— Manager · 2026-07-13
