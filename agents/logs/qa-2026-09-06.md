# QA log — 2026-09-06 (weekly review; Manager on behalf of the Web Developer)

**No build today** — the weekly review adds no page and changes no built file; the only
tree changes that touch the site are a static key file under `web/public/` (invisible to
every `*.html` sweep, like `robots.txt`) and a default-off step in the `verify` job.
State was read from git and the origin, never from a brief (state-verification rule).

## State (named plainly)
- **Local = origin = `fad150f`** (the 09-04 block, pushed 2026-09-04 10:28 +04). Actions
  **run 70: success** — the `verify` job's third live run, green (08-30 / 09-02 / 09-04).
  Working tree clean at open. Origin serves the 09-04 build: `sitemap-0.xml` read live this
  morning, 82 `<loc>`, every `<lastmod>` = `2026-09-04T06:28:56Z` except `/valence/`
  (08-18) — consistent with the `Base.astro` chrome bump in `fad150f`.
- **2026-09-05 was a dark day — no run, no brief.** Today's daily run had not started at
  09:10 +04 when this review opened (weekly at 09:00, daily ~08:00 — the 08-23
  concurrency note applies: the later writer reconciles, never assumes it is alone).
- **Push cadence this window: two pushes, both within ~a day of staging.** The 08-30
  standing question is answered by behaviour (RUNBOOK addendum today).

## Findings of the review (no build required — read from git + the origin)

### 1. Held-file dates leak into live pages' `<lastmod>` — reached production twice
`sitemapLastmod.mjs` resolves the five index pages as *max(chrome, newest file under
`web/src/content/`)* with no regard to `approved:`. Verified by arithmetic against git:
- `9886987` (pushed 08-30 12:05 +04 = 07:58Z) touched **only** the held Sierra Leone AR
  file + two og PNGs — no chrome. The 08-31 QA log records "origin sitemap lastmod
  2026-08-30T07:58". Five live pages told a crawler they changed; no served byte had.
- `642fef2` (pushed 09-02 13:39 +04 = 09:39Z) touched **only** the held Sudan EN draft +
  its still. The 09-03 log records "sitemap lastmod 09-02 09:39Z; home last-modified
  09-02 09:40Z". Same leak, second instance.
The held-leak sweep asserts zero held **slugs** in dist/sitemap and was green both times;
it never enumerated **dates**. Sixth member of the enumeration family. RUNBOOK rule
codified today; the fix is the Web Developer's first item on 09-07 (see forward question).

### 2. Three banked Ed05 pairs, zero Verifier verdicts
Zambia (08-27), Sierra Leone (08-30), Sudan (09-04) carry Editor pair verdicts and Arabic
gates and **no `<date>-<slug>-verification.md`**. The ≤3 ceiling is reached, not breached.
RUNBOOK rule codified; ledger opened at `content-drafts/_EDITION_05_STATUS.md`; the
Verifier's first verdict (Zambia) is scheduled 09-07, one per run in banking order.

### 3. The week's near-miss that *did* reach production, and is now fixed live
The English home's canonical/og:url pointed at a redirect (`/madar` → `/madar/`) from
05-25 to 09-04 — 101 days — found by assertion #9 on first contact (09-03), fixed by
derivation in `Base.astro`, on the origin since run 70. Confirmed live this morning: the
served sitemap's home entry and its hreflang cluster agree on `…/madar/`.

## Staged today, proved per #35 without egress
- **`web/public/66ec26c5232c206b2c4714fd916a29a8.txt`** — IndexNow key file (public by
  protocol design), under `/madar/` so the keyLocation covers every `/madar/` URL.
- **`astro-pages.yml` → `verify` job gains a fourth step, switch `INDEXNOW: "off"`.** It
  reads the URL set from the **served** `sitemap-0.xml` (never the tree), verifies the
  key file is served, builds the JSON payload, and — only when the switch is `on` — POSTs
  it to `api.indexnow.org`. Runs after the byte-compare, so it cannot announce a stale
  origin. Best-effort on the index's answer (warning, not deploy failure); **loud** on our
  own side (zero URLs or an unserved key file fail the job even when off — the key file is
  a promise the build must keep, #37). YAML re-parsed clean (3 jobs, verify = 4 steps);
  `bash -n` clean. **Proved with `curl` stubbed to fixtures:** control (82-URL sitemap,
  correct key, switch off) → exit 0, payload valid, 82 URLs, keyLocation correct; bite 1
  (empty sitemap) → exit 1; bite 2 (wrong key served) → exit 1; bite 3 (key unreachable)
  → exit 1. The live POST self-proves in the judging environment on the first push after
  the founder turns the switch on (#16). **Push note: the commit touches
  `.github/workflows/` — workflow-scope PAT.**

## Forward question (required per the 08-23 ruling) — for the 09-07 run, before anything new
**Make the held-file leak impossible, then bound the chrome bump.** (a) In
`sitemapLastmod.mjs`, restrict `newestArticle` and `forContent` to files whose frontmatter
is `approved: true` (parse the frontmatter; a held file contributes no date anywhere).
(b) Prove per #35 in the route-around build: *bite* — a scratch commit touching a held
file must move no `<lastmod>`; *control* — a scratch commit touching an approved file
must move exactly the dates it should; then discard the scratch commits. (c) Add the
date assertion to the held-leak sweep: no sitemap `<lastmod>` newer than the newest
commit touching an approved file or a chrome file. (d) Carried, not answered: the 09-04
deploy moved 82 dates for a one-page head change — an output-bounded lastmod (per-URL
content hash across deploys, which only the deploy pipeline can know) is the honest
instrument; the Web Developer proposes a design, the review decides next Sunday.
Note the irony and accept it once: the fix itself touches `web/src/lib` and will bump all
82 dates one more time.

— Manager · 2026-09-06 (weekly review)

---

# QA log — 2026-09-06, daily run (Web Developer) — appended to the review's log, not a second file

**Concurrency, named:** the weekly review opened at 09:00 and this daily run at 09:10; the
review wrote the section above, the IndexNow key file, the `verify` switch, the RUNBOOK
addendum, the INDEX consolidation (#1–#43 into §3) and `_EDITION_05_STATUS.md` while this
run was reading a 444-page PDF. Per the 08-23 note the later writer reconciles: this run
**appends** here and to the INDEX, edits the workflow in a different job than the review
did (build vs verify — YAML re-parsed: 3 jobs, build 9 steps, verify 4 steps, `INDEXNOW: off`
intact), and re-checks the tree before the push block. Nothing of the review's was
overwritten; the review's 09-07 schedule (held-date leak fix, Verifier's Zambia verdict)
stands as written.

**Build:** route-around per standing recipe (rsync → `/tmp/madar-run-0906`, node_modules
symlinked, session-unique `VITE_CACHE_DIR` + log paths, `.git` rsynced so the lastmod
resolver sees a real tree at `fad150f`; rsync's one complaint is the stale-lock boundary).
`CI=true astro build` → **exit 0, 81 routed pages (+VALENCE = 82 served)**. The review's key
file rides into `dist/` from `public/` as a static file — not a page, not in the sitemap,
correct (the 08-17 rule covers pages meant to be *found*; a key file is meant to be *fetched
by URL*).

## State
- **Local = origin = `fad150f`** at open (09:10); Actions run 70 success; the origin serves
  the 09-04 build (home `last-modified` 09-04 06:31Z; canonical `…/madar/` live — the 101-day
  redirect-as-canonical is gone from the origin). **`qa_live_drift`: CLEAN — 82 URLs, 0
  lastmod drift, 4 sampled heads identical.** Second clean read in the operation's history
  (first 08-31).
- **2026-09-05 was a dark day** (no run, no brief). One clean recovery day, no double-batch.
- Working tree at open: clean. At 11:24 the review's files began landing (see above).

## Assertions (today's `/tmp` build)
| Check | Result |
|---|---|
| astro build (CI=true, real work tree) | exit 0 · 81 routed + valence |
| held-leak (zambia, sleic, sudan-cant-wait) | **0 page / 0 sitemap hits** — 3 held EN + 3 held AR; assets only on disk |
| dist parity | 38 EN / 38 AR |
| sitemap | 82 loc / 82 lastmod |
| feeds | EN 38 items · AR 38 items |
| og:image ending .svg | 0 |
| qa_consumer_surface | exit 0 CLEAN |
| qa_a11y_lang | exit 0 PASS (2,993 nodes, 0 defects) |
| qa_ar_language | exit 0 CLEAN (235 fields) |
| qa_reachability | exit 0 PASS (82/82, 0 orphans) |
| qa_jsonld | exit 0 CLEAN (152 nodes, 608 promises dereferenced) |
| qa_robots | exit 0 PASS |
| qa_hreflang_clusters | exit 0 PASS (82 URLs · 40 clusters · 2 allowlisted) |
| **qa_body_links — NEW, standing assertion #10** | **exit 0 PASS** — 76 approved pages · 150 related promises · 571 source promises · 0 in-body links (a count) |
| qa_live_drift (origin vs dist) | **CLEAN** |

## The 09-04 forward question — answered, and the answer began with a silent pass
The question was: *nothing reads an article body for the links it promises the reader.*
The obvious assertion — every in-body internal link dereferences — was written first and
found **0 links to check in 76 articles**: no piece in either language composes a markdown
link in its body; the reader's links are the **sources list** and the **related rail**,
both generated from frontmatter. That is the 08-16 trap exactly (an assertion with nothing
to enumerate has failed), so the surface was re-derived to what an article *actually*
promises, and each promise is asserted by existence in the built page (#37):

1. **`related:`** — every declared slug names an approved article in the same collection
   *and* is rendered as an `<a href>` inside `<nav class="related">`. `RelatedReading.astro`
   drops unresolvable slugs **silently by design** ("a parked or renamed piece can never
   break the build or leave a dead link") — correct for the reader, and a green light wired
   to nothing for the operation: a typo'd slug vanishes and the build says exit 0. Bite 1
   proved it: a bogus slug added to an approved EN article, the site **rebuilt — exit 0,
   silent** — then `qa_body_links` **FAIL(1)** naming slug and page.
2. **`sources[].url`** — every declared URL is rendered as a link in `<ol class="sources">`,
   and no approved page carries a placeholder source (the template's own
   `PLACEHOLDER SOURCE` / `example.com` convention — legal on a held file, a defect on an
   approved one). Bite 2 (one source anchor removed from a built AR page) → **FAIL(1)** naming
   URL and page; bite 3 (a placeholder title on an approved page) → **FAIL(1)**.
3. **In-body links** — enumerated and *reported as a count*, internal ones dereferenced.
   The zero is printed as a zero.

Control on the real build: silent (PASS). Wired into the CI **build** job as the third
QA step (after robots and clusters), so a promise an article breaks fails the deploy.
**Push note: the commit touches `.github/workflows/` (two edits today — the review's
`verify` switch and this build step) — workflow-scope PAT.**

## Forward question (required) — and its order
The review's forward question (held-file dates leaking into `<lastmod>`) is **first** on
09-07, as scheduled above. Carried behind it, named so it is not lost: today's assertion
proves an approved article's 571 external promises are *rendered*; nothing proves they are
*alive*. A source URL can die without a byte of ours changing, which is why it does not
belong in the deploy gate — it belongs in the **weekly read** as a sampled sweep (HEAD on
the 571, dead ones recorded as observations with their first-dead date, the Editor decides
supersede-vs-archive per #41). Candidate: `qa_sources_alive.py`, run by the review, never
by CI. Second, smaller: the schema gains an optional `countries:` list when the Africa
ruler piece lands (Editor's decision 3 in the commission addendum) — `madar_stats.py` must
count the union the same day, or the statistics panel under-reports the piece's coverage.

— Web Developer · 2026-09-06 (daily run, 11:3x +04)

---

## Publish gate — run in writing before staging today's push (Manager)

1. **Editorial approvals:** the push flips **no** `approved:` flag. Today adds the PASEC
   report read (`recon/2026-09-06-…`), the Editor's commission addendum (blocker 1 closed,
   three decisions), ruling #44 / row 39 + INDEX addendum, `qa_body_links.py` + its CI
   step, the Growth note, this log section, the Manager status, Brief 67 — **plus the
   weekly review's block** (review HTML, RUNBOOK addendum, INDEX consolidation + archive
   file, `_EDITION_05_STATUS.md`, IndexNow key file + `verify` switch, its QA section
   above). Zero held-leak proved in today's dist (3 EN + 3 AR held). PASS (nothing ships
   editorially).
2. **Arabic gate:** no Arabic composition today; the three banked pairs hold their gates
   (08-27 / 08-30 / 09-04). PASS.
3. **Stills / assets on disk:** nothing new needs an asset; the three held slugs' stills
   and og cards are on disk (re-listed). PASS.
4. **Clean build + QA:** CI=true build exit 0; ten standing assertions clean, the new one
   proved both ways; parity 38/38; **drift CLEAN against the origin** at open. **Reader-
   visible change in this push: none** — the key file is fetch-by-URL, the switch is off,
   the new CI step gates the next deploy. PASS.

**Gate verdict: a one-day daily block plus the concurrent weekly's block — no editorial
ship, one new standing assertion, one ruling, one draft-blocker closed with three
decisions, one default-off distribution switch for the founder's hand. Cleared for the
founder's one block; workflow-scope PAT required.**

---

## Addendum, 11:35 — the Growth audit changed the corpus, so the build and the gate were re-run

Growth's rail dead-end audit (`agents/growth/2026-09-06-rail-dead-end-audit.md`, Editor
approved in writing) changed **24 frontmatter files** (12 EN + 12 AR, `related:` only, no
`approved:` flip): six Edition-01 pages had **no rail at all**, six Edition-03/04 pages had
**no piece pointing at them**, and Singapore's Arabic rail had **drifted** from its English
twin. `qa_body_links` gained the matching assertion the same hour — **twin rails agree, and
the rail graph has no dead ends** — and was proved per #35 in the right order: **bite 4** on
the pre-change corpus → **FAIL(25)** (the Singapore twin drift + 24 dead-end lines, both
languages); **control** on the corrected corpus, rebuilt from scratch → **PASS** (196 related
promises now, up from 150; 38 twin rails compared; 0 dead ends). All nine other assertions
re-run on the rebuilt dist: clean, parity 38/38, sitemap 82/82, feeds 38/38, held-leak 0. The
rails render in both languages with Arabic meta labels (Mosul's Arabic rail: Palestine,
Türkiye, Sudan — inspected). `qa_live_drift` reads CLEAN because `<lastmod>` derives from
commit dates and the change is uncommitted; on the push, CI will move the 24 rail pages'
dates and nothing else — which is the *correct* shape the review's forward question asks
for. **Publish-gate item 4 is amended: reader-visible change in this push = twelve pages
gain a rail or a rail entry (both languages).** Gate still cleared.

— Web Developer · 2026-09-06

**Brief 67 checked before staging (08-23 rule, founder-facing surface):** `<main class="container">` wrapper present and closed; every composed element balanced (article/section/header/div/nav/p/code/h1–h3/span/strong/em); the statistics panel present exactly once with its CSS block present exactly once; section order Yesterday → Today → Statistics → Tomorrow → push block → footer verified by position; every repo path the brief cites exists on disk; the commit message is single-line ASCII with no `!` or `$`. The `<style>` block is byte-identical to Brief 66's, which rendered correctly. Rendered inspection in a browser was not possible from this sandbox this run (no Chromium; the pane cannot open local files) — structural assertions stand in for it and are named as such.
