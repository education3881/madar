# QA log — 2026-08-28 (Web Developer)

**Build:** route-around per standing recipe (rsync repo → /tmp/madar-run, symlink
node_modules, session-unique VITE_CACHE_DIR + log path; the only rsync failures were
the known FUSE artifacts ".git/index 2"/".git/index 3", routed around, real .git intact).
`CI=true npx astro build` → **exit 0, 82 pages.**

## State (named plainly)
- **Push gap: 5 days.** origin/main = `5e16db2` (08-23) — the live site serves the
  08-23 block. Local carries stranded `f66162d` (08-25) + uncommitted 08-26 + 08-27
  + today. Two gap shapes still stacked; today adds a third uncommitted day on top.
- `qa_live_drift`: **84 named drifts** live-vs-tree (exit 1 by design against unpushed
  work) — unchanged count from 08-27; the reader is still waiting on the same delivery.

## Assertions (all against today's /tmp build)
| Check | Result |
|---|---|
| astro build (CI=true, real clone) | exit 0 · 82 pages |
| held-leak (`sleic-outcomes`) | **0 hits in dist** — new SL piece fully held |
| dist parity | 38 EN / 38 AR |
| qa_consumer_surface | exit 0 CLEAN · sitemap 82 loc / 82 lastmod · og:image raster |
| qa_a11y_lang | exit 0 · 2,993 nodes · 0 defects |
| qa_ar_language | exit 0 · 40 AR pages · 235 chrome fields CLEAN |
| qa_reachability | exit 0 · 82/82 reachable · 0 orphans · depth 1/11/40/30 |
| feeds | 38 items EN · 38 items AR, both parse |
| schema caps (held pieces) | SL dek 194/200 (trimmed from a first draft at exactly 200), title 47/100; Zambia 177/200 |
| NEW: BreadcrumbList | **76/76 article pages**, both languages; JSON parses; array-form `jsonLd` renders; AR crumbs in Arabic with correct absolute AR paths |

## Notes for the wave gate (not defects today)
- Zambia + Sierra Leone og PNG cards do not exist yet (`/og/*.png` generated at flip
  from the stills). Held pages are not served, so `qa_consumer_surface` is correctly
  silent — but **card generation is a flip-day checklist item** for both Ed05 pieces.
- `qa_consumer_surface` reports "pages checked: 81" against sitemap 82 (VALENCE is
  page-checked under its own rules) — pre-existing accounting, verified not a regression.

## Forward question (required per the 08-23 ruling)
Structured data is now *emitted* on 76 pages and *parses* — but nothing asserts it
against the consumer's contract. A typo'd `@type`, a breadcrumb `item` pointing at a
page not in dist, or a citation URL dropped by a refactor would pass every check we
own: presence is checked, meaning is not. Candidate next assertion: a `qa_jsonld`
that (a) validates each node against a minimal internal contract (required keys,
absolute URLs), and (b) dereferences every breadcrumb `item` and `workTranslation`
back to a file in dist — the #37 pattern (assert by existence) applied to the
machine-readable layer.

— Web Developer · 2026-08-28

---

## Publish gate — run in writing before staging today's push (Manager)

1. **Editorial approvals:** the push flips **no** `approved:` flag. Sierra Leone EN is
   staged `approved: false` (Editor's five tests pending on the pair, per the Zambia
   cadence); Zambia pair remains banked and held. **Zero held-leak proved in today's
   dist** — nothing unapproved reaches a reader. PASS (nothing to gate; nothing ships).
2. **Stills on disk:** 2026-08-28-sierra-leone-sleic-outcomes.svg committed alongside
   its article in the same block (asset-with-article rule, 06-10 finding). PASS.
3. **Clean build + QA:** CI=true build exit 0, 82 pages; all four standing assertions
   clean; parity 38/38; feeds 38/38. PASS.
4. **Arabic gate:** no AR ships today; AR routing questions flagged in writing to the
   Arabic Editor (ساكي / كباكرا / مصطفى / NYAF-stays-Latin). N/A-with-record.

**Gate verdict: staged push is content-staging + chrome only — approved corpus
unchanged. Cleared for the founder's one block.**
