# QA log — 2026-08-29 (Web Developer)

**Build:** route-around per standing recipe (rsync repo → /tmp/madar-run-0829, symlink
node_modules, session-unique VITE_CACHE_DIR + log paths; .git rsynced minus the FUSE
"index 2/3" artifacts — the lastmod resolver requires a real work tree in CI mode and
correctly failed loud on the first, git-less copy). `CI=true npx astro build` →
**exit 0, 82 pages.**

## State (named plainly)
- **Push gap: 6 days — fourth consecutive day the staged block went unrun.**
  origin/main = `5e16db2` (08-23); local HEAD = `a340782` (08-28), which itself
  carries the stranded `f66162d` (08-25). Working tree was clean at open — the
  08-28 block committed everything; nothing new is at risk, and nothing has
  reached a reader since 08-23. Per the 08-28 note: tomorrow's weekly review
  should treat **founder-push availability as the constraint**, not output.
- `qa_live_drift`: **84 named drifts** live-vs-tree, unchanged from 08-27/08-28 —
  the same delivery, still waiting.

## Assertions (all against today's /tmp build)
| Check | Result |
|---|---|
| astro build (CI=true, real work tree) | exit 0 · 82 pages |
| held-leak (`sleic-outcomes`, `zambia-free-education-act`) | **0 hits in dist** — both Ed05 pairs fully held, incl. today's new AR file |
| dist parity | 38 EN / 38 AR |
| qa_consumer_surface | exit 0 CLEAN · sitemap 82 loc / 82 lastmod · og:image raster |
| qa_a11y_lang | exit 0 · 2,993 nodes · 0 defects |
| qa_ar_language | exit 0 · 40 AR pages · 235 chrome fields CLEAN |
| qa_reachability | exit 0 · 82/82 · 0 orphans · AR side 40/40 depth ≤2 |
| feeds | 38 items EN (rss.xml) · 38 items AR, both parse |
| schema caps (held pieces) | **SL AR dek 185/200 with tashkeel** (checked at compose), title 30/100 |
| **NEW: qa_jsonld** | **exit 0 CLEAN · 152 nodes · 608 promises dereferenced to dist** |

## New standing assertion — `qa_jsonld` (the 08-28 forward question, answered)
Presence was checked; meaning was not. `agents/tools/qa_jsonld.py` now asserts the
JSON-LD layer against the consumer's contract: every block parses; breadcrumb
positions are sequential and every `item` **dereferences to a file in dist** (#37);
Article required keys present; `url` / `@id` / `mainEntityOfPage` / `image` /
`workTranslation` dereference; `inLanguage` **agrees with the page's own path
language** (#36 — the exact axis the 08-23 Arabic-chrome defect lived on, now closed
in the machine layer too); citation URLs absolute; no SVG images (08-18 family).
**Proved both ways per #35:** control = today's build, 0 defects, 152 nodes, 608
dereferences; bite = a dist copy with one retargeted breadcrumb, one missing JSON-LD
image PNG, one flipped inLanguage → **FAIL(3)**, each defect naming page + field,
silent on the 79 untouched pages. Standing assertion count: **six.**

## Forward question (required per the 08-23 ruling)
The origin drift check (`qa_live_drift`) and every dist assertion run at BUILD time,
on demand. Nothing runs at DEPLOY time: the day a push lands, no check automatically
confirms the origin went clean — a human must remember to re-run drift after the
Actions run. Candidate: a post-deploy verification step **inside the workflow
itself** (curl the live sitemap + one sampled head after deploy-pages completes,
fail the run on mismatch), so ruling #16's "judging environment" includes the
minutes *after* the push, not only the build.

— Web Developer · 2026-08-29

---

## Publish gate — run in writing before staging today's push (Manager)

1. **Editorial approvals:** the push flips **no** `approved:` flag. Today adds the
   Sierra Leone **Arabic composition**, held `approved: false`; Zambia pair banked
   and held. **Zero held-leak proved in today's dist** for both pairs. PASS
   (nothing ships).
2. **Arabic gate:** SL AR composed by the Arabic Content Creator and **verified by
   the Arabic Editor in writing** (`verdicts/2026-08-29-sierra-leone-sleic-outcomes-ar-gate.md`):
   7/7 named humans CONFIRMED (كباكرا per the new row-33 labial-velar rule, ساكي،
   غولد، فيرغال تيرنر، كاستيلنو، جيفريز، ماكلين); مصطفى flag moot (name absent from
   final EN); NYAF stays Latin per #38. PASS.
3. **Stills on disk:** no new still needed — the AR file shares
   `2026-08-28-sierra-leone-sleic-outcomes.svg`, already staged with its EN. PASS.
4. **Clean build + QA:** CI=true build exit 0, 82 pages; all **six** standing
   assertions clean; parity 38/38; feeds 38/38. PASS.

**Gate verdict: staged push is held-content + tooling + logs only — approved corpus
unchanged. Cleared for the founder's one block.**
