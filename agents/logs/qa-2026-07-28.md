# QA log — 2026-07-28

**Run:** daily. **Gate context:** bench draft 2 (england-report-cards-first-term) composed and held; no change to `web/src`.

## Build
- `npx astro build` via the standing route-around (/tmp copy + symlinked `node_modules` + isolated `VITE_CACHE_DIR`): **65 page(s) built, exit 0**, sitemap-index.xml emitted. No warnings on content collections.

## Parity (live corpus = approved:true only)
- EN approved:true: **30** · AR approved:true: **30** → **100% parity**.
- Held in build path (flag = the hold): 7 EN + 1 AR (the earlier Ed04 drafts + us-naep AR). Unchanged from 07-27.
- Today's England draft: held in `content-drafts/staged-ed0304/articles/` — **not in the build path at all** (double-safe: flag *and* out-of-tree). `approved: false` confirmed.

## Held-leak sweep
- `grep -rl "approved: true" content-drafts/staged-ed0304/` → **empty**. No staged flag carries `true`.
- England draft `approved:` line = `false` (frontmatter line 42).

## Brand chrome
- Wordmark/glyphs: no `<img … wordmark>` refs in `web/src` — inline SVG intact.

## Figure integrity (this run)
- Every load-bearing England figure traced to a **served-text primary** (Schools Week survey page + gov.uk education-inspection blog read as full text; NAHT judgment via coverage + judiciary.uk PDF). Clears ruling **#26** — no dashboard-only or unquotable figure used.
- Dek cap: composed at **205/200**, caught **at compose** (not at the build gate), trimmed to **199/200** before hand-off — the 07-27 enforcement addendum executing at the ruler it names.

## Open items (unchanged, carried to gate)
- Ed04 ships whole or not at all: no flag flips. Remaining to a complete wave — ~2 bench drafts, AR for 7 of 8, stills ×10, `editions.ts` registry entry (second gate), intra-wave `related:` reconciliation, final live re-fetch of every register at the pre-ship sweep.
- `staged-ed0304/` cleanup (`git rm -r`) whenever convenient — cosmetic, non-blocking.

**Verdict: PASS.** Build green, parity 30=30, zero leak, chrome intact.
— Manager / QA · 2026-07-28
