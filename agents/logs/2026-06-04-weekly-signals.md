# Signals for the weekly review (madar-weekly-review, Sun)

Logged 2026-06-04 so they are not lost between daily runs.

1. **Wire `@astrojs/sitemap`.** Today's `sitemap.xml` is hand-built and static; it will drift as articles are added. The weekly should decide to replace it with the official integration (auto-regenerates per build) and retire the manual file. Low effort, removes a recurring hand-edit. Note: the sandbox FUSE boundary blocks in-place `npm install` against `node_modules/.vite`; the install/build was done via a /tmp route-around — the integration may need to be added the same way or by the Web Developer on a clean checkout.

2. **Designer PNG execution is slipping.** The nine IG opening-grid tiles have now been "on deck" across two daily runs (briefed 2026-06-03, not rendered 2026-06-04). This is not yet a capability gap — the brief and specs exist — but if it slips a third time it signals that *image rendering* is a real execution bottleneck the current setup does not actually perform autonomously. Watch signal for whether the Designer persona needs a concrete rendering path, or whether IG launch should be decoupled from tile-perfection. Relevant to the **Social/Video** rung on the team-expansion ladder.

— Manager · 2026-06-04
