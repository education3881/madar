# QA log — 2026-08-30 (Web Developer)

**Build:** route-around per standing recipe (rsync repo → /tmp/madar-run-0830,
symlink node_modules, session-unique VITE_CACHE_DIR + log paths, .git rsynced
minus the FUSE "index 2/3" artifacts). `CI=true npx astro build` →
**exit 0, 82 pages.**

## State (named plainly)
- **Push gap: 7 days — fifth consecutive day the staged block went unrun.**
  origin/main = `5e16db2` (08-23); local HEAD = `a340782` (08-28) + the full
  08-29 day on disk uncommitted (brief 61, SL AR composition, AR gate,
  `qa_jsonld`, row 33). Nothing lost; nothing delivered since 08-23. Today's
  weekly review treats founder-push availability as the constraint.
- `qa_live_drift`: **84 named drifts**, unchanged since 08-27 — same delivery,
  still waiting.

## Assertions (all against today's /tmp build)
| Check | Result |
|---|---|
| astro build (CI=true, real work tree) | exit 0 · 82 pages |
| held-leak (`sleic-outcomes`, `zambia-free-education-act`) | **0 hits in dist** — both Ed05 pairs fully held, both og cards on disk |
| dist parity | 38 EN / 38 AR |
| qa_consumer_surface | exit 0 CLEAN · sitemap 82 loc / 82 lastmod · og:image raster |
| qa_a11y_lang | exit 0 · 2,993 nodes · 0 defects |
| qa_ar_language | exit 0 · 40 AR pages · 235 chrome fields CLEAN |
| qa_reachability | exit 0 · 82/82 · 0 orphans |
| qa_jsonld | exit 0 CLEAN · 152 nodes · 608 promises dereferenced |
| feeds | 38 items EN · 38 items AR, both parse |

## The 08-29 forward question — answered in the workflow itself
`astro-pages.yml` gains a third job, **`verify`** (needs: deploy): download the
exact artifact `deploy-pages` published, poll the origin up to 5 minutes until
`sitemap-0.xml` matches byte-for-byte, then byte-compare a five-file bilingual
sample (`index.html`, `ar/index.html`, `rss.xml`, `ar/rss.xml`,
`sitemap-index.xml`). Any mismatch fails the run loudly. #16's judging
environment now includes the minutes after the push; #17's second gate reports
its own failure instead of waiting for a human to run drift.

**Probe selection is the day's ruling (#39, row 34):** the obvious probe,
`sitemap-index.xml`, is deploy-invariant on the live 08-23 build (no lastmod)
and would have false-passed through the very July outage this job answers; the
08-25 resolver then made it variant — probe validity itself drifted within one
week. Probe of record: `sitemap-0.xml`, the widest single-file fingerprint
(82 URLs + lastmod). **Proved per #35 against real state:** bite = today's
stale origin (probe DIFFERS, index DIFFERS); control = dist vs itself, silent.
YAML parses (3 jobs), shell syntax checked. The job's first live run is the
next push — which is also its first real-world bite opportunity.

## Forward question (required per the 08-23 ruling)
The verify job proves the origin serves the deployed bytes; nothing yet proves
the *feeds' consumers* see the new items promptly (a feed reader polls on its
own schedule and caches by ETag/Last-Modified). Candidate: assert the origin's
ETag/Last-Modified headers change across a deploy for both feeds, inside the
same verify job — cheap, same artifact, closes the cache axis of delivery.

— Web Developer · 2026-08-30

---

## Publish gate — run in writing before staging today's push (Manager)

1. **Editorial approvals:** the push flips **no** `approved:` flag. Today adds
   the Sierra Leone **pair verdict** (BANKED, held), ruling #39/row 34, the
   `verify` workflow job, two og cards, this log, and Brief 62. **Zero
   held-leak proved in today's dist** with both cards on disk. PASS (nothing
   ships).
2. **Arabic gate:** no new AR content today; the SL AR gate passed in writing
   08-29 and is countersigned by today's pair verdict. PASS.
3. **Stills on disk:** no new piece; both Ed05 stills committed-side, both og
   cards now beside them. PASS.
4. **Clean build + QA:** CI=true build exit 0, 82 pages; all six standing
   assertions clean; parity 38/38; feeds 38/38. PASS.

**Gate verdict: staged push is held-content + verdict + tooling + workflow +
logs only — approved corpus unchanged. Cleared for the founder's one block.
Note for the founder: this push's Actions run is the maiden run of the
`verify` job — for the first time, the pipeline itself will confirm the origin
went current.**
