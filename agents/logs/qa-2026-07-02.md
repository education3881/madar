# QA pass — 2026-07-02 (recovery run after the 06-26→07-01 dark days)

**Web Developer + Manager · run concurrently with the delayed weekly review (separate session — see coordination note at bottom)**

## State verification (first action, per standing rule)

- `git fetch origin main` → local HEAD **= origin/main at f46d05b** (2026-06-25). Working tree clean at run start. The 06-25 push landed; no unpushed work stranded across the gap.
- **Gap named:** six dark days, 2026-06-26 → 2026-07-01 (app not launched), including the 06-28 weekly. Longest gap to date (1 → 3 → 6). The weekly is running delayed today in parallel.
- Live site: sandbox egress blocks `education3881.github.io` this run — **"live = current" is inferred from HEAD = origin** (deploy fires on push; last push 06-25), not directly observed. Honest caveat, same as 06-25.
- `.git/index.lock`: present, FUSE-unremovable, routed around as standing.

## The build — sitemap integration landed (the seven-times-deferred chore)

- **`@astrojs/sitemap` integration shipped**: `astro.config.mjs` wired (with `i18n` block emitting EN↔AR hreflang alternates), `package.json` + `package-lock.json` updated (`npm ci` on Actions stays in sync), `robots.txt` → `sitemap-index.xml`.
- Build verified via the banked tmpfs route: **exit 0, 41 pages**, `sitemap-index.xml` + `sitemap-0.xml` generated — **41 URLs, 80 hreflang links, both files XML-well-formed**, Brazil EN+AR present.
- **Legacy `public/sitemap.xml`:** FUSE blocks deletion (`rm` → operation not permitted). Routed around: replaced with a **stub `<sitemapindex>`** delegating to `sitemap-0.xml`, so the old URL keeps resolving and can never drift again. Header comment forbids future hand-patching.
- **Known small regression, accepted:** the generated sitemap emits `hreflang` en/ar pairs but not `x-default` (the hand-patched file had it). Non-blocking; revisit only if Search Console flags it.
- Rebuild re-verified **after** the rails edits and with the stub in place: exit 0, 41 pages.

## Rails (the 06-25 carried item, closed)

- **Uruguay → Brazil** and **Egypt → Brazil** back-links added in EN + AR frontmatter (4 files). Egypt swaps its Sierra Leone link for Brazil (keeps 3, strengthens the literacy-measurement rail); Uruguay appends (1 → 2). Verified in built HTML: both directions render.

## Standing checks

- **Parity:** 18 EN = 18 AR (frontmatter-derived, `madar_stats.py`). No orphaned `related:` slugs (all point at existing articles).
- **Brand chrome:** untouched this run (no template edits); last verified clean 06-25.
- **No content pages changed** except the 4 rails frontmatters; no dek/title caps touched.

## Coordination note (new failure-mode observed, handled)

A **concurrent session** (the delayed weekly review) wrote `agents/RUNBOOK.md`, `agents/guidebook/INDEX.md`, the 06-15 build-route note, and `agents/reviews/2026-07-02-weekly-review.html` **while this daily was running** — first observed instance of *two sandbox sessions racing on the same tree* (the push-race memory covered founder-vs-session, not session-vs-session). Handled by: not editing the weekly's files until its consolidation was signed, appending the daily INDEX addendum after, and composing tonight's push block to carry **both** runs' files in one commit. If this recurs, the RUNBOOK's new recovery-run rule should gain a session-lock convention (e.g., a `.run-in-progress` marker in /agents/logs).
