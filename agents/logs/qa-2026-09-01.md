# QA log — 2026-09-01 (Web Developer)

**Build:** route-around per standing recipe (rsync repo → /tmp/madar-run-0901
excluding the FUSE phantoms `index 2`/`index 3` and `*.lock`, node_modules
symlinked, session-unique VITE_CACHE_DIR + log paths, .git rsynced so the
lastmod resolver sees a real work tree at `9886987`). `CI=true npx astro
build` → **exit 0, 81 routed pages (+VALENCE = 82 served).**

## State (named plainly)
- Origin current at `9886987` (08-30 push). **The 08-31 block went unrun —
  one full day of work (recon, commission, ruling #40, feed-cache step,
  wave packet, Brief 63) sits staged in the working tree, not lost.**
  Today's block carries both days. Second staged-day in the new PAT era;
  per the 08-30 standing note, one unrun day is a founder-availability
  fact, not a process alarm — flagged, not escalated.
- `qa_live_drift`: **CLEAN — 82 URLs, 0 lastmod drift, 4/4 sampled heads
  identical** (second consecutive clean read). Correct on both sides:
  nothing approved has changed since 08-30, so dist and origin should
  byte-agree, and they do.

## Assertions (all against today's /tmp build)
| Check | Result |
|---|---|
| astro build (CI=true, real work tree) | exit 0 · 81 routed + valence |
| held-leak (zambia, sleic, **sudan**) | **0 page/sitemap hits** — 3 held EN + 2 held AR; on-disk hits are assets only (2 og cards + 3 stills), per the 08-30 assets-not-pages ruling |
| dist parity | 38 EN / 38 AR |
| qa_consumer_surface | exit 0 CLEAN |
| qa_a11y_lang | exit 0 PASS |
| qa_ar_language | exit 0 CLEAN |
| qa_reachability | exit 0 PASS · 0 orphans |
| qa_jsonld | exit 0 CLEAN |
| qa_live_drift (origin vs dist) | exit 0 CLEAN — second consecutive |
| **qa_robots (NEW, assertion #8)** | **exit 0 PASS** — robots present, 1 Sitemap line == derived URL, target exists, 0 disallows, legacy stub resolves |
| sitemap | 82 loc / 82 lastmod |
| feeds | EN 38 items · AR 38 items |

## The 08-31 forward question — answered same-day
`robots.txt` was enumerated for the first time and found **already present
and already correct** (allow-all; names `sitemap-index.xml`; the retired
`/sitemap.xml` carries the 07-02 stub comment and points at a file that
exists). The defect was that nothing asserted any of it — the silent-pass
family again. Answer: **`qa_robots.py`** (agents/tools), standing assertion
#8: exists → exactly one Sitemap line whose URL is *derived* from
astro.config's own site+base (#36) → advertised file exists in dist (#37) →
zero Disallow lines → legacy stub's `<loc>`s resolve. **Proved per #35
before landing:** bite 1 = robots.txt removed (1 defect), bite 2 =
`Disallow: /ar/` injected (1 defect), control = real build (silent). Wired
into the CI build job (`astro-pages.yml`, build step after `npm run build`),
so a drifted crawler surface fails the deploy before anything publishes.
YAML re-parsed clean (3 jobs); the step re-run locally against today's dist.

**⚠ Push-block note (carried from 08-31, still true): this commit touches
`.github/workflows/astro-pages.yml` — the push needs the PAT with
`workflow` scope (the one that ran the 08-30 block has it).**

## Forward question (required per the 08-23 ruling)
The Sudan pair, when it completes, needs its og card at the wave gate
(Zambia + Sierra Leone cards exist; Sudan's still is on disk, its card is
not). Beyond flip-day mechanics, the open verification gap is now the AR
side of the crawler surface: qa_robots asserts one sitemap for both
languages, but no check has ever asserted that the *hreflang cluster* a
crawler reassembles from our pages matches the sitemap's own URL set —
i.e., that every sitemap URL appears in exactly one hreflang cluster and
no cluster names a URL the sitemap omits. Candidate: extend qa_robots or
qa_jsonld with a cluster-vs-sitemap set-compare.

— Web Developer · 2026-09-01

---

## Editor's draft-day note (first pass, not the pair verdict)

The Sudan EN draft was read against the commission on hand-off:
- **Frame holds:** two stories, joined by exactly one orienting sentence
  ("No served source connects…"); no sentence asserts program identity
  between CWTL and the wartime spaces; the lineage gate language survives
  into the piece itself, which is the commission's park-on-sight test
  passed at compose.
- **Trial carried whole:** direction + significance in the trial's own
  scope words; null on hope and the negative self-esteem finding ride with
  the authors' caveat; multipliers absent (body unread — tandfonline
  serves nothing to this sandbox; ceiling honoured).
- **Registers:** BRIDGES figures all marked as announced plans; the 8m/
  half-of-buildings denominators from the PR only, never chained to the
  19m advocacy figure (which appears only inside a source-title annotation,
  vintage-marked); pre-war vintages marked in-sentence (Aroma "late 2022 —
  pre-war, and the vintage matters").
- **Counts at compose:** title 52/100 · dek 192/200. Count line present.
- **Held:** `approved:false`, zero build leak proved above. AR composition
  is next (native-AR story; the routing flags from the commission stand:
  فاطمة عباس طاهر restore-rule via the AR mirror; Addise routes Ethiopian
  by heard vowel, CONFIRM before ship; CWTL keeps Latin + gloss per #28a;
  Makanna's Arabic spelling recovered from UNICEF's AR edition, never
  invented). Pair verdict follows AR, per the Zambia/SL cadence.

— Editor · 2026-09-01

---

## Publish gate — run in writing before staging today's push (Manager)

1. **Editorial approvals:** the push flips **no** `approved:` flag. It adds
   the held Sudan EN draft + its still, qa_robots + CI wiring, ruling #41/
   row 36 + INDEX addendum, this log, Brief 64 — plus everything the unrun
   08-31 block already staged. Zero held-leak proved in today's dist. PASS
   (nothing ships).
2. **Arabic gate:** no new AR content today; Sudan AR flags recorded for
   the Arabic Editor. PASS.
3. **Stills on disk:** Sudan still written in the same run as the article,
   before this gate. PASS.
4. **Clean build + QA:** CI=true build exit 0; all eight standing
   assertions clean; parity 38/38; drift CLEAN. PASS.

**Gate verdict: staged push is a held draft + still + tooling + logs on top
of yesterday's staged day — approved corpus unchanged. Cleared for the
founder's one block, workflow-scope PAT note stands.**
