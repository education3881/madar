# QA + Publish-gate log — 2026-08-02 (Sunday)

Recovery run after a 3-day dark gap (07-30 / 07-31 / 08-01). Today's content increment: **Arabic composition for `australia-naplan-reset`** (Ed04 bench draft; EN verified 2026-07-17). Nothing ships — Ed04 remains a held wave (`approved: false` everywhere).

## State verification (before any work)
- `git branch` → on `main`; `git fetch origin main` → local HEAD == origin/main == **729d836** (Brief 40, 2026-07-29); working tree clean.
- Live site re-fetched: `sitemap-0.xml` serves the **30-piece live corpus** (30 EN + 30 AR mirrors + structural), reciprocal hreflang on homepage/AR/editions, **zero Ed04 slugs** (naplan / naep / singapore / england etc. all absent). Live == origin == 729d836. **Current.**
- Gap named plainly: 07-30, 07-31, 08-01 dark — three missed days. Per standing rule, one clean recovery day, no forced double-batch. Also: today is Sunday → the separate `madar-weekly-review` task is due; flagged to the founder, not run here (this is the daily).

## Content QA — the new AR file
- Placement: `web/src/content/articles-ar/2026-07-16-australia-naplan-reset.md` at `approved: false` (build path, filtered out — parallels how us-naep's AR is held). Mirror copy also in `content-drafts/staged-ed0304/articles-ar/` (backup, `approved: false`).
- Frontmatter field set **identical** to the shipped us-naep AR (title, dek, date, edition, country, region, level, themes, type, related, contains_composites, englishVersion, sources, hero, approved).
- Schema-critical lengths: **dek 172/200**, **title 26/100** — both under cap.
- `region: Oceania` — accepted by the content schema (the EN Australia file already carries it and syncs clean; first Oceania piece in the corpus).
- `englishVersion: 2026-07-16-australia-naplan-reset` — resolves the EN file's declared `arabicVersion:` (previously an unfulfilled forward reference; now a reciprocal pair once the wave flips).
- `related:` mirrors the EN (us-naep, nz-ncea, oman) — the "measurement" internal cluster.

## Build (the judging environment, route-around)
Route-around per the standing sandbox lock (`.git/index.lock` + vite-FUSE boundary): `rsync` clean tree to `/tmp/madar-b`, symlink `node_modules`, `VITE_CACHE_DIR=/tmp`.
- `astro sync` → **exit 0**, "Synced content" (all collection schemas valid, including the new AR).
- `astro build` → **65 page(s) built, Complete!**, exit clean.
- `dist` audit: **0** held Ed04 slugs in output; **30 EN articles + 30 AR articles** rendered = published parity **30 = 30**; 65 total HTML pages. The new Australia AR is correctly **held out** (approved:false).

## Held-leak sweep
- `approved: true` among `content-drafts/staged-ed0304/` → **0**.
- 7 Ed04 EN drafts in `web/src/content/articles/` all `approved: false`; only us-naep + (new) Australia AR present in `articles-ar/`, both `approved: false`. No latent `true` introduced.

## Arabic gate (transliteration verification — Arabic Editor)
Named humans in the piece: **2**.
| Name (EN) | AR | Status |
|---|---|---|
| David de Carvalho (former ACARA CEO) | ديفيد دي كارفالو | **CONFIRMED** — Portuguese-origin surname, Arabic-media-standard كارفالو |
| Stephen Gniel (current ACARA CEO) | ستيفن غنيل | **PROVISIONAL** — pronunciation not confirmable from served-text primaries; best-effort form, resolve at pre-ship audio/phonetic check (ruling #28b) |
Institutional acronyms glossed per ruling #28a (gloss + Latin tag on first mention): ACARA, NAPLAN→نابلان, AERO, AARE. Level/proficiency labels: متجاوِز / قوي / نامٍ / بحاجةٍ إلى دعمٍ إضافي (carried consistently, Developing rendered "نامٍ (يعمل نحو)" not "يرسُب" per the EN's own anti-misreading design).

## Brand chrome
Wordmark inline (CSS-only fallback intact) on the daily brief; no `<img src>` brand chrome introduced. Brief carries the stats-panel CSS once.

## Verdict
Build clean, parity 30=30, zero held-leak, one PROVISIONAL transliteration correctly flagged (not shipped). **No `approved` flips.** P0/P1: none.

## Residual (for the wave gate, not today)
- Resolve `Gniel` transliteration (PROVISIONAL) at pre-ship sweep.
- AR still owed for 6 of 8 (india, singapore, nz, chile, netherlands, england) — the binding parallel path.
- Stills ×10, `editions.ts` Ed04 registry entry (second gate), intra-wave `related:` reconciliation, final live re-fetch of load-bearing registers.
- `staged-ed0304/` duplicate cleanup (`git rm -r`) whenever convenient (standing).
