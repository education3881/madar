# QA log — 2026-07-17

**State at open:** HEAD == origin/main == `68e9b00`; working tree clean; deploy **run 44 success** (2026-07-16 06:13 UTC) for exactly this commit. **Live site verified by direct fetch:** homepage serves Edition 03, and the head carries the **hreflang triple (en / ar / x-default) + canonical on live** — closing the 07-16 lineage-only caveat with the thing itself. Direct live *article* head-check still blocked by the fetch-tool provenance boundary (observation per #20, not a verdict); og:image absoluteness remains lineage-verified through green run 44. No missed day: 07-16 ran and pushed.

## Build (route-around: fresh /tmp dir — the 07-16 build dir was permission-locked by the FUSE boundary; routed around, not fought)

- `npx astro build`: **exit 0, 65 pages** (post-fix tree).
- **Zero leakage:** Australia draft (held `approved: false`) builds no page, no sitemap entry.
- **Parity:** dist articles **30 EN = 30 AR**.

## Verification (Verifier's third verdict — see `verdicts/2026-07-17-ed04-australia-naplan-verification.md`)

- **Australia NAPLAN: FAIL one item → fixed same-run → CLEARED.** ¶4 carried "more than 1.3 million students" against 2024's register wording "**almost** 1.3 million" (2025 is the "over" year). One-word fix, applied to `web/src` **and** the staging copy; ruling #23 filed. Every other trace-list item re-fetched live and confirmed — the three equity decimals (30.8/7.6, 22.8/71.9, 69.1/53.9) now traced to the release's own background section (medianet full wire copy) **and** the blob-host Commentary (reads as text — new route, banked).

## Flag sweep (3 surfaces) — seventh catch, largest class

- **Twenty latent `approved: true` flags found in `staged-ed0304/`** — the **Ed03** staging copies (10 EN + 10 AR), stale since the 07-07 ship. Outside the build path, so no leakage risk, but exactly the class the sweep rule exists for. **All twenty set `false`; zero `true` flags now exist anywhere outside `web/src/content/`** (verified line-anchored, both trees).
- Build path holds exactly **30 EN + 30 AR** `approved: true` — matches the live corpus.
- `.fuse_hidden*` ghosts in the articles dirs re-checked: **gitignored** (existing rule, line 32) — no `git add -A` sweep risk.

## Frontmatter gates

- Australia dek **194** code points ✓ (≤200), title 26 ✓ — re-counted post-fix on the file as written.
- Hero still: **not on disk** — Designer's Ed04 batch (10), unchanged gate item.

## Observations

- NAPLAN 2026 platform outage (11 Mar, day one) surfaced during the negative-claim sweep — the draft's fenced claims survive; ACARA newsroom re-check stays a gate action per recon §6.
- Stale `/tmp/madar-build` from a prior session is permission-locked; today's build used a dated dir (`/tmp/madar-build-20260717`). Pattern: date the build dir, never reuse.

— Manager (QA function) · 2026-07-17

## Post-push addendum (afternoon)

- Founder pushed `f5170d5` at 12:26; git-level landing verified same hour (HEAD == origin, tree clean, all 31 files).
- **Observation (per #20):** founder reported the live page briefly unreachable shortly after the push; self-resolved within minutes. Most consistent with the deploy-swap window of run 45 (or residual GitHub REST-API flakiness from the 07-16 22:21–23:50 UTC incident). No Pages platform incident open on 07-17. Fetch provenance boundary + Chrome extension disconnect blocked direct run-45 verification this afternoon — **tomorrow's run must confirm run 45 green via the Actions API as its first act** and re-verify the live site serves `f5170d5` content. Not logged as a defect; logged so the next session doesn't plan off an unverified deploy.
