# QA pass — 2026-07-09
**Run by:** Manager (autonomous daily). **Headline: the P0 deploy outage is RESOLVED; the live site is current at 07-07.**

## Deploy / live state (the big change since Brief 28)
- **GitHub Actions run 38** (commit `26b9877`, the 07-07 push) — **build SUCCESS + deploy SUCCESS**, 2026-07-07 19:54Z. Run 37 (07-06) also green. The `deploy-pages@v4` failures that defined Brief 28 have cleared — the founder flipped Pages **Source → GitHub Actions** (Action 1) and the deploy went through. Ruling #17 closed by resolution.
- **Live sitemap** (`/sitemap-0.xml`, HTTP 200) serves the full corpus through 2026-07-07: **10 Ed03 pieces live in EN + AR**, plus Morocco (07-06) and Kenya (07-04). Held Ed04 pieces correctly **absent**. Last-stale marker (Brazil, 06-25) is gone.
- **Live render spot-check** — `/articles/2026-07-07-sudan-exam-continuity/` (HTTP 200): brand chrome intact (inlined bilingual wordmark renders), hero still present, sources list complete, related-reading filter working (Chad + Iraq both resolve), footer reads **"Edition 03 · 2026"**. AR alternate linked. Clean.

## Corpus integrity
- **Published (live) parity: 30 EN = 30 AR = 100%.** All 10 Ed03 pieces `approved: true` in both languages and `edition: 3`.
- **`editions.ts`:** Ed03 registered `status: 'current'` (July 2026, "throughline" motif, `edition-03.svg` cover); Ed02 closed. The 10 live pieces are correctly grouped — **no orphans**.
- **Held drafts:** 5 Ed04 EN (chile, india-parakh, nz, singapore, us-naep) + 1 AR (us-naep) at `approved: false` — do not build/list/sitemap (confirmed absent from live sitemap). Intended held state; second gate (Ed04 absent from `editions.ts`) also holds.
- **Dek caps:** all approved deks ≤ 200 code points (incl. tashkeel) — CLEAN. Even held drafts pass, so no build-breaker lurking (Chile trimmed 201→189 at compose).
- **Orphaned `related:` refs among approved pieces:** CLEAN. (Held us-naep → netherlands ref is graceful-filtered; noted for the Ed04 ship gate.)

## Changes staged today (no site-source touched)
- Paper trail + tooling only: Ed04 us-naep verification verdict, Ed04 status memo, Ed03 launch packet, guidebook entry + INDEX row, **`madar_stats.py` honesty fix** (panel now reports the live/approved corpus and discloses held drafts), this QA log, Brief 29, the untracked Ed03 gate log riding the push.
- **No `web/src` files changed → the green deploy (run 38) remains valid.** No publish gate required (no new content publishing; no `approved` flips).

## Open (not blocking)
- `content-drafts/staged-ed0304/` holds 15 redundant duplicate drafts — harmless clutter, dedupe when convenient (bash rm won't reliably persist through the FUSE boundary; do at a founder-side cleanup or via a tracked `git rm`).

**Verdict: site healthy and current; no defects; nothing to fix.**
