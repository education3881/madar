# Web Developer QA — 2026-07-06 (P0: the deploy pipeline has been failing since 2026-07-02 21:33)

**Result: P0 found, root-caused, fix verified in the working tree — heals on the founder's next push.**
**Live-site status: STALE at the 2026-06-25 deploy (11 days).** Kenya (shipped 07-04) is publicly 404. The sitemap integration (landed 07-02) never deployed.

## Symptom and the decisive markers

- Live `robots.txt` still advertises the legacy `/madar/sitemap.xml` (the 0707e0b push changed it to `sitemap-index.xml`).
- Live `/madar/sitemap-index.xml` → **404**; legacy `/madar/sitemap.xml` → 200 (the old static 41-URL file).
- Live Kenya EN/AR pages → **404**.
- `gh-pages` branch ruled out as Pages source (last commit 2026-05-26; June content deployed fine via the artifact flow).

## Root cause — proven by reproduction, both directions

The 07-02 push (`0707e0b`) added `@astrojs/sitemap` to `package.json` with a **lockfile edit that left `package-lock.json` out of sync** (at minimum the transitive `@types/ms` entry is absent). The Actions workflow runs `npm ci`, which **hard-fails on any lockfile desync**:

- `npm ci` against **origin's** lockfile (clean /tmp checkout, 07-04 evening): **EUSAGE — "package.json and package-lock.json are not in sync"** → the build job dies before astro runs → no artifact → no deploy. Every push since 07-02 21:33 (both 07-02 pushes) failed exactly here.
- `npm ci` against the **working tree's** lockfile (this morning): **exit 0, 449 packages** → `npm run build`: **exit 0, 43 pages, sitemap-index + sitemap-0 (43 URLs)** — the exact Actions sequence, green end-to-end.

The healing lockfile has been sitting in the working tree since the 07-04 morning run: the local `npm install` (run to restore the missing sandbox package) silently regenerated the sync (+6 lines, incl. `@types/ms`) — recorded that day as "benign transitive addition," which understated it: **it is the pipeline fix.**

## Corrections of record (honesty over tidiness)

1. **Brief 26 / qa-2026-07-04:** "package.json + lockfile verified intact, so the Actions production build was never at risk" — **wrong.** The files *individually* carried the dependency; their *sync* — the thing `npm ci` actually checks — was broken. The production pipeline was already failing when that sentence was written.
2. **Brief 25 (07-02):** "the deploy with the generated sitemap is already running" — the deploy *ran and failed*; the integration never reached Pages.
3. **Brief 26's morning "live = current" check** read `/madar/sitemap.xml` = 41 URLs as the integration's output; 41 was also the hand-patched file's count — an **ambiguous marker**. The decisive marker (`sitemap-index.xml`) was named in Brief 25's Tomorrow and not checked until the 07-04 evening session. Ruling #16 (filed today) makes the unambiguous-marker check the rule.

## Why three gates missed it

Compose-gate (07-02) verified the integration *built locally* via a route that ran `npm install` implicitly; ship-gate (07-04) verified `package.json`/lockfile *contents*, not sync; live-gate (07-04 morning) read an ambiguous marker as confirmation. Each check was individually reasonable; none of them was `npm ci` — **the only check that runs what the runner runs.** That is the durable lesson, now codified (ruling #16): *when dependencies change, the gate runs `npm ci` from the committed lockfile; live verification reads a marker only the new deploy can serve.*

## What the founder's next push fixes

One push carries: the healed lockfile (pipeline fix) + Kenya EN/AR + the 07-04 and 07-06 records. Expected sequence after push: Actions `npm ci` green → 43 pages → Pages serves Kenya + `sitemap-index.xml` (43 URLs) + corrected `robots.txt`. Next run verifies all three markers live.

## Standing QA (this run)

- Build (Actions-exact, /tmp checkout): **exit 0, 43 pages, parity 19=19**, sitemap 43 URLs, hreflang pairs present.
- Working tree unchanged since the 07-04 gate PASS except additive records; no content drift. Publish-gate verdicts for Kenya remain valid.
- Egress note: `api.github.com` unreachable from the sandbox (Actions run-history unverifiable from here); `github.io` reachable; `git fetch` flaky (TLS) but succeeded this run — origin confirmed at `4f26638`.

— Web Developer · 2026-07-06
