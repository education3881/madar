# QA — 2026-07-07 (Tuesday)

## P0 — deploy outage RE-DIAGNOSED and corrected (the headline)
Verified per ruling #16's own instruction ("verify the healing, not assume it"). The three markers still fail live: Kenya `/articles/2026-07-04-kenya-cbe-grade10-pathways/` **404**; `sitemap-index.xml` **404**; live `sitemap.xml` newest entry still **Brazil 06-25**. Origin is at `f217a0f` (the 07-06 push landed). So the site is stale **despite** the push.

Actions jobs API (run #37 = `f217a0f`) gives the true cause:
- **`build` job SUCCESS** — `npm ci` ✓, `npm run build` ✓, `upload-pages-artifact` ✓. **The 07-06 lockfile fix worked.**
- **`deploy` job FAILURE** — `actions/deploy-pages@v4` → "Deployment failed, try again later." Failed on **4 consecutive runs** (07-02 → 07-06). Last good deploy `f46d05b` (06-25).

**Correction of record:** Brief 27's "one push repairs the pipeline" is wrong — the build was already the healthy half; the **publish** step is the failure, and it is a **Pages configuration** problem (Source not set to "GitHub Actions", or a `github-pages` environment protection rule blocking `main`; a legacy `gh-pages` branch exists). **The founder's push cannot fix this — it needs a Settings change.** Full anatomy + reusable diagnostic in `agents/guidebook/2026-07-07-deploy-pages-is-its-own-failure-class.md` (ruling #17).

## Build triage — the founder-directed Ed03/Ed04 sprint batch
The morning parallel run left the full batch in `web/src/content/`. Build state, established in the Actions-exact `/tmp` environment (FUSE cache blocks in-place builds, as always):

- **Full working tree = BUILD FAILS.** First hard error: `2026-07-07-chile-simce-return` (Ed04) **dek > 200 chars** — a real schema defect. astro halts at the first bad entry, so more may sit behind it. This alone would have broken the deploy build had it been committed. Proof the un-gated batch was **not** verification-passed.
- **Morocco-close unit (committed content + Morocco only) = BUILD CLEAN.** 45 pages, `sitemap-index.xml` (45 URLs), Morocco EN + AR both render, hero still resolves.

## Parity
- **Shipping state: 20 EN = 20 AR (100%).** Morocco closes Edition 02 at 10.
- **Staged (held) batch:** Ed03 = 10 EN + 10 AR (parity intact); Ed04 = 5 EN + **1** AR (US only) — parity gap **by design** (edition incomplete, 5 of 10 drafted). Held in `content-drafts/staged-ed0304/`, out of the build.

## Action taken this run
Moved the 15 EN + 11 AR + 10 stills of the un-gated 07-07 batch to `content-drafts/staged-ed0304/` (reversible; manifest + restore command in `_EDITION_03_04_SPRINT.md`). Working tree now builds clean for the Ed02-close push; `git add -A` is safe again. Un-gated content is preserved on disk, not in the live build.

## Hand-off to the verification wave (before Ed03 can ship)
Independent audit still owed on all 10 Ed03 + 5 Ed04 pieces: figure-by-figure against compose notes/recon, route-2 gap-statement presence, dek caps (Chile confirmed over — fix at ≤200), AR named-human transliteration sign-off, sibling-spacing across the cluster, still-on-disk. **Editor verdicts remain Manager-held; none issued for Ed03/Ed04 this run.**

— Manager (Quality) · 2026-07-07
