# QA log — 2026-08-09

**Run:** daily autonomous · **Manager** · first run after the Edition-04 launch (post-publish verification day)

## State verification (Step 1, before any plan)
- `HEAD == origin/main == c6f2121` · working tree clean · no missed day (08-08 pushed: Ed04 launch + two founder VALENCE commits).
- **Deploy verified in the judging environment (#16/#17):** Actions runs **58, 59, 60 all green** (58 = Ed04 publish, 59–60 = founder's `/valence/` page). Live sitemap fetched: **81 URLs**; all 8 Ed04 slugs resolve **EN + AR** with reciprocal hreflang. "The measurement question" is live, verified in place.

## Build (tmpfs route-around: rsync → /tmp, symlink node_modules, VITE_CACHE_DIR)
- `astro sync` exit 0 · `astro build` → **81 pages, Complete!, exit 0**.
- dist parity: **38 EN / 38 AR**.
- **All 8 Ed04 hero stills resolve** in dist (swept every `/stills/*.svg` ref in built article HTML — zero missing).
- Wordmark inline (`<svg>` in homepage head), **zero** external `<img …wordmark>` refs.

## Flag state (inverted after the wave flip)
- `approved: false` count in build path: **0 EN / 0 AR** — the corpus is all-approved by design now; the held-leak sweep's assertion inverts from *zero-leak* to *all-approved*, and it passes.

## Founder surface audit — `/valence/` (2 commits, 08-08, 1,600 lines)
- "VALENCE — The Chemistry of Education", static page in `web/public/valence/` (build passthrough; outside sitemap/content collection by construction — unlisted, by design).
- **Privacy posture: intact.** 1 inline script, **zero external script src**, zero trackers (the single "plausible" hit is the English word in prose, not the analytics service). 308 `doi.org` citation links + PubMed/ERIC/OECD — an evidence page, consistent with house sourcing culture.
- No action taken; observation filed (it is the founder's surface).

## Housekeeping
- **`staged-ed0304/` cleanup attempted, FUSE-blocked** (`rm`/`git rm` → EPERM on every file — the standing sandbox boundary; route around, don't fight). Every staged file verified **mirrored in the build path** (35/35 filenames matched in `web/src/content/`), so deletion is safe and rides the founder's push block as `git rm -r content-drafts/staged-ed0304`.

## Verdict
Clean run, green across the board. Edition 04 confirmed live and whole in the judging environment; the corpus stands at 38/38, 100% parity, 35 countries; zero defects found; one housekeeping item routed to the push block.
