# QA log — 2026-08-10

**Run:** daily autonomous · **Manager**

## State verification (Step 1, before any plan)
- `HEAD == local == origin/main == c6f2121` · **but the working tree is NOT clean**: the entire 08-09 output (Brief 48, weekly review, Ed05 recon 1, guidebook row 15, QA log, RUNBOOK flag-sweep-inversion rule, INDEX consolidation, Issue 01 sweep note, stats snapshot) sits **uncommitted — the 08-09 push never happened.** Named plainly: a push gap, not a dark day (both 08-09 runs executed). No work lost; everything rides today's block, including the `git rm -r content-drafts/staged-ed0304` housekeeping line.
- **Live surface verified current:** deployed sitemap fetched — all 8 Ed04 slugs resolve EN + AR with reciprocal hreflang; live == origin == c6f2121. Nothing published is stale; only the *staging* layer (briefs, recon, guidebook) is behind.

## Build (tmpfs route-around: rsync → /tmp, symlink node_modules, VITE_CACHE_DIR)
- Boundary note for the ledger: `/tmp` files from prior sessions are owned by another uid — `rm`/`>>redirect` on them fails EPERM. **A failed redirect silently prevents the command from running**, and a stale same-named log then reads as a fresh result. Today's first "build" read yesterday's log; caught because dist was missing, re-run with session-unique paths. Rule of thumb banked: *unique log paths per run; a log you didn't watch being written is not evidence* (#16 family, applied to our own tooling).
- Real run: `astro sync` exit 0 · `astro build` → **81 pages, Complete!, exit 0**.
- dist parity: **38 EN / 38 AR** · sitemap-0.xml: **81 URLs**.
- **All hero stills resolve** in built HTML, EN + AR surfaces swept — zero missing.
- Wordmark inline (`<svg>` in homepage), **zero** external wordmark `<img>` refs.
- hreflang **triple** (en / ar / x-default) present in article heads · `og:image` absolute (07-16 fix holds).

## Flag state (post-flip mode — all-approved assertion, per the 08-09 RUNBOOK rule)
- `approved: false` count in build path: **0 EN / 0 AR** — passes.

## Housekeeping
- `staged-ed0304/` deletion still FUSE-blocked from the sandbox; `git rm -r` line carried forward in today's push block (35/35 files verified mirrored in build path on 08-09; unchanged since).

## Verdict
Green. Corpus 38/38, 100% parity, live surface current. One process finding (stale-log trap) banked; one gap named (08-09 push), remedied by today's block.
