# QA pass — 2026-06-24 (recovery-day run)

**Run by:** Manager (Web Developer lane) · **Build:** real `astro build`, tmpfs recipe (`VITE_CACHE_DIR=/tmp/vmad`, node_modules symlinked).

## Results — CLEAN

| Check | Result |
|---|---|
| **Build** | **exit 0**, **39 pages** built in 2.44s. No compiler errors, no EPERM (tmpfs route). |
| **Sitemap** | `dist/sitemap.xml` = **39 `<loc>`** — matches page count and the live `83f730e` commit (39 urls). |
| **EN/AR parity** | **17 EN = 17 AR**, identical slug sets, **zero** EN-only or AR-only gaps. AR routes built = 19 (17 articles + `/ar/` + `/ar/editions/`). |
| **Brand chrome** | Wordmark **inlined** (3 SVG/path refs on homepage; not `<img src>`). The single `img src…glyph` grep hit is the **Indonesia hero-still alt-text** (describes an ampersand-form glyph in the artwork) — hero stills are correctly external SVG assets; **not a chrome violation.** |
| **Orphan / placeholder scan** | No lorem-ipsum or `TODO:` in built pages. "placeholder" hits = the about page's **CSS class** (`.placeholder`, `.placeholder__lead`) + `::placeholder` pseudo-elements — intentional design, present since the about page was authored; not a regression. |

## Live-site verification — noted limitation

Local `HEAD` == `origin/main` == **`83f730e`** (confirmed via `git fetch`); GitHub Actions deploys on push to `main`, so the live site (`education3881.github.io/madar`) reflects `83f730e` (Oman piece 7, 17+17, 39 urls). **Direct HTTP fetch of the live site was blocked by the web_fetch provenance rule this run** (the URL must appear in a prior message/result to be fetchable). The deploy chain is confirmed deterministically through git rather than an HTTP read. No action needed; flagged for transparency.

## Verdict
Live product is clean and current. No Web Developer tickets opened. No content shipped today (recovery-day recon, no new pages), so no new-page gate items.
