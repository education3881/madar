# QA log — 2026-07-22

**Run:** daily · **Manager/Editor** · state verified before planning (git fetch + Actions API-eligible + live homepage fetch). HEAD == origin/main == `e3b3f57` (Brief 36, 07-21). Live site current (Edition 03, 10 pieces, homepage re-fetched clean). Working tree was clean at start.

## Build
- **Route-around build** (per sandbox-FUSE lock boundary): `/tmp` copy of `src` + `astro.config.mjs` + `public`, `node_modules` symlinked, `VITE_CACHE_DIR` in `/tmp`, `.fuse_hidden*` ghosts stripped from the build copy. One synchronous call (background/`/tmp` do not survive call boundaries this session).
- **Result: `astro build` exit 0 — 65 page(s) built.** No schema errors on the second pass; parity **30 EN = 30 AR** live.
- **P1 caught + fixed this run (compose-side, pre-gate):** the Netherlands draft's first dek was 250 code points; the collection schema caps dek at **200**. Build failed loud (`InvalidContentEntryDataError … dek: String must contain at most 200`). Rewrote dek to **188**; rebuild clean. Lesson reinforced: the dek cap is a build gate, not a style note — check length at compose (this is the #16 family working as intended — the build judged the thing).

## Flag sweep (the hold is the `approved` flag, not the brief — #18 family)
- **`.md` only (ghosts excluded):** EN `approved: true` **30** / `approved: false` **7**; AR `approved: true` **30** / `approved: false` **1**. Live corpus = **60** (30/30); held = **8** (7 EN + 1 AR).
- **New held piece:** `2026-07-07-netherlands-doorstroomtoets.md` (`approved: false`) — EN held count 6 → **7**. Builds **no page** and **no sitemap entry** (confirmed: `find dist -path "*netherlands*"` empty; 31 EN article dirs = 30 slugs + parent).
- **`.fuse_hidden` ghosts:** the unanchored `grep -rl` inflated the true-count to 84 by matching **gitignored** ghost files (`git check-ignore` → IGNORED; `git ls-files` shows none tracked). No sweep risk; `git add -A` will not stage them. Anchored `*.md` glob is the correct sweep surface — noted for the sweep rule.
- **Zero `true` flags outside the build path.** Ed04's seven held EN drafts + one held AR draft are the only `false` flags; everything else in `web/src/content` is the live corpus.

## Brand chrome / links
- Wordmark + glyphs inline on live (verified in the homepage fetch — the full bilingual SVG wordmark renders, no `<img src>` for brand chrome). Masthead, hreflang alternates (en/ar/x-default), canonical all present on live homepage head.
- **Internal-linking:** the Netherlands draft **resolves us-naep's forward `related:` ref** (`2026-07-07-netherlands-doorstroomtoets` now exists on disk); NL's own `related:` → us-naep + singapore-psle + oman all resolve inside the wave/live corpus. No orphan refs introduced.

## Sandbox notes (carried)
- `.git/index.lock` "Operation not permitted" surfaced again on a `git` write attempt — the recurring FUSE boundary; routed around, the founder's push block clears it (`rm -f .git/index.lock`).
- `.fuse_hidden` ghosts (gitignored) re-confirmed harmless; strip from `/tmp` build copies as standard.

## Verdict
- **P0: none. P1: one (dek length) — found and fixed same-run.** Build green, parity intact, held-draft leakage zero. Nothing for the founder's eye beyond the standing items (staged-ed0304 `git rm`, the domain call).

— Manager/Editor · 2026-07-22
