# QA log — 2026-08-16 (daily run)
**Mode:** post-flip (all-approved assertion) · staging/ops-only day, no corpus change · run by the Manager on the Web Developer's checklist

## State verification (Step 1, before any plan)
- `git branch` → `main` (local `gh-pages` present, untouched). `git fetch origin main` clean.
- **`HEAD == origin/main == 232319c`**, unchanged since **2026-08-11**. No `.git/index.lock`.
- **Three runs are behind the push, and two days are genuinely dark. Named plainly:**
  - **2026-08-12** — full run completed (Brief 51, recon 4 Sierra Leone, guidebook row 21, growth slice 2, qa log). **Never pushed.**
  - **2026-08-13** — full run completed (Brief 52, recon 5 Sudan, guidebook row 22, Africa cluster pre-wire, qa log). **Never pushed.**
  - **2026-08-14 (Friday)** — **no run, no brief.** Dark day.
  - **2026-08-15 (Saturday)** — **no run, no brief.** Dark day.
  - **2026-08-16 (today)** — this run. Block carries all three runs.
- Distinction held: 08-12/08-13 are **push gaps** (work exists on disk, safe, unpushed). 08-14/08-15 are **dark days** (no work produced). The first is a staging failure; the second is a missed day under the Charter's proof-of-work contract. Both are reported, neither is softened.
- Live surface: origin and the deployed site agree — Edition 04 serving, corpus unchanged. Nothing live is stale; the local tree is ahead, never behind.
- Brief numbering: 52 filed (unpushed) 08-13 → today is **Brief 53**. Numbering does not skip for dark days — no brief was written for 08-14/08-15 and none is backfilled.

## Build (route-around per the banked recipe; session-unique log path)
- Fresh `mktemp -d` build dir · `cp -RL` inputs · `node_modules` symlinked · `VITE_CACHE_DIR` on a fresh tmpfs dir · direct binary invoke (`node ./node_modules/astro/astro.js`) · session-unique log path (stale prior-session logs are the banked trap that fakes green).
- `astro sync` **exit 0** · `astro build` **exit 0 — 81 page(s) built, Complete!** (4.78s)

## Assertions (all pass)
| Assertion | Result |
|---|---|
| dist parity | **38 EN / 38 AR** |
| approved flags in build path | **zero `approved: false`** — all-approved holds (post-flip mode) |
| hero stills | **76 / 76** `hero.src` refs resolve on disk (38 EN + 38 AR against **38** stills in `web/public/stills`) |
| sitemap | **81 URLs** in built `sitemap-0.xml` |
| hreflang | triple on built homepage — `en` / `ar` / `x-default` (+ the ar nav link) |
| wordmark | inline SVG (`aria-label="Madār — مدار"`), **zero** `<img>` wordmark |
| orphaned `related:` refs | **zero** across EN + AR frontmatter (full sweep, both directions) |

## Defects filed
None.

## Notes
- **One checklist correction made this run, worth banking.** The first hero-resolution sweep keyed on `heroImage:` and returned "0 refs, 0 missing" — a *silent pass on an empty set*, which is the most dangerous shape a green check can take. The corpus key is a nested `hero:` block with a `src:` child. Re-run against the real key: 76/76 resolve. **Rule for the checklist: an assertion that finds zero items to check has failed, not passed** — every sweep must report its denominator, and a denominator of zero is a defect in the sweep, not a clean result. This is the #16 verify-in-the-judging-environment family applied to our own tooling.
- New content this run is **staging/ops only** — recon 6, guidebook row 23 / ruling #29 + INDEX addendum, the Africa head-terms surface audit, this log, Brief 53. Nothing enters the build path; the live corpus is unchanged.
- **Publish gate not invoked** — nothing is staged for publication. No Editor verdict, no AR pass, no approved flips required or made.
- Growth's head-terms audit specifies on-page requirements for the best-system piece; those are **requirements handed forward**, not corpus edits, and they ride Ed05 integration through the gate along with the 08-13 cluster wiring plan.

---

## Addendum — weekly-review pass, same day (2026-08-16, later session)

The weekly review ran after this daily log was filed and executed a **new** check that the daily checklist did not carry. Recorded here so the day's QA record is complete in one place.

### Site-chrome link sweep (first execution of the rule codified today)

Every internal `href`/`src` emitted by the layout and its components was enumerated **from the built HTML** across seven representative pages (EN + AR home, EN about, EN + AR editions, EN + AR article) and resolved against `dist`.

**First run — 2 defects, both pre-existing and both P1 by duration:**

| Defect | Age | Surface |
|---|---|---|
| Footer **RSS** link → `/madar/rss.xml` — no feed, no route, no dependency; never existed | since initial publish **2026-05-25** (83 days) | every page, **both languages** |
| Header + footer **About** → `/madar/ar/about/` — the Arabic About page has never existed | since the components were written | every page, **Arabic edition only** |

Neither had ever been caught, because 35 prior QA passes resolved only the links the *content* generates (`related:` refs, hero stills, hreflang pairs, article slugs) and none had enumerated the links the *layout* generates.

**Both fixed in this session:**
- **RSS** — real bilingual feeds built (`web/src/lib/feed.ts`, `web/src/pages/rss.xml.ts`, `web/src/pages/ar/rss.xml.ts`), dependency-free, plus `<link rel="alternate" type="application/rss+xml">` autodiscovery in every page head and the AR footer repointed to the AR feed.
- **AR About** — the Arabic chrome now resolves to `#about` on the Arabic home, where the approved Arabic About copy is already live (`id="about"` added). **This is an interim fix, and it is named as one:** the standalone Arabic About page is commissioned to the Arabic Editor, and the `/ar/about/` href is restored when it ships. The comment in both components says so, so the interim cannot silently harden.

### Re-verification after the fixes

- `astro sync` exit 0 · `astro build` **exit 0 — 81 page(s), Complete!** (page count unchanged; feed endpoints are not pages)
- Chrome sweep re-run: **98 targets enumerated, 0 unresolved**
- `dist/rss.xml` + `dist/ar/rss.xml`: well-formed RSS 2.0, **38 items each**, correct per-feed `<language>`, absolute `atom:link rel="self"`, unique permalink GUIDs, RFC-822 `pubDate`
- Autodiscovery present in built EN home, AR home and article HTML; `id="about"` present on the AR home; AR chrome emits `/madar/ar/#about`
- Regressions: dist parity **38 EN / 38 AR**, sitemap **81 URLs**, zero `approved: false` in the build path — all unchanged

### Note for the ledger

This is the second time in one day that a green check was found to be scoped too narrowly — the daily run's own `heroImage:` sweep returned "0 refs, 0 missing" on an empty set, and the chrome sweep had never enumerated its surface at all. The two findings are the same defect in different clothes and are recorded together in the weekly review, §2: **an assertion is only as good as the set it enumerates, and a sweep that reports no denominator is not evidence.**

### Push-gap reconciliation (required by the ≥3-day rule codified today)

The rule this review writes says: at ≥3 days unpushed, reconcile the working tree against the brief ledger and re-check origin **before** composing a push block, and write the reconciliation into the QA log. Run here, so the rule is satisfied on the day it is written rather than from next week.

**Ledger reconciliation — every artefact each unpushed brief claims, verified present and complete on disk:**

| Run | Artefacts claimed | On disk |
|---|---|---|
| 2026-08-12 (Brief 51) | recon 4 Sierra Leone, guidebook row 21, engagement-list slice 2, QA log | all present |
| 2026-08-13 (Brief 52) | recon 5 Sudan, guidebook row 22, Africa cluster pre-wire audit, QA log | all present |
| 2026-08-16 (Brief 53) | recon 6 best-system-in-Africa, ruling #29 + row 23, head-terms surface audit, QA log, Ed05 plan update | all present |
| 2026-08-16 (weekly) | review, ruling #30, INDEX consolidation, 2 RUNBOOK rules, Charter correction, feeds + autodiscovery, AR About fix, growth note, this log | all present |

**Origin re-check — and this is where the rule paid for itself.** `git log origin/main -1` returned **`47ac212`, 2026-08-16 22:00** — not `232319c`, which is what the state block at the top of this log recorded a few hours earlier. **Brief 53's push block had already been executed while the weekly review was mid-session** (28 files, 2,229 insertions).

Consequences, recorded plainly:

1. **The five-day push gap is closed** — 08-12, 08-13 and the 08-16 daily are all at origin.
2. **The Arabic About fix was swept mid-edit.** At `47ac212`, `SiteFooter.astro` carries the corrected AR *feed* href but the **dead** AR *About* href, and `SiteHeader.astro` did not land at all. The deploy now in flight therefore serves **working feeds and a still-broken Arabic About link**. Nothing incorrect is published and no work was lost — but a chrome fix is live in a half-applied state, which is precisely what the 2026-06-14 push-race rule exists to prevent.
3. **`INDEX.md` and `RUNBOOK.md` went to origin in their pre-reconciliation state**, so the guidebook at origin numbers the lineage ruling `#29` (colliding with the daily run's ranking ruling) and the RUNBOOK still reads "34 QA passes." Both corrected in the working tree; both ride the second push.
4. **Two push blocks existed.** Brief 53's fired; the weekly review's has been rewritten as a seven-file tail rather than a duplicate of work already at origin.

**The finding for the ledger.** The 2026-06-14 push-race rule anticipates a *founder* push landing mid-run and instructs a re-check of `origin` before composing the block. It does not anticipate a **sibling scheduled run** doing the same thing — and the state check as written is correct but insufficient, because in that case what moves first is not origin but the *working tree*, and the run cannot see the other run at all. No third rule is written this week: the ≥3-day reconciliation happens to catch it, as it did here. Flagged for 2026-08-23 — if this recurs, the fix is a run-marker file (a session claims the working tree while it is editing), not more prose.

**Verdict:** green on the product, amber on the process. Corpus and build unaffected throughout; one live defect (AR About) knowingly outstanding until the second push lands.
