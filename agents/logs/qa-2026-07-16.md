# QA log — 2026-07-16 (Web Developer)

**State at open:** HEAD == origin/main == `2c88013`; working tree clean (verified via temp-index route; live `.git/index.lock` unremovable across the FUSE boundary — routed around, no fights). Deploy run **43 success** (2026-07-15 17:31 UTC) for exactly this commit — live site current by the #16→#18 layer contract. Direct live-page fetch unavailable this run (fetch-tool provenance boundary — an environment constraint, logged as an observation per #20, not a site verdict); hreflang-on-live verified **by lineage**: the hreflang commit (`e5eecba`, 07-12) is an ancestor of the deployed HEAD, and today's build of the same tree emits the triples deterministically. Queue a direct live-page head-check for the next run where the fetch path allows.

## Build (route-around: /tmp copy + symlinked node_modules + VITE_CACHE_DIR)

- `npx astro build`: **exit 0, 65 pages** — twice this run (before and after the Base.astro fix).
- **Zero leakage:** new Ed04 draft (`2026-07-16-australia-naplan-reset`, `approved: false`) builds **no page**, appears in **no sitemap entry** (0 matches); the one grep hit for "Australia" in dist is prose in the Iraq/Mosul piece (UNDP partner mention — innocuous).
- **Parity:** dist articles **30 EN = 30 AR**. Sitemap-0.xml: **65 `<loc>`**.
- **hreflang:** EN/AR/x-default triple present in built article heads (Brazil spot-check).
- **robots.txt** points at `sitemap-index.xml`; legacy `/sitemap.xml` confirmed as the deliberate 07-02 stub (comment in file) — **not** a drift defect; finding withdrawn after reading the file.

## Defect found + fixed this run

**P2 — `og:image`/`twitter:image` were relative URLs** (`/madar/stills/…`). Social scrapers (OG/Twitter) require absolute URLs; every share card would have dropped its image. Fix in `src/layouts/Base.astro`: resolve `ogImage` against `Astro.site` (same pattern the file already used for hreflang alternates). Post-fix build verified: `https://education3881.github.io/madar/stills/…` on article pages and index; 65pp exit 0; no other head changes.

## Frontmatter gates at compose (upstream rule, 2026-06-24)

- Australia dek: **194 code points** ✓ (≤200); title 26 ✓. Counted on the file as written, not scratch (per the 07-04 refinement).

## Staging hygiene (flag-sweep rule, 3 surfaces)

- New draft mirrored to `content-drafts/staged-ed0304/articles/` with `approved: false` — staging and `web/src` agree; no latent `true` introduced (7th-instance prevention, not detection).

— Web Developer · 2026-07-16
