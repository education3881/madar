# QA log — 2026-08-12 (daily run)
**Mode:** post-flip (all-approved assertion) · staging-only day, no corpus change · run by the Manager on the Web Developer's checklist

## State verification (Step 1, before any plan)
- `HEAD == origin/main == 232319c` · tree carried one pre-existing working-tree edit (`_EDITION_05_PLAN.md` scope-confirmation line — rides today's block) · no `.git/index.lock`
- Live surface fetched and verified: homepage serves **Edition 04**, hreflang triple present (en / ar / x-default), inline wordmark present, footer current
- No missed day: Brief 50 filed 08-11, today is Brief 51

## Build (route-around per the banked recipe; session-unique log path)
- Fresh `mktemp -d` build dir · `cp -RL` inputs · `node_modules` symlinked · `.fuse_hidden*` swept · `VITE_CACHE_DIR` on fresh tmpfs · direct binary invoke
- **`astro build` exit 0 — 81 page(s) built, Complete!** · log: `build-1786511457-2.log` (session-unique)

## Assertions (all pass)
| Assertion | Result |
|---|---|
| dist parity | **38 EN / 38 AR** |
| approved flags in build path | **zero `approved: false`** (only non-article `.gitkeep`s lack the flag) — all-approved holds |
| heroes | all **38** stills resolve on disk (`web/public/stills/`) |
| sitemap | **81 URLs** in built sitemap; sierra-leone slugs present |
| hreflang | triple (en/ar/x-default) on built homepage |
| wordmark | inline SVG (`aria-label="Madār — مدار"`), zero `<img>` wordmark |

## Defects filed
None.

## Notes
- New content this run is **staging/ops only** (recon 4, guidebook row 21, growth slice 2, this log, Brief 51) — nothing enters the build path; live corpus unchanged.
- TMPDIR on this session resolves to `/sessions/…/tmp` (not `/tmp`) — the recipe's `mktemp -d` handles it transparently; noted so future sweep commands look in `$TMPDIR` first.
