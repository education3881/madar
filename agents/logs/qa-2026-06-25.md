# Web Developer QA — 2026-06-25 (Brazil ship, sprint piece 8 of 10)

**Result: PASS — clean.** Real `astro build` run this session (not a structural proxy).

## Build (real, exit 0)
- Route: tmpfs recipe per `guidebook/2026-06-15-astro-build-in-sandbox-vite-cache-route.md`. **One environment fix worth banking:** `cp -a` now fails writing into `/tmp` ("preserving times: Operation not permitted") and a stale `/tmp/mbuild` from a prior session was un-clearable — switched to `mktemp -d` for a fresh build dir and `cp -RL` (no archive/preserve) for the inputs; `node_modules` symlinked; ran `./node_modules/.bin/astro build` directly (avoids `npx` trying to install astro@7). Clean.
- **Exit 0. 41 page(s) built in 2.21s.** Both Brazil pages generated:
  - `dist/articles/2026-06-25-brazil-crianca-alfabetizada/index.html`
  - `dist/ar/articles/2026-06-25-brazil-crianca-alfabetizada/index.html`
- Schema validation passed with no fixes needed at the gate — because the **dek cap was enforced at compose** (codified rule): EN dek **196**, AR dek **193** (≤200, tashkeel counted). The cap did **not** bite this ship — the first sprint piece where it was clean at the gate rather than caught at it.

## Parity & counts
- **EN 18 = AR 18 = stills 18.** Identical slug sets; Brazil present on both sides + its still on disk.
- **Sitemap = 41 URLs** (hand-patched: Brazil EN + AR added with hreflang en/ar/x-default; `lastmod` 2026-06-25). XML re-validated **well-formed** (minidom parse OK).

## Brand chrome & links
- **Brand chrome clean.** Brazil article page: exactly **1 inline `<svg>`** (the masthead wordmark) and **1 `<img>`** (the hero still). Grep for any `<img … wordmark …>` → **NONE**. The "never ship brand via `<img src>`" rule holds.
- **Hero still** referenced correctly in both languages: `src="/madar/stills/2026-06-25-brazil-crianca-alfabetizada.svg"` (correct `/madar/` base; file on disk in this commit — no 404 risk, the push-race invariant respected: asset written immediately after the article text).
- **Related rails resolve:** Brazil → `2026-05-31-uruguay-eduia-lab-ceibal` + `2026-06-18-egypt-npda-arabic-literacy`, both present and linked in the built HTML (EN). Graceful-degrade unaffected.
- **AR renders RTL** (`dir="rtl"` present).

## Carried / not-blocking
- **`@astrojs/sitemap` integration still pending** (the operation-controlled half of the domain cutover; flagged since the 06-04 weekly, now a **7th** hand-patch). Today is a **ship day** (Brazil, per the 06-24 reset), not a pure buffer day, so the proven hand-patch was used rather than risking an `npm install @astrojs/sitemap` against the FUSE unlink ban on a ship day. **Recommend the 06-26 buffer day land the integration** (clean Web-Dev checkout or the tmpfs route with a real install), retiring the hand-patch before Search Console/domain bind. Until then the publish gate keeps re-validating the static sitemap against parity (done above).
- Live-site fetch: sandbox can't reach `education3881.github.io` this run; "live = current" is inferred from `HEAD == origin/main` (verified 9f8ebc0). Flagged, not hidden — same honest caveat as prior runs.

— Web Developer · 2026-06-25
