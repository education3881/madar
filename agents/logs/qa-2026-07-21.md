# QA log — 2026-07-21

**State at open:** HEAD == origin/main == `f5170d5`; working tree carried one uncommitted file (the 07-17 QA log's own post-push addendum — folded into today's commit). **Dark days named: 07-18, 07-19, 07-20 — including the 07-19 Sunday weekly, missed.** Three days, no runs; recovery-run rule applies (one clean day, no double-batch; the delayed weekly is owed and noted for the next weekly slot).

## Carried-forward action from the 07-17 addendum — CLOSED first, as ordered
- **Run 45 confirmed green via the Actions API:** `completed / success`, head_sha `f5170d5`, created 2026-07-17 08:26 UTC — the exact commit of the push. Runs 38–45 now all green.
- **Live site re-verified by direct fetch:** homepage serves Edition 03 current; the founder's brief unreachability on 07-17 afternoon is consistent with the deploy-swap window, closed as a non-defect.

## Build (route-around: dated /tmp dir + node_modules symlink + VITE_CACHE_DIR; note — /tmp does NOT persist across sandbox calls this session; the whole pipeline must run in one call)
- `npx astro build`: **exit 0, 65 pages** (post-NZ-re-cut tree).
- **Parity:** dist articles **30 EN = 30 AR**.
- **Zero leakage:** NZ piece (held `approved: false`, edited today) builds no page, no sitemap entry; all six Ed04 drafts stay out of the build.

## Flag sweep (3 surfaces)
- Build path: exactly **60** `approved: true` (30 EN + 30 AR) — matches the live corpus.
- `content-drafts/` (incl. `staged-ed0304/`): **zero** true flags. The 07-17 defusal holds.
- **24 `.fuse_hidden*` ghosts** in the articles dirs carry stale frontmatter — all **gitignored** (existing rule), no `git add -A` sweep risk, and the build ignores them (page count proves it). Watch item only.

## Verification (Verifier's fourth verdict — scoped re-check; see `verdicts/2026-07-21-ed04-nz-ncea-scoped-recheck.md`)
- **NZ CLEARED.** Item 1 (recount clause) confirmed in place; item 2 re-cut on the actual Event 1 2026 figures (NZQA canonical tables, fetched live today: reading 60.5 / writing 56.8 / numeracy 51.7). Applied to `web/src` **and** staging; ledger reconciled. **Ed04 = 6 drafted / 6 verified — drafted-slate verification complete.**

## Live-site checks (judging environment)
- **og:image / twitter:image absolute on a live article page** (Sudan piece fetched directly) — the 07-16 Base.astro fix now verified on live, not just lineage. Share cards will render. **P2 closed in the judging environment.**
- **hreflang triple + canonical verified live at article level** (en / ar / x-default, correct per-article URLs) — extends the 07-17 homepage-only verification.
- The fetch provenance boundary that blocked article-level checks on 07-17 did not bind today (observation per #20 — the boundary is intermittent, keep re-attempting rather than caching the failure).

## Sandbox notes
- Recurring `.git/index.lock` unlink warning surfaced on `git status` — routed around (read operations unaffected); the push block carries `rm -f .git/index.lock` as always.
- Background (`nohup … &`) processes do not survive bash-call boundaries in this session, and neither did `/tmp` contents between calls — the build route-around must be a single synchronous call. Banked here for future runs.

— Manager (QA function) · 2026-07-21
