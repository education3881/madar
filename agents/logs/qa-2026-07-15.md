# QA — 2026-07-15 (Web Developer pass, daily run)

**Context:** 07-14 was a dark day (no run; the founder's 07-14 07:48 push carried the 07-13 run's work). State verified before planning per the standing rule: local HEAD = origin/main = `bae7ee1`; working tree clean at session start; live site re-fetched and current (Edition 03 masthead, hreflang triple in head, Chad lead resolving).

## Build (route-around: mktemp -d + cp -RL + symlinked node_modules + VITE_CACHE_DIR)
- `astro build`: **exit 0, 65 pages, 2.23s** — matches the last known-good page count.
- **Ed04 leakage: zero.** No chile/naep/parakh/psle/ncea slugs in `dist/articles/` — all five drafted Ed04 pieces held correctly by `approved: false` + registry absence (the two-gate hold).
- **Parity: 30 EN = 30 AR** article pages in dist; corpus flags 30/30 `approved: true` both trees.
- **Sitemap: 65 URLs**; sitemap-index created.
- **hreflang** head-pairs present on homepage (en / ar / x-default), consistent with the 07-12 implementation.

## Flag hygiene (#18 family)
- **6th latent-flag instance found and defused — new location class: `content-drafts/parked/`.** The Jordan double-shift and Colombia Escuela Nueva parked drafts (both 2026-05-25, pre-dating flag discipline) carried `approved: true`. Outside the build path — no leakage risk today — but the flag-is-the-hold invariant should hold everywhere a frontmatter flag exists: both set `false` with a dated comment. The sweep rule now names three surfaces: `web/src/content/{articles,articles-ar}/`, `content-drafts/staged-ed0304/` (pending the founder's tracked `git rm`), and `content-drafts/parked/`.
- Ed04 five: `approved: false` on disk, all confirmed during the Chile verification (verdict §16).

## Content change verified in place
- Chile one-clause fix (verdict item 1, ruling #22) applied and re-read on disk: "neither fall statistically significant, by the agency's own reading" — present at line 46; no other content changes this run. The fixed file builds (it is excluded from output by its flag, as designed; the build exercised frontmatter parsing).

## Brand chrome / links
- Wordmark inline SVG confirmed in the live homepage fetch (v0.3 extracted-outline version, no `<img src>` chrome). Live lead + secondary stills resolving. No orphaned refs introduced today (Chile `related:` both live — verdict §14).

**P0/P1:** none. The deploy pipeline is not exercised today (no push yet); per #16/#17, "live" claims wait for the founder's push + Actions run + Pages deploy, verified next run.

— Web Developer · 2026-07-15
