# QA log — 2026-09-04 (Web Developer)

**Build:** route-around per standing recipe (rsync repo → /tmp/madar-run-0904
excluding the FUSE phantoms, node_modules symlinked, session-unique
VITE_CACHE_DIR + log paths, .git rsynced so the lastmod resolver sees a real
work tree at `642fef2`; rsync's one complaint — `.git/index` failed
verification — is the stale-lock boundary and did not affect the resolver:
82/82 lastmod). `CI=true astro build` → **exit 0, 81 routed pages (+VALENCE =
82 served)**. Run once; nothing in today's block changes a built page.

## State (named plainly)
- **Local = origin = `642fef2`** (the 09-01 block, pushed 2026-09-02). The
  **09-03 block is staged and unrun — one day**: 8 new files + 7 modified,
  nothing lost, nothing contaminated. Origin serves the 09-02 build (sitemap
  lastmod 09-02 09:39Z; home `last-modified` 09-02 09:40Z); the home's
  canonical on the origin is still `…/madar` (the redirect) until the block
  runs. Per the 08-16 rule the push block moves to the top of today's brief
  with the day count. Today's run started 09:32 +04 — on time.
- `.git/index.lock` from 09-03 22:15 is present and not removable from the
  sandbox (FUSE boundary, standing); the push block's `rm -f` handles it.
- `qa_live_drift`: **DRIFT(1) = `index.html` canonical**, origin `…/madar`,
  local `…/madar/` — exactly the staged fix, expected; clean on the next push.

## Assertions (all against today's /tmp build)
| Check | Result |
|---|---|
| astro build (CI=true, real work tree) | exit 0 · 81 routed + valence |
| held-leak (zambia, sleic, sudan-cant-wait) | **0 page / 0 sitemap hits** each — 3 held EN + 3 held AR; on-disk hits are assets only (still + og card per slug) |
| dist parity | 38 EN / 38 AR |
| sitemap | 82 loc / 82 lastmod |
| feeds | EN 38 items · AR 38 items |
| og:image ending .svg | 0 |
| qa_consumer_surface | exit 0 CLEAN |
| qa_a11y_lang | exit 0 PASS |
| qa_ar_language | exit 0 CLEAN |
| qa_reachability | exit 0 PASS · every page reachable from its own front door |
| qa_jsonld | exit 0 CLEAN · every promise dereferences |
| qa_robots | exit 0 PASS |
| **qa_hreflang_clusters (allowlist now asserted)** | **exit 0 PASS** — 82 URLs · 40 clusters · 2 no-alternate pages, both allowlisted |
| qa_live_drift (origin vs dist) | DRIFT(1) = the staged canonical fix, expected |

## The 09-03 forward question — second half, answered
The 09-03 log noted that `qa_hreflang_clusters` *named* the two no-alternate
pages (About, VALENCE) but did not *assert* them: a third would change the
output and not the exit code — the 08-16 silent-pass trap in a new coat.
**Fixed:** the script now carries `NO_ALTERNATE_ALLOWED = {"about/", "valence/"}`
(About's Arabic lives at `/ar/#about` since the 08-18 nav fix; VALENCE is a
standalone English instrument) and fails **both ways** — a no-alternate page
outside the list (an EN page shipped without its AR twin, or a one-sided
held-slug leak), and a listed page that now emits alternates or is not built
(a stale allowlist). The instruction in the defect text is the orphan-sweep
rule's: give it a twin or edit the list *in the same commit*.
**Proved per #35:** control (real build) silent; bite 3 — About duplicated as
`about-2/` and added to a sitemap copy → **FAIL(1)** naming it; bite 4 — About
removed from the list while still no-alternate → **FAIL(1)** naming it; bite 5
— a `ghost/` entry added to the list → **FAIL(1)** "stale allowlist entry".
No workflow change: the CI build step already runs the script, so the
assertion is live on the next push. (The push still needs the workflow-scope
PAT — the *09-03* commit in the same block touches `astro-pages.yml`.)

## The 09-03 forward question — first half, disposed
The dated `site:` observation was recorded by Growth today
(`agents/growth/2026-09-04-…`), as an observation for the weekly, never a
figure. QA does not own it and will not assert it.

## Forward question (required per the 08-23 ruling)
Ten checks now read `dist` or the origin; every one of them enumerates *URLs
and their metadata*. **None reads the body of an article for what it
promises the reader**: a `related:` slug resolves (checked), but nothing
asserts that a piece's in-body cross-reference to another Madār piece
("this publication covered … when Sudan flew its examinations") actually
links to it, or that a piece whose frontmatter claims `contains_composites:
false` contains no unattributed composite figure. The first is mechanical and
cheap — an in-body mention of a live slug's subject with no `<a>` to it is a
reader-facing broken promise of the softest kind; candidate `qa_body_links`:
for every article, every `[text](/…)` internal link dereferences to dist,
and every `related:` slug is linked at least once from the body *or* the
rail (the rail is generated; the assertion is that the rail exists on the
page). Test it before adding anything new. Second, carried: the two
no-alternate pages are the only English pages without an Arabic twin — About
has been one-sided since 05-25; that is an editorial call, not a QA fix, and
it is now visible in a list someone must edit to change.

— Web Developer · 2026-09-04

---

## Editor — pair verdict summary (full verdict in `content-drafts/verdicts/2026-09-04-ed05-sudan-pair-verdict.md`)
Sudan PAIR **PASS, all five — BANKED.** Numeral multiset identical across EN/AR
(zero one-sided figures, zero count drift); prohibited figures absent from
both by grep; lineage gate, announced-vs-reached (three forms), the trial's
four registers, and the spine (4/4 positions) all survive the crossing; caps
EN 52/192, AR 42/178. Edition 05 = three complete pairs, all held
`approved:false` for the wave gate. Slot #3 re-verified and **COMMISSIONED**
(CC II) — see the recon and commission files; eight draft-blockers, no draft
date set.

---

## Publish gate — run in writing before staging today's push (Manager)

1. **Editorial approvals:** the push flips **no** `approved:` flag. Today adds
   the Sudan pair verdict, the slot-3 re-verification + commission, ruling
   #43 / row 38 + INDEX addendum, the `qa_hreflang_clusters` allowlist
   assertion, the Growth note, this log, the Manager status, Brief 66 —
   **plus the whole 09-03 block** (Sudan AR + AR gate + og card, ruling #42,
   assertion #9 + CI wiring, the Base.astro canonical fix, Brief 65). Zero
   held-leak proved in today's dist (3 EN + 3 AR held). PASS (nothing ships
   editorially).
2. **Arabic gate:** Sudan AR verdict PASS on file (09-03); pair verdict PASS
   on file (today); both files held. PASS.
3. **Stills / assets on disk:** Sudan still (09-01) + og card (09-03); Zambia
   and Sierra Leone stills + cards (08-25/08-28/08-30). Nothing new today
   needs an asset. PASS.
4. **Clean build + QA:** CI=true build exit 0; all nine standing assertions
   clean with the allowlist assertion proved both ways; parity 38/38; drift =
   the one staged field. **Reader-visible change in this push: one** (from
   09-03) — the English home's canonical and og:url gain their trailing
   slash. PASS.

**Gate verdict: a two-day block (09-03 + 09-04) — one held AR composition,
one head-level fix to the live home, two assertions (one new, one hardened),
two rulings, one commission, two briefs. Cleared for the founder's one
block; workflow-scope PAT note stands (the 09-03 commit touches
`.github/workflows/astro-pages.yml`).**
