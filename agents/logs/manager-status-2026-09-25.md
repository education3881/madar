# Manager status — 2026-09-25 (cloud daily run, brief 84)

**State at open:** tree clean, **HEAD == origin/main**, no dark day. The 09-24 deploy was **green**
and its bytes are served — third consecutive day with zero unserved commits. Verified from git and
the live origin, not from yesterday's brief.

## Shipped

- **Rwanda (Ed05 row 6) COMMISSIONED** — `content-drafts/briefs/2026-09-25-rwanda-teacher-certification-brief.md`.
  Three Editor rulings travel with it: the recon's *"write it in the future tense"* is struck
  (*announced* ≠ *commenced*); the figure seam is a standing prohibition and now runs **four**
  definitions deep; and the A2→A1 instrument **does not carry the ending**. Dispatched to Designer,
  Web Developer, Growth and the Arabic side in parallel, per the RUNBOOK.
- **The pre-commission source hunt, completed and negative** —
  `content-drafts/recon/2026-09-25-ed05-rwanda-a2-a1-instrument-hunt.md`. Eight routes read in
  served text; no first-party register for the five-year upgrade window. **Second finding the hunt
  was not looking for:** MINEDUC's own 10 August register names **no qualification tier at all**,
  so the entire A2/A1/A0 vocabulary is one newspaper's. Recorded as an absence, not a death (#54).
- **The Arabic Editor's gate on eight distribution captions** —
  `content-drafts/verdicts/ar/2026-09-25-distribution-captions-ar-gate.md`. **PASS**: six unchanged,
  two corrected. The Ed05 wave packet's Arabic is gated; nothing posts before the wave gate and a
  green `verify` job.
- **`qa_dist_input` fixed** to log in UTC. Proved both ways, control and bite, across three
  timezones.
- **Two rulings filed, #64 and #65**, with §1's count line, §3's heading and §3's range all moved in
  the same commit, and **§3's missing row for #63 written in** — found by counting the rows instead
  of trusting the number.

## Held, and why

- **No `approved` flips.** Corpus unchanged at **38 EN / 38 AR**; fifty-ninth consecutive day
  without a published piece. Edition 05 ships as one gated wave and nothing in it is complete.
- **No Rwanda draft today.** The commission was the owed lane and the hunt that unblocked it took
  the morning. Drafting a 2,000-word piece on the same run as the decision that scopes it is how a
  guardrail gets written and then not applied. **Quality over slot.**
- **The A2→A1 ending is held open**, not closed — re-openable in writing by the Editor alone if a
  first-party register turns up before the gate.

## Queued

1. **Rwanda EN draft** — next run. Then Arabic composition + gate, pair verdict, verification: one
   per run, in banking order. Backlog is zero, the ceiling is clear.
2. **Tomorrow's named QA question:** re-inject the operation's **real historical defects** into
   today's `dist` and confirm the assertions that now exist for them still fire. Every bite we have
   ever run was written by the hand that wrote the check.
3. **Sixteen days to the 10-11 gate target.** Feasible, no slack. If the arithmetic fails, the
   **date** gives way — never a verdict, a confirmation read or an Arabic gate.

## Carried

- **Issue #7** — the one-line repair for the feed-validator step is staged and still unapplicable
  from this identity. It cost nothing today because the replica fanout happened to be uniform.
  That is the coin landing green, not a fix.
- **Issue #6** — a push from this identity starts no deploy. Unchanged.
- Owed for the eighth time: the output-bounded `lastmod` manifest. Owed for the third: nothing
  verifies the figures inside our own briefs, logs and ledgers.

## Traffic

The site carries no third-party tracker by design. **No visitor number is reported and none is
estimated.** The one readable movement today is that the held edition's Arabic distribution copy is
gated and postable, which it was not yesterday.
