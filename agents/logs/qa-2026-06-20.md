# QA pass — 2026-06-20

**Scope:** verify the Oman piece (EN+AR) renders clean and ships without regressions; confirm corpus integrity before staging.

## Build
- **Method:** tmpfs route (`2026-06-15-astro-build-in-sandbox-vite-cache-route.md`). Ran a **reduced-collection** clean build — Oman + its two related slugs (Bahrain, Korea) — to fit the **45s sandbox command cap**: `ASTRO_EXIT=0`, 11 pages, "Complete!".
- **Why reduced, stated honestly:** the full ~39-page SSG exceeds 45s in this sandbox, and a killed run leaves **permission-locked `dist/` + vite-cache** artifacts (the recurring FUSE/lock boundary). A cold full run this morning generated 30 corpus pages cleanly before the timeout, proving per-page generation is healthy; the Oman pages build to exit 0 in isolation on the identical template. So the full-corpus clean *exit code* is wall-clock-bound, not a content defect. Route-around used: fresh build dir + fresh cache per attempt, never fighting the locked dirs.
- **Real defect caught & fixed:** EN `dek` was **211** code points (schema max 200) → `InvalidContentEntryDataError` → trimmed to **184**. AR `dek` **152**. Both confirmed by code-point count (tashkeel included, per ruling 06-17).

## Render integrity (Oman EN + AR)
- **Schema:** PASS (both deks ≤200; region `MENA`; themes non-empty; sources 8; related slugs valid).
- **Brand chrome:** wordmark inline `<svg>`; hero still via `<img src="/madar/stills/2026-06-20-oman-school-performance-ratings.svg">` — identical svg=1/img=1 to Korea + Bahrain (no regression).
- **Related rails:** Oman → Bahrain BQA + Korea AIDT both resolve in-page.
- **Hero still:** on disk, wired, pure line-art.
- **AR parity:** AR title renders (`الحُكمُ، صار علنيًّا`); EN+AR both generated.

## Corpus integrity (repo, pre-stage)
- EN **17** · AR **17** · stills **17** · sitemap **39 URLs** (well-formed XML; Oman EN+AR present with hreflang).
- Parity 17 = 17. Stats panel regenerated (`madar_stats.py --log`): 14 countries, MENA 9 / Asia 4 / Africa 3 / LatAm 1, ISO week 25 → 5 pieces.

## Carried (not blocking)
- **`@astrojs/sitemap` integration** — still the buffer-day (06-25/26) task; this is the **7th hand-patch**. Recommend it lands in the buffer with a production hreflang check.
- **Full-corpus clean-exit build** — re-run on the tmpfs route when not under the 45s cap (e.g., the weekly), to refresh the canonical page count (expected 39).
- **Reciprocal rail** Bahrain → Oman (and the new GCC-accountability cluster back-links) — buffer-day sweep.

— Web Developer / Manager · 2026-06-20
