# QA log — 2026-09-03 (Web Developer)

**Build:** route-around per standing recipe (rsync repo → /tmp/madar-run-0903
excluding the FUSE phantoms, node_modules symlinked, session-unique
VITE_CACHE_DIR + log paths, .git rsynced so the lastmod resolver sees a real
work tree at `642fef2`). `CI=true astro build` → **exit 0, 81 routed pages
(+VALENCE = 82 served)** — run twice today: once before and once after the
Base.astro canonical fix below.

## State (named plainly)
- **Local = origin = `642fef2`** (the 09-01 block, pushed 2026-09-02 13:39
  +04). Actions run 69 on that SHA: **conclusion success** — a run-level
  success means all three jobs passed, so the `verify` job's second live run
  (its first with the feed-cache step) and the robots gate's first CI run
  are both green. (Job-level detail not fetched — the API's per-job URL sat
  outside this sandbox's provenance set; the run-level conclusion is the
  evidence.) Origin serves the 09-02 build (sitemap lastmod 09-02 09:39Z).
- **2026-09-02 was a dark day — no run, no brief.** Named plainly; today is
  the recovery run in the fixed shape (state from git + origin, not from the
  brief; one piece of content advanced, not two; the gap stated).
- **Today's run is late** — 21:55 +04, not 08:00. A closed app delays the
  run to the next launch rather than dropping it; the day's five functions
  are delivered in full, but a founder reading at breakfast found nothing.
- `qa_live_drift` before the fix: **CLEAN — 82 URLs, 0 lastmod drift, 4/4
  sampled heads identical** (third consecutive clean read). After the fix:
  **DRIFT(1), exactly the field we changed** — `index.html` canonical, origin
  `…/madar`, local `…/madar/`. That is the tool biting on a fix that is ahead
  of the origin, which is its job; it goes clean again on the next push.

## Assertions (all against today's /tmp build, post-fix)
| Check | Result |
|---|---|
| astro build (CI=true, real work tree) | exit 0 · 81 routed + valence |
| held-leak (zambia, sleic, sudan-cant-wait) | **0 page/sitemap hits** — 3 held EN + **3 held AR** (Sudan AR composed today); on-disk hits are assets only (3 og cards + 3 stills, Sudan's card generated today) |
| dist parity | 38 EN / 38 AR |
| qa_consumer_surface | exit 0 CLEAN |
| qa_a11y_lang | exit 0 PASS · 2993 nodes · 0 defects |
| qa_ar_language | exit 0 CLEAN · 235 fields |
| qa_reachability | exit 0 PASS · 82/82 · 0 orphans |
| qa_jsonld | exit 0 CLEAN · 152 nodes · 608 promises dereferenced |
| qa_robots | exit 0 PASS |
| **qa_hreflang_clusters (NEW, assertion #9)** | **exit 0 PASS** — 82 sitemap URLs · 40 clusters · 2 named no-alternate pages (about, valence) |
| qa_live_drift (origin vs dist) | DRIFT(1) = the canonical fix, expected; CLEAN pre-fix |
| sitemap | 82 loc / 82 lastmod |
| feeds | EN 38 items · AR 38 items |

## The 09-01 forward question — answered, and it bit on first contact
**`qa_hreflang_clusters.py`** (agents/tools), standing assertion #9: every
page-level cluster names its own canonical and dereferences to dist (#37);
every cluster is reciprocal; every sitemap URL sits in exactly one cluster or
is a *named* no-alternate page; no cluster names a URL the sitemap omits; and
the sitemap's own `xhtml:link` alternates equal the page-level cluster (#36
across two files). **Proved per #35:** control = today's real build — and the
control FAILED(4), all four defects one root cause: **the English home's
canonical and og:url were `https://education3881.github.io/madar` (no
trailing slash) while its own hreflang cluster, the Arabic home's cluster and
the sitemap entry all say `…/madar/`.** `Astro.url.pathname` for the root
route under `base` is `/madar`; every other route ends in a slash. GitHub
Pages 301s `/madar` → `/madar/`, so the front door's canonical has pointed at
a redirect since the first publish on 2026-05-25 — 101 days, invisible to
every sweep because none compared a page's canonical with its cluster.
**Fixed in `Base.astro`:** the canonical path is derived from the served
shape (directory routes always end in a slash), with the reason in the
source comment. Post-fix control silent; bite 1 (one AR page's `hreflang=en`
retargeted) → FAIL(10) naming the double-membership and the sitemap
disagreement; bite 2 (a held Sudan slug's cluster injected into About) →
FAIL(8) naming the URL absent from the sitemap. Wired into the CI build job
after the robots gate; YAML re-parsed clean (3 jobs, 8 build steps).

**⚠ Push-block note: this commit touches `.github/workflows/astro-pages.yml`
again — the push needs the PAT with `workflow` scope (the 09-02 push had it).**

## Forward question (required per the 08-23 ruling)
Nine assertions now read `dist`; one reads the origin. Nothing yet reads
**what a crawler has actually indexed** — with Search Console still unbound
(domain decision open), the only external signal available is a `site:`
query, which is a search summary and not a served register (#41). Candidate
for the next run: not a new assertion but a *record* — a dated `site:` count
in the weekly review as an observation, never a figure, so that when the
domain binds, the before-picture exists. Second, smaller: `qa_hreflang_clusters`
treats the two no-alternate pages as an allowlist by naming them; the day a
third appears, the output changes but the exit code does not — decide whether
that list should be asserted, not just printed.

— Web Developer · 2026-09-03

---

## Arabic Editor — gate summary (full verdict in `content-drafts/verdicts/2026-09-03-sudan-cant-wait-to-learn-ar-gate.md`)
Sudan composed in Arabic (~1,570 words at register), **Gate 4 PASS**, held
`approved:false`. Twelve named humans: three CONFIRMED from the owner's served
Arabic editions (فاطمة عباس طاهر، طاهر أنور علي، جابر منصور أدوما), nine by
convention, none PROVISIONAL. All four commission flags disposed (Makanna →
«مكانّا» via a UN Arabic register; Addise → بيلاي أديسي confirmed in writing;
CWTL → the owner's own «التعليم لا ينتظر»). Dek 178/200 with tashkeel, title
42/100, counted at compose. Pair verdict tomorrow.

---

## Publish gate — run in writing before staging today's push (Manager)

1. **Editorial approvals:** the push flips **no** `approved:` flag. It adds
   the held Sudan AR composition + its gate verdict + the Sudan og card, one
   EN-side source-annotation correction (still held), ruling #42 / row 37 +
   INDEX addendum, `qa_hreflang_clusters` + CI wiring, the Base.astro
   canonical fix, the wave-packet addition, this log, Brief 65. Zero held-leak
   proved in today's dist (3 EN + 3 AR held). PASS (nothing ships editorially).
2. **Arabic gate:** Sudan AR verdict PASS on file; `englishVersion` ↔
   `arabicVersion` written both ways; held. PASS.
3. **Stills / assets on disk:** Sudan still (09-01) + Sudan og card (today),
   both before this gate. PASS.
4. **Clean build + QA:** CI=true build exit 0 twice; all **nine** standing
   assertions clean; parity 38/38; drift = the one expected field.
   **Reader-visible change in this push: one** — the English home's canonical
   and og:url gain their trailing slash (a fix to an approved page's head, no
   body change). PASS.

**Gate verdict: staged push is a held AR composition + assets + tooling + one
head-level fix to the live home page. Cleared for the founder's one block;
workflow-scope PAT note stands.**
