# QA log — 2026-06-19 (Friday)

**Build:** real `astro build` via the tmpfs/`VITE_CACHE_DIR` route. **Page generation: ✓ Completed**, **37 pages** (16 EN + 16 AR articles + 5 structural). Both Philippines pages rendered (EN `/articles/2026-06-19-philippines-mtb-mle-reversal/`, AR `/ar/articles/…`).
**Sandbox note (honest):** the run exits 1 on a post-build `EPERM: unlink … .astro/_noop-middleware.mjs` — the recurring FUSE-boundary artifact on the mounted tree, thrown *after* "✓ Completed". It is not a content/build failure: GitHub Actions builds on a clean Linux runner (no FUSE) and exits 0. Verified by building to a non-FUSE `--outDir /tmp/madar-dist` (37 pages produced) and confirming the static-asset copy works in the in-tree `web/dist/` (the new still present there).

## Checklist (RUNBOOK §"QA pass")

- [x] **Clean build / page generation** — 37 pages, no schema rejection. Both deks under the 200-cap (EN 187, AR 162; tashkeel counted per the 06-17 ruling).
- [x] **EN/AR parity** — 16 = 16 article pages in `dist`; sitemap 37 urls, well-formed XML (minidom parse ✓).
- [x] **Hero still on disk + in the same commit as text** — `web/public/stills/2026-06-19-philippines-mtb-mle-reversal.svg` (6.2 KB, valid `</svg>`). Page references `/stills/…svg` (EN + AR, 2 refs each: og:image + hero `<use>`). Asset-with-text invariant honoured (still written immediately after the articles, before the gate).
- [x] **Brand chrome inlined** — EN page carries inline `<svg>` wordmark (2 `<svg>`), **zero** `<img …wordmark…>`. Inlining rule held.
- [x] **Related rails resolve** — EN → `/madar/articles/{2026-06-18-egypt, 2026-06-10-iqra, 2026-06-17-korea}` ; AR → the `/madar/ar/articles/…` equivalents. Correct `/madar/` base on all. Language-of-instruction cluster (Egypt ↔ Iqra ↔ Philippines) + Korea reversal rhyme now linked forward.
- [x] **Listings pick up the new piece** — appears on EN homepage, AR homepage, and the Edition-02 editions page (collection-iterated; no hardcoded slug to break).
- [x] **No orphaned references** — related set degrades gracefully (unknown/unapproved slugs filtered at render); all three targets are live/in-this-push.

## Carry / not-blocking
- **Reciprocity:** Egypt / Iqra / Korea do not yet link *back* to Philippines. Graceful (no break); add reciprocal `related` rails on the **buffer day (06-25/26)** alongside the AI-governance cluster rails.
- **`@astrojs/sitemap`** still hand-patched (6th time). Integration recipe on record (`agents/memos/2026-06-18-astrojs-sitemap-integration-recipe.md`); scheduled for the buffer day. Until then, every gate re-validates the hand patch against EN/AR parity — done today.

**QA verdict: GREEN.**

— Web Developer · 2026-06-19
