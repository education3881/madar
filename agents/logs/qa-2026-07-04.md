# Web Developer QA — 2026-07-04 (Kenya ship, sprint piece 9 of 10)

**Result: PASS — clean.** Real `astro build` run this session.

## Build (real, exit 0)

- Route: **clean checkout to `/tmp/madar-web`** (rsync of source excluding `node_modules`/`dist`/`.astro`, `node_modules` copied in, `.vite` cache cleared). The FUSE unlink ban surfaced in a **new spot** this session: not only `node_modules/.vite` but *any* end-of-build unlink on the mount (`dist/_noop-middleware.mjs`, then `.astro/_noop-middleware.mjs`) — even on files created this session. The mounted tree can no longer host a *completing* build at all; the clean-checkout route is now the only honest local build. Banked to the guidebook (build-route note updated).
- **One real defect found and fixed before the gate:** `@astrojs/sitemap` was absent from this sandbox's `node_modules` (installed 07-02 in that session's environment only). `package.json` + `package-lock.json` were verified to carry it (`^3.7.3`) — **so the GitHub Actions production build was never at risk** — and it was installed locally (`--cache /tmp/npm-cache`) for the QA build.
- **Exit 0. 43 pages built.** Both Kenya pages generated:
  - `dist/articles/2026-07-04-kenya-cbe-grade10-pathways/index.html`
  - `dist/ar/articles/2026-07-04-kenya-cbe-grade10-pathways/index.html`
- **Dek cap:** EN **199** clean at compose (first cut 220, trimmed before any build). AR caught at the build gate at **211** — the composed dek grew after its pre-count (the count was run on a draft string, not the final one). Trimmed to **170** (tashkeel counted), rebuilt clean. **Lesson logged:** the compose-time count must be run on the *file as written*, not on the drafting scratch — a one-line `python3 -c` against the saved frontmatter, after the Write, is the rule now.

## Parity & counts

- **EN 19 = AR 19 = stills 19.** Identical slug sets; Kenya present both sides + still on disk in this commit.
- **Sitemap: integration-generated, 43 URLs** (19 EN articles + 19 AR + 5 pages), Kenya EN+AR present with correct `hreflang` en/ar pairs. No hand-patch — first ship day fully on the `@astrojs/sitemap` integration.

## Brand chrome & links

- **Brand chrome clean.** Kenya article page: exactly 1 inline `<svg>` (masthead wordmark); grep for `<img …wordmark…>` → NONE.
- **Hero still** referenced in both languages with correct `/madar/` base; file on disk in the same commit (push-race invariant respected: still written immediately after article text, before any log).
- **Related rails resolve both directions:** Kenya → Sierra Leone + Brazil (built HTML verified); reciprocals Sierra Leone → Kenya and Brazil → Kenya present in EN **and** AR built pages.
- **AR renders RTL** (`dir="rtl"`).

## Publish gate — run in writing (per RUNBOOK §3)

1. **Editor verdict `approve` on file** — `content-drafts/verdicts/2026-07-04-kenya-cbe-grade10-pathways.md`; article in `web/src/content/articles/` with `approved: true` ✓
2. **Arabic Editor verdict on file** — `content-drafts/verdicts/ar/2026-07-04-kenya-cbe-grade10-pathways.md`; AR full-pass (composition verified, transliterations ruled, figures spot-checked 1:1) ✓
3. **Designer hero still on disk & committed this run** — `web/public/stills/2026-07-04-kenya-cbe-grade10-pathways.svg` ✓
4. **Clean build + QA spot-check** — exit 0, 43 pages, parity 19=19, sitemap 43 w/ hreflang, chrome inlined, rails resolve ✓
5. **Origin re-check before composing the push block** — done in the closing message of this run (push-race rule).

**GATE: PASS — staged for the founder's push.**

## Carried / not-blocking

- Live site checked this session **directly** (HTTP 200, 41 URLs pre-ship — current with origin). After Vini pushes, Actions deploys in ~90s; next run re-verifies 43.
- The stale `.astro-stale-*` / `.vite-stale-*` rename-asides left on the mount are gitignored-pattern candidates; harmless, swept by any future clean checkout. (FUSE will not let this session delete them.)

— Web Developer · 2026-07-04
