# Web Dev daily QA — 2026-05-29

## Live-site check (https://education3881.github.io/madar/)

- **Homepage** renders. All seven Issue 01 pieces visible in the listing. Wordmark renders inline (currentColor inherits correctly; no broken `<img>` fallback case). Latin/Arabic bilingual tagline renders in both scripts. Hero stills load on the listing cards.
- **Article pages (spot-check on Iraq, Kuwait, Ghana)** render. Hero stills load. Source URLs in the article are present in the page footer. Arabic `<lang="ar" dir="rtl">` toggle works on the EN→AR cross-link.
- **About page** renders.
- **RSS** (https://education3881.github.io/madar/rss.xml) renders.
- **404** — not separately tested.

## Disk-state cross-checks

- 7 EN articles in `/web/src/content/articles/` ✓
- 7 AR articles in `/web/src/content/articles-ar/` ✓
- 7 hero stills in `/web/public/stills/` ✓
- All 7 EN files cross-link to a present AR file via `arabicVersion:` ✓
- All EN and AR files have `approved: true` ✓
- All EN files reference a hero still that exists on disk ✓
- `.bak` debris from the 2026-05-28 `sed` in-place edits — cleaned up today via `rm -f web/src/content/articles/*.bak`. None remain.

## Build status

- `npx astro build` in the sandbox still hits the same `node_modules/.vite` EPERM ACL issue noted in the 2026-05-28 brief. The sandbox cannot unlink files in the vite cache directory.
- **This is irrelevant to deploy.** GitHub Actions builds in a clean container that does not have the ACL issue, and the live site is the proof that the deploy build is clean. The published HTML on education3881.github.io/madar is the canonical artefact; the sandbox build is a convenience that has been unavailable for two days running.
- Recommendation: if Vini wants a local build before any future push, run `npx astro build` on the host machine (not the sandbox). If anything fails, the offending piece parks and we fix.

## What did not run today

- Lighthouse / CWV audit — not in the daily QA routine yet; queued for the weekly QA.
- Substack embed check — Substack is the confirmed distribution channel but no embed has been added to the site yet.
- Mobile cross-browser pass — homepage looks correct on the live URL via WebFetch (mobile-meta tags present); a real device pass is queued for the weekly QA.

## Standing items

- Web Developer's "between-publishes QA" rule remains in force. Today's pass is the third consecutive daily check since the wordmark incident on 2026-05-26. No regressions of the broken-wordmark class.
- Fourth publish gate (Arabic Editor verdict) added today is enforced by the Manager, not the build. Future deploys with new AR content require a verdict file in `/content-drafts/verdicts/ar/`.

— *Web Developer · 2026-05-29*
