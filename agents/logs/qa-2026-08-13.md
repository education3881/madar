# QA log — 2026-08-13 (daily run)
**Mode:** post-flip (all-approved assertion) · staging-only day, no corpus change · run by the Manager on the Web Developer's checklist

## State verification (Step 1, before any plan)
- `HEAD == origin/main == 232319c` (unchanged since 08-11) · **the 08-12 block never pushed** — Brief 51, recon 4, guidebook row 21, growth slice 2 and qa-2026-08-12.md sit uncommitted in the working tree. A push gap, not a dark day: the work is safe on disk and rides today's block, which now carries two days. No `.git/index.lock`.
- Live surface fetched and verified: homepage serves **Edition 04**, hreflang triple present (en / ar / x-default), inline wordmark present, footer current. The live site and origin agree; only the local tree is ahead.
- Brief numbering: 51 filed (unpushed) 08-12 → today is **Brief 52**.

## Build (route-around per the banked recipe; session-unique log path)
- Fresh `mktemp -d` build dir · `cp -RL` inputs · `node_modules` symlinked · `VITE_CACHE_DIR` on fresh tmpfs · direct binary invoke · log `build-<epoch>-<pid>.log` (session-unique)
- **`astro build` exit 0 — 81 page(s) built, Complete!**

## Assertions (all pass)
| Assertion | Result |
|---|---|
| dist parity | **38 EN / 38 AR** |
| approved flags in build path | **zero `approved: false`** — all-approved holds (post-flip mode) |
| heroes | all **38** stills resolve on disk; every article frontmatter still resolves |
| sitemap | **81 URLs** in built sitemap-0.xml |
| hreflang | triple on built homepage (en / ar / x-default + the ar nav link) |
| wordmark | inline SVG (`aria-label="Madār — مدار"`), zero `<img>` wordmark |
| orphaned `related:` refs | **zero** across EN + AR frontmatter (full sweep) |

## Defects filed
None.

## Notes
- New content this run is **staging/ops only** (recon 5, guidebook row 22 + INDEX addendum, growth cluster pre-wire audit, this log, Brief 52) — nothing enters the build path; live corpus unchanged. Publish gate not invoked (nothing staged for publication).
- The growth pre-wire audit found two pre-`related:`-era orphan *nodes* (ghana-cbe, egypt-ekb — zero graph edges; distinct from orphaned *refs*, which are zero). Wiring plan staged; edits ride Ed05 integration through the gate.
