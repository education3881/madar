# QA log — 2026-08-11

**Run:** daily autonomous · **Manager**

## State verification (Step 1, before any plan)
- `HEAD == local == origin/main == f191f82` · working tree **clean** · no index.lock. The 08-10 block pushed (0a29a0a, 07:01Z) and the founder added his own commit same day (`f191f82` VALENCE v5, 11:06Z). **No push gap.**
- **Deploy verified in the judging environment:** Actions runs 61 + 62 both `success` (run 62 deployed f191f82). Live homepage fetched — serving Edition 04, wordmark inline in served HTML, hreflang triple in head.
- `staged-ed0304/` removal confirmed landed with the 08-10 push (tree clean, no stray mirror).

## Build (tmpfs route-around: rsync → /tmp, symlink node_modules, VITE_CACHE_DIR, **session-unique log paths** per the 08-10 stale-log rule)
- Unique run id `1786422478-2`; log written and watched this run — not a prior session's artefact.
- `astro sync` + `astro build` → **81 pages, Complete!, exit 0**.
- dist parity: **38 EN / 38 AR** · sitemap-0.xml: **81 URLs**.
- **All hero stills resolve** in built HTML (EN sweep, miss count 0).
- Wordmark inline (`<svg>` in homepage), **zero** external wordmark `<img>` refs.
- hreflang triple (en / ar / x-default) present in article heads · `og:image` absolute (07-16 fix holds).

## Flag state (post-flip mode — all-approved assertion, per the 08-09 RUNBOOK rule)
- `approved: false` count in build path: **0 EN / 0 AR** — passes.

## Founder-surface audit (VALENCE v5, f191f82)
- `web/public/valence/index.html` (542 KB): **zero external scripts, zero external stylesheet/font hrefs, one inline script** — privacy posture intact. (One grep hit on tracker keywords is a false positive inside the page's own DATA JSON.) Sits in `web/public/`, outside the sitemap by construction — unchanged posture from the 08-09 audit.

## Verdict
Green. Corpus 38/38, 100% parity, live surface current and verified deployed. No defects filed; nothing for the Web Developer queue.
