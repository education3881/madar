# QA log — 2026-08-31 (Web Developer)

**Build:** route-around per standing recipe (rsync repo → /tmp/madar-run-0831,
symlink node_modules, session-unique VITE_CACHE_DIR + log paths, .git rsynced
minus the FUSE artifacts — first attempt failed on the lastmod resolver's
no-repo guard exactly as designed, .git rsync fixed it). `CI=true npx astro
build` → **exit 0, 81 routed pages (+VALENCE = 82 served).**

## State (named plainly)
- **The seven-day push gap is CLEARED.** Founder ran the 08-30 block; local =
  origin = `9886987`; working tree was clean at open. Origin serves the 08-30
  build (sitemap lastmod 2026-08-30T07:58).
- **`qa_live_drift`: CLEAN — 82 URLs, 0 lastmod drift, 4/4 sampled heads
  identical. First clean read in the check's history.** Yesterday's 84 drifts
  → 0 through the deploy whose maiden `verify` job was built to prove this.
- **Not verifiable from this sandbox today:** the Actions run's own
  green/red for the maiden `verify` job (API provenance-blocked this session;
  browser approval unavailable unattended). The origin state above is the
  ground truth the job asserts, and it is CLEAN — noted rather than assumed.

## Assertions (all against today's /tmp build)
| Check | Result |
|---|---|
| astro build (CI=true, real work tree) | exit 0 · 81 routed + valence |
| held-leak (zambia, sleic) | **0 hits in sitemap/dist** — both pairs held |
| dist parity | 38 EN / 38 AR |
| qa_consumer_surface | exit 0 CLEAN |
| qa_a11y_lang | exit 0 PASS |
| qa_ar_language | exit 0 CLEAN |
| qa_reachability | exit 0 PASS · 0 orphans |
| qa_jsonld | exit 0 CLEAN |
| qa_live_drift (origin vs dist) | **exit 0 CLEAN — first ever** |
| sitemap | 82 loc / 82 lastmod |

## The 08-30 forward question — answered in the same job that named it
`astro-pages.yml`'s `verify` job gains a second step, **"Feed cache validators
work for a polling reader"**: both feeds must serve ETag + Last-Modified, and
a conditional GET with the served ETag must return **304** — the mechanism a
feed reader actually uses to decide "anything new?". Values are logged per run
so cross-deploy ETag variance is comparable by eye (#39: an ETag that never
changes is an invariant probe wearing a cache header). The step runs *after*
the byte-compare, so it structurally cannot false-pass on a stale origin.
YAML parses (3 jobs); `bash -n` clean on both run steps. **Honest limit:** the
304 mechanics could not be probed live from this sandbox today (web-fetch
shows no response headers; browser approval unavailable in an unattended run)
— the step self-proves in the judging environment on the next push, the same
staging discipline the verify job itself used on 08-30 (#16).

**⚠ Push-block note for the founder: this commit touches
`.github/workflows/astro-pages.yml` — the push needs the PAT with `workflow`
scope (the one that ran the 08-30 block has it).**

## Forward question (required per the 08-23 ruling)
The delivery chain is now asserted end to end: build → artifact → origin bytes
→ feed validators. What no check has ever read is the *inbound* half of
discoverability: whether any crawler actually consumes what we serve —
`robots.txt` has never been enumerated by any sweep (does one exist in dist?
does it point at the sitemap index? does it accidentally disallow anything?),
and nothing asserts that the sitemap URL advertised to crawlers is the one the
origin serves. Candidate: a one-line dist assertion that `robots.txt` exists,
names the live `sitemap-index.xml` URL, and disallows nothing we want crawled.

— Web Developer · 2026-08-31

---

## Publish gate — run in writing before staging today's push (Manager)

1. **Editorial approvals:** the push flips **no** `approved:` flag. Today adds
   the Sudan re-verification + commission (recon file, Editor brief), ruling
   #40/row 35 + INDEX addendum, the feed-cache verify step, the held wave
   packet (social-drafts), this log, and Brief 63. Zero held-leak proved in
   today's dist. PASS (nothing ships).
2. **Arabic gate:** no new AR content today; AR routing flags for Sudan are
   recorded in the commission for the Arabic Editor's future gate. PASS.
3. **Stills on disk:** no new piece. PASS.
4. **Clean build + QA:** CI=true build exit 0; all seven standing assertions
   clean including the first-ever CLEAN drift read; parity 38/38. PASS.

**Gate verdict: staged push is recon + commission + tooling + packet + logs
only — approved corpus unchanged. Cleared for the founder's one block, with
the `workflow`-scope note above.**
