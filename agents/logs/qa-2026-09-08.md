# QA log — 2026-09-08, daily run (Web Developer + Manager's publish gate)

## State at open (09:15 +04)

- **local = origin = `fad150f`** (the 09-04 block; run 70 success). `git fetch origin main`
  run before any planning — the brief is yesterday's artefact, never today's state.
- **A full 09-06 day sits UNCOMMITTED in the working tree** — Brief 67, the weekly review,
  `_EDITION_05_STATUS.md`, `qa_body_links.py`, the rail audit + 24 rail files, the PASEC
  read, ruling #44, the guidebook archive, the IndexNow key file and two `astro-pages.yml`
  edits. The 09-06 block **went unrun**. Nothing lost; nothing delivered until it runs.
- **2026-09-05 and 2026-09-07 were dark days** — no run, no brief. Named plainly.
- Therefore this push carries **two runs' work** (09-06 + 09-08) and the origin has served
  the 09-04 build for four days.
- Recurring stale `.git/index.lock` hit the scratch tree on first `git commit` — routed
  around (`rm -f`), not fought.

## Build

Route-around per standing recipe: rsync → `/tmp/madar-run-0908`, `node_modules` symlinked,
session-unique `VITE_CACHE_DIR` and log paths, `.git` present so the lastmod resolver sees a
real non-shallow work tree. `CI=true astro build` → **exit 0, 81 routed pages (+VALENCE = 82
served)**, twice (once before the Zambia edit, once after).

## 1. The held-date `<lastmod>` leak — FIXED, proved both ways (#35)

The 09-06 review's forward question, answered in the order it specified.

**(a) The resolver.** `sitemapLastmod.mjs` now reads `approved:` from each content file's
frontmatter and **a held file contributes no date on either path** — excluded from the index
pages' `newestArticle` scan and from `forContent`. The `forContent` branch is unreachable
today (a held piece has no route) and is written anyway so the invariant is total rather
than incidental. Fail-loud, in the spirit of the module's own three-failures rule: an
unreadable or missing `approved:` field is fatal, never defaulted to published; an empty
content directory is fatal (an approval sweep that finds nothing to check has failed).

**(b) Proof, on real state, in the judging shape.**

| | `/`, `/ar/`, `/editions/`, `/about/` | the sampled article page |
|---|---|---|
| baseline (no scratch commit) | 09-04 | 09-04 |
| **BITE** — scratch commit touching **only the held Zambia EN draft**, OLD resolver | **09-08 05:22 — moved** | 09-04 |
| **BITE** — same commit, NEW resolver | **09-04 — held** ✔ | 09-04 |
| **CONTROL** — scratch commit touching **only an approved EN file** | **09-08 05:24 — moved** ✔ | **EN moved, AR twin did not** ✔ |

The bite reproduces the production defect exactly (`9886987` 08-30, `642fef2` 09-02: four
index pages moved for a held-only commit). The control proves the check still fires on real
change and does not over-fire — the Arabic twin correctly stayed put. Scratch commits
discarded; scratch tree restored to `fad150f`.

**The control also caught a defect in my own fix, which is the point of having one.** My
first probe appended to a filename that did not exist, creating a stray `.md` with no
frontmatter. The resolver **refused to build** rather than assume it was published — correct
behaviour, surfaced by accident. It did reveal a real weakness: I had parsed a fixed
60-line window. Today's deepest `approved:` sits at line 47, which is comfort, not a
guarantee. Replaced with a parse of the frontmatter **block** (open `---` to close `---`),
which cannot be outgrown by a long sources list.

**(c) Standing assertion #11 — `qa_lastmod.py`**, the output-side twin. Asserts: every
`<loc>` carries a `<lastmod>`; no lastmod exceeds the **ceiling** = max(newest commit
touching an approved content file, newest chrome commit), derived from git rather than
asserted (#36); nothing in the future; and the ceiling itself resolves (an empty approved
set or chrome set is a failure, not a pass). Proved per #35 — **control silent** on the real
build; **bite 1** an injected above-ceiling date → FAIL(2); **bite 2** a stripped lastmod →
FAIL(1); control silent again after restore. Wired into the CI **build** job as the fourth
QA step. YAML re-parsed: **3 jobs, build 10 steps, verify 4 steps, `INDEXNOW: off` intact.**

**Push note: the commit touches `.github/workflows/` — workflow-scope PAT required** (this
step, plus the two edits carried from 09-06).

**(d) Carried, not answered:** the chrome bump is still unbounded. Measured today and
designed — see the Growth note; decision goes to the 09-13 review.

## 2. Assertions — all clean

| Assertion | Result |
|---|---|
| `astro build` (CI=true, real work tree) | **exit 0** · 81 routed + VALENCE |
| `qa_robots` | PASS — 1 Sitemap line == derived URL, target exists, 0 disallows |
| `qa_hreflang_clusters` | PASS — every cluster reciprocal, dereferences, equals the sitemap set |
| `qa_body_links` | PASS — related + sources promises rendered, twin rails agree, **0 dead ends** |
| **`qa_lastmod` (NEW, #11)** | **PASS — 82 URLs, 82 lastmod, newest 09-04 ≤ ceiling 09-04; 76 approved / 6 held, held contribute no date** |
| `qa_a11y_lang` | PASS — every text node agrees with its resolved language |
| `qa_ar_language` | CLEAN — no Latin-script controlled vocabulary in Arabic chrome |
| `qa_reachability` | PASS — every page reachable from its own front door |
| `qa_jsonld` | CLEAN — every promise parses, agrees, dereferences |
| `qa_consumer_surface` | CLEAN — og:locale/type/site_name, **raster** og:image, print block |
| `qa_live_drift` | **CLEAN** — origin matches dist: 82 URLs, 0 lastmod drift, 4 sampled heads identical |

**Parity:** 38 EN / 38 AR built article pages — **MATCH**.
**Sitemap:** 82 `<loc>`, 82 `<lastmod>`.
**Feeds:** `rss.xml` 38 items / 38 enclosures; `ar/rss.xml` 38 items / 38 enclosures; both
well-formed under a real XML parse.
**Held-leak:** all three held slugs → **0 pages, 0 sitemap entries, 0 feed items** in both
languages; assets on disk only.

**Drift is CLEAN even though this run edited the Zambia pair in both languages** — because
the pair is held and produces no served bytes. That is the fix's premise demonstrated on
live state rather than argued.

## 3. Publish gate — run in writing (Manager)

| Gate | Result |
|---|---|
| Editor / Verifier verdict on anything shipping | **Nothing ships editorially.** Corpus unchanged at 38 EN / 38 AR, 35 countries. **No `approved` flag flipped.** |
| Zambia pair | **Verifier verdict FAIL-with-1-item, filed** (`verdicts/2026-09-08-zambia-free-education-act-verification.md`); item fixed in-run in both languages and both `sources[]` titles; **stays `approved: false`** and returns to the queue for a confirmation read at the wave gate. A fix authored today is not verified today by the same desk. |
| Arabic gate | The Zambia AR edit is a **one-clause correction inside an already-gated piece**, not a recomposition: numeral multiset re-checked after the edit — **zero one-sided figures**, EN-only set unchanged (all seven spelled out in Arabic as before); dek 191/200 and title 31/100 unchanged. Full AR re-gate is **owed at the wave gate** and recorded there. |
| Hero stills + og cards on disk | Zambia still 7,094 B, og card 6,526 B (raster); all three held pairs complete |
| `astro build` clean | **exit 0**, twice |
| Held flags | 3 EN + 3 AR held, verified on disk; **no stale `approved: true` duplicate** anywhere in the repo (swept all 7 files bearing the Zambia slug — the 07-11 trap) |

**Gate result: PASS to stage. Nothing ships editorially; the push carries infrastructure,
verification, research and briefs only.**

## 4. Forward question (required per the 08-23 ruling) — for the 09-09 run

Today's fix makes a held file contribute no *date*. The held-leak sweep already proves it
contributes no *page*, no *sitemap entry* and no *feed item*. **Nothing yet proves a held
file contributes no *bytes at all* to `dist`.** Its hero still and og card are on disk and
are copied into `dist/` from `public/` — correctly, as assets, and they were generated
before the flag flips. But an og card is a **derivable promise** (#36): it is derived from a
piece that is not published. A crawler that guesses the card path fetches an image for an
article that returns 404, and we would not know.

Candidate for 09-09, behind the Sierra Leone verdict: extend `qa_lastmod`'s sibling sweep to
enumerate **held assets in `dist/`** and decide, once, in writing, whether a held piece's
card should ship early (harmless, and ready at the gate) or be withheld (no orphan asset).
Not a defect until decided — an unexamined surface, which is how the last two leaks began.

— Web Developer + Manager · 2026-09-08
