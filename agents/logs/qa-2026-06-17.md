# Web Developer QA — 2026-06-17

**Method:** real `astro build` via the tmpfs + `VITE_CACHE_DIR` route ([[2026-06-15-astro-build-in-sandbox-vite-cache-route]]); checks run against the **tmpfs `dist/`** (never the FUSE copy — stale-HTML inflation). Live URL not fetched (egress wall; git/origin parity + Actions deploy is the truth).

## Build

- **Exit 0 · 33 pages** (31 prior + Korea EN + Korea AR). Both Korea routes present in `dist/`.
- **Defect caught & fixed before green:** EN and AR `dek` both exceeded the 200-char content-schema cap → `InvalidContentEntryDataError`. Trimmed both (AR also de-tashkeel'd in the dek, since combining marks count toward the cap); rebuilt → clean. This is the schema doing its job; logged so the dek-length check stays in the pre-stage routine.

## Spot-check (built HTML)

| Check | Result |
|---|---|
| EN/AR article-page parity | **14 = 14** |
| Korea hero still in `dist/stills/` | present (6.1 KB) |
| Korea EN spine voice (Kim Cha-myung) | present |
| Lee collision (Jae-myung / Ju-ho / Hyun-joon) | all three present & distinct, EN + AR |
| Related rail (Korea → Uruguay/Indonesia/Qatar) | all three resolve |
| Caption taxonomy | register-style **"Curated 12"** / «كيوريتد ١٢»; **zero** `Issue 0x` drift on the new pages |
| Homepage (founder cover-removal) | `current-edition__cover` absent (0); `prev-edition__cover` retained; wordmark inlined |
| Sitemap | 33 `<url>`, Korea EN+AR present |

No P0/P1 defects. Brand chrome intact (wordmark inlined, CSS fallback unaffected). Cleared.

— Web Developer · 2026-06-17
