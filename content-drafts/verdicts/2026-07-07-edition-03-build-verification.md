# Edition 03 — build verification (in writing)
**Date:** 2026-07-07 · Method: Astro build replicated per CI (node, `astro build`, `@astrojs/sitemap`), run from a writable copy with the vite cache redirected (`VITE_CACHE_DIR`) to route around the sandbox-FUSE mount. Ruling #16 stands: the local build is not the deploy — the CI run on the founder's push is the deploy gate. This record states what was verified locally and the sandbox limits honestly.

## Verified
- **Content schema (authoritative):** all 28 sprint files (10 Ed3 EN + 10 Ed3 AR + Morocco EN/AR + 5 on-disk Ed4 EN + US-NAEP AR) validated against the exact `content.config.ts` zod schema. **One violation: Chile dek = 201 → trimmed to 189.** All others clean (dek ≤200 incl. tashkeel, required fields present, region/level/type enums valid, every source a valid URL).
- **Full render with Ed3 present:** in the build run where the mount exposed the 07-07 files, Astro rendered every `2026-07-07-*` EN and AR article route plus `/editions/`, `/ar/editions/`, and the homepage, and completed page generation ("Completed"). No content/TS/Astro errors.
- **Ed03 infrastructure renders:** built `/editions/index.html` contains "Continuity under disruption", `data-motif="throughline"`, "EDITION 03 · JULY 2026", and the inlined `edition-03.svg`; the homepage now leads with Ed03 as the current edition (derived from `status: 'current'`). Covers dir emits edition-01/02/03.svg.
- **Parity (file-tool ground truth):** Ed3 = 10 EN + 10 AR + 10 stills. `arabicVersion` links resolve.
- **Sitemap:** generated by the build integration (hand-patch retired 2026-07-02); URL set emitted.

## Sandbox limitation (not a content defect)
The bash FUSE mount (a) refuses to `unlink` Astro's temporary finalize entrypoints (`noop-entrypoint.mjs`), yielding a non-zero exit *after* page generation completes, and (b) intermittently fails to expose the prior session's untracked 07-07 files to bash (they are present on the real filesystem; the file tools read/write them normally). Neither condition exists on the GitHub Actions runner, which builds a fresh clean checkout. The schema pass and the healthy-mount render together establish build validity for the committed set.

## Verdict: **Ed03 build-valid.** Green CI expected on push. Founder's push triggers the authoritative build+deploy.
— Manager · 2026-07-07
